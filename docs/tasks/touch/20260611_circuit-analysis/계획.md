---
name: circuit-analysis-계획
purpose: 회로 구성·분석 문서 구성·작성 방식·검증 전략
type: 계획
maturity: stable
tags: [touch, iqs323, circuit, study, plan]
---

# 계획 — IQS323 터치 회로 구성·분석

**TL;DR**: `개선/`에 README(인덱스·로드맵) + `회로 구성·분석.md`(12절: 개요~개선 검토) 작성. 회로 사실은 JPG ground truth, 데이터시트 대조·구현/이슈 해석은 분석으로. 기존 회로-구성.md C54 정정. 서브에이전트 fan-out 검증(회로 사실·데이터시트·구현정합·구조). 승인 plan 전문: 루트 `docs/plans/eventual-mixing-sphinx.md`.

## 문서 구성 (`회로 구성·분석.md`)

개요 → 전체 회로도(JPG 정확 ASCII+부품표) → IC 스펙 → 핀 연결 전수 → 전원부(C54 4.7µF·§2.5 대조) → 디지털 I/F → 아날로그 채널 3종(RC 계산·CalCap) → 커넥터 → 데이터시트 §2.5 대조표 → 현재 구현↔회로 관계 → 이슈의 회로적 해석 → 개선 검토 포인트.

## README

스터디 목적·구조·`회로 구성·분석.md` 등록 + 향후 로드맵(구현 적정성·autoATI vs FIXED·드리프트·채터링/콜드부트·ESD/guard).

## 작성 방식

- 회로 사실 = JPG ground truth. 데이터시트 대조는 `데이터시트/` 링크. 구현/이슈 해석은 분석(추정 명시).

## 검증

서브에이전트 fan-out — ① 회로 사실(JPG 대조, C54) ② 데이터시트 인용 ③ 구현·이슈 정합(요구사항 문서) ④ 구조(frontmatter·link). 치명·중 0까지 루프(≤3).

## 완료 기준

README+회로 구성·분석.md 생성·JPG 100% 일치·C54 4.7µF·데이터시트 인용 정확·할루시네이션 0·기존 문서 C54 정정·broken link 0.

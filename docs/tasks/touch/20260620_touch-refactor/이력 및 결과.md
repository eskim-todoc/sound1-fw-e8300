---
name: 터치 코드 3레이어 리팩토링 이력 및 결과
purpose: 본 작업의 시계열 로그와 완료 요약
type: tasks-이력
maturity: in-progress
tags: [touch, iqs323, refactor, 이력]
---

# 터치 코드 3레이어 리팩토링 — 이력 및 결과

**TL;DR**: Full ATI 터치 제어를 시간 상수 자동 파생 + 3레이어 분리 + 테스트 용이 구조로 전면 재구조화하는 작업의 로그. ultracode 워크플로우(Opus 4.8 xhigh).

---

## 시계열 로그

### 2026-06-20

- **착수**: 은수님 지시 — 시간 상수(폴링·롱터치·절전 wake) 자동 파생 + 터치 코드 전면 리팩토링(3레이어·테스트 용이·작고 간단) + 워크플로우 문서화.
- **명령 해석**: 테스트 방식·리팩토링 강도 질문 → 은수님 답 = **순수 로직 분리(HW 의존은 모듈 호출 격리)** + **전면 3레이어 재구조화**.
- **전제**: 20260619 Full ATI 구현(미커밋, 정적 검증 PASS) 위에서 동작 보존하며 구조 개선. 같은 브랜치 `claude_feature_touch-fullati-control`. 현 터치 코드 2848줄(드라이버 1463·로직 555·헤더/설정).
- **단계 ① 완료**: `요구사항.md`·이력 작성.
- **단계 ② 착수**: 오케스트레이터 입력·페르소나 json 후 Workflow fan-out(현 코드 구조 분석 + 3레이어 설계 + 테스트 경계 + 시간 상수 파생 + adversarial 검증 + 종합).

- **단계 ② 완료**: Workflow 8노드(현코드 매핑 + 설계 5 + adversarial 검증 1 + 종합 1, xhigh, 858k 토큰). 한자 0.
  - **3레이어 설계**: L1(`tdc_drv_iqs323` 불변 + `tdc_touch_port.h` 약한 추상·mock 치환) / L2(`tdc_touch_fsm` 순수, HW include 금지, `fsm_step` 단일 진입, static 14개→상태 구조체) / L3(`tdc_touch.c` 노말 + `tdc_touch_adapter_sleep` 절전).
  - **급소 race 구조 차단**: read 실패 시 입력 전부 false 정규화 → stale 캐시 자체 불가능.
  - **시간 상수**: `tdc_touch_time.h` 단일 + `TDC_TOUCH_MS_TO_CNT` 올림 파생(ms 변경=카운트 자동). 등가 1:1 검증.
  - **검증 07**: [보존]24·[위험]5·[누락]2 → 보수적 결정 3(쿨다운 카운트·절전 L2 미경유·init L2 밖) + 테스트 4종(RF5b·ST4b·LT0·BT2) + RESET 초기값으로 회귀 0.
  - **테스트**: 호스트 gcc T1 유닛·T2 모듈(포트 mock)·T3 파생(_Static_assert). 프레임워크 없음.
  - 레이어 구조 Mermaid valid. `분석.md`·`인덱스.md` 작성.
- **단계 ③ 완료**: `계획.md` — 마이그레이션 F1~F6(F4 L2 순수화·F5 L3 정리가 핫스폿), 호스트 테스트로 회귀 0 정적 보증, main.c 공개 API 5개 불변.
- **단계 ④ 승인**: F1~F6 일괄 + 전체 T1/T2/T3.
- **단계 ④ 구현(fork) 일부 + 방향 전환(2026-06-20)**: fork가 3레이어 소스 9개(time·port·fsm·adapter_sleep·calib) + 테스트 일부 구현(rate limit 중단). 내가 호스트 테스트 인프라(runner·mock·Makefile) 보완 → gcc 부재로 빌드 시도 중 **은수님 방향 정정**.
  - **정정 핵심**: 목적은 내가 테스트를 돌리는 게 아니라 **의료기기 SW 밸리데이션 대비 — 코드가 작고·간결·명확·테스트 쉬운 구조**. 그리고 **HAL/driver식 추상 레이어 탈피**. Rev.1 3레이어(포트 추상·mock·어댑터 분리)는 반대로 과도하게 무거웠음.
  - **결정**: 미커밋 코드 전부 git 되돌림(소스 6개 restore + 신규 9개·tests 삭제, develop 원점). docs(설계·분석)는 보존. **Rev.2: 2~3파일 간결 구조로 재설계**.
- **Rev.2 착수**: 요구사항 §2 갱신(간결 2~3파일·HAL 탈피·제어방법=FullATI 단순안 간결 재구현). 간결 설계 Workflow(에이전트-로그/Rev2-간결/).
- **Rev.2 단계 ②③ 완료**: Workflow 6노드(제어방법 경계 + 순수FSM·I2C직접·연결 설계 + adversarial 검증 + 종합, xhigh, 804k 토큰). 한자 0.
  - **최종 3파일+헤더2**: `tdc_touch_logic.c/.h`(순수 FSM, HW include 0·전역 0, mock 0 유닛테스트) · `tdc_touch_iqs323.c/.h`(I2C 직접, 1291→약 360줄, 880줄 삭제) · `tdc_touch.c/.h`(얇은 연결, 공개 API 3종 불변) · `tdc_touch_time.h` · config(토글).
  - **검증 05**: [간결·보존]13·**[과추상]0**(HAL/driver 탈피 달성)·[누락 봉합]4 → 99 단일 확정.
  - **봉합 4건**: 시그니처 단일화·캐시 미신설·boot 가드(`read_ok && curr!=TOUCH`)·get_state struct wrapper.
  - **제어방법 8대 1:1 매핑**(B99 이벤트 13종 동등), 시간상수 자동 파생, 순서도 2개 valid.
  - `분석.md`·`계획.md` Rev.2 전면 갱신, `인덱스.md`(Rev2).
- **단계 ④ 진입 대기**: 은수님 승인(구현 방식). 봉합 4건·계획은 99 권고 채택.

- **Rev.2 단계 ④ 구현(fork) + 검증·정리**:
  - fork가 간결 3파일 구현(rate limit로 일부 중단 → 내가 테스트 인프라 보완 중 은수님 정정으로 fork 결과는 채택). 최종 net **−1404줄**(2411 삭제·1007 추가), 2848→1095줄.
  - **1차 검증(5노드)**: 빌드정합·제어보존 PASS. [치명] ① Power Mode 0xC0 덮음(re_ati 0x04/reseed 0x08이 Power bits를 Normal로 — 데이터시트 A.30 확정) → re_ati 0x54·reseed 0x58(Power No ULP+CH timeout disable 보존, 4곳) ② ui_command 삭제 헤더 → 정리.
  - **강화 검증(7노드, 은수님 지시)**: **할루시네이션 0**(B3 제어방법·B4 레지스터 정본 1:1, E1~E10·S1~S3 전수 동등)·기본 빌드 영향 0(삭제 심볼 가드 안)·계획 부합 PASS. [치명] SLEEP_MEASURE/RTT_TUNING dev 죽은 블록 댕글링 → main.c·ui_command·config 물리 제거. [경미] delay_ms ci_util.h include·Beta 주석 중립화.
  - **최종**: 삭제 심볼·dev 토글 잔존 0(주석만), 전처리기 균형 전 파일 OK, 한자 0.
- **단계 ④ 완료(정적)**: 빌드·실기·호스트 유닛테스트는 다음주 Eclipse [실측 게이트]. Eclipse Debug/subdir.mk stale은 재인덱싱 시 자동 해소. 정량 8종(Beta 해석·계수·t_ati 등) 실기 1회 주입.

## 완료 요약 (2026-06-20)

**구조**: 2848줄(2파일 HAL/driver) → **1095줄 3파일**(net −1404). `tdc_touch_logic.c/.h`(순수 FSM, HW include 0·전역 0·mock 0 유닛테스트) + `tdc_touch_iqs323.c/.h`(I2C 직접, 추상 0) + `tdc_touch.c/.h`(얇은 연결) + `tdc_touch_time.h`(시간상수 파생). 추상(포트·vtable·mock·어댑터) 0.
**검증**: 1차 5노드 + 강화 7노드(할루시네이션 0·기존코드 영향 0·계획 부합). 치명 3건(Power Mode·ui_command·dev 댕글링) 수정. 공개 API 4종 불변(호출처 0변경).
**핵심**: 의료기기 SW 밸리데이션 대비 — 순수 제어 로직 분리(테스트 용이)·간결·명확. 제어방법(FullATI 단순안) 기능 동등.

---
name: B1 기존 코드 전수 영향 검증
purpose: 터치 간결 3파일 리팩토링이 기존 코드(삭제 심볼·헤더·매크로 소비자)를 빌드/회귀 차원에서 깨는지 grep 전수
type: 에이전트-로그
maturity: in-progress
tags: [touch, iqs323, refactor, rev2, verification, regression, build]
---

# B1 기존 코드 전수 영향

**TL;DR**: 기본 빌드(SLEEP_MEASURE_MODE=0·RTT_TUNING 미정의)는 깨지지 않는다 — 삭제 심볼(`tdc_drv_iqs323_*`)의 모든 잔존 소비자는 `#if` 가드 안에서 0으로 평가되어 컴파일 제외된다. 단 **[치명] 1건**: 진단 빌드 변종 `SLEEP_MEASURE_MODE=1` 활성 시 `tdc_drv_iqs323_sleep_measure_dump()`(main.c:927)·`tdc_touch_consume_sleep_request()`(main.c:777) 호출부가 정의 삭제된 심볼을 참조 → 미정의 링크/컴파일 에러. 제공자는 계획대로 삭제됐으나 가드된 소비자 5+곳을 미정리한 계획 미완. `2__cm3_factory`는 git 미추적·diff 무관(범위 외). verdict FAIL.

## 1. 검증 범위·방법

- 대상 트리: `src/2__cm3/`(리팩토링 대상, git 추적). `src/2__cm3_factory/`는 gitStatus `?? src/2__cm3_factory/`로 **미추적**이며 diff가 전혀 건드리지 않음 → 본 리팩토링 영향 범위 외(정보성).
- 정적 정합(gcc 부재). 전처리기 규칙: `#if` 내 미정의 식별자는 0으로 평가.

## 2. 삭제 심볼 소비자 전수 (① 항목)

`grep -rn "tdc_drv_iqs323" src/2__cm3/` 결과 잔존 참조 7건 — 전부 가드 안:

| 위치 | 참조 심볼 | 가드 | 기본 빌드 |
|---|---|---|---|
| main.c:927 | `tdc_drv_iqs323_sleep_measure_dump()` | `#if TDC_TOUCH_SLEEP_MEASURE_MODE`(909~930) | 제외(0) |
| tdc_ui_command.c:713 | `tdc_drv_iqs323_show_tuning()` | `#if TDC_TOUCH_RTT_TUNING`(698~755) | 제외(0) |
| tdc_ui_command.c:717 | `tdc_drv_iqs323_clear_tuning()` | 〃 | 제외(0) |
| tdc_ui_command.c:721 | `tdc_drv_iqs323_apply_tuning()` | 〃 | 제외(0) |
| tdc_ui_command.c:736 | `tdc_drv_iqs323_set_normal_tuning()` | 〃 | 제외(0) |
| tdc_ui_command.c:740 | `tdc_drv_iqs323_set_sleep_tuning()` | 〃 | 제외(0) |
| tdc_ui_command.c:21 | (주석만, 심볼 아님) | — | 무관 |

- `tdc_iqs323_tuning_t t;`(tdc_ui_command.c:725)도 `#if TDC_TOUCH_RTT_TUNING` 안 → 타입 정의 부재여도 제외되어 기본 빌드 무해.

## 3. 가드 매크로 정의 상태 (③ 항목)

- `TDC_TOUCH_RTT_TUNING`: `#define` **부재**(config·.cproject·Debug/makefile 어디에도 없음). `#if`에서 0 평가 → main.c:52·962, tdc_ui_command.c:698·773 4블록 전부 컴파일 제외. tdc_ui_command.c:21~22 주석이 이 의도를 명문화.
- `TDC_TOUCH_SLEEP_MEASURE_MODE`: tdc_touch_config.h:30~31 `#ifndef → #define 0`(운용 기본 0). main.c:317·703·776·909, tdc_ui_command.c:921 블록 기본 제외.
- `TDC_TOUCH_ATI_CALIB_MODE`·`CRX1_DUMMY/REF/VSS_ENABLE`·`CRX0_DISCHARGE_LOG`·`PROX_THRESHOLD_ENABLE`·`TDC_DRV_IQS323_ATI_DUMP_ENABLE`: 소스 참조 0건(전부 삭제 완료). 미정의 #if·미정의 사용 잔존 없음.
- `TDC_DRV_IQS323_*` 접두 매크로 비-Debug 소스 잔존 **0**.

## 4. [치명] 잠재 빌드 깨짐 — SLEEP_MEASURE_MODE=1 변종 (④ 항목)

계획 99 §M3([파일]99_종합_간결구조.md:302)은 SLEEP_MEASURE를 "빌드도구 3종 약 540줄 삭제" 대상으로 명시 → 제공자 삭제는 계획 의도. 그러나 **가드된 소비자가 main.c·tdc_ui_command.c에 잔존**:

- `tdc_drv_iqs323_sleep_measure_dump()` — 정의: 삭제(diff: 옛 D 963행 영역 `-void tdc_drv_iqs323_sleep_measure_dump(void)` 삭제, 신규(+) 재추가 0). 호출: main.c:927 잔존(가드 909~930 안).
- `tdc_touch_consume_sleep_request()` — 정의·선언: 삭제(diff:2216 `-bool tdc_touch_consume_sleep_request(void)` / diff:2486 `-bool tdc_touch_consume_sleep_request(void);`, 둘 다 옛 tdc_touch.c/.h의 `#if TDC_TOUCH_SLEEP_MEASURE_MODE` 안. 신규(+) 재추가 0). 호출: main.c:777 잔존(가드 안). working tree 2__cm3 systemControl 전체 정의/선언 부재 재확인.

결과: `TDC_TOUCH_SLEEP_MEASURE_MODE=1`로 빌드 시 두 심볼 모두 미정의 → 컴파일/링크 에러. 기본 빌드(=0)에서는 발현 안 됨. 옛 코드는 호출부·정의부가 동일 가드 안에서 짝을 이뤄 정합했으나, 리팩토링이 정의만 제거하고 가드된 호출부를 미정리해 짝이 깨짐.

- 계획 정합 관점: M4 "main.c 호출처 0변경"·M5 "미사용 매크로 0"과 배치되는 미완 — 진단 변종 정합성을 위해 main.c:317·703·776·909·927, tdc_ui_command.c:920~926의 SLEEP_MEASURE 블록과 `s_sleep_measure_mode`·`tdc_on_sleep_measure_cmd`(main.c:319·323) 일괄 제거 또는 호출부 봉합 필요.
- `tdc_on_sleep_measure_cmd`(main.c:323 정의, 가드 안)·`s_sleep_measure_mode`(main.c:319, 가드 안)는 자기완결(같은 가드 짝) → 그 자체는 미정의 아님.

## 5. 삭제 헤더 include 전수 (② 항목)

- `grep -rn 'tdc_drv_iqs323\.h' src/2__cm3/`: 라이브 `#include` **0건**. tdc_ui_command.c:21은 제거 사실을 적은 주석. main.c는 include를 `tdc_touch_iqs323.h`로 교체(diff:12). → 삭제 헤더 미해결 include로 인한 깨짐 없음.
- 신규 헤더 `tdc_touch_iqs323.h` 소비자: main.c·tdc_touch.c·tdc_touch_logic.h·tdc_touch_iqs323.c 모두 존재.
- initialize.c: `#include <tdc_touch.h>`만(공개 API 경유), 삭제 심볼·헤더 직접 참조 0. tdc_touch_init_begin()(initialize.c:449)은 신규 tdc_touch.h가 계속 제공.

## 6. 비가드(기본 빌드 포함) 시그니처 변경 정합

read_status 시그니처가 옛 2-out 포인터 → struct out으로 변경됐고, 비가드 호출부 전부 갱신 확인:

- main.c:866 `tdc_touch_iqs323_read_status(&st) && st.pressed` — 신규 헤더 선언(tdc_touch_iqs323.h:68)·struct 멤버 `pressed`(56~62) 일치.
- main.c:933 `apply_sleep_settings()`·941 `reseed()` — 헤더 선언(90·78) 일치.
- tdc_touch.c:93·143·223 read_status 호출도 신규 시그니처 정합.
→ 기본 빌드 비가드 경로 깨짐 없음.

## 7. Eclipse 빌드도구 (⑤ 정보성)

- `Debug/Cortex-M3-src/systemControl/subdir.mk:20·37`이 삭제된 `tdc_drv_iqs323.c/.o`를 여전히 빌드 대상으로 나열, `.map`·`.d`·`.o`·`.o.lst`도 옛 산출물. 이는 **재인덱싱 전 스테일 생성물** — Eclipse가 재빌드 시 소스 목록 갱신해 subdir.mk 재생성하므로 클린 리빌드에 무해(정보성). 단 stale subdir.mk를 그대로 incremental make에 쓰면 부재 .c 컴파일 시도로 실패 가능 → 리빌드/재인덱싱 권고.

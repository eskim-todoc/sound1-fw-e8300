---
name: B99 강화검증 종합 — 터치 간결 3파일 리팩토링 잔존위험·최종판정
purpose: 6노드(B1~B6) 강화검증 통합 — 잔존 치명/경미 전수·기존코드 영향 최종판정·계획부합·할루시네이션 적출 종합·수정 우선순위·PASS 조건
type: 에이전트-로그
maturity: in-progress
tags: [touch, iqs323, refactor, rev2, verification, hallucination, regression, synthesis]
---

# B99 강화검증 종합 — 잔존위험·최종판정

**TL;DR**: 6노드(B1~B6) 통합 + 핵심 사실 코드 재검증 결과 — **기본 빌드(`SLEEP_MEASURE_MODE=0`·`RTT_TUNING` 미정의) 컴파일·링크 차단 [치명] 0건, 할루시네이션(근거 없는 로직·레지스터값) [치명] 0건**. 단 **진단 변종 빌드 [치명] 1건**: `TDC_TOUCH_SLEEP_MEASURE_MODE=1` 활성 시 `tdc_touch_consume_sleep_request()`(main.c:777)·`tdc_drv_iqs323_sleep_measure_dump()`(main.c:927)가 정의 삭제된 심볼을 참조 → undefined. 두 호출 모두 `#if TDC_TOUCH_SLEEP_MEASURE_MODE` 가드 안이라 기본 빌드 미발현이나, 계획 99 §M3 "SLEEP_MEASURE 삭제"가 정의만 제거하고 가드된 호출부를 미정리한 봉합 미완. **계획 부합 PASS**(파일·시그니처·삭제목록·봉합4건·시간상수·추상0 전수 1:1), **할루시네이션 적출 0**(6대 제어·레지스터 15주소·System Status 4비트·Power Mode 보존 0x54/0x58/0x50 전부 정본 1:1). [경미] 6건은 빌드 비차단(스타일·헤더위생·죽은코드·carryover·실측게이트). **최종: 진짜 문제 1건(진단변종 댕글링)만 수정하면 PASS** — 기본 운용 빌드는 결함 0. verdict **FAIL**(진단변종 [치명] 미수정 기준), 댕글링 봉합 시 PASS.

---

## 0. 종합 기준·노드 verdict 요약

| 노드 | 스코프 | verdict | 치명 | 경미 |
|---|---|---|---|---|
| B1 기존코드 전수영향 | 삭제심볼·헤더·매크로 소비자 회귀 | **FAIL** | 1 (SLEEP_MEASURE=1 댕글링) | 1 (subdir.mk 스테일) |
| B2 계획부합 | 99 Rev2 설계 1:1 | PASS | 0 | 2 (M3 물리삭제 미완·union 미사용) |
| B3 제어방법 할루시네이션 | logic.c 6대 동작 vs B99/99fullati | PASS | 0 | 1 (쿨다운 방식 치환) |
| B4 레지스터 할루시네이션 | iqs323.c write/read vs 데이터시트 | PASS | 0 | 2 (Hysteresis carryover·Beta 주석) |
| B5 빌드정합 심화 | 시그니처·미정의심볼·전처리·include | PASS | 0 | 3 (delay_ms·subdir.mk·dump 댕글링) |
| B6 순수성·테스트 | logic 순수성·호스트 유닛테스트 | PASS | 0 | 1 (time.h TIMER_PRESCALE 자체완결) |

- **종합 원칙**: 본 종합은 6노드 결론을 그대로 합산하지 않고, [치명]·핵심 [경미]를 **코드/플래그로 직접 재검증**(아래 §5 재검증 로그)해 할루시네이션 0을 보증. 중복 적출(SLEEP_MEASURE 댕글링·subdir.mk 스테일)은 1건으로 통합.
- 라벨: [치명]=빌드/링크 차단 또는 회귀 또는 근거없는 로직/값. [경미]=빌드 비차단(스타일·헤더위생·죽은코드·문서갭).

---

## 1. ① 잔존 [치명] 전수 (수정 필요) + [경미] (권고)

### 1.1 [치명] — 전수 1건

| # | 항목 | 위치 | 근거(코드 재검증) | 발현 조건 |
|---|---|---|---|---|
| C1 | 진단변종 빌드 댕글링 2심볼 | main.c:777 `tdc_touch_consume_sleep_request()` · main.c:927 `tdc_drv_iqs323_sleep_measure_dump()` | 두 심볼 트리 전체 **정의 0**(grep: 호출만 1건씩, 정의/선언/신규 대체 API 부재). 둘 다 `#if TDC_TOUCH_SLEEP_MEASURE_MODE` 가드 안(main.c:776~785, 909~930). 가드 매크로 = config.h:31 `#define ...SLEEP_MEASURE_MODE 0` | `SLEEP_MEASURE_MODE=1` 빌드 시 undefined symbol. **기본 빌드(=0) 미발현** |

- **왜 [치명]인가**: 옛 코드는 정의부·호출부가 동일 `#if SLEEP_MEASURE_MODE` 가드 안에서 짝을 이뤄 정합했다. 리팩토링이 **정의만 삭제**(diff에서 `tdc_drv_iqs323.c`의 dump 정의·옛 tdc_touch.c/.h의 consume 정의·선언 삭제, 신규 재추가 0)하고 **가드된 호출부 2곳을 미정리**해 짝이 깨졌다. 진단 변종을 켜는 개발자 시나리오에서 컴파일/링크 에러.
- **왜 기본 운용 빌드엔 무영향인가**: config.h:31이 0 고정, 전처리기가 블록 전체 제거 → 댕글링 심볼이 토큰화조차 안 됨. 운용 펌웨어 결함 0.

### 1.2 [경미] — 전수 6건 (권고, 빌드 비차단)

| # | 항목 | 위치 | 근거 | 영향 |
|---|---|---|---|---|
| W1 | Eclipse `subdir.mk` 스테일 | Debug/.../systemControl/subdir.mk:20·37·54 | 삭제된 `tdc_drv_iqs323.c/.o/.d` 등재(재검증 확인), 신규 `tdc_touch_iqs323.c`·`tdc_touch_logic.c` 미등재. gitignore+미추적, diff 무포함 | raw `make` 시 부재 .c 컴파일/신규 .o 미빌드 링크 실패 가능. **Eclipse refresh/rebuild 시 자동 재생성** → 정상 워크플로우 비문제. 권고: 빌드 전 프로젝트 refresh 1회 (B1·B5 중복 → 통합) |
| W2 | `delay_ms` include 누락(암시적 선언) | tdc_touch.c:199 `delay_ms(20)` | 선언처 = ci_util.h:22 유일. tdc_touch.c include(hw.h·ci_timer.h·ci_printf.h·LedOutput.h)가 ci_util.h 미전이(재검증: ci_timer.h·ci_printf.h가 ci_util/delay_ms 미참조, B5 53헤더 BFS 미도달). 플래그 `-std=gnu11`·**-Werror 부재**(subdir.mk:62 재검증) | 암시적 선언 경고만, 링크 정상(delay_ms는 ci_util.c 정의). `-Werror=implicit-function-declaration` 도입 시 [치명] 승격. 수정 1줄: `#include <ci_util.h>` |
| W3 | M3 빌드도구 물리삭제 미완 | main.c·tdc_ui_command.c `#if RTT_TUNING`/`SLEEP_MEASURE` 블록 | 99 §M3 "삭제" 문언 대비 #if 가드 컴파일제외만. tdc_ui_command.c:698~755·773~775 균형 `#if`/`#endif` 재검증, RTT_TUNING 미정의→0 | 컴파일 영향 0(가드 차단). C1과 동일 봉합으로 동시 해소 가능. tdc_ui_command.c:21~22 주석이 의도 명시 |
| W4 | iqs323.c 비트필드 union 0개 | tdc_touch_iqs323.c | 99 §1.2(2)는 union 2개 유지 명시, 실제 직접 비트연산(:327~329)·직접 바이트(:255) | 계획보다 더 간결(추상0 강화), 동작 영향 0 — 미달 아닌 초과달성 |
| W5 | Touch Hysteresis 바이트 배치 (carryover) | tdc_touch_iqs323.c:264 | hyst=8을 MSB 전체 바이트에 write → A.17 필드 bits[15:12]엔 hyst>>4=0만 반영. 단 삭제된 구 드라이버 `msb.touch_hysteresis:8 //[8:15]`와 byte-동일(carryover, 신규 오류 아님). 99 §6 실측게이트#3 기존 인지 | fabrication 아님. 실측 1회 판별로 해소 예정 |
| W6 | time.h TIMER_PRESCALE_128 자체완결 결여 | tdc_touch_time.h:42 | src 트리 define 정의처 0(재검증), SDK 의존. time.h include 0건. logic.c 미전개(재검증: 전개 0건)라 컴파일 무해 | 순수성 위반 아님. 헤더 위생 [경미]. 권고: "ci_timer.h 선행 include 필수" 명시 |

- **W7 [경미·정보성] 쿨다운 방식 치환**(B3): 정본 카운트-동결형 → 코드 절대시각-진행형(logic.c:142·145 재검증). read-hold/부팅무시 2초+ 직후 약간 일찍 재발행 가능하나 ati_active 2차 차단(:141) 유지로 폭주 방지 보존. load-bearing 무영향. **할루시네이션 아님 — 의도적 설계 치환**.
- **W8 [경미·정보성] Beta 주석 역방향**(B4): 주석(:273~280) "B1 느림>B2 빠름"이 데이터시트 공식 damping=Beta/256(해석_A)과 역방향. 값(B1=0x0808>B2=0x0202)은 [실측 게이트] 안전기본값(99 §6 #1, 해석_A/B 미확정) → fabrication 아님, 실측 1회 해소.

---

## 2. ② 기존 코드 영향 최종 판정 (빌드 가능 여부)

| 빌드 구성 | 컴파일/링크 | 판정 | 근거 |
|---|---|---|---|
| **기본 운용**(`SLEEP_MEASURE_MODE=0`·`RTT_TUNING` 미정의) | **가능** | **PASS** | 삭제심볼 라이브 참조 0(가드 밖 0건 재검증), 삭제헤더 라이브 include 0, 공개시그니처 17/17 결선(iqs323 10·touch 4·logic 3), main.c 비가드 호출처 전수 갱신(read_status struct out·apply_sleep·reseed), 전처리기 짝 균형·#error 비성립, include 순환 0 |
| **진단변종**(`SLEEP_MEASURE_MODE=1`) | **불가** | **FAIL** | C1 — 댕글링 2심볼 undefined |
| **튜닝변종**(`RTT_TUNING` 정의 시) | 불가(범위 외) | — | tdc_ui_command.c `tdc_iqs323_tuning_t t`(:725)·`tdc_drv_iqs323_*_tuning`(:713~740) undefined. 단 RTT_TUNING은 트리에 정의 자체가 없어 활성화 경로 부재 — 잠재 위험만 |

- **최종 판정**: **기존 코드 영향 = 기본 운용 빌드 0(빌드 가능)**. 회귀 가능 경로는 (1) 진단변종 빌드 활성화, (2) raw make로 스테일 subdir.mk 사용 — 둘 다 **개발/디버깅 시나리오 한정**, 운용 펌웨어 비발현.
- `src/2__cm3_factory/`는 git 미추적(`?? src/2__cm3_factory/`) 사본 — 리팩토링 스코프 밖, 영향 판정 제외.

---

## 3. ③ 계획 부합 여부

- **PASS** (B2 1:1 대조). ① 파일구성 8(3코어+헤더2), ② 공개시그니처 13종 전수, ③ 삭제목록 9종(CALIB·SLEEP_MEASURE 도구·RTT_TUNING·FIXED ATI·CalCap·방전·margin·prox·CRX1변종) 신규파일 부재, ④ 봉합 4건(시그니처 단일화·전역캐시 미신설·boot 가드 `read_ok && curr!=TOUCH`·get_state struct wrapper) 명문대로, ⑤ 시간상수 time.h 단일+파생, ⑥ 추상 0(vtable·port·mock·함수포인터 0).
- **미달 2건은 [경미]**: W3(M3 물리삭제 문언 미달, 컴파일 영향 0)·W4(union 초과달성). 치명적 계획 이탈 0.
- 단 계획 자체의 M3 "삭제" 정의가 **호출부 봉합을 누락 명시**했다면 그것이 C1의 근원 — 계획 ↔ 구현 정합은 맞으나 계획이 진단변종 정합성을 보장하지 못한 구조적 갭(아래 §4).

---

## 4. ④ 할루시네이션 적출 종합

- **할루시네이션(근거 없는 로직·레지스터값·날조 심볼) [치명]: 0건.**

| 영역 | 검증 | 결과 |
|---|---|---|
| 제어 6대(B3) | logic.c vs B99 E1~E10·S1~S3·99fullati 게이트 | 근거없는 로직·동작 누락·왜곡 0. 상태매핑/read-hold·Re-ATI 게이트 4조건·stuck 30/60/90·롱터치 2400·부팅무시 봉합-다·쿨다운 전부 정본 1:1 |
| 레지스터(B4) | iqs323.c vs 데이터시트 06/02 | 주소 15/15·System Status 비트 4/4·Power Mode 보존 0x54/0x58/0x50(재검증 :358/:364/:376/:382/:284)·ATI Full 0x040C·Conv 0x057F·events 0x52·CH timeout MSB 0x07 전부 1:1. 날조 주소·값 0 |
| 시그니처(B5) | 공개 .h↔.c 17/17 | 결선 일치, 가공 시그니처 0 |
| 순수성(B6) | logic 금지헤더 include 0 | 컴파일러 강제, 가공 의존 0 |

- 적출 2건(W5 Hysteresis·W8 Beta 주석)은 **fabrication 아님**: W5는 삭제된 구 드라이버와 byte-동일 carryover(99 실측게이트#3 기존 인지), W8은 [실측 게이트] 안전기본값의 주석↔공식 정합 불일치(실측 1회 해소). 둘 다 "근거 없는 값 주입"이 아니라 "기존 인지된 실측 미확정 갭".
- **6노드 주장 자체의 할루시네이션 검증**: §5 재검증 로그로 모든 핵심 주장을 코드/플래그 직접 대조 — 6노드 인용 [파일:라인] 전부 실측 일치, 노드 보고서의 할루시네이션 0.

---

## 5. 종합 재검증 로그 (코드/플래그 직접 대조)

본 종합이 6노드를 신뢰만 하지 않고 직접 확인한 사실(할루시네이션 차단):

1. **C1 댕글링**: `grep consume_sleep_request|sleep_measure_dump src/2__cm3/` → 각 main.c 호출 1건만, 정의/선언/신규 대체 API 0건. main.c:776~785·909~930 가드 구조 Read 확인. config.h:31 `#define ...SLEEP_MEASURE_MODE 0` 확인.
2. **삭제심볼 라이브 0**: `grep tdc_drv_iqs323 (Cortex-M3-src·Gen1_5, Debug 제외)` → main.c:927(가드)·tdc_ui_command.c:713~740(가드)·:21(주석)만. 가드 밖 라이브 참조 0.
3. **컴파일 플래그**: subdir.mk:62 `-std=gnu11`만, `-Werror`/`-Wall`/`-Wextra`/`-Wundef` grep 0건 확인 → 암시적선언·미정의#if 경고만, 빌드 비차단.
4. **subdir.mk 스테일**: :20·37·54 tdc_drv_iqs323.c/.o/.d 등재, 신규 .c 미등재 확인.
5. **delay_ms**: ci_util.h:22 유일 선언, tdc_touch.c include 목록에 ci_util.h 없음, ci_timer.h·ci_printf.h가 ci_util/delay_ms 미참조 확인.
6. **TIMER_PRESCALE_128**: src 트리 define 정의처 0건, time.h:42 참조, logic.c 전개 0건 확인.
7. **Power Mode 보존**: iqs323.c write_register 0xC0 = re_ati 0x54(:358/:376)·reseed 0x58(:364/:382)·beta_power 0x50(:284)·apply_sleep 0x00(:417)·ack 0x01(:215) 확인 — 단독 0x04/0x08 덮음 0.
8. **Re-ATI 게이트**: logic.c:140~146 4조건(노터치·!ati_active·ati_error·now_ms>=cooldown) Read 확인.

---

## 6. ⑤ 수정 우선순위 (코드 위치·수정안)

| 우선 | 항목 | 심각도 | 코드 위치 | 수정안 |
|---|---|---|---|---|
| **1** | C1 진단변종 댕글링 | [치명] | main.c:776~785(`consume_sleep_request`)·909~930(`sleep_measure_dump`) | **택1**: ⓐ 두 가드 블록(+`s_sleep_measure_mode`·`tdc_on_sleep_measure_cmd` main.c:319·323)을 일괄 물리 삭제(99 M3 "삭제" 문언 완수) — 진단도구를 신 구조에서 폐기. ⓑ 신 API로 대체: dump → `tdc_touch_iqs323_read_status()` 기반 재구현, consume → 신 sleep request 경로 신설. **권고 ⓐ**(계획 M3 의도 = 삭제, 진단도구 부활 필요 시 별도 작업) |
| 2 | W2 delay_ms 암시적 선언 | [경미]→승격가능 | tdc_touch.c:15~24 include | `#include <ci_util.h>` 1줄 추가 (`-Werror` 도입 대비 선제) |
| 3 | W1 subdir.mk 스테일 | [경미] | Debug/.../subdir.mk (자동생성) | 빌드 전 Eclipse 프로젝트 refresh/clean-rebuild 1회 (코드 수정 아님, 운영 절차) |
| 4 | W3 RTT_TUNING 죽은 호출 | [경미] | main.c·tdc_ui_command.c #if 블록 | C1 ⓐ와 동시 물리 삭제로 99 M3 완수(택). 또는 잔존 유지(컴파일 무해) |
| 5 | W6 time.h 헤더위생 | [경미] | tdc_touch_time.h:40~42 | 주석 "ULP 타이머 매크로는 ci_timer.h 선행 include 필수" 보강 |
| 6 | W5·W8 실측게이트 | [경미] | iqs323.c:264·273~280 | 실기 시험 write 1회로 Hysteresis 비트폭·Beta 해석 확정 후 값/주석 정정(99 §6 실측게이트#1·#3 절차) |
| — | W4 union 미사용 | [경미] | — | 무수정(초과달성, 동작 영향 0) |

---

## 7. 최종 결론 — 진짜 문제 없는지·PASS 조건

- **기본 운용 빌드 결함 0**: 컴파일·링크 차단 [치명] 0, 할루시네이션 [치명] 0, 계획 부합 PASS, 회귀 0. **운용 펌웨어는 진짜 문제 없음**.
- **진단 변종 빌드 결함 1**: C1 — `SLEEP_MEASURE_MODE=1` 시 댕글링 2심볼 undefined. 운용 비발현이나 **개발자가 진단모드를 켜는 순간 빌드 깨짐**이 실재 → 본 강화검증의 단일 [치명].
- **PASS 조건**: **우선순위 1(C1) 봉합 완료** = PASS. 권고로 W2(delay_ms 1줄)·W1(refresh) 동반. [경미] 나머지(W3~W8)는 PASS 차단 아님(스타일·실측게이트·문서갭).

**verdict: FAIL** — 진단변종 [치명] C1 미수정 기준. **C1 봉합 시 PASS**(기본 운용 빌드는 이미 결함 0). 할루시네이션 0 확정.

---
name: B2 계획부합 검증 (Rev2 강화)
purpose: 99 Rev2 간결구조 설계와 실제 간결 3파일 구현을 1:1 대조해 파일구성·시그니처·삭제목록·봉합4건·시간상수·추상0의 계획 이탈/누락을 적출
type: 에이전트-로그
maturity: in-progress
tags: [touch, iqs323, refactor, rev2, verification, plan-conformance, signature, seam]
---

# B2 계획부합 검증 (Rev2 강화)

**TL;DR**: 99 Rev2(`99_종합_간결구조.md`) 설계와 실제 구현을 1:1 대조. ① 파일구성(3코어+헤더2=8) 일치, ② 공개 시그니처(logic 3·iqs323 6+보조4·touch 4) 전수 일치, ③ 삭제목록(CALIB·SLEEP_MEASURE 도구·RTT_TUNING·FIXED ATI·CalCap·방전·margin·ATI_MULT/COMP·tuning·prox) 신규파일에서 전부 부재, ④ 봉합 4건(시그니처 단일화·전역캐시 미신설·boot 가드 `read_ok && curr!=TOUCH`·get_state struct wrapper) 명문대로 적용, ⑤ 시간상수 time.h 단일·파생매크로 4종, ⑥ 추상 0(vtable·port·mock·adapter·함수포인터 0). **verdict PASS**. [경미] 2건: (가) M3 계획이 명시한 빌드도구 블록 물리 삭제가 main.c/ui_command.c 에서 미완(가드 `#if`로 컴파일 제외만, `tdc_drv_iqs323_*` 죽은 호출 잔존 — 컴파일은 됨), (나) iqs323.c 비트필드 union 0개(계획은 2개 유지 명시 — 더 단순화된 방향). 치명 0. 코드 근거 [파일:라인].

---

## 0. 범례·대조 기준

- 정본: `99_종합_간결구조.md`(이하 99) §1~§7. 실제: 변경/신규 8파일 + main.c + tdc_ui_command.c.
- 약어: `L`=tdc_touch_logic.{c,h}, `I`=tdc_touch_iqs323.{c,h}, `T`=tdc_touch.{c,h}, `TM`=tdc_touch_time.h, `CFG`=tdc_touch_config.h, `M`=main.c, `UI`=tdc_ui_command.c.
- 라벨: [일치]=설계=구현 · [경미]=빌드/회귀 무영향 이탈 · [치명]=빌드/회귀 영향. [미명시]=정본 무규정.
- 본 노드(B2) 스코프는 **계획 부합**. 빌드 정합 심화는 B5, 기존코드 영향은 B1 소관 — 중복분은 교차참조만.

---

## 1. 파일 구성 대조 (99 §1.1)

| 99 설계 | 실제 | 판정 | 근거 |
|---|---|---|---|
| 3코어(.c/.h) + 헤더 2 = 8파일 | `tdc_touch{,_logic,_iqs323}.{c,h}` + `tdc_touch_time.h` + `tdc_touch_config.h` = 8 | [일치] | `ls tdc_touch*.*` = 정확히 8 |
| `tdc_drv_iqs323.c/.h` 삭제 | 디스크 부재, `git status` `D ...tdc_drv_iqs323.{c,h}` | [일치] | 파일시스템 + git 양측 확인 |

- 99 §1.1 의 "3파일 확정"(연결 별도)·헤더 2 분리 그대로 구현. 합본 2파일 옵션은 채택 안 함 — 99 결정과 일치.

## 2. 공개 시그니처 대조 (99 §1.2)

### 2.1 logic 공개 3 (99 §1.2(1))

| 99 시그니처 | 실제 [L.h] | 판정 |
|---|---|---|
| `void tdc_touch_logic_init(state*, uint32_t now_ms)` | `:80` 동일 | [일치] |
| `void tdc_touch_logic_set_boot_ignore(state*, uint32_t now_ms)` | `:83` 동일 | [일치] |
| `void tdc_touch_logic_step(state*, const in*, out*)` (cfg 인자 없음) | `:86~88` 동일, cfg 인자 0 | [일치] |

- enum/struct 도 99 §1.2(1) 그대로: `tdc_touch_act_t` 5값(NONE/HOLD/RE_ATI/RESEED/MCLR `TDC_TOUCH_ACT_*` 접두) [L.h:24~31], `tdc_touch_boot_event_t` 4값 [L.h:34~40], `in` 5필드(now_ms/read_ok/pressed/ati_error/ati_active) [L.h:43~50], `out` **5필드**(action/long_touch/curr_state/state_changed/boot_event) [L.h:53~60], `logic_state` 10필드 [L.h:63~75]. **봉합-가 out 5필드 확정**과 1:1.

### 2.2 iqs323 공개 6 + 보조 (99 §1.2(2))

| 99 동작 | 실제 [I.h] | 판정 |
|---|---|---|
| 1 `read_status(status_t*)` struct 4-tuple | `:68` 동일 (struct ok/pressed/ati_error/ati_active [I.h:56~62]) | [일치] |
| 2 `apply_settings(void)` | `:72` 동일 | [일치] |
| 3 `re_ati(void)` | `:75` 동일 | [일치] |
| 4 `reseed(void)` | `:78` 동일 | [일치] |
| 5 `mclr(void)` | `:81` 동일 | [일치] |
| 6 `is_ati_done(void)` | `:84` 동일 | [일치] |
| 보조1 `apply_sleep_settings(void)` | `:90` 동일 | [일치] |
| 보조2 `set_ulp/clear_ulp/is_ulp` | `:92~94` 동일 | [일치] |

- 99 §1.2(2) "read API struct 4-tuple(3포인터 폐기)" 확정 그대로. 3포인터 시그니처 잔존 0 (신규 I 파일 내).

### 2.3 touch 공개 4 (99 §1.2(3))

| 99 시그니처 | 실제 [T.h] | 판정 |
|---|---|---|
| `void tdc_touch_init_begin(void)` | `:42` 동일 | [일치] |
| `bool tdc_touch_process(void)` | `:49` 동일 | [일치] |
| `bool tdc_touch_get_state(tdc_touch_state_t*)` | `:53` 동일 | [일치] |
| `const char *tdc_touch_state_name(tdc_touch_state_t)` | `:56` 동일 | [일치] |

- 99 "공개 API 3종 시그니처 100% 불변(main.c 호출처 0변경)" — 4종 모두 불변(99 본문 "3종"은 state_name 제외 표기, §7.2 표는 4종 일관). main.c 호출처 변경 여부는 B1 소관.

## 3. 삭제 목록 대조 (99 §5 M3·§7.2)

신규 8파일에서 99 삭제 대상 심볼 전수 grep — **전부 부재**(잔존 매치는 전부 (가) 호환 유지 enum (나) 토글 보존 (다) 삭제를 서술한 주석 3종).

| 99 삭제 대상 | 신규파일 잔존 | 판정 |
|---|---|---|
| ATI_CALIB 모드·calib_candidate 테이블·calib_find/read/re_ati | 0 | [일치] |
| SLEEP_MEASURE_MODE 덤프 함수 | 0 (`CFG:30~31` 은 **토글 보존**, default 0 — 99 §1.1 "빌드 토글만" 부합) | [일치] |
| RTT_TUNING tuning 구조·set/show/clear/apply | 0 | [일치] |
| FIXED ATI(write_ati_compensation·ATI_MULT/COMP·보드변종 보상값) | 0 (`I.c:355` 은 "FIXED 제거" 서술 주석) | [일치] |
| CalCap 더미채널(PATTERN_DEF·cal_cap_rx/tx) | 0 (`I.c:243` 은 "CalCap 더미/방전 없음" 서술 주석) | [일치] |
| CRX0 방전(discharge_crx0·write_register_discharge) | 0 | [일치] |
| touch margin(read_touch_margin) | 0 | [일치] |
| prox_settings·PROX threshold | 0 | [일치] |
| CRX1 변종(FLOATING/VSS/REF/DUMMY) | 0 | [일치] |

- `TDC_TOUCH_STATE_CALIBRATION_ERROR` enum [T.h:32] 잔존은 99 §1.2(3)·코드 주석 "(미사용) enum/문자열 호환 위해 유지"와 **의도적 일치** — 삭제 대상 아님.

> [!IMPORTANT]
> **[경미-가] M3 빌드도구 물리삭제 미완 (변경파일 M·UI)**: 99 §5 M3 는 "빌드도구 3종(RTT_TUNING·ATI_CALIB·SLEEP_MEASURE) 삭제"를 명시했으나, **신규 I 파일에서만 삭제**됐고 `M`·`UI` 의 호출 블록은 **물리 삭제 대신 `#if` 가드로 컴파일 제외**만 됨. 잔존 죽은 호출: `M:927 tdc_drv_iqs323_sleep_measure_dump()`(`#if TDC_TOUCH_SLEEP_MEASURE_MODE` 내, `CFG:31` 0 정의 → 제외), `UI:713~740 tdc_drv_iqs323_{show,clear,apply}_tuning/set_{normal,sleep}_tuning`(`#if TDC_TOUCH_RTT_TUNING` 내, 매크로 **미정의 → `#if`에서 0** → 제외), `M:962/UI:773 #if TDC_TOUCH_RTT_TUNING` 블록도 동일. **컴파일 영향 0**(가드로 차단, `UI:21~22` 주석이 의도 명시)이나, M3 "삭제" 문언 대비 **잔존 = 계획 미달**. 빌드 정합 정밀 확인은 B5, 죽은 심볼 회수는 B1 교차. `src/2__cm3_factory/` 의 동일 잔존은 **본 리팩토링 스코프 밖 untracked 사본**(git `?? src/2__cm3_factory/`)이라 B2 대상 아님.

## 4. 봉합 4건 대조 (99 §1.2 CAUTION·§7.4)

| 봉합 | 99 확정 | 실제 | 판정 |
|---|---|---|---|
| **가** 시그니처 단일화 | step `(state,in,out)` cfg 폐기·in `now_ms`·out 5필드·struct read·`TDC_TOUCH_ACT_*`·04 로그 `out.boot_event`/`curr_state` 재작성 | step 시그니처 [L.h:86] · in.now_ms [L.h:45] · out 5필드 [L.h:53~60] · 연결층 로그가 `out.state_changed`+`out.curr_state` [T.c:154~159]·`out.boot_event` switch [T.c:160~179] 사용 | [일치] |
| **나** 전역 ATI 캐시 미신설 | `s_last_ati_*` 신설 안 함 | `s_last_ati`/`last_ati` grep 0 (L·I·T) | [일치] |
| **다** boot_ignore 해제 가드 | `in->read_ok && curr != TOUCH` 명문 | `L.c:119` `if (in->read_ok && curr_state != TDC_TOUCH_STATE_TOUCH)` 정확 일치 | [일치] |
| **라** get_state struct wrapper | `read_status` struct 위 wrapper(ati 무시·ULP 게이트 없음) | `T.c:219~229` `read_status(&st)` 후 `st.pressed ? TOUCH : NOT_TOUCH`, ati 미참조·ULP 게이트 없음 | [일치] |

- 봉합-가 핵심(99 §1.2(3) IMPORTANT "Rev.1 8필드 `out.prev_state`/`boot_released_log`/`boot_ignore_led_request` 참조 금지"): 연결층 `T.c` 가 Rev.1 8필드 0참조, 직전상태는 연결 자체 보관 `s_log_prev_state` [T.c:39,158] — 99 재작성 지시 그대로.

## 5. 시간상수 단일화 대조 (99 §3)

| 99 §3 | 실제 [TM] | 판정 |
|---|---|---|
| `TDC_TIME_TO_CNT(ms,interval)` 올림 파생 | `:15` 동일식 | [일치] |
| 노말 ms 상수(POLL/LONG/INIT/BOOT_WARN/STUCK/COOLDOWN/HOLD) | `:18~26` 7종 동일값(200/2400/2500/5000/30000/2000/2000) | [일치] |
| 파생 카운트 2종(COOLDOWN_CNT·READ_FAIL_HOLD_CNT) | `:28~31` `TDC_TIME_TO_CNT(...,POLL)` | [일치] |
| ULP(WAKE/LONG_TOUCH/CNT·PRESCALE/TIMEOUT) | `:34~43` 동일(200/2200, CNT=11, TIMER_PRESCALE_128/62) | [일치] |
| 0-division 가드 | `:46~48` `#if (POLL==0)||(WAKE==0) #error` | [일치] |
| `M:794~805` ULP `#define` 이관 | `M` 로컬 ULP `#define` 0 (grep 0) — TM 단일 소유 | [일치] |

- 99 §3.1 파생 등가성(`TO_CNT(2200,200)=11`·`(2000,200)=10`) 식 그대로. 실측 등가 검증은 B6/실기 소관.

## 6. 추상 0 대조 (99 §1.1 IMPORTANT·§1.2)

| 99 금지 | 실제 | 판정 |
|---|---|---|
| 포트 헤더·vtable·mock·어댑터·L1/L2/L3 명명 0 | `vtable`/`mock`/`adapter`/`->ops`/`_port_`/함수포인터 `typedef.*(*` grep = `I.c:8` 주석 1건("함수 포인터/vtable 0" 서술)뿐, 실 심볼 0 | [일치] |
| logic 순수성(hw/ci_timer/ci_printf/iqs323/Led include 금지) | `L.c` include = `tdc_touch_logic.h`+`tdc_touch_time.h`만 [L.c:12~13], `L.h` = stdbool/stdint/`tdc_touch.h`만 [L.h:19~21] — 금지 include 0 (컴파일러 강제) | [일치] |
| "추상의 전부 = 연결층 `switch(out.action)`" | `T.c:182~209` action switch 1덩어리 | [일치] |

- 내부 static 계층(99 §1.2(2) 평평 3층): `i2c_write/read → wait_window_closed/force_window_open → write_register/read_register` [I.c:45~208] 위→아래 단방향, 시퀀스 보조 `ack_reset/confirm_reset/sensor_setup/touch_settings/events_enable/beta_power_settings/wait_ati_done_blocking` [I.c:213~307] — 99 명세와 일치. logic static `stuck_eval` 1개 [L.c:17] — 99 "내부 static 1개" 그대로. touch static `try_finish_init` [T.c:69] + 연결소유 5 static [T.c:35~39] — 99 §1.2(3) 일치.

> [!NOTE]
> **[경미-나] 비트필드 union 2개 미사용 (더 단순화 방향)**: 99 §1.2(2) 는 "비트필드 union `reg_system_status`·`reg_sensor_setup` 2개만 .c 내부 유지, 나머지 직접 바이트 write"로 명시. 실제 `I.c` 는 **union 0개** — status 디코드도 `(lsb & (1u<<5))` 직접 비트연산 [I.c:327~329], setup 도 직접 바이트 [I.c:255]. 계획 대비 union 2개 누락이나 **방향이 더 간결**(추상 0 정신 강화)이고 동작 영향 없음 — 계획 미달이 아니라 **간결화 초과 달성**. 레지스터 비트 매핑의 정합(0xC0 LSB/MSB 등)은 B4 할루시네이션 소관.

## 7. 매핑·마이그레이션·테스트경계 (99 §2·§4·§5)

- 99 §2 제어 8대 ↔ 파일/함수 매핑: 부팅(`T:try_finish_init`+`I:mclr/is_ati_done/apply_settings`)·노말폴링게이팅(`T:process`)·상태매핑/hold(`L:step`)·롱터치(`L:step out.long_touch`)·Re-ATI게이트(`L:step` 4조건 [L.c:140~146]→`T:re_ati`)·stuck3단계(`L:stuck_eval`→`T:reseed/re_ati/WDT_RESET` [T.c:190~202])·Beta/Power(`I:beta_power_settings`)·단일채널(`I:sensor_setup`)·절전(`M:ULP`+`I:apply_sleep_settings/get_state`) — 99 §2 표와 함수 단위 1:1. 동작 동등(이벤트표 E1~E10·S1~S3)은 B3/실기 소관, B2는 **배치 일치**만 확인.
- 99 §5 M1~M6 산출물: M1(TM)·M2(L)·M3(I)·M4(T)·M5(CFG)·M6(실기) 중 **M1~M5 코드 산출물 전부 디스크 존재**. M3 빌드도구 삭제 부분미달은 §3 [경미-가]. M6 실기 게이트는 미완(실측 영역).
- 99 §4 테스트 경계: 순수 FSM(L) 호스트 테스트 가능 구조(전역/static 0, in→out 결정론) — `L.c` 전역 0·static 함수 1(stuck_eval, 상태는 인자) 확인. 테스트 코드 작성은 99대로 향후(밸리데이션 팀) — 미작성이 계획 이탈 아님.

## 8. 적출 종합

| # | 심각도 | 항목 | 근거 | 계획 대비 |
|---|---|---|---|---|
| 1 | [경미] | M3 빌드도구 블록 물리삭제 미완 — `M:927`·`UI:713~740`·`M:962`/`UI:773` 죽은 `tdc_drv_iqs323_*` 호출이 `#if`(미정의/0) 가드로 컴파일 제외만, 물리 삭제 안 됨 | §3 IMPORTANT | 99 §5 M3 "삭제" 문언 미달(컴파일 영향 0) |
| 2 | [경미] | iqs323.c 비트필드 union 2개 미사용 — 99 명시(2개 유지) 대비 0개, 직접 비트연산 | §6 NOTE | 간결화 초과(미달 아님), 동작 영향 0 |

- **치명 0**. 파일구성·시그니처(13종 전수)·삭제목록(9종)·봉합 4건·시간상수·추상 0 — 계획과 1:1 부합.
- [경미] 2건 모두 **빌드/회귀 무영향**: #1은 죽은코드 잔존(가드 차단), #2는 계획보다 더 단순한 방향. 정밀 빌드 정합은 B5, 레지스터 비트 정합은 B4, 죽은 심볼/사본 회수는 B1 교차.

**verdict: PASS** (치명 0, 경미 2 — 계획 부합 충족).

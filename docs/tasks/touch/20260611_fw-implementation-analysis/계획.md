---
name: fw-implementation-analysis-계획
purpose: 펌웨어 분석 문서 구성·작성·검증 전략
type: 계획
maturity: stable
tags: [touch, iqs323, firmware, study, plan]
---

# 계획 — IQS323 터치 펌웨어 구현·시퀀스 분석

**TL;DR**: `개선/펌웨어 구현·시퀀스 분석.md`(14절: 개요~SW 쟁점) 작성. 코드 직접 Read ground truth, 레지스터값 데이터시트 교차, 추정 명시. 서브에이전트 fan-out 검증(코드 정합·대조·추정·구조). 승인 plan: 루트 `docs/plans/eventual-mixing-sphinx.md`.

## 문서 구성

개요(2계층) → 파일·함수맵 → 빌드설정 → 초기화 시퀀스 → apply_settings 9단계 → 채널설정(sensor_setup) → ATI FIXED → 200ms 방전 → 터치 판정·마진 → 롱터치 FSM → 전원모드 연계(노말/절전 2단계) → 통신(폴링+RDY) → 요구사항·데이터시트 대조 → SW 쟁점.

## 작성 방식

- 코드 사실=직접 Read 라인. 레지스터값은 데이터시트 [`06`] 비트와 교차. 추정·미확인 "추정/확인 필요".
- 코드 내부 불일치(주석 vs define/상수)는 관찰로 기록.

## 검증 (fan-out)

① 코드 정합(touch / drv / config / main 분할, 실제 코드 재대조, 레지스터값·라인·상수 1건 불일치=치명) ② 요구사항·데이터시트 대조 ③ 추정 표기(할루시네이션) ④ 구조. 치명·중 0까지 루프(≤3).

## 완료 기준

문서 생성·코드 100% 일치·대조 정확·할루시네이션 0·개선안 단정 없음·README 갱신·broken link 0.

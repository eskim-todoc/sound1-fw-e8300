---
name: 외부 레퍼런스 통합 분석 이력 및 결과
purpose: 본 작업의 시계열 로그와 완료 요약
type: tasks-이력
maturity: in-progress
tags: [touch, iqs323, reference, 이력]
---

# 외부 레퍼런스 통합 분석 — 이력 및 결과

**TL;DR**: Azoteq 외부 자료 4종을 v3 제어 방향에 맞춰 정독·매핑·정리하는 작업의 시계열 로그. 단계 ①~④ 진행 기록과 최종 요약을 누적한다.

---

## 시계열 로그

### 2026-06-19

- **착수**: 은수님 지시 — `docs/참고/touch`에 추가한 외부 자료(example-code·AZD004·AZD125 + User Guide)를 현 제어 운영 방향에 맞춰 분석·정리. 개발/구현 참조용. 페르소나 적절량.
- **명령 해석**: 분석 범위 모호 → 질문. 은수님 답 = "지목 3종 + User Guide v1.1"(4종). AZD115(유도)·AZD135(착용) 제외.
- **환경 파악**: `pdftotext -layout`로 4종 텍스트 추출(azd004 1877줄·azd125 2057줄·userguide 1293줄·arduino 252줄). `.ino` 298줄. 작업 목록.md 미존재(폴더 운영).
- **자료 구조 확인**: azd004 §4(Signal Conditioning: MULT/COMP/ATI 4단계)·§5(Filtering)·§6(Linearised Counts·민감도)·§7(Debounce/Hysteresis). azd125 §5(Grounding/Battery)·§8(Temperature·Follow UI)·§9(Water). → 미해결 3항목(ATI 실패·환경 민감도·드리프트 자동보정)의 답 후보 식별.
- **단계 ① 완료**: `요구사항.md`·`이력 및 결과.md` 작성.
- **단계 ② 착수**: 오케스트레이터 입력 작성 후 Workflow fan-out(정독 5 + 매핑 2 + 검증 2 + 종합 1).
- **Workflow 1차 실패**: 전 노드 `API Error 400` — schema property 키가 한글(파일경로 등)이라 `^[a-zA-Z0-9_.-]` 패턴 위반. → 키를 영문(file_path/findings/unresolved/implication)으로 교체, 참조 코드 2곳 동반 수정 후 재실행.
- **Workflow 2차 성공**: 10노드 완료(775k 토큰, 75 tool use, ~12분). 전 노드 `.md` 생성, 한자 0(grep clean).
- **핵심 성과**: 온도 드리프트 정량 발견 — AZD125 §8.2.4 실측(25→50°C 센서 −450cnt, 약 −18cnt/°C, **방향=카운트 감소**). 그동안 "데이터시트 미명시"였던 미해결_2를 부분 해소.
- **검증 결과**: 08 할루시네이션 노드가 정독 인용 50여 건 원문 대조 → [확인됨]42·적출 8([과장]3·[근거없음]1·[섹션오류]4). 핵심 적출=정독의 "수분 카운트 급증"을 자기 커패시턴스 방향과 반대인 [방향 오류]로 잡아 **감소로 정정**. 99 종합이 전부 반영.
- **v3 판정**: D1~D5 + Follow UI 미채택 전부 **보강**(반증 0). 아키텍처 수정 불필요, 구현 세부 정정 2건(수분 방향·halt timeout 조건부 표현)만 문서·주석 레벨.
- **단계 ② 완료**: `분석.md`·`에이전트-로그/인덱스.md` 작성.
- **단계 ③ 착수**: `계획.md` 작성 — 영속 참고 문서 위치·구조 + 직전 작업 반영 단계화.

- **단계 ③ 승인**: 은수님 결정 — 영속 문서 위치=**신규 폴더 `적용가이드/`**, 직전 작업(20260618) **함께 반영**.
- **단계 ④ 구현**:
  - ④-1 영속 문서 `참고/touch/적용가이드/IQS323-제어운영-적용가이드.md`(§1~§10 + 개발 체크리스트) 작성.
  - ④-2 직전 20260618 분석.md §10에 외부 앱노트 보강 소절 추가(온도 정량·Follow UI 적용불가·D2 두 자료 독립 근거). **정정 대상 부재 확인** — 직전 문서의 "급증"은 전부 "손 뗌 counts 급상승"(올바름), 정정 불필요.
  - ④-3 인덱스 갱신: `참고/touch/README.md`(적용가이드·개선 폴더 추가), `적용가이드/README.md` 신설.
- **단계 ④-4 검증**: 기계검증(한자 0·링크 OK·frontmatter OK) PASS. adversarial 2노드 — **원문대조 PASS**(인용 19건 전수 [확인됨], §2.3 인과 주장 경미 과장 1건), **정합성 FAIL**(절전 시퀀스 순서 역전 등 3건).
  - **정정**: 영속 문서 §6·§9.3 절전 순서를 코드 실제 순서(`main.c:944~952`: apply_sleep_settings→ci_power_sleep→i2c_prescale→reseed, **RESEED는 클럭 강하 이후**)로 수정. §3.2 단정 완화, §2.3 인과 주장 완화.
  - **검증 노드 false positive 적발**: 정합성 노드가 "20260618 §10도 순서 역전"이라 지적했으나 grep 확인 결과 §10 본문(line 33·52~53)은 올바른 순서를 정확히 기록 → 정정 불필요. 오류는 영속 문서/99 한정.
  - 재검증: 핵심 결함(절전 순서)을 코드 ground truth로 확정 정정, 나머지는 표현 완화(자명) → 재 fan-out 생략, 기계 재검사로 한자 0 확인.

---

## 완료 요약 (2026-06-19)

**변경**: 영속 참고 문서 1종(`적용가이드/IQS323-제어운영-적용가이드.md`) + 폴더 README + 상위 README 갱신 + 직전 §10 보강. tasks 4단계 산출물 + 에이전트 로그 12파일(00·01~09·99·인덱스).
**검증**: 10노드 분석 Workflow + 2노드 검증 Workflow. 원문대조 PASS(인용 19건 [확인됨]). 한자 0·링크 전수 OK. 절전 순서 결함 1건 코드 대조로 정정.
**핵심 성과**: ① 온도 드리프트 정량 발견(AZD125 §8.2.4, −18cnt/°C 감소 방향) — 미해결_2 부분 해소. ② Follow UI 적용불가 확정 → 노터치 게이트 Re-ATI가 유일 능동 보정 수단. ③ v3 D1~D5 전부 보강(반증 0), D2는 두 자료 독립 근거. ④ 데이터시트 확인 잔여 4과제 명시.
**비범위/후속**: 제품 코드 변경 없음. 데이터시트 확인 4항목(포화 한계·Disabled Reseed/halt·Filter Beta 기본값)은 §10 후속 과제. Phase −1 버그 수정은 별도 작업 유지.

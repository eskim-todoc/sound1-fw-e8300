---
name: 오케스트레이터 입력 — func_sleep() 구조 정리
purpose: 분석·계획·검증 페르소나 fan-out의 공유 컨텍스트(코드 ground truth·확정 답변·페르소나 임무 정의)
type: tasks/에이전트-로그
applies_to: [Sound1]
maturity: stable
tags: [touch, sleep, refactor, orchestration, persona, 상태:진행]
---

# 오케스트레이터 입력

**TL;DR**: 페르소나 5(분석) + 1(계획) + 2(검증) = 8노드 fan-out. `func_sleep()` 순수 구조정리(동작 100% 동일)를 위한 코드 ground truth와 확정 답변을 공유 컨텍스트로 각 노드에 주입. 구현은 계획 승인 후 별도.

## 목표

`main.c`의 `func_sleep()`을 다음 4축으로 리팩토링 설계:
1. 모든 지역변수를 함수 최상단으로 이동
2. 디버그 메시지 최소화 + `ci_printe` 금지, 최대 에러 상태만 `ci_printw`, 나머지 전부 `ci_printd`
3. 주석 정리(WHY만 남기고 WHAT 제거)
4. while(1) ULP 루프 바디를 이름 있는 정적 함수로 분리 → 루프 자체는 한눈에 파악 가능한 짧은 호출 시퀀스로

**절대 제약: 동작(로직)은 100% 동일해야 한다.** 이것은 순수 구조 리팩토링이며 버그 수정·기능 추가가 아니다.

## 확정 답변 (인터뷰 2026-07-02)

- 동작 변경 범위: **순수 구조정리만**. 직전 세션에서 논의한 LED 표시 공백(ATI 재시도 중 LED 없음·복구 성공 시 미소등)은 이번 범위에서 **제외**.
- 적용 범위: **`func_sleep()`만**. `fake_func_sleep()`(무선 테스트용 쌍둥이 함수, `main.c:810~1031`)은 제외.

## 코드 ground truth

### `func_sleep()` 전문 (`main.c:1033~1219`, 현재 uncommitted 상태 포함)

```c
int func_sleep(void)
{
    int ret;

    SYS_WATCHDOG_REFRESH(); /* Refresh the watchdog at very first time */

    /* 절전 노터치 baseline RESEED 는 ULP 루프 내 '첫 NOT_TOUCH 시 1회'로 이동했다(아래).
     * 진입 초입의 무한 'WAIT TOUCH RELEASE' 루프는 손 미해제·임계 오인 시 무한 스턱이라 제거. */

    ci_printi("[INFO] CM3 IS PREPARING TO ENTER SLEEP MODE \r\n");

    ci_printi("[INFO] FIRST OF ALL, TRY TO POWER OFF FPGA BACKTRL H/W \r\n");

    // FPGA의 백텔 하드웨어 전원 OFF를 위한 구문이다.
    // I2C 레지스터의 sw_reset 만으로도 백텔 하드웨어 전원 OFF가 되는지 확인이 필요하다.

    ret = (int) write_FPGA_reset();
    ci_printi("[LP] FPGA reset : %s \r\n", ret ? "success" : "failed");

    cfx_cm3_sharedMemoryAll.systemShare.enter_ULP_mode_Command_CM3_to_CFX = 1; /* Command CFX to enter ULP mode */

    ResetNRF();                                     /* Reset nRF */
    NRF_Off_Command();                              /* Disable nRF */
    snd_qcc_set_mode(SND_QCC_MODE_SHUTDOWN);        // QCC 셧다운
    Sys_GPIO_Set_Low(DIO_PIN_INDEX_for_FPGA_SLEEP); /* Disable FPGA */
    OnOff_3V_PMIC_CM3_to_CFX(false);                /* Disable 3.3V, 1.2V PMIC */
    turnOffLED();                                   // LED 끄기

#if 1
    while (1) /* Wait for the CFX to enter ULP mode */
    {
        if (cfx_cm3_sharedMemoryAll.systemShare.enter_ULP_mode_Command_CM3_to_CFX == 0)
        {
            ci_printi("[LP] CFX : sleep \r\n");
            break;
        }
    }
#endif

    /* 절전 IQS323 설정은 노말과 동일하게 유지(전용 sleep settings 제거 — 운용 임계 그대로,
     * is_ulp 플래그 미사용). CM3 클럭만 ci_power_sleep 로 절감한다. */

    // Uninitialize(); /* Disable peripherals and DIOs */

    ci_power_sleep(); /* SYSCLK 30.72M → 2.56M, SLOWCLK 유지 */
    // ci_fake_power_sleep();

    i2c_set_master_prescale(I2C_MASTER_PRESCALE_6 /*I2C_MASTER_PRESCALE_21*/); /* SCL ~ 122 kHz 유지 (저속 방지) */
    // i2c_set_master_prescale(I2C_MASTER_PRESCALE_240); /* 본래 노말 모드 설정 그대로 */

    /* RESEED 는 ULP 루프 내 '첫 NOT_TOUCH 시 1회'로 이동(손 떼야 절전 노터치 baseline 동기). */

    ci_timer_init_prescaled(TDC_TOUCH_ULP_TIMER_PRESCALE, TDC_TOUCH_ULP_TIMER_TIMEOUT_VALUE); /* ~ 100 ms 주기 */

    SYS_WATCHDOG_REFRESH();

    /* 절전 ULP 터치 감시 (부팅 boot_ignore 철학):
     *  - sleep_ignore 구간: 첫 '400ms(2폴링) 연속 노터치' 확정 전까지 터치 무시(손 댄 채 진입 대응).
     *    확정 시 RESEED 1회로 절전 노터치 baseline 동기 후 게이트 해제.
     *  - 노터치가 10초 넘게 확정 안 되면(계속 터치/오염) 강제 RESEED 로 현재 상태를 baseline 끌어와 탈출.
     *  - 게이트 해제 후 '400ms 연속 터치' 시 재부팅 → 노말 모드 복귀(30/60/90 stuck 불요). */
    bool              sleep_ignore         = true; /* 첫 노터치 확정 전 터치 막힘 */
    int               notouch_cnt          = 0;    /* 연속 NOT_TOUCH 샘플 (게이트 해제 기준) */
    int               ignore_elapsed       = 0;    /* 게이트 지속 샘플 (10s 강제 RESEED 기준) */
    int               touch_cnt            = 0;    /* 게이트 해제 후 연속 TOUCH 샘플 (재부팅 기준) */
    tdc_touch_state_t ulp_state_prev       = TDC_TOUCH_STATE_RESET;
    int               ati_error_reboot_cnt = 0;

    while (1)  // ULP loop
    {
        SYS_WAIT_FOR_INTERRUPT; /* ULP_WAKE_MS 동안 idle */
        SYS_WATCHDOG_REFRESH(); /* 워치독 3.28s 대비 매 웨이크업마다 refresh */

        tdc_touch_iqs323_status_t st;
        bool                      ok    = tdc_touch_iqs323_read_status(&st);
        tdc_touch_state_t         state = st.pressed ? TDC_TOUCH_STATE_TOUCH : TDC_TOUCH_STATE_NOT_TOUCH;

#if (TDC_TOUCH_DEBUG_PRINT_ENABLE)
        /* 절전 ULP 계측 (노말 폴링과 동일 포맷) — LTA/Counts/절대임계/밴드초과 */
        if (ok)
        {
            tdc_touch_iqs323_debug_t dbg;
            if (tdc_touch_iqs323_read_debug(&dbg) && dbg.ok)
            {
                uint16_t delta    = (dbg.lta > dbg.counts) ? (uint16_t) (dbg.lta - dbg.counts) : 0;
                uint16_t abs_thr  = (uint16_t) (((uint32_t) TDC_TOUCH_IQS323_THRESHOLD * dbg.lta) / 256u);
                uint16_t pabs_thr = (uint16_t) (((uint32_t) TDC_TOUCH_IQS323_PROX_THRESHOLD * dbg.lta) / 256u);
                ci_printd("[T] LTA=%3u  CNT=%3u  D=%3u  THR=%3u  (k=%3u  H=%3u)  %s   PTHR=%3u (pk=%3u)  %s \r\n", dbg.lta, dbg.counts, delta, abs_thr, TDC_TOUCH_IQS323_THRESHOLD, TDC_TOUCH_IQS323_HYSTERESIS, (state == TDC_TOUCH_STATE_TOUCH) ? "T" : ".", pabs_thr, TDC_TOUCH_IQS323_PROX_THRESHOLD, st.prox ? "P" : ".");
            }
        }
#endif

        /* 절전 ATI 에러 복구(나안): 직접 Re-ATI 없이 재부팅 → 노말 복귀해 Re-ATI 게이트로 복구.
         * ati_active(정상 ATI burst) 중에는 제외, 실제 에러(!ati_active && ati_error)만. */
        if (ok && st.ati_error && !st.ati_active)
        {
            if (5 < ati_error_reboot_cnt)
            {
                ci_printw("\r\n[TOUCH] SLEEP: ATI ERROR -> REBOOT (recover in normal) \r\n");

                turnON_RedLED();
                delay_ms(250); /* RTT 드레인 */
                SYS_WATCHDOG_REFRESH();
                delay_ms(250);
                SYS_WATCHDOG_RESET();
            }
            else
            {
                // ati 에러 발생 시 한번 복구를 시도해본다.
                ci_printw("\r\n[TOUCH] SLEEP: ATI ERROR -> RECOVER (in sleep) \r\n");
                tdc_touch_iqs323_re_ati();
                ati_error_reboot_cnt++;
            }
            //delay_ms(20); /* RTT 드레인 */
            //SYS_WATCHDOG_RESET();
        }
        else if (ok && !st.ati_error && !st.ati_active)
        {
            ati_error_reboot_cnt = 0;
        }

        if (sleep_ignore)
        {
            ignore_elapsed++; /* read 성패 무관 게이트 지속 시간 */

            if (ok && state == TDC_TOUCH_STATE_NOT_TOUCH)
            {
                notouch_cnt++;
                if (notouch_cnt >= TDC_TOUCH_ULP_NOTOUCH_DEBOUNCE_CNT)
                {
                    tdc_touch_iqs323_reseed();
                    sleep_ignore = false;
                    ci_printi("[TOUCH] SLEEP: notouch confirmed -> RESEED, gate open \r\n");
                }
            }
            else /* TOUCH 또는 read 실패 → 노터치 확정 보류 */
            {
                notouch_cnt = 0;
            }

            /* 첫 노터치가 10초 넘게 확정 안 됨(계속 터치/오염) → 강제 RESEED 로 baseline 끌어와 탈출 */
            if (sleep_ignore && ignore_elapsed >= TDC_TOUCH_ULP_FORCE_RESEED_CNT)
            {
                tdc_touch_iqs323_reseed();
                ignore_elapsed = 0;
                notouch_cnt    = 0;
                ci_printw("[TOUCH] SLEEP: notouch timeout 10s -> forced RESEED \r\n");
            }
        }
        else /* 게이트 해제 후 — 터치 발생 시 재부팅 */
        {
            if (ok && state == TDC_TOUCH_STATE_TOUCH)
            {
                touch_cnt++;
                if (touch_cnt >= TDC_TOUCH_ULP_REBOOT_TOUCH_CNT)
                {
                    ci_printi("\r\n[TOUCH] SLEEP: touch detected -> REBOOT \r\n");

                    Sys_GPIO_Set_High(DIO_PIN_INDEX_for_LED_color_R);
                    Sys_GPIO_Set_Low(DIO_PIN_INDEX_for_LED_color_G);
                    Sys_GPIO_Set_High(DIO_PIN_INDEX_for_LED_color_B);

                    delay_ms(250); /* RTT 뷰어 로그 드레인 대기 */
                    SYS_WATCHDOG_REFRESH();
                    delay_ms(250); /* RTT 뷰어 로그 드레인 대기 */
                    SYS_WATCHDOG_RESET();
                    /* 도달 불가 — 칩 리셋 */
                }
            }
            else
            {
                touch_cnt = 0;
            }
        }

        if (ok && ulp_state_prev != state)
        {
            ci_printv("[TOUCH] ULP STATE: %s -> %s \r\n",
                      tdc_touch_state_name(ulp_state_prev), tdc_touch_state_name(state));
            ulp_state_prev = state;
        }
    }

    return 0;  /* 도달 불가 — 컴파일러 만족용 */
}
```

### 현재 ci_print* 호출 전수 (func_sleep() 내부, 라인은 위 코드 블록 기준)

| 현재 레벨 | 메시지 | 현재 성격 |
|---|---|---|
| `ci_printi` | "CM3 IS PREPARING TO ENTER SLEEP MODE" | 진입 알림(정보) |
| `ci_printi` | "FIRST OF ALL, TRY TO POWER OFF FPGA BACKTRL H/W" | 진입 알림(정보) |
| `ci_printi` | "FPGA reset : %s" (success/failed) | 결과 알림(성공/실패 혼재) |
| `ci_printi` | "CFX : sleep" | 진입 알림(정보) |
| `ci_printd` | "[T] LTA=... " (계측 한 줄) | 주기 계측(이미 printd) |
| `ci_printw` | "ATI ERROR -> REBOOT (recover in normal)" | **최종 리부팅 직전**(가장 심각) |
| `ci_printw` | "ATI ERROR -> RECOVER (in sleep)" | 재시도 중(회복 가능, 최종 아님) |
| `ci_printi` | "notouch confirmed -> RESEED, gate open" | 정상 진행 알림 |
| `ci_printw` | "notouch timeout 10s -> forced RESEED" | 이상 상황이지만 자동 복구(비치명) |
| `ci_printi` | "touch detected -> REBOOT" | **재부팅 직전**(터치스턱, 가장 심각) — 현재 printw 아님 |
| `ci_printv` | "ULP STATE: %s -> %s" (상태 천이 추적) | 상세 추적(verbose) |

`ci_printe`는 func_sleep() 내부에 현재 0건.

### `ci_print*` 레벨 정의 (프로젝트 전역, `ci_printf.h`)

- `ci_printe` = error(빨강) — 이번 요구사항상 func_sleep() 내 사용 금지
- `ci_printw` = warn(노랑) — 요구사항상 "최대 에러 상태"만
- `ci_printi` = info(초록)
- `ci_printd` = debug(기본색) — 요구사항상 "일반적인 메시지" 전부 여기로
- `ci_printv` = verbose

목표 최종 상태: func_sleep() 내부에는 **`ci_printw`(최대 에러)와 `ci_printd`(그 외 전부)만** 남는다. `ci_printi`/`ci_printv`는 각각 성격에 따라 재분류.

### 관련 상수·타입 (참고, 정의 위치는 `tdc_touch_time.h`/`tdc_touch_iqs323.h` 등)

- `TDC_TOUCH_ULP_NOTOUCH_DEBOUNCE_CNT`, `TDC_TOUCH_ULP_FORCE_RESEED_CNT`, `TDC_TOUCH_ULP_REBOOT_TOUCH_CNT` — ULP 게이트 임계값
- `TDC_TOUCH_IQS323_THRESHOLD`, `TDC_TOUCH_IQS323_HYSTERESIS`, `TDC_TOUCH_IQS323_PROX_THRESHOLD` — 절대임계 계산용
- `tdc_touch_iqs323_status_t`(멤버: `pressed`, `ati_error`, `ati_active`, `prox`), `tdc_touch_iqs323_debug_t`(멤버: `ok`, `lta`, `counts`)
- `tdc_touch_state_t` enum: `TDC_TOUCH_STATE_RESET`, `TDC_TOUCH_STATE_NOT_TOUCH`, `TDC_TOUCH_STATE_TOUCH` + `tdc_touch_state_name()` 문자열 변환 함수
- 호출 함수: `tdc_touch_iqs323_read_status()`, `tdc_touch_iqs323_read_debug()`, `tdc_touch_iqs323_reseed()`, `tdc_touch_iqs323_re_ati()` — 전부 기존 드라이버 함수, 시그니처 변경 없음(순수 구조정리라 이 함수들 자체는 손대지 않음)

### 프로젝트 코딩 컨벤션 (검증 페르소나 기준)

- 신규 함수/심볼은 **`tdc_` 접두어** 필수 (루트 CLAUDE.md §7)
- **중괄호는 단일 문이어도 항상 필수** (사용자 명시 규칙)
- 주석은 WHY(비직관적 근거)만, WHAT(코드 재서술) 금지
- `SYS_WATCHDOG_REFRESH()`가 while(1) 루프 최상단에서 **매 iteration 무조건** 호출되는 순서는 반드시 보존(하드웨어 워치독 ~3.28s 타임아웃 대비, 이걸 건드리면 회귀 위험)

## 페르소나 (8노드)

| 노드 | 역할 | 파일 | 비중축 |
|---|---|---|---|
| 01 | 분석_변수구조 — 전 지역변수 목록화, 함수 최상단 배치 시 초기화/재대입 안전성 검토, `#if` 게이트 변수 처리안 | 01_분석_변수구조.md | 분석 |
| 02 | 분석_로직분리 — while(1) 바디를 논리블록으로 분해, 정적 함수 시그니처(상태는 포인터 전달) 제안 | 02_분석_로직분리.md | 분석 |
| 03 | 분석_ci_print레벨 — 위 표 11건 전수 재분류(printw/printd 2단), 메시지 문구·개수 최소화안 | 03_분석_ci_print레벨.md | 분석 |
| 04 | 분석_주석정리 — 현재 인라인 주석 전수 검토, 유지/삭제/축약 판정(WHY 기준) | 04_분석_주석정리.md | 분석 |
| 05 | 분석_진입시퀀스 — while 루프 진입 전 상단부(FPGA/PMIC/QCC 종료, clock/I2C 전환)도 함수 분리 대상인지 범위 판단 | 05_분석_진입시퀀스.md | 분석 |
| 11 | 계획_통합설계 — 5개 분석 통합, 최종 리팩토링 계획(신규 함수 목록·시그니처·변수표·ci_print 매핑표·주석방침·구현순서·회귀위험) | 11_계획_통합설계.md | 계획 |
| 21 | 검증_동작보존 — 계획대로 구현 시 동작 100% 동일 여부 adversarial 검증(재초기화 타이밍·부수효과 누락·워치독 refresh 순서) | 21_검증_동작보존.md | 검증 |
| 22 | 검증_스타일정합 — tdc_ 접두어·중괄호·주석정책 등 프로젝트 컨벤션 준수 검증 | 22_검증_스타일정합.md | 검증 |

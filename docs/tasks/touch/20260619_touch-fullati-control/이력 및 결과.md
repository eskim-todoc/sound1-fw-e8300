---
name: Full ATI 터치 제어 재설계 이력 및 결과
purpose: 본 작업의 시계열 로그와 완료 요약
type: tasks-이력
maturity: in-progress
tags: [touch, iqs323, full-ati, 이력]
---

# Full ATI 터치 제어 재설계 — 이력 및 결과

**TL;DR**: FullATI 타임아웃 단순안을 펌웨어로 구현하는 작업의 시계열 로그. ultracode 모드(Opus 4.8 xhigh 페르소나)로 분석/계획/검증을 워크플로우 오케스트레이션한다.

---

## 시계열 로그

### 2026-06-19

- **착수**: 은수님 ultracode 지시 — 20260618 브레인스토밍 수렴안(FullATI 타임아웃 단순안)을 펌웨어로 구현. 페르소나·json 전부 문서화. 새 폴더 분리.
- **Git 정리(선행)**: 미커밋 터치 docs 전부 2커밋(Chore 빌드·Docs 터치) → `claude_develop` --no-ff 머지(52e7831) → origin push 완료. cm3_factory(110MB)는 `.gitignore` 제외. 작업 목록: 20260618 완료 + 20260619 2건 등재.
- **새 브랜치·폴더**: `claude_feature_touch-fullati-control` 생성(claude_develop 기준) + `docs/tasks/touch/20260619_touch-fullati-control/`.
- **단계 ① 완료**: `요구사항.md`(은수님 16항목 + 발굴 8항목 + 정량 4항목) + 본 이력.
- **단계 ② 착수**: 오케스트레이터 입력 + 페르소나 정의 json 작성 후 Workflow fan-out(분석 8 + adversarial 검증 2 + 종합 1 = 11노드, effort xhigh).

- **단계 ② 완료**: Workflow 11노드(분석 8 + 검증 2 + 종합 1, effort xhigh, Opus 4.8, 1.47M 토큰) 성공. 전 노드 한자 0.
  - **검증 09(정량)**: [확인됨] 54·[단위오류] 9·[과장/근거없음] 0. **핵심 정정**: Touch Threshold는 raw가 아니라 `계수×LTA/256` 비율계수 → 부등식 **계수<32**, Base 상향 무관, 계수 12~24 하향이 본질.
  - **검증 10(회귀)**: [해소] 16·[잔존위험] 19·**[신규회귀] 5건**(부팅 Re-ATI 부재·reseed 감도 동반·절전 이중 write·read 실패 hold-last·auto-ATI 3-상태 미구분) → 선행 수정 필수, 원자적 3쌍·신규 API 2종.
  - **급소(검토_2)**: "터치 중 Re-ATI 보류" 게이트 필수. LTA halt는 자동 경로만, SW 강제 경로는 게이트가 방어.
  - `분석.md`·`에이전트-로그/인덱스.md` 작성.
- **단계 ③ 완료**: `계획.md` — Phase P0~P6 빌드가드 단계(안전망·API → 단일채널 폐기 → Full ATI 원자적 전환 → Re-ATI 게이트 → Beta·Power → SW 타임아웃 → 게이트·절전 정리) + 실측 게이트 14항목 처리 전략.
- **단계 ④ 승인**: 은수님 결정 — 전체 일괄 구현 / 절전 ULP 2.2초 유지(30초는 노말 전용) / 안전 기본값(계수 16·N 10·Base 200) + RTT 로깅.
- **단계 ④ 구현(fork)**: P0~P6 일괄 구현. 5파일 +458/−25. 신규 API 3종(`reseed_only`·`re_ati_trigger` 공개·`read_status_full`), 빌드가드 9종(`FULL_ATI_ENABLE` 등). 원자적 3쌍·급소 게이트(`proc_re_ati_gate`)·신규회귀 5건 반영. 절전 경로 미변경. 한자 0.
- **단계 ④-검증(5노드 adversarial, xhigh)**:
  - 빌드정합 **PASS**(API 선언↔정의·매크로·전처리기·외부심볼)
  - 회귀차단 **PASS**(신규회귀 5건 차단·원자적 3쌍·기존 보존, 경미 4)
  - 레지스터정합 **PASS**(0x040C·Base 0x00C8 Target800/Band100·**계수16<32**·Beta 비대칭·Power No ULP·1MHz 전부 정확)
  - 가드스타일 **PASS**(가드 독립·접두어·한자0, 경미 2)
  - 급소게이트 **FAIL [치명]** → **수정**: read 실패 시 ATI 캐시(`s_last_ati_error/active`) false 무효화. stale 값 기반 Re-ATI(터치를 노터치 학습) 원천 차단.
  - **급소 재검증 PASS**: 누출 차단 확인 + 누출 경로 3개 비-누출 + 부작용 없음(proc_long_touch·boot_touch_ignore·stuck) + 한자 0.
- **단계 ④ 완료(정적)**: 전수 PASS. **빌드·플래시·실기 동작은 은수님 Eclipse 환경 필요** — RTT 로깅으로 실측 게이트 값 확정 대기.

- **동작 분석(8노드 xhigh, 빌드 전 정적)**: 은수님 지시 — 선언/호출 정합 재검증 + 타이밍·콜스택·이벤트 시퀀스 동작 예측 + 순서도. 분석 6 → adversarial 검증 1 → 종합 1.
  - **선언정합 재검증**: 빌드 차단 0건(시그니처 6/6·가드 대칭 3/3·orphan 0). [실측 게이트 2건]: SYS_WATCHDOG_RESET SDK 헤더(추정-강)·-Werror 비기본 조합.
  - **핵심 정정**: 코드 정본 **폴링 200ms·롱터치 2400ms**(요구사항_9·설계 "100ms"·"3초" 표기 정정). 30초 stuck은 ms 절대시간이라 무영향.
  - **타이밍**: 부팅 최악 1tick 3.8초+α(WDT 안전), 부팅 Re-ATI 최악 1.3초 블로킹. 절전은 신규 로직 호출 0(미관여).
  - **B7 검증**: 동작 결론 전수 [확인됨], 적출은 라인 인용 오류 4건뿐(B99 절대 라인 통일).
  - **순서도 5개 Mermaid valid**(부팅·노말 1tick·Re-ATI 게이트·SW 3단계 flowchart·process 상태머신). 한자 0.
  - 산출: `에이전트-로그/동작분석/B1~B7+B99+인덱스`, `분석.md §8` 추가.
- **은수님 확인 대기(빌드 후)**: 폴링 200ms vs 100ms(요구사항_9), 절전 SW 30초 미적용(요구사항_4 의도).

<!-- 이후 빌드·실측 결과 누적 -->

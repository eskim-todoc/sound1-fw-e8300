---
name: Full ATI 동작분석 오케스트레이터 입력
purpose: 구현된 Full ATI 코드를 선언/호출 정합 재검증 + 타이밍·콜스택·이벤트 시퀀스 관점에서 동작 예측하는 fan-out 입력
type: 에이전트-로그
maturity: in-progress
tags: [touch, iqs323, full-ati, 동작예측, 콜스택, 타이밍, 순서도, orchestrator]
---

# 00 오케스트레이터 입력 — Full ATI 동작 분석

**TL;DR**: 빌드 전(다음주 빌드 예정) 정적 상태에서, 구현된 Full ATI 코드를 ① **선언/호출 정합 재검증**(전수) + ② **타이밍·콜스택·이벤트 시퀀스 동작 예측**(부팅·노말루프·Re-ATI 게이트·SW 타임아웃·절전·동시성)으로 다방면 분석한다. 분석 6 → adversarial 검증 1 → 종합 1(순서도·콜스택·이벤트·상태 통합 영속 문서). effort xhigh. 한자 절대 금지.

---

## 1. 대상 (각 노드 Read)

- **변경 diff**: `/home/eskim/.claude/jobs/8518f6a1/tmp/fullati_impl.diff`
- **변경 파일**: `src/2__cm3/Cortex-M3-src/systemControl/{tdc_drv_iqs323.c,tdc_drv_iqs323.h,tdc_touch.c,tdc_touch.h,tdc_touch_config.h}`, `src/2__cm3/Cortex-M3-src/main.c`
- **호출 상위**: `main.c`(func_normal·func_sleep·메인루프), `initialize.c`(MCLR), 터치 폴링 진입점
- **설계 정본**: `에이전트-로그/99_종합_설계수렴.md`, `분석.md`

## 2. 분석 목표

### 선언/호출 정합 재검증 (은수님 1차 지시)
- 신규/변경 함수(reseed_only·re_ati_trigger 공개·read_status_full·proc_re_ati_gate·proc_stuck_timeout·apply_filter_power_settings)의 **호출↔선언↔정의** 전수.
- 빌드가드별(FULL_ATI 0/1, RE_ATI_GATE 0/1, BETA_POWER 0/1, GATE 0/1) 심볼 가용성·orphan static·미정의 참조.
- include 체인·외부 심볼(SYS_WATCHDOG_RESET 등) 접근성.

### 동작 예측 (타이밍·콜스택·이벤트·상태)
- **부팅**: MCLR→auto-ATI(Full POR)→apply_settings(sensor_setup·Full ATI·Beta·Power·부팅 Re-ATI·RESEED)→READY. 각 단계 콜스택·블로킹·타이밍.
- **노말 루프**: 100ms 폴링 콜스택(tdc_touch_process→get_state→read_status_full→proc_re_ati_gate). 이벤트별(터치 진입/해제/유지/드리프트/ATI에러/read실패/롱터치) 상태 천이.
- **Re-ATI 게이트**: 발동 조건 4개·쿨다운·터치 중 보류 타이밍.
- **SW 타임아웃**: proc_stuck_timeout 30초 3단계(reseed_only→Re-ATI→MCLR) 타이밍·재카운트.
- **절전**: func_sleep 877 대기→967 ULP(미변경)와 신규 노말 로직 경계.
- **동시성·경계**: wait_re_ati_done 블로킹(100ms 폴링 충돌), read 실패·0xEEEE 글리치, race.

## 3. 노드 (8)

| 단계 | 노드 |
|---|---|
| 분석(6) | B1 선언호출정합 · B2 부팅시퀀스 · B3 노말루프이벤트 · B4 ReATI게이트타이밍 · B5 SW타임아웃에스컬레이션 · B6 절전경계동시성 |
| 검증(1) | B7 동작예측할루시네이션(adversarial — 예측↔코드 대조) |
| 종합(1) | B99 동작분석순서도(순서도·콜스택·이벤트·상태 통합 영속 문서) |

## 4. 산출 규칙

- 각 노드 결론을 `에이전트-로그/동작분석/<id>.md`에 Write(frontmatter+TL;DR). 한자 절대 금지(한글만). 코드 라인 `[파일:라인]` 근거.
- 동작 예측은 [확정](코드 명시)/[추정](합리적 예측)/[실측 게이트](실기 필요) 라벨.
- 순서도는 Mermaid(특수문자 라벨 금지 — 콜론·대괄호 중첩·∈ 회피).
- B99는 콜스택 트리·이벤트→상태 천이표·타이밍 다이어그램·순서도 통합.

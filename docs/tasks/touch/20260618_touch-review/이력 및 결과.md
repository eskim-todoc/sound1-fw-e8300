---
name: touch-review-이력
purpose: IQS323 터치 제어 재검토 — 누적 분석 결론을 단일 구현안으로 수렴시키는 작업의 시계열 이력 및 결과
type: 이력
maturity: in-progress
tags: [touch, iqs323, auto-ati, reseed, convergence, implementation]
---

# 이력 및 결과 — 터치센서 재검토 (분석 결론 → 구현 수렴)

**TL;DR**: 그동안 누적된 IQS323 터치 제어 분석 9건(만장일치 "Reseed baseline 오염" + 하이브리드 권고)을 **단일 구현안으로 통합**하여 실제 펌웨어에 수렴시키는 작업. 미완료 터치 작업 3건(`crx1-esd-guard`·`touch-rtt-tuning`·`eunsu-consensus-review`)을 중단하고 본 작업으로 재출발.

---

## 배경 (착수 경위)

- 2026-06-18 은수님 「터치센서 재검토」 지시.
- 착수 시점 작업 트리에 IQS323 관련 미커밋 변경 3파일 존재(`main.c`·`tdc_drv_iqs323.c`·`tdc_touch_config.h`) — SLEEP_ATI 매크로 보드 변종별 분리, `TDC_BOARD_VARIANT` → `MINI` 전환, clang-format 재정렬 + 한글 주석 em-dash 인코딩 손상 혼재.
- 그동안 분석 9건(`circuit-analysis`·`fw-implementation-analysis`·`control-redesign-study`·`eunsu-opinion-review`·`discharge-autoati-coexist`·`j3-charge-accumulation` 등)이 누적됐으나 **실제 구현 수렴 없이 미완료** 상태.
- 은수님 방향: **분석 결론 → 구현 수렴** (구현 중심).

---

## 이력

| 일시 | 단계 | 내용 |
|---|---|---|
| 2026-06-18 | 사전정리 | 미커밋 변경 리뷰: `?` 손상이 삼항 연산자(정상)와 em-dash 손상(복원 대상) 2종 혼재 확인. HEAD에 이미 커밋된 손상 ~40곳 + 이번 미커밋 신규 손상 6곳 식별. |
| 2026-06-18 | 사전정리 | 신규 손상 6곳 복원(main.c 744·839·859, iqs323 657·671·685) + SLEEP_ATI 주석을 코드 사실에 맞춰 정정("보드 변종 무관 공통" → "보드 변종별 normal ATI와 동일값, 절전 전용 실측 미적용"). 기존 손상 ~40곳은 별도 커밋 예정. |
| 2026-06-18 | 사전정리 | 미완료 터치 작업 3건(`crx1-esd-guard`·`touch-rtt-tuning`·`eunsu-consensus-review`) 중단 처리 — 각 이력 및 결과.md에 중단 사유·승계 방침 기록(은수님 지시). |
| 2026-06-18 | ①요구사항 | 본 재검토 작업 폴더 신설, 이력 및 결과.md 생성. 누적 권고 2종(제어 재설계·하이브리드) 정독 서브에이전트 기동. |
| 2026-06-18 | ①요구사항 | 권고 2종 추출 완료 — 두 권고 상보적 일치 확인(공통: Reseed 진입 baseline 오염 + ATI Disabled 드리프트 2뿌리, 단계 Phase 0→실측→Phase 1 I²C관리+노터치 게이트→Phase 2 ATI Full+비대칭 BETA→Phase 3). 통합 로드맵으로 `요구사항.md` 작성. 범위 확정(질문_1·2) 은수님 승인 대기. |
| 2026-06-18 | ①요구사항 | 은수님 범위 확정 — 결정_1: 전체 설계 → 단계 구현, 결정_2: 미커밋 변경 별도 정리 커밋 먼저. `요구사항.md §7` 확정 반영. |
| 2026-06-18 | 정리커밋 | 별도 정리 커밋 `d754e99` (`claude_feature_touch-sleep-ati-cleanup`) — 코드 3파일(SLEEP_ATI 보드별 분리·MINI 전환·em-dash 신규 6곳 복원·주석 정정·clang-format). Author/Committer 김은수, AI 트레일러 없음. 문서 변경은 working tree 유지. 머지는 승인 후. |
| 2026-06-18 | ②분석 | 코드 위치 grep 재확정(서브에이전트, 정리 커밋 후 라인 기준) → `분석.md` 작성. 확정: RESEED 무조건 발행 2곳(`iqs323.c:1146`/`:955`), ATI Disabled, BETA write 0건(POR default), `write_and_verify` 값 비교 부재, 주석 불일치 3건. 2뿌리 코드 근거 + 권고 상보 종합. |
| 2026-06-18 | ③계획 | `계획.md` 작성 — Phase 0~3 전체 설계(Mermaid 의존도) + 실측 게이트 4종 + 단계별 빌드 가드·독립 롤백·커밋 전략. Phase 0(무위험·즉시) 우선, Phase 1b 이후 실측 의존. ④ 진입 은수님 승인 대기. |
| 2026-06-18 | 재검토(은수님) | 은수님 대안 제시 — ATI Full I2C 실패를 복잡 핸들러 대신 "실패=auto-ATI로 간주 100ms 재시도 + 10회 연속 실패면 고장 판정" retry-and-count 전략으로 대체 가능한지 검증 요청. 가정_2("터치 중 auto-ATI 미동작") 정확성 포함. 8 페르소나(분석5·반증2·수렴1) 오케스트레이션 착수 — `에이전트-로그/v1/00_오케스트레이터-입력.md` 작성, 1단계 분석 5개 병렬 기동. Phase 1a(I2C 통신관리) 설계가 결과에 따라 전면 수정될 수 있음. |
| 2026-06-18 | 분석(1단계) | 분석 5개 완료(01~05). 요약 — 가정_1(auto-ATI 중 I2C 실패): **확인**(RDY window 미열림, DS §8.4). 가정_2(터치 중 auto-ATI 미동작): **조건부**(정상 터치 성립, 부팅 터치 Re-ATI 발동 가능, freeze≠Re-ATI 트리거 혼동). 전략(실패=auto-ATI 간주): **구조적 결함**(현 코드 통신실패↔ATI진행 미구분, 3-상태 로직 필요). 판별(10회=고장): **부분 기각**(burst 시간상관 함정, POR auto-ATI 1.5초가 100ms 기준 15회 연속 → 정상 부팅 오판). 구현: 은수님 안 ~19줄 vs 폴링 ~73줄, **결합(폴링 1차+카운트 안전망) 권고**. |
| 2026-06-18 | 반증(2단계) | 반증 2개 완료(06·07). 06(가정공격): **ATI Full 전제 자체가 미검증**(FIXED 유지만으로 I2C 블로킹 원천 제거 — Sound1 환경서 LTA가 ATI Band 이탈할 환경변화 실재하는지 미확인=논리 점프), ATI_Error 영구먹통 경로(Error→Active=0→read 성공→ati_error 무시→0xC0 bit2 미발행), 결합 상보성 허위. 07(failmode): 11개 실패모드(Critical 1·High 4), 최우선 FM-01(POR 오판)·FM-05(stuck-touch 롱터치 오발)·FM-04(2초 블라인드 탭 묵살)·FM-08(절전 클럭 강하 타임아웃 단축). 3단계 수렴 서기 기동. |
| 2026-06-18 | 수렴(3단계) | 서기(08) 완료 — `08_synthesis.md`+`인덱스.md` 작성. 최종 판정: 가정_1 타당·가정_2 조건부·전략 구조적결함·판별 부분기각. **retry-and-count 조건부 채택(5보완: ①ATI_Error 핸들러 최우선 ②READY 후 카운트 ③K=실측후 ④실패시 NOT_TOUCH ⑤3-상태)**. 상위 갈림길 = ATI Full vs FIXED 유지(실측5 선결). 한자 前 2건 한글 교정, 析 0건 확인. 분석.md §8 신설 + 계획.md Rev.1(갈림길 §0-A·실측 +3종·Phase 1a 우선순위 역전) 반영. ④ 진입 은수님 승인 대기. |
| 2026-06-18 | 지침시정 | 은수님 지적 — 8 페르소나 검증을 루트 §5 **`Workflow` 도구 우선** 원칙 위반하고 `Agent` 도구 폴백으로 진행(effort:high를 프롬프트 텍스트로만 전달, `Agent` 도구엔 effort 인자 없어 실제 미적용·세션 기본 상속). 은수님 승인: ① 이탈 인정 ② **`Workflow` 도구로 재실행**(effort 실제 적용). 기존 8 노드(01~08) 결과는 내용상 유효하나 방법론 재구성 예정. |
| 2026-06-18 | 재검증(v2) | 은수님 추가 입력 3건 수신 — ①`개선/RDY 윈도우 타이밍/` 신규 문서 참조, ②통신 윈도우 비용(2-1 read 시 stop→윈도우 닫힘→대기)·스트리밍 윈도우 200ms(2-2 force_window_open 불필요?), ③auto-ATI=Re-ATI 여부 + **FIXED LTA delta 터치 판정 유효성(3-1, 핵심 갈림길 근거)**. 신규 명제 G~J로 `00_오케스트레이터-입력-v2.md` 작성. **`Workflow` 도구**로 8 페르소나(분석5·반증2·수렴1) 재실행 — Run ID `wf_cd2a7f2c-77d`, model sonnet·effort high 실제 적용. 노드 10~17 산출 예정. |
| 2026-06-18 | 브레인스토밍(공장캘리) | 은수님 구체안 — 공장초기화 시 auto-ATI MULT/COMP+noTouch counts 저장 → 이후 Fixed로 사용 + RESEED 없이 저장 타겟 직접 비교 + 주기 방전. `브레인스토밍_부팅터치모호성.md` §6-A 평가 추가. **결론: 보드편차·터치부팅·ATI게인왜곡 ✅ 해결**(기존 ati-calib-mode가 MULT/COMP 읽기 구현=전제 충족). **유일 약점 = 환경 드리프트**(LTA 제거로 고정 타겟이 온도·경년 못 따라감 → false touch/둔감). |
| 2026-06-19 | 시스템전제검증 | 은수님 제시 4전제 코드 검증 — ①500ms+(LED360+로딩, 부분), ②MCLR 매 부팅(사실, `initialize.c:449`), ③진입=롱터치/**복귀=WDT리셋 재부팅**(절전·부팅 baseline 통합), ④IQS323 상시 ON·**충전시 절전미진입**·**크래들 뚜껑닫힘=터치폴링 중단(노터치 보장·baseline 갱신 미구현)**. 추가: 매 부팅 AutoATI 1회→고정값→ATI Disabled+CH timeout 이중차단, 절전진입 RESEED. |
| 2026-06-19 | 오케스트레이션(v3) | 은수님 지시 — Fixed+AutoATI 조합 100%근접안 도출. `에이전트-로그/v3/00_오케스트레이터-입력-v3.md` 작성 + **`Workflow` 균형 구조**(분석8→계획8→검증8→종합1 + adversarial 8×3회 재검증 + 최종1, 약 53에이전트, model sonnet·effort high) 실행 — Run ID `wf_00f30296-20c`. v3 노드 산출 예정. |
| 2026-06-19 | 데이터시트조회 | 은수님 질문 2건 — ①ATI 실패 조건 ②환경 민감도 정량. 데이터시트 전수 조회. **ATI_Error: ATI 완료 후 counts가 Target±Band(Sound1 350~450) 밖, 자동재시도 없음. 터치 중 ATI=counts 하향 이탈로 실패. COMP 10/5-bit, MULT·Cx한계 미명시(AZD004), Self-cap max 400pF.** **환경 민감도(온도계수·전압·터치Δ) 데이터시트 전혀 없음** — 동작범위만(-40~85°C·1.71~3.6V). → 드리프트 마진은 실측(블로킹_A) 필수 확정. 데이터시트 정공법 Reference UI(§7.3)는 Sound1 CRX1 회로상 불가. 분석.md §10 반영. |
| 2026-06-19 | 문서수정 | 은수님 보고 — `99_최종종합.md` 상태머신 Mermaid 파싱 에러(stateDiagram transition label 내 중첩 `[lo,hi]`·`∈`·`:`·줄바꿈). safe label(특수문자 제거)로 재작성, Mermaid Chart 도구 validate=True 확인 후 교체. 한자 0건 유지. |
| 2026-06-19 | v3완료·반영 | Workflow 완료(53에이전트·553만토큰·59분). **아키텍처 수렴 확정: "공장 절대기준→노터치 게이트→조건부 Re-ATI"** 32 adversarial 노드+대안비교 3R 기각 0. **🔴 즉시 실행 버그 2건(현장)**: ①`wait_re_ati_done()` WDT 갱신 0건(최악 ~2.3초 blocking 중 WDT 리셋 위험, ATI_CALIB_MODE 경로 잠재 현장버그) ②`re_ati_trigger()` MSB=0x00→CH_TIMEOUT(0x07) 소멸. 5층 아키텍처+상태머신 Mermaid, Phase −1~4 계획, 빌드가드 #error 9종, 블로킹 A·B·D·G·J·L·M+EEPROM전달. 한자 10파일 교정(0건 확인). `99_최종종합.md`+인덱스 v3(6행). 분석.md §10·계획.md Rev.3 반영. |
| 2026-06-18 | 브레인스토밍(race) | 은수님 2지적 — ①"노터치 판단할 거면 auto-ATI가 낫다"(RESEED는 baseline만, Re-ATI는 게인+comp+baseline 전부 → 우월), ②"판별~실행 사이 터치 발생 race는 벗어날 수 없다". `브레인스토밍_부팅터치모호성.md` §6-B 추가. **결론: ① 맞음 → 공장값 게이트+Re-ATI = control-redesign 권고(ATI Full+노터치 게이트)로 수렴(한 바퀴 돌아 도달). ② race 제거 불가 맞음, 단 치명도 낮음**(저확률+자기치유+사후검증재시도+크래들이면 소멸). 핵심 메시지: 모든 cap touch가 cold-start race 보유=완벽 해 원리적 부재, 목표는 제거 아닌 "확률최소화+자기치유"=다층 방어로 무시가능 수준 수렴. |
| 2026-06-18 | 정정(RESEED 제약) | 은수님 지적 — RESEED는 현재 counts로만 seed(§5.5.1), 임의값(공장값) 불가. 직전 "LTA 초기값=공장값" 개선안 정정. **수정: 공장값을 LTA 값이 아니라 "RESEED 노터치 게이트 *기준*"으로** — 부팅 시 현재 counts가 공장값 윈도우 안이면 노터치→RESEED, 크게 낮으면 터치 의심→RESEED 보류→손뗌 회복 시 RESEED. **LTA 직접 write 불요**. 보드편차·터치부팅·드리프트(RESEED 후 LTA IIR) 모두 해결 = control-redesign 노터치 게이트 + 공장 절대기준 결합. |
| 2026-06-18 | 브레인스토밍 | 은수님 핵심 문제 제기 — 부팅 터치 시 auto-ATI 게인 선점/RESEED baseline 오염을 막을 방법 부재, "터치 여부·손뗌·애매 부분터치를 알 수단이 없음". `브레인스토밍_부팅터치모호성.md` 작성. **문제 본질 = Cold-Start Touch Ambiguity(미지수 2개[보드 기준·터치 여부]·방정식 1개=불능)**. 해결 = 미지수 하나 외부 고정. 5방안 발산(A 절대캘리브·B 손뗌감지·C 하이브리드·D 크래들·E 통계윈도우) + 비교표 + 다층 권고(D→A/E→B→C) Mermaid. 방전은 baseline과 독립=별도 HW/주기 방전 잔존. 미해결: raw counts readout·크래들 부팅 가용·드리프트 폭·방전 효력 실측. 은수님 핸드폰 리모트 확인 중. |
| 2026-06-18 | 개념정리(연결) | 은수님 지적("두 층위가 어느정도 관련 있다") 반영 — `개념정리_터치동작모델.md` §3-A 신설. **두 층위의 연결고리 = MULT(게인)**: ATI가 정한 MULT/COMP가 noTouch counts 절대값 + 터치 Δcounts 진폭을 동시 스케일(§5.9 line 555·557·633·639) → Δ가 곧 delta로 층위② 입력. 부팅 auto-ATI 게인 선점 흐름 Mermaid + 보드편차 ATI Full/Fixed 대비표(노터치 delta는 양쪽 ≈0, 갈리는 건 터치 Δ 진폭=감도) + 부팅 노터치/터치 게인 선점 차이. 결론: "부팅 노터치 상태 ATI 1회" 필수 근거 = 게인을 noTouch 기준 선점. Δcounts 정량 charge-transfer 수식은 [확정 필요]. |
| 2026-06-18 | 개념정리 | 은수님 5의문(drift→LTA→Band·RESEED 수렴·보드편차 threshold·Re-ATI 정규화·thr<Band 무한터치) 혼란 진단 → `개념정리_터치동작모델.md` 작성. 핵심 진단: **ATI(counts 절대값 정규화) ↔ 터치판정(delta=LTA−Counts) 두 층위를 혼동**. Mermaid 3종(층위분리·정상시퀀스·깨짐4종) + 5의문 해소표 + 원인별 해법표. 결론: 방법=원인별 가드 조합(부팅 노터치 ATI·노터치 게이트·stuck 가드·포화 감지·ATI_Error 핸들러), control-redesign 권고와 일치 — 은수님 의문이 그 방법의 필요성을 역증명. 미확정: thr vs ATI Band 정량(의문5)·counts 분해능 → 정밀검증 대기. |
| 2026-06-18 | v2완료·반영 | Workflow 완료(8에이전트·56만토큰). **명제_G 참**(read 1회=force×2, 최악 130ms — 2-1 비용 큼 확인), **명제_H 거짓**(200ms는 watchdog 상한, force 구조 필수 — 2-2 오해 정정), **명제_I 조건부참**(알고리즘 동일·트리거 다름), **명제_J 조건부참**(FIXED LTA delta는 ATI 독립·정상 범위 유효=은수님 직관 맞음, **단 counts 포화 시 침묵 실패**). 핵심: Event Mode가 통신비용 근본해법으로 부상. 한자 析·前·末 3건 교정(Python 유니코드), 한자 0건 확인. 분석.md §9 신설 + 계획.md Rev.2(실측V2·V3 추가·Phase0 포화감지 stub·Phase1a 보완_3 130ms·Event Mode 격상·갈림길 §0-A 보완) 반영. `인덱스.md` v2 노드 등재. |

---

## 완료 요약

*(작업 완료 후 작성)*

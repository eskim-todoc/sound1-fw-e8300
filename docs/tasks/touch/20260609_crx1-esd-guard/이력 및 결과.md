---
name: crx1-esd-guard-이력
purpose: CRX1 채널을 ESD 방전 경로로 활용하는 방안 조사 작업의 시계열 이력 및 완료 요약
type: 이력
maturity: experimental
tags: [iqs323, crx1, esd, guard, touch]
---

# 이력 및 결과 — CRX1 ESD 방전 경로 활용 조사

**TL;DR**: IQS323 CRX1 채널을 이용해 터치 먹통(ESD 누적) 문제를 SW/HW로 완화하는 방안 조사.

---

## 이력

| 일시 | 단계 | 내용 |
|---|---|---|
| 2026-06-09 | ①요구사항 | 작업 개시 — 브랜치 `claude_feature_touch-crx1-esd-guard` 생성 |
| 2026-06-09 | ②조사 | IQS323 데이터시트 분석 완료 — 조사.md 작성 완료 |
| 2026-06-09 | ④구현 | 절전 설정 순서 재구성 + ULP 타이머 200ms 변경 — main.c, tdc_drv_iqs323.c/.h, IQS323-RESEED.md 수정 |
| 2026-06-09 | ④구현 | TDC_TOUCH_CRX1_VSS_ENABLE 플래그 구현 — tdc_touch_config.h 신규 상수, tdc_drv_iqs323.c sensor_setup() 분기 추가 |
| 2026-06-09 | ④구현 | TDC_TOUCH_CRX1_REF_ENABLE 플래그 구현 (방법 C) — Channel Setup 레지스터(0x70) 신규 매핑, sensor_setup() REF/VSS/기본 3분기 재구성, VSS_ENABLE↔REF_ENABLE 상호 배타 #error 추가 |
| 2026-06-09 | ④구현 | 노말모드 200ms 폴링 + CRX0 VSS 방전 기능 — POLL_INTERVAL 200ms·LONG_TOUCH_MS 2800ms, tdc_drv_iqs323_discharge_crx0() 신규 구현, TDC_TOUCH_CRX0_DISCHARGE_ENABLE 플래그(기본값 1) |
| 2026-06-09 | 버그 | CRX0 방전 기능 — WAIT WINDOW CLOSE soft timeout (20 ms) 무한 반복. RTT에서 POWER-ON 초기화 도중부터 루프 확인. |
| 2026-06-09 | ④버그수정 | CRX0 방전 기능 비활성화(기본값 0) — IQS323 프로토콜 비호환. CH0(유일 활성 채널) disable 시 IC measurement cycle 중단 → RDY HIGH 유지 → soft timeout 루프. re-enable 후 ATI(~100 ms+) 재시작도 원인. tdc_touch_config.h 주석에 실패 근거 기록. |
| 2026-06-09 | ④구현 | TDC_TOUCH_CRX1_DUMMY_ENABLE 플래그 추가 — Prox Input Control(0x43) Internal Reference(bit 13) 활성화로 외부 CRX1 핀 완전 무시. C52 유무·쇼트 무관, CH1 내부 활성화 → IC measurement cycle 유지 → CH0 discharge 재활성화 가능. tdc_touch_config.h·tdc_drv_iqs323.h/.c 3파일 수정. |
| 2026-06-09 | 실측실패 | DUMMY+DISCHARGE 실측(tests/CRX1 Dummy, CRX0 Discharge 로그.md) — 초기화 중 0x43에 reserved 위반값(0x0020) 써서 통신 먹통 → 전체 설정 미적용 → 오감지 → 자동 절전. |
| 2026-06-09 | ②분석 | 데이터시트 정밀 검증 — Internal Reference(0x43 bit13)는 온도/전류 측정용(더미 채널 아님), 0x43 reset=0x01CF인데 0x0020은 reserved bit7(=1)·bit1-0(=11) 위반이 직접 원인. 가설 4종 cross-check. 분석.md 작성. |
| 2026-06-09 | 참고문서 | IQS323-CalCap-더미채널.md 신규 — 데이터시트 레지스터(0x40·0x43·0x44) 정밀 분석, Internal Reference 오용 사례, CalCap 측정 메커니즘, reset value표, 데이터시트 모순 3종 기록. |
| 2026-06-09 | ③계획 | CalCap 더미 채널 재설계 — 0x44(CalCap size 1pF)+0x40(CalCap Rx/Tx) 설정, 0x43 미수정(reset 유지). 모든 값 reset 기준 산출. 계획.md 작성. |
| 2026-06-09 | ④구현 | CalCap 더미 채널 구현 — tdc_drv_iqs323.h(PROX_INTERNAL_REF 제거, PATTERN_DEF/CALCAP 상수 추가), tdc_drv_iqs323.c(sensor_setup DUMMY 분기 0x44→0x40 CalCap Rx/Tx→0x70 교체), tdc_touch_config.h(주석 CalCap 갱신). 검증: 상수 정합·분기 무결·문서 링크 PASS. 빌드는 Eclipse 환경 위임. |
| 2026-06-09 | 실측+분석 | CalCap 2차 실측(tests/CRX1 Dummy, CRX0 Discharge 로그 2.md) — 초기화 FAIL 전부 소멸(0x43 먹통 해결 확인). 그러나 STATE: CAL_ERROR로 터치 미인식. 원인: CH1 CalCap auto-ATI 수렴 실패 → 전역 ati_error(System Status bit6) → get_state 단락. 데이터시트 §5.11(any channel)·코드(read_status 전역 비트) 교차 검증. |
| 2026-06-09 | ④구현 | CH1 ATI Disabled 추가 — 0x46(Sensor1 ATI Setup)=0x08/0x04(ATI Mode=000), CH0의 0x36과 동일. ATI 미실행으로 §5.11 error 체크 스킵 → 전역 ati_error 차단. tdc_drv_iqs323.h(0x46 주소+DUMMY_ATI 상수), .c(DUMMY 분기 0x46 write 추가). 검증: 상수 정합 PASS. **실측 대기.** |
| 2026-06-09 | 문서재구성 | 참고/touch 2폴더 분리(레퍼런스/·이슈해결/)+README 색인. IQS323-CalCap-더미채널.md 분해 → 레퍼런스/IQS323-레지스터-맵.md(데이터시트 종합) + 이슈해결/2026-06_CRX1-ESD-더미채널.md(이슈→해결 전말). 위키링크 3곳 갱신. |
| 2026-06-09 | 실측+분석 | 3차 실측(로그 3) — CH1 ATI Disabled(0x46) 적용됐으나 여전히 CAL_ERROR. 원인: MCLR 직후 auto-ATI가 sensor_setup보다 먼저 돌아 CH1 reset(외부 CRx1+C52) ATI 실패로 전역 ati_error를 이미 set. 0x46은 예방(이후 ATI 차단)일 뿐 set된 비트 치료 못 함(§5.11 수동 re-ATI만 클리어). 데이터시트 A.2 전체 비트맵 확인: 채널별 ATI error 레지스터 없음, CH0 Touch는 채널별 정확. |
| 2026-06-09 | ④구현 | ati_error 무시 — get_state에서 if(ati_error)→CAL_ERROR 단락 제거, CH0 Touch(pressed)만으로 판정. 노말·절전 공통 경로(get_state) 한 곳 수정으로 양쪽 커버. CALIBRATION_ERROR enum은 호환 위해 유지(미발생). tdc_touch.c/.h 수정. 근거: 운용 모드 ATI 미사용+전역 비트+CH0 Touch 채널별 정확. **실측 대기.** |
| 2026-06-09 | 문서 | 이슈해결 문서에 이슈4(auto-ATI 타이밍)+최종해결(ati_error 무시) 추가, 레지스터-맵 §2/§8 보강(채널별 ATI error 부재·auto-ATI 타이밍 함정). |
| 2026-06-09 | ④구현 | 방전 로그 범람 해결 — write_register_discharge 전용 헬퍼 추가(write_and_verify→write only, verify read-back 제거로 통신 절약), TDC_TOUCH_CRX0_DISCHARGE_LOG 플래그(기본 0)로 방전 write 로그 억제(다른 시퀀스·에러 로그 무손상). config 7번 주석 갱신(동작불가→DUMMY 전제 동작)+8번 신규. 검증: 헬퍼 정합·매크로 가시성 PASS. |

---

## 완료 요약

*(작업 완료 후 작성)*

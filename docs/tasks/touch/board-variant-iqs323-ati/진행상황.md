---
name: 진행상황 — IQS323 ATI 보상값 보드 변종 매크로 정리
purpose: 작업 진행 상태 라이브 추적
type: tasks/진행상황
applies_to: [Sound1]
tags: [touch, iqs323, board-variant, progress]
---

# IQS323 ATI 보상값 보드 변종 매크로 정리 — 진행상황

**TL;DR**: tdc_drv_iqs323.c L502~517 영역을 `TDC_BOARD_VARIANT` 매크로 분기로 정리 commit 완료. LED `TDC_FW_VARIANT` 작업과 동일 브랜치에서 사용자 빌드/실기 검증 대기.

작업 브랜치: `claude_feature_ota-dfu-fw-variant-led` (base: `claude_develop`, LED 작업과 공유)
시작: 2026-05-08
최종 갱신: 2026-05-08

---

## 1. 4 단계 진행

| 단계 | 산출물 | 상태 |
|---|---|---|
| ① 요구사항 | [`요구사항.md`](요구사항.md) Rev.0 | 완료 |
| ② 분석 | [`분석.md`](분석.md) Rev.0 | 완료 |
| ③ 구현계획 | [`구현계획.md`](구현계획.md) Rev.0 | 완료 |
| ④ 구현 | tdc_drv_iqs323.c 단일 commit | 완료 (사용자 빌드 + 실기 검증 대기) |

---

## 2. 변경 사항

### 2.1 코드

| 파일 | 변경 |
|---|---|
| [`src/2__cm3/Cortex-M3-src/systemControl/tdc_drv_iqs323.c`](../../../../src/2__cm3/Cortex-M3-src/systemControl/tdc_drv_iqs323.c) | L502~517 영역의 `#if 1 // Mini Board` / `#else // Develop Board` 형태를 `TDC_BOARD_VARIANT` 매크로 + `#if/#elif/#else #error` 분기로 교체 |

코드 diff 요약:
- 헤더 주석 블록 (변종 ↔ ATI 표 + 사용법 + `Board_is_*` 와의 관계 메모)
- `TDC_BOARD_VARIANT_MINI` (=0), `TDC_BOARD_VARIANT_DEVELOP` (=1) 매크로 정의
- `#ifndef TDC_BOARD_VARIANT` 디폴트 = MINI
- `#if (... == MINI)` / `#elif (... == DEVELOP)` / `#else #error`
- ATI 6 개 매크로 (`TDC_DRV_IQS323_ATI_*_LSB/MSB`) 이름·값·사용처 무변경

### 2.2 문서

| 파일 | 변경 |
|---|---|
| [`요구사항.md`](요구사항.md) | 신규 |
| [`분석.md`](분석.md) | 신규 |
| [`구현계획.md`](구현계획.md) | 신규 |
| [`진행상황.md`](진행상황.md) | 신규 (본 파일) |
| [`docs/tasks/list.md`](../../list.md) | 활성 1 행 추가 + 갱신 이력 1 행 추가 |
| [`docs/tasks/LED/ota-dfu-fw-variant/진행상황.md`](../../LED/ota-dfu-fw-variant/진행상황.md) | LED commit hash `356a1e5` 반영 (소소한 fix) |

---

## 3. Commit

| Hash | 메시지 |
|---|---|
| `103ff17` | refactor(touch): IQS323 ATI 보상값 보드 변종을 빌드 매크로 분기로 정리 |

---

## 4. 검증 대기 항목

V1. Mini (디폴트) 빌드 — 매크로 미정의 상태 컴파일 통과, ATI 값이 Mini 측정값으로 매핑. **사용자 실기 터치 검증** (LED 작업과 동시 검증 사이클).
V2. Develop 빌드 — `#define TDC_BOARD_VARIANT TDC_BOARD_VARIANT_DEVELOP` 후 컴파일 통과, ATI 값이 Develop 측정값.
V3. (선택) `TDC_BOARD_VARIANT 99` 시 `#error` 빌드 실패.
V4. 회귀 — IQS323 다른 동작 (read_register/write_register/Re-ATI/FSM) 무변경.

---

## 5. 후속

- 사용자 V1 (Mini, LED 와 동시 검증) 통과 후 → 이력.md 작성 + list.md 활성→완료 이동 (LED 작업과 일괄 처리)
- 머지: `claude_feature_ota-dfu-fw-variant-led` → `claude_develop` (LED + 보드 두 commit 함께)

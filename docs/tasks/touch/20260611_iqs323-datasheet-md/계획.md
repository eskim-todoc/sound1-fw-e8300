---
name: iqs323-datasheet-md-계획
purpose: 데이터시트 md 정리 분할 구조·작성 방식·검증 전략
type: 계획
maturity: stable
tags: [touch, iqs323, datasheet, docs, plan]
---

# 계획 — IQS323 데이터시트 원문 md 정리

**TL;DR**: `데이터시트/`에 README+01~08 작성(원문 충실, 페이지 표기, frontmatter+TL;DR). Claude 직접 작성(PDF 수치 재확인). 작성 후 PDF 페이지구간별 검증 에이전트 ~9개 + 구조검증 1개 fan-out, 치명·중 결함 0까지 수정 루프(≤3회). 승인된 계획 전문은 루트 `docs/plans/eventual-mixing-sphinx.md`.

## 분할 구조 (확정)

`README` + `01_개요·전기·타이밍` · `02_proxfusion동작` · `03_하드웨어설정` · `04_부가기능·UI` · `05_i2c인터페이스` · `06_레지스터레퍼런스`(§9+부록A 통합, 핵심) · `07_오더링·패키지` · `08_개정이력·이슈`.

## 내용 정책

- 원문 충실(값·비트·주소·기본값·공식·표 보존), 영문 원어 병기, 섹션 머리에 `원문 p.NN`.
- frontmatter `type: 데이터시트` 신규 분류 + TL;DR. GFM 표·GitHub Alert. ASCII 다이어그램 금지.
- 06: 주소순, 레지스터별 (주소·이름·기본값·비트필드 표) 묶음. §9 주소표는 상단 인덱스.

## 검증 전략

- **1차 정확성**: PDF 페이지구간 직접 Read = ground truth, 대응 md와 대조 → 누락/오류/할루시네이션/오해석 + 심각도. 01~08 각 1개 + 06 이중 = ~9개.
- **2차 구조**: frontmatter·TL;DR·broken link·번호 일관성·README↔파일 일치 (1개).
- 수정 루프: fan-in 구조화 → 치명·중 수정 → 재검증 ≤3회.

## 완료 기준

치명·중 0건 · frontmatter/TL;DR 전수 · broken link 0 · 06이 부록 A와 100% 일치(이중검증 합치) · README 3축 확장.

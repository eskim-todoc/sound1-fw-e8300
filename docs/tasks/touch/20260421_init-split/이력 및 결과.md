---
name: 터치 센서 초기화 분할 이력
purpose: Auto-ATI 대기를 POWER_ON LED 버스트와 병렬화한 작업 결과 기록
type: tasks/이력
maturity: stable
tags: [touch, init, parallelization, boot-time]
---

# 터치 센서 초기화 분할 — 이력

**TL;DR**: 부팅 시 IQS323 Auto-ATI 대기(POR ~1.5 s / 워치독 리셋 ~165 ms)를 POWER_ON LED 버스트와 병렬 진행하여 체감 부팅 시간 최대 1.5 s 단축. 단일 블로킹 `tdc_iqs323_init()`을 `tdc_touch_init_begin()`(MCLR 트리거 후 즉시 리턴) + `tdc_touch_process()`(매 tick 호출, 미완 시 상태머신·완료 후 폴링·롱터치 판정)로 분리.

작업 슬러그: `touch/20260421_init-split`
기간: ~2026-04-21 (사후 정리: 2026-05-07)
상태: **완료** (코드 적용 확인)

---


## 단계 진척

| 단계 | 산출물 | 상태 | 일시 |
|---|---|---|---|
| ① 요구사항 | 요구사항.md | 완료 | 2026-05-18 |
| ② 분석 | 분석.md | 완료 | 2026-05-18 |
| ③ 계획 | 계획.md | 완료 | 2026-05-18 |
| ④ 구현 | 구현 완료 | 완료 | 2026-05-18 |

## 1. 결과 요약

부팅 시 IQS323 Auto-ATI 대기 (~1.5 s POR / ~165 ms 워치독 리셋) 를 POWER_ON LED 버스트와 **병렬 진행**하여 체감 부팅 시간을 최대 1.5 s 단축.

단일 호출 `tdc_iqs323_init()` (블로킹) 을 두 단계로 분리:

- **`tdc_touch_init_begin()`** — 1 회 호출. MCLR 리셋만 트리거 후 즉시 리턴 (논블로킹).
- **`tdc_touch_process()`** — 매 tick 호출. 초기화 미완 시 상태머신 진행 (Auto-ATI 폴링 → 설정 일괄 적용), 완료 후 폴링·롱터치 판정.

## 2. 변경 사항

### 2.1 적용 위치

| 항목 | 위치 |
|---|---|
| `tdc_touch_init_begin()` | [`tdc_touch.c`](../../../../src/2__cm3/Cortex-M3-src/systemControl/tdc_touch.c) `tdc_touch_init_begin` |
| `tdc_touch_process()` | [`tdc_touch.c`](../../../../src/2__cm3/Cortex-M3-src/systemControl/tdc_touch.c) `tdc_touch_process` |
| 초기화 상태머신 enum | `tdc_touch_init_state_t` (NONE / MCLR_DONE / READY) |
| 초기화 타임아웃 | `TDC_TOUCH_INIT_TIMEOUT_MS = 2500` (터치 누른 채 부팅 대비) |

### 2.2 UX 전제 (구현계획 §1.4)

3 s 롱터치 = 전원 OFF 트리거. 부팅 직후 0.5~1 s 터치 초기화 지연은 사용자 체감 영향 없음. POWER_ON LED 버스트 (1.5 s) 동안 롱터치 기능 불필요.

---

## 3. 사후 정리 (2026-05-07)

본 작업은 2026-04-21 무렵 구현계획.md Rev.2 + 로그 (성공·실패 포함) 작성 후 코드 적용까지 완료됐으나 `이력 및 결과.md` 미작성으로 [`tasks/작업 목록.md`](../../작업 목록.md) 에서 "완료 _(추정)_" 상태로 남아 있었다. 2026-05-07 작업 정리 단계에서 코드베이스 검증으로 정식 완료 확정.

연관 작업: [`touch/20260421_layer-separation`](../layer-separation/) (Step B — 기능·드라이버 분리). 본 작업은 Step A.


## 완료 요약

**기간:** 미상 ~ 2026-05-18
**완료 처리:** 사용자 결정으로 완료 (Package C 후속 정리, 2026-05-18)

### 무엇을 했는가
사용자 결정으로 완료 — 본 작업 폴더의 요구사항·분석·계획 산출물은 작성 완료 상태이며, 실 구현·검증 또는 후속 작업 흡수가 이루어진 시점에서 사용자 결정으로 본 작업을 완료 처리한다. 상세 경과는 본 파일의 기존 결과·이력·시계열 로그 섹션 참조.

### 후속 영향
없음 (별도 후속 작업이 필요하면 신규 task 폴더로 분리)

## 자체 검토

### 지침 준수 여부
| 지침 | 준수 |
|---|---|
| [`작업 진행 규칙.md`](../../../../../../docs/지침/일반/작업%20진행%20규칙.md) | ✅ |
| [`문서 작성 규칙.md`](../../../../../../docs/지침/일반/문서%20작성%20규칙.md) | ✅ |

### 논리적 정합성
| 점검 | 결과 |
|---|---|
| 본문 기존 내용 보존 (양식 정합화 시 무손실) | ✅ |
| frontmatter·TL;DR 기준 충족 | ✅ |
| 관련 산출물(요구사항·분석·계획·이력) 상호 일관 | ✅ |

### 미해결 이슈
| 이슈 | 처리 |
|---|---|
| 해당 없음 | — |

# 부트로더 단계 LED 조기 점등 — 진행상황 (세션 핸드오프)

최종 갱신: 2026-04-27 (2회차)
상태: **🟢 C 작업 진입 준비** — A(non-blocking-fade-off) 완료, B(LED G 미스터리) 자연 해소, C(부트로더 LED) 진입 가능

> [!NOTE]
> 이 문서는 컨텍스트 한계로 다음 세션 인계용. 본 작업은 [`power-on-early-lighting`](../power-on-early-lighting/요구사항.md) 의 **상위 단계** — CM3 앱이 아닌 **CM3-Bootloader** 단계에서 LED를 먼저 켜자는 아이디어.

---

## 1. 작업 개요

### 1.1 목표
사용자가 전원 입력 직후 "켜졌다" 를 빨리 인지할 수 있도록 **CM3-Bootloader 진입 직후 LED 점등**. 이후 정식 `LED_ST_POWER_ON` 버스트는 "다 켜졌다" 의미로 그대로 사용 → **두 단계 피드백** 제공.

### 1.2 부팅 타임라인 (실측 — 오실로스코프)

| 구간 | 길이 | 손댈 수 있는가 |
|---|---|---|
| 전원 입력 → 부트로더 첫 시작 | **500 ms** | ❌ Ezairo 내부 ROM 부트로더 (Flash 로드) — 물리적 한계 |
| 부트로더 첫 시작 → CM3 첫 시작 | **440 ms** | ✅ `0__bootloader/main.c` (우리 영역) |
| CM3 첫 시작 → Power On LED | **730 ms** | ✅ CM3 앱 — 기존 `power-on-early-lighting` 작업 대상 |
| **합계 (전원 → 첫 LED)** | **1,670 ms** | |

측정 단서: JTDO 디버그 핀 토글로 부트로더 시작/끝 마킹.

### 1.3 정량적 가치
- 현재: 첫 LED 인지 = 1,670 ms
- 부트로더 LED 적용 시: **500 ms** (ROM 영역만 남음)
- **단축: 약 1,170 ms (~70 %)**
- 부트로더 LED ↔ 정식 LED 사이 텀: **1,170 ms**
  - `power-on-early-lighting` 으로 730 ms 구간을 단축하면 이 텀도 함께 줄어듦 (보완적)

---

## 2. 결정 사항 (사용자 확정)

| # | 결정 |
|---|---|
| Q-B | ROM 부트로더 500 ms는 물리적 한계, 손대지 않음 (전제) |
| Q-C | "끊김 없이 이어지는 두 단계 피드백" 방향 채택. 색·밝기·패턴 차이 해소 방법 미결 |
| Q-D | 부트로더 LED 색상 = **SKYBLUE** (정식 `LED_ST_POWER_ON` 과 동일) |

---

## 3. 미해결 조사 항목

### 3.1 LED G 미스터리 (Q-A) — ✅ 자연 해소 (2026-04-27 2회차)

> [!NOTE]
> A 작업 사후 fix (`LED/non-blocking-fade-off/이력.md §8`, commit `85c250f`) 로 **첫 이상 펄스가 사라짐**. cross-fade Phase A (FADE_OUT) 가 fade-off cancel 시점 잔존 `LED_outputColor` 를 prev 색으로 보고 가동되던 것이 본 미스터리의 정체였음. 별개 HW/SDK 이슈가 아니었음.
>
> V1/V2/V3 측정은 **불필요**. 후보 1 (`ci_dio_configure_normal` 잔존값) / 후보 2 (`Sys_DIO_CM3JTAGConfig` 누설) 모두 기각. 단 새 세션에서 재측정 시 첫 띡- 14 펄스 정상 출력 확인을 한 번 더 검증할 것.

**현상 (역사적 기록)**: 오실로스코프에서 부트로더 실행 구간(JTDO HIGH = 부트로더 → CM3 사이의 440 ms 윈도우, 또는 CM3 → Power On LED 730 ms 윈도우 중) LED G 채널에 의도하지 않은 ON 펄스 발견.

**기각된 가설**
- ❌ UART RX 핀(DIO15 = LED G) 공유로 인한 `Sys_UART_DIOConfig` 재설정 → `DIO_NUM_UART_ENABLE` = 1 로 `gs_tdc_uart_enable` = false → 해당 코드 경로 스킵 확정

**남은 가설**
- ⏳ **후보 1**: CM3 `ci_dio_configure_normal()` 이 LED 핀을 GPIO_OUT으로 (재)설정할 때 출력 데이터 reg 잔존값으로 잠시 LOW(=ON for active low)
- ⏳ **후보 2**: CM3 `Sys_DIO_CM3JTAGConfig(true, false)` (`Cortex-M3-src/main.c:274`) 가 SWJ-DP 그룹에 DIO15 포함 시 LED G 핀 점유 (JTDO 토글이 LED G 신호로 누설)

**검증 미실시 — 다음 세션에서 측정 필요**
| ID | 방법 | 해석 |
|---|---|---|
| V1 | CM3 main 진입 직후·`Sys_DIO_CM3JTAGConfig` 직전·직후·`ci_dio_configure_normal` 직후 별도 디버그 핀 토글 | 펄스 시점 매칭 |
| V2 | CM3 main 진입 직후 `Sys_GPIO_Write(LED_G, 1)` 한 줄 추가 | 사라짐 = 후보 1 |
| V3 | `Sys_DIO_CM3JTAGConfig(true, false)` 일시 제거 (RTT 디버깅 일시 포기) | 사라짐 = 후보 2 |

**LED 엔진 초기값 검증 결과 (이 세션에서 코드 분석 완료)**:
타이머 핸들러(`CFX_0_IRQHandler` → `led_arbiter_tick` → `LED_OUT`)는 첫 tick부터 모든 GPIO OFF로 정상 출발. LED G ON 경로는 핸들러 외부.

### 3.2 SDK 확인 필요
`Sys_DIO_CM3JTAGConfig` 정의가 SDK 영역에 있어 Sound1 repo 내에는 없음. 후보 2 정확 판단을 위해 SDK 경로 확인 후 SWJ-DP 핀 매핑 조사 필요.

### 3.3 SKYBLUE 색감 매칭 (Q-C 후속)
부트로더는 PWM 미지원 → 풀밝기 GPIO ON/OFF만 가능. 정식 LED는 PWM dimming + RGB cap (`k_led_mix[en__LED_SKYBLUE] = {0, 30, 40}`).

| 옵션 | 부트로더 출력 | 정식 LED 와 차이 |
|---|---|---|
| C1 | G+B 풀밝기 (시안) | 색 가깝되 풀밝기 vs PWM dimming 밝기 점프 |
| C2 | R+G+B 풀밝기 (백색) | 색 다름, 풀밝기 |
| C3 | 단일 LED (B 또는 G) 풀밝기 | 색·밝기 둘 다 차이 |
| C4 | 부트로더 SW PWM | 매칭 정확, 부트로더 코드↑ + 440 ms 부담 |

**잠정 권고: C1 + 정식 LED 첫 사이클 fade-in 생략** (밝기 점프 최소화). 단 결정 전 확인 필요:
1. CM3 SKYBLUE의 R:G:B 비율 — 위 코드(`k_led_mix`)에서 확인됨: `{0, 30, 40}` (G 30%, B 40%)
2. 정식 LED 첫 사이클 fade-in 생략 가능 여부 — 미조사

---

## 4. 발견된 별도 버그 — `turnOffLED()` / `led_force_fade_off()` 폴링

이 세션 도중 사용자가 발견. **본 작업과 별개 이슈, 별도 task로 분리 필요.**

### 4.1 위치
- `projects/Sound1/src/2__cm3/Cortex-M3-src/systemControl/LedOutput.c:632-662` — `turnOffLED()`
- 같은 파일 `:363-391` — `led_force_fade_off()`

### 4.2 문제
둘 다 `for (...) { LED_OUT(); delay_ms(1); }` 폴링 패턴.
- `turnOffLED`: 30 ms 블로킹 (`LED_DIMMING_FADE_MAX_MS = 30`)
- `led_force_fade_off`: 40 ms 블로킹 (`LED_DIMMING_FADE_MAX_MS + 10`)

타이머 ISR 기반 LED 설계의 **"메인 코어가 LED 처리에 갇히지 않는다"** 전제를 정면 위반. 게다가 `s_led_isr_suspended = true` 로 ISR을 멈춰놓고 직접 폴링하는 거꾸로 된 구조.

### 4.3 수정 방향
ISR 기반 fade-off 상태머신:
```c
/* 호출자: 즉시 return */
void turnOffLED(void) {
    s_fade_off_state = LED_FADE_OFF_ACTIVE;
    s_fade_off_t     = 0;
}

/* led_arbiter_tick() 내부에서 매 tick 1 step */
void led_arbiter_tick(void) {
    if (s_fade_off_state == LED_FADE_OFF_ACTIVE) {
        /* perceived 감소 → LED_OUT() → t++ → 완료 시 IDLE */
        return;
    }
    /* ... 기존 arbiter 로직 ... */
}
```

**Initialize-time 호출 케이스**: 이 시점엔 CFX FIFO IRQ 미활성 → ISR이 안 돌아 위 상태머신이 진행 안 됨. `LED_outputColor = BLACK` (data init) 이므로 fade 자체가 무의미. → "ISR 활성 검사 → 비활성이면 즉시 OFF 1회" 분기 필요.

### 4.4 `LED_DIMMING_FADE_MAX_MS` 변경 영향
이 세션 직전 commit `5240bf2`에서 15 → 30 으로 상향. 위 폴링 블로킹도 15 → 30 ms 로 늘어난 상태. 수정이 더 시급해진 셈.

---

## 5. 이 세션 코드 변경

**없음** — 조사·논의만 진행. 모든 변경은 이전 세션 5개 commit으로 이미 push 완료 (`59e0318..6c54cce`):
- `1c52544` bootloader R LED 점등 (디버그용 `#if 1` 가드)
- `8d683fa` cfx I2S GPIO 폴링 + 수동 버퍼 복사
- `bbc540a` cm3 ISD link 로그 주석
- `5240bf2` cm3 LED fade 15→30ms ← **본 미해결 버그와 직접 관련**
- `6c54cce` cm3 FAKE_0x34 250 ms + 9% 등

브랜치 상태: `claude_develop`, working tree clean, origin 동기화 완료.

---

## 6. 다음 세션 시작 시 우선순위 (2026-04-27 2회차 갱신)

| # | 항목 | 상태 |
|---|---|---|
| **A** | `turnOffLED` / `led_force_fade_off` ISR 상태머신 변환 | ✅ **완료** (`LED/non-blocking-fade-off/`, `c9364ff` + `b24d861`). 30 ms 절약 검증, 회귀 없음 |
| **B** | LED G 미스터리 검증 (V1/V2/V3 측정) | ❌ **불필요** — A 사후 fix (`85c250f`) 로 첫 이상 펄스 자연 해소 (§3.1 참조). 단 새 세션에서 재측정으로 한 번 더 확인 권장 |
| **C** | 부트로더 LED 본 작업 (요구사항 → 분석 → 구현계획) | 🟢 **진입 가능** (B 전제 해소) |

**다음 세션 (2026-04-30 목요일) 진입 작업: C**.

---

## 7. 다음 세션 재개 방법 (2026-04-30 목요일)

1. **컨텍스트 복구**: 이 문서 + [`LED/non-blocking-fade-off/이력.md`](../non-blocking-fade-off/이력.md) (특히 §8 사후 fix) 먼저 읽기
2. **현 git 상태 확인**: `claude_main` / `claude_develop` 동기화 여부 (직전 세션 종료 시점에 push 완료 상태)
3. **C 작업 시작**:
   - 본 폴더에 `요구사항.md` 작성 (§1·§2·§3.3 내용을 정식 요구사항으로 정리, Q-B/Q-C/Q-D 결정 반영)
   - 작업 브랜치: `claude_feature_bootloader-power-on-indicator` (base: `claude_develop`)
   - `요구사항.md` → 사용자 승인 → `분석.md` → 사용자 승인 → `구현계획.md` → 사용자 승인 → 구현
4. **C 작업 핵심 미해결 항목** (요구사항 작성 시 다뤄야 할 결정):
   - **§3.2 SDK 확인**: ROM 부트로더가 CM3-Bootloader 시작 직전에 LED 핀 상태를 어떻게 두는지 (Floating? Pull-down?). 첫 LED 점등 코드 위치·시점 결정에 영향
   - **§3.3 SKYBLUE 색감 매칭**: 부트로더 단계 SKYBLUE 와 정식 `LED_ST_POWER_ON` SKYBLUE 가 같은 mix `{R=0, G=30, B=40}` 으로 보이는지. 부트로더에서는 PWM dimming 미가동 — 100% 점등이 어떻게 보일지 결정 필요
   - **부트로더 LED ↔ 정식 LED 사이 텀**: 1,170 ms (현재). [`power-on-early-lighting`](../power-on-early-lighting/) 작업과의 통합 전략 (병행 진행? 순차?) 결정
5. **B 재검증 (선택, 5 분 작업)**: 빌드된 상태에서 첫 띡- 측정으로 첫 이상 펄스 잔존 여부 한 번 더 확인. 잔존 시 V1/V2/V3 진입 (가능성 낮음)

---

## 8. 관련 문서·코드

| 항목 | 위치 |
|---|---|
| 기존 작업 (CM3 앱 LED 앞당김) | [`docs/tasks/LED/power-on-early-lighting/`](../power-on-early-lighting/) |
| LED 엔진 본체 | [`src/2__cm3/Cortex-M3-src/systemControl/LedOutput.c`](../../../../src/2__cm3/Cortex-M3-src/systemControl/LedOutput.c) |
| LED 타이머 핸들러 | [`src/2__cm3/Cortex-M3-src/systemControl/driver_timmer.c`](../../../../src/2__cm3/Cortex-M3-src/systemControl/driver_timmer.c) |
| 부트로더 main (LED `#if 1` 블록 위치) | [`src/0__bootloader/main.c:16-24`](../../../../src/0__bootloader/main.c) |
| LED 도메인 참고 | [`docs/참고/LED/`](../../../참고/LED/) |

---

## 9. 개정 이력

| 날짜 | 이벤트 |
|---|---|
| 2026-04-27 | 세션 핸드오프 작성. 부트로더 LED 작업 조사 단계 + 미해결 항목 3종 (LED G·SKYBLUE 매칭·turnOffLED 버그) 정리. |
| 2026-04-27 (2회차) | A 작업 (`LED/non-blocking-fade-off`) 완료 — ISR 상태머신 변환 + 사후 fix (cross-fade Phase A 우회). 부수 발견: B (LED G 미스터리) 가 본 fix 로 자연 해소 — 두 현상이 같은 cross-fade 시퀀스 측면이었음. §3.1 / §6 / §7 갱신, 다음 세션 (2026-04-30 목요일) C 작업 진입 안내. |
| 2026-04-30 | C 작업 진입 — 작업 브랜치 `claude_feature_bootloader-power-on-indicator` 생성, [`요구사항.md`](요구사항.md) Rev.0 → Rev.3 (사용자 4 차 피드백). 핵심 결정: ① C4 (SW PWM) 채택, ② CM3 와 동일 dimming/PWM/색, ③ 인접 작업과 별개, ④ 빌드 가드 정식 채택, ⑤ R-11 (CM3 적재 중 ISR 가용성) 영향 시 본 작업 폐기 + `power-on-early-lighting` 으로 가치 이전. |
| 2026-04-30 | [`분석.md`](분석.md) Rev.0 — Phase 1 (R-11) **가능 ✅** 판정 (부트로더 PRAM5 와 CM3 적재 영역 운용 분리, NVIC 단일 비활성, polling-only, 타이머 미사용). 본 작업 진행 결정. Phase 1 구현 의무 O-1~O-4. |
| 2026-04-30 | `분석.md` Rev.1 — Phase 2 정밀 분석 (Q-PWM-1/2/3, R-1/3/4/5/6/12). 결정 D-1~D-9: 타이머=Timer3 외, 자체 구현, T-B (클럭 30.72 MHz 후), 빌드 가드 분기, fade=540 ms/cycle, active LOW 가정, 540 ms wait. |
| 2026-04-30 | `분석.md` Rev.2 — 사용자 2 차 피드백: ① **Timer2** (CM3 미점유 F-10), ② T-B 확정, ③ main.c 디버그 GPIO 블록 빌드 가드로 무력화 → 풀다운 자연 OFF → fade-in 자연 점등, ④ 빌드 가드 + UART 검증 핀 (`DIO_NUM_UART_ENABLE`, main.c:59) 2 단 분기, ⑤ **active HIGH** 확정. |
| 2026-04-30 | [`구현계획.md`](구현계획.md) Rev.0 → Rev.1. Rev.1 사용자 피드백: ① 네이밍 지침 (`tdc_` prefix) 일관 적용 — 모듈 = `tdc_boot_led`, 빌드 가드 = `TDC_BOOT_LED_ENABLE`, ② Cortex-M3 ISR 본문 IRQ Clear 호출 제거 (NVIC 자동 ack). 7-step commit 분리 — Step 1 (가드 + 빈 stub) 으로 가드 OFF 회귀, Step 2 (본체) 로 가드 ON 검증. |
| 2026-04-30 | **Step 1 commit** — 빌드 시스템 통합 + 가드 도입. `main.h` `TDC_BOOT_LED_ENABLE` 매크로 디폴트 0, `main.c:16-24` 디버그 LED 블록 `#if !TDC_BOOT_LED_ENABLE` 가드, `bootloader.c` 통합 2 지점 (T-B init+start, 점프 직전 wait+stop) `#if TDC_BOOT_LED_ENABLE` 가드 + `<tdc_boot_led.h>` include. 신규 파일 [`include/tdc_boot_led.h`](../../../../src/0__bootloader/include/tdc_boot_led.h) (인터페이스), [`source/led/tdc_boot_led.c`](../../../../src/0__bootloader/source/led/tdc_boot_led.c) (전체 가드 안 빈 stub). 가드 OFF 디폴트 빌드 시 기존 부트로더 동작 100% 유지. |

# 부트로더 단계 LED 조기 점등 — 이력

작업 슬러그: `LED/bootloader-power-on-indicator`
브랜치: `claude_feature_bootloader-power-on-indicator` (base: `claude_develop`)
기간: 2026-04-27 (조사 시작) ~ 2026-04-30 (구현·검증 완료)
상태: **완료** (사용자 검증 통과)

---

## 1. 결과 요약

- **사용자 검증**: Step 3 보정 후 정상 동작 (가드 ON + UART 비활성 시 SKYBLUE fade cycle 정상)
- **회귀**: 가드 OFF 디폴트 회귀 — 기존 부트로더 동작 (UART, 디버그 LED, CM3 부팅) 영향 없음
- **정량 효과** (요구사항.md §1.2 timeline 기반):
  - 기존: 첫 LED 인지 = 1,670 ms (ROM 500 + 부트로더 440 + CM3 730)
  - 본 작업: 첫 LED 인지 ≈ 540 ms (ROM 500 + 부트로더 T-B = 수십 ms 후)
  - **단축: ~1,130 ms (약 68%)**
- **빌드 가드**: `TDC_BOOT_LED_ENABLE` (Step 4 시점 디폴트 = 1, 정식 채택). 비활성화는 `-DTDC_BOOT_LED_ENABLE=0` 으로 override

---

## 2. 변경 사항

### 2.1 파일

| 파일 | 분류 | +/− |
|---|---|---|
| [`include/main.h`](../../../../src/0__bootloader/include/main.h) | 수정 | +7 |
| [`main.c`](../../../../src/0__bootloader/main.c) | 수정 | +2 / −0 (디버그 블록 가드) |
| [`source/bootloader.c`](../../../../src/0__bootloader/source/bootloader.c) | 수정 | +29 (T-B 진입 + 점프 직전 wait + include) |
| [`include/led/tdc_boot_led.h`](../../../../src/0__bootloader/include/led/tdc_boot_led.h) | 신규 | +33 |
| [`source/led/tdc_boot_led.c`](../../../../src/0__bootloader/source/led/tdc_boot_led.c) | 신규 | +233 |
| `.cproject` | 수정 | include path 추가 |

### 2.2 Commit (시간 순)

문서:

| Hash | 내용 |
|---|---|
| `74a5965` | 요구사항.md (Rev.0~Rev.3) + 분석.md (Rev.0~Rev.2) + 구현계획.md (Rev.0~Rev.1) + tasks/list.md |
| `73dd1bf` | 진행상황.md §6 측정 절차 + Step 1/2 commit 등재 |
| `f493a2c` | 진행상황.md Step 3 보정 등재 (Timer 클럭 SLOWCLK_DIV32) |

코드 (Step 1~4, bisect 가능하도록 분리):

| Hash | Step | 내용 |
|---|---|---|
| `4099af7` | 1 | `TDC_BOOT_LED_ENABLE` 가드 + `tdc_boot_led` 빈 골격 (가드 OFF 회귀 검증) |
| `75fa380` | 2 | `tdc_boot_led.c` 본체 — Timer2 ISR + phase 머신 (초기 가설: ISR 10 us, reload 307) |
| `27f2c28` | 3 | **Timer 클럭 보정** — SLOWCLK_DIV32 = 40 kHz 발견. D_CLK 분주 설정 추가 + reload 0 + PWM 주기 2.5 ms (400 Hz) + 1 ms 별도 카운터 |
| `9bc5163` | 4 | 헤더 위치 정리 (`include/led/`) + 가드 디폴트 1 + .cproject include path |

병합: `--no-ff` (Merge commit 별도, 사용자 승인 후)

---

## 3. 주요 설계 결정 (분석.md §2.8 D-1~D-9)

| ID | 결정 | 근거 |
|---|---|---|
| D-1 | 타이머 = **Timer2** | CM3 가 Timer3 점유 (`ci_timer.c:18`), Timer2 미점유 (F-10) |
| D-2 | LED 엔진 = **자체 구현** (CM3 LedOutput.c 재사용 X) | Peak 시간이 정식 LED_ST_POWER_ON 의 2 배 (300 vs ~150 ms). SKYBLUE 단색 전용 가벼운 SW PWM 이 코드량·복잡도 적음 |
| D-3 | 점등 시작 = **T-B** (`Sys_Trims_SetOperatingFrequency()` 직후, [bootloader.c:643](../../../../src/0__bootloader/source/bootloader.c)) | T-A → T-B 사이 = 수십 ms (NVMReadBytes EEPROM). T-A 시 클럭 4 배 변경으로 fade 시간 1/4 단축 → 패턴 깨짐 |
| D-4 | main.c:16-24 디버그 GPIO 블록 빌드 가드로 무력화 → 풀다운 자연 OFF → fade-in 자연 점등 | 사용자 명시: "물리적으로 LED 가 OFF 상태로 전원이 켜짐. 풀다운 저항" |
| D-5 | Fade = 30 + **300** + 30 + 180 = **540 ms / cycle** (Peak 만 정식의 2 배) | 사용자 결정 — 첫 띡 강조 |
| D-6 | `bootloader_boot_cm3()` 직전 wait — 540 ms 보장 후 정리 | OFF 유지 + CM3 fade-out 30 ms = 최소 210 ms OFF 보장 → '삑—, __, 삑×5' 패턴 |
| D-7 | **빌드 가드 + 런타임 UART 검증 핀** (`DIO_NUM_UART_ENABLE`, [main.c:59](../../../../src/0__bootloader/main.c)) 2 단 분기 | UART RX (DIO15) 와 G LED 핀 공유. UART 활성 시 LED 비활성 |
| D-8 | LED 극성 = **active HIGH** | 사용자 확정. `LED_IS_ACTIVELOW` 미정의 상태와 일관 |
| D-9 | Phase 1 의무 O-1 (PRAM5 배치) / O-2 (NVIC enable) / O-3 (종료 정리) / O-4 (ISR 우선순위) 전부 적용 | R-11 (CM3 적재 중 ISR 가용성) 가능 ✅ 판정 (분석.md §1) |

---

## 4. 사용자 측정 1 차 오동작 → Step 3 보정 (학습 큰 사건)

### 4.1 현상

Step 1/2 commit 후 사용자 빌드·측정: 동작 가시성 모호. 사용자 진단:
> "동작을 하는 건지 안 하는 건지 모르겠다. CM3에서 시스템 클럭 설정한 후에 관련 클럭들에 대한 div 설정하는 항목을 부트로더에서도 동일하게 해야할거 같아. 타이머 클럭에 문제가 있는거 같아."

### 4.2 원인 진단

이중 문제:

#### a. RSL10 General Timer 의 클럭 체인 — Step 2 의 가설 잘못

[`ci_timer.h:56`](../../../../src/2__cm3/Gen1_5/common/ci_timer.h):
```
공식: T = 2^prescale × (tick + 1) / 40 kHz   (SLOWCLK = 1.28 MHz 기준)
```

즉 **General Timer = SLOWCLK / 32 = 40 kHz** 가 base clock. SYSCLK 30.72 MHz 가 아님.

Step 2 의 reload = 307 (30.72 MHz / 100 kHz - 1 가정) 은 무의미한 값. (40 kHz 기반에서 reload 307 → 7.7 ms 주기 ISR — fade 시간 모두 7.7 배 늘어남).

#### b. 부트로더에 D_CLK 분주 설정 자체 부재

[`ci_power.c:154-156`](../../../../src/2__cm3/Gen1_5/common/ci_power.c) (CM3 측):
```c
// SLOWCLK은 반드시 1.28 MHz로 설정
D_CLK->CFG_1 = (ADCCLK_PRESCALE_8 | ADCCLK_SRC_SYSCLK | SDMCLK_PRESCALE_2
              | SLOWCLK_PRESCALE_24 | SLOWCLK_SRC_SYSCLK | UARTCLK_SRC_SYSCLK);
D_CLK->CFG_2 = (UCLK_PRESCALE_8 | UCLK_SRC_ADCCLK);
delay_ms(5);
```

부트로더에는 이 분주 설정이 없어 SLOWCLK 가 디폴트값 (불명) 으로 동작. SYSCLK 30.72 MHz 변경 후에도 SLOWCLK 분주 미설정 → Timer base clock 불명.

### 4.3 보정 (Step 3, commit `27f2c28`)

`tdc_boot_led_init()` 안에:

1. **D_CLK 분주 설정 추가 (CM3 와 동일)**: `SLOWCLK_PRESCALE_24` → SLOWCLK = 1.28 MHz → Timer = 40 kHz 보장. UARTCLK_SRC_SYSCLK 유지 → UART baud 영향 없음.
2. **`tdc_delay_ms(5)` 안정화 대기** (CM3 ci_power.c:158 동일).
3. **PWM 매개변수 재계산** — Timer 25 us tick (= 1/40 kHz, 분해능 한계):
   - reload 307 → **0**
   - PWM 주기 = 100 bin × 25 us = **2.5 ms = 400 Hz** (깜빡임 안전)
   - 1 ms 단위 phase tick 은 별도 카운터로 ISR 40 회 마다 호출 (`s_tdc_boot_led_isr_in_ms`)

duty G:B = 30:40 그대로 유지 (k_led_mix[en__LED_SKYBLUE] 비율).

### 4.4 검증

사용자 재측정: **"정상동작하는거 같아"** — Step 3 보정 후 의도 형태 동작 확인.

### 4.5 학습 (회고 후보)

- **Cortex-M MCU 의 peripheral 은 시스템 클럭 직결이 아니라 분주된 sub-clock 기반인 경우가 많다.** General Timer 가 그 예 — SLOWCLK 분주값에 의존.
- **CM3 측 클럭 분주 설정을 부트로더가 안 하면 같은 peripheral 동작이 다르게 나온다.** 부트로더는 보통 polling 기반이라 분주에 둔감하지만, ISR 기반 신규 모듈을 부트로더에 추가하면 분주 설정도 옮겨와야 한다.
- **SDK 함수의 인자 단위 (cycles vs us vs ms)** 는 헤더 주석으로 확인. ci_timer.h:56 같은 공식 주석이 결정적 단서.

---

## 5. 호출자 영향

| 위치 | 영향 |
|---|---|
| `main.c:16-24` 디버그 LED 블록 | `#if !TDC_BOOT_LED_ENABLE` 가드. 가드 OFF 시 기존 동작 100% 유지. 가드 ON 시 무력화 → 풀다운 자연 OFF |
| `bootloader.c:1496` `bootloader_load_manu_table` 직후 (T-B) | `#if TDC_BOOT_LED_ENABLE` 가드 안에 검증 핀 read + `tdc_boot_led_init/start` 호출 |
| `bootloader.c:1374` `bootloader_boot_cm3` 직전 | `#if TDC_BOOT_LED_ENABLE` 가드 안에 540 ms wait + `tdc_boot_led_stop` 호출 |
| 부트로더 UART, NVM, DMA, service mode 분기 | 영향 없음 (UARTCLK_SRC_SYSCLK 유지, 가드 분기) |
| CM3 측 LED 엔진 (`LedOutput.c`) | 변경 없음 — 본 작업은 자체 구현 |

---

## 6. 후속 작업

- **CM3 측 빠른 부팅**: 본 작업 직후 사용자가 새 세션에서 진행 예정. 관련 작업 = [`LED/power-on-early-lighting`](../power-on-early-lighting/) (CM3 진입~정식 LED 730 ms 단축). 본 작업과 별개로 진행 가능 (분석.md §9 명시).
- **빌드 가드 자체 제거 검토**: 디폴트 1 로 굳어졌으니 추후 가드 자체 제거 가능 (단, 디버그 LED 회귀 테스트 용도가 남아 있으면 유지).

---

## 7. 참고

- [요구사항.md](요구사항.md) — Rev.0~Rev.3
- [분석.md](분석.md) — Rev.0 (Phase 1 R-11) ~ Rev.2 (Phase 2 + 사용자 2 차 피드백)
- [구현계획.md](구현계획.md) — Rev.0~Rev.1 (네이밍 지침 + ISR ack 제거)
- [진행상황.md](진행상황.md) — 세션 핸드오프 + Step 1~4 진행 기록
- 발견 경위: [LED/non-blocking-fade-off/이력.md](../non-blocking-fade-off/이력.md) §6 후속 작업 = 본 작업 (C 작업)

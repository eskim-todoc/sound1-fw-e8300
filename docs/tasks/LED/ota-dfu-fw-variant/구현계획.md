---
name: 구현계획 — OTA DFU FW 이미지 변종별 LED 색상 정리
purpose: LedOutput.c 변경 단계별 절차·검증·롤백
type: tasks/구현계획
applies_to: [Sound1]
tags: [LED, ota-dfu, fw-variant, plan]
---

# OTA DFU FW 이미지 변종별 LED 색상 정리 — 구현계획

**TL;DR**: 단일 commit. ① LedOutput.c 디스크립터 표 위에 변종 매크로 블록 + 색 매크로 추가, ② 디스크립터 표 두 줄 (`LED_ST_IN_USE`, `LED_ST_POWER_ON`) 을 색 매크로 사용으로 변경, ③ 인라인 코멘트 정리. 패턴 동작 무변경, 디폴트는 App (운용).

작성일: 2026-05-08

---

## 1. 변경 파일

| 파일 | 변경 |
|---|---|
| [`src/2__cm3/Cortex-M3-src/systemControl/LedOutput.c`](../../../../src/2__cm3/Cortex-M3-src/systemControl/LedOutput.c) | 변종 매크로 블록 추가 + 디스크립터 표 두 줄 |

---

## 2. Step 1 — 변종 매크로 블록 추가

위치: `k_led_patterns[]` 정의 직전 (현 L187 `Pattern Descriptor Table` 헤더 주석 블록 위).

```c
/* ========================================================================
 *  OTA DFU 테스트용 FW 이미지 변종 — LED 색상 분기
 * ========================================================================
 *  하나의 펌웨어 소스가 운용용 App 이미지와 Factory Reset 이미지 두 변종으로
 *  빌드되어 OTA DFU 테스트 시 어느 이미지가 동작 중인지 LED 색으로 즉시
 *  식별 가능하도록 한다.
 *
 *  변종         | LED_ST_IN_USE | LED_ST_POWER_ON
 *  -------------|---------------|------------------
 *  APP (운용)   | WHITE         | SKYBLUE
 *  FACT_RESET   | PURPLE        | WHITE
 *
 *  사용:
 *   - 운용/정식 빌드: 매크로 미정의 → 자동 디폴트 APP. 별도 작업 불필요.
 *   - Factory Reset 테스트 빌드: 본 파일 디폴트를 FACTORY_RESET 으로 변경
 *                                또는 빌드 옵션 -DTDC_FW_VARIANT=TDC_FW_VARIANT_FACTORY_RESET.
 * ======================================================================== */
#define TDC_FW_VARIANT_APP             0
#define TDC_FW_VARIANT_FACTORY_RESET   1

#ifndef TDC_FW_VARIANT
#define TDC_FW_VARIANT  TDC_FW_VARIANT_APP   /* 디폴트: 운용 (APP) */
#endif

#if (TDC_FW_VARIANT == TDC_FW_VARIANT_FACTORY_RESET)
    #define TDC_FW_LED_IN_USE_COLOR    en__LED_PURPLE
    #define TDC_FW_LED_POWER_ON_COLOR  en__LED_WHITE
#elif (TDC_FW_VARIANT == TDC_FW_VARIANT_APP)
    #define TDC_FW_LED_IN_USE_COLOR    en__LED_WHITE
    #define TDC_FW_LED_POWER_ON_COLOR  en__LED_SKYBLUE
#else
    #error "TDC_FW_VARIANT 미지원 값. TDC_FW_VARIANT_APP 또는 TDC_FW_VARIANT_FACTORY_RESET 만 허용."
#endif
```

---

## 3. Step 2 — 디스크립터 표 두 줄 변경

기존:
```c
[LED_ST_IN_USE]         = { en__LED_PURPLE/*en__LED_WHITE*/,   0,    0,    0 },  // 흰색 지속 ON
...
[LED_ST_POWER_ON]       = { en__LED_WHITE/*en__LED_SKYBLUE*/, 180, 360,  4 },   // SKYBLUE ON 180ms / OFF 180ms × 4회 버스트
```

변경:
```c
[LED_ST_IN_USE]         = { TDC_FW_LED_IN_USE_COLOR,   0,    0,    0 },  // 지속 ON (App=WHITE / FactRst=PURPLE)
...
[LED_ST_POWER_ON]       = { TDC_FW_LED_POWER_ON_COLOR, 180, 360,  4 },   // ON 180ms / OFF 180ms × 4회 버스트 (App=SKYBLUE / FactRst=WHITE)
```

---

## 4. Step 3 — 빌드 검증 (수동 / 사용자)

본 작업은 컴파일러 호출 단계까지 포함하지 않음 (Eclipse 빌드 환경). 사용자에게 다음 두 케이스 빌드 요청:

V1. 디폴트 (App) 빌드 — 본 파일 변경 그대로 컴파일. IN_USE = WHITE, POWER_ON = SKYBLUE.
V2. Factory Reset 빌드 — `#define TDC_FW_VARIANT  TDC_FW_VARIANT_FACTORY_RESET` 로 한 줄 디폴트 변경 후 컴파일. IN_USE = PURPLE, POWER_ON = WHITE.

(V3. 의도적 잘못된 값 — `#define TDC_FW_VARIANT  99` 시 `#error` 발동 — 선택 검증)

---

## 5. 위험·롤백

### 위험_1: ci_ble_control_boot.c 미커밋 변경 동거
working tree 에 LedOutput.c 와 ci_ble_control_boot.c 두 파일 변경 동거 상태. 본 작업 commit 시 LedOutput.c 만 stage. ci_ble_control_boot.c 는 working tree 그대로 둔다 (별도 작업).

### 위험_2: 변종 매크로가 다른 파일에서 사용
`grep -n "TDC_FW_VARIANT" src/` 로 다른 파일에서 동일 매크로 사용 여부 사전 확인. 충돌 시 매크로 명 조정 또는 헤더화 검토.

### 롤백
단일 commit 이라 `git revert <commit>` 또는 본 commit 직전 상태로 `git reset` 가능. 사용자 작업 흐름에 영향 없음.

---

## 6. Commit 메시지 안

```
refactor(led): OTA DFU 변종별 LED 색을 빌드 매크로 분기로 정리

- LedOutput.c 디스크립터 표의 코멘트-스왑 형태 (en__LED_PURPLE/*en__LED_WHITE*/) 제거
- TDC_FW_VARIANT (APP / FACTORY_RESET) 매크로 + TDC_FW_LED_*_COLOR 추상화 도입
- 디폴트 = APP (운용). Factory Reset 빌드는 매크로 한 줄 변경으로 전환

LED_ST_IN_USE: APP=WHITE / FACT_RESET=PURPLE
LED_ST_POWER_ON: APP=SKYBLUE / FACT_RESET=WHITE

패턴 타이밍·dimming·GPIO 매핑 등 다른 LED 동작 무변경.

Refs: docs/tasks/LED/ota-dfu-fw-variant/
```

---

## 7. 머지 절차

1. `git checkout claude_develop`
2. `git merge --no-ff claude_feature_ota-dfu-fw-variant-led`
3. `git push origin claude_develop`
4. (사용자 승인 후) `git branch -d claude_feature_ota-dfu-fw-variant-led` + `git push origin --delete claude_feature_ota-dfu-fw-variant-led` (원격에는 push 안 했으므로 로컬만)

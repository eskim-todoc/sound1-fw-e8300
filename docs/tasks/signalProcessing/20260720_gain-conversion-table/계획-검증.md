---
name: Gain Conversion Table 계획 검증
purpose: 계획.md의 지침 준수·논리 정합성·근거를 자체 검증하고 승인 게이트 진입 판정
type: tasks
maturity: experimental
tags: [gain-table, verification, plan]
---

# Gain Conversion Table — 계획 검증 (⑥)

**TL;DR**: 계획 6단계를 요구사항 6종·분석 결론과 교차 대조. 커버리지 100%, 삽입 지점 라인 전수 재확인 완료. 정정 1건(테이블 dB 컬럼 표기 오차는 무해)과 보완 2건(U3 잔존 근거 명시, 실패 응답 에코 규칙)을 반영. **승인 게이트 진입 가능**.

## 1. 지침 준수

| 점검 | 판정 | 근거 |
|---|---|---|
| ⑦ 진입 전 승인 게이트 명시 | ✅ | 계획 §0 승인 필요 결정 2건 + §4 병합 승인 |
| 신규 심볼 `tdc_` 접두어 | ✅ | 함수 4종·신규 파일 2종 전부 적용. 구조체 멤버 예외는 §0 결정_2로 **명시 승인 요청**(임의 판단 아님) |
| 리팩터링 계층 분해 + 테스트 순서 | ✅ | Step별 검증 라벨 + "전제" 표기, §2 검증 계획과 라벨 일치 |
| CP949 비호환 문자 금지 | ✅ | 계획 코드 스니펫에 `—`·`≈`·`µ`·`►` 없음. §2 정적 검증 항목에도 포함 |
| Git 규칙(브랜치·--no-ff·AI 표시 금지) | ✅ | §4. 무관 변경(`4__eeprom/.cproject`) 제외 명시 |
| 코드 변경 0 (⑤ 단계) | ✅ | `git status` 상 소스 변경 없음 |

## 2. 요구사항 커버리지

| 요구사항 | 대응 Step | 판정 |
|---|---|---|
| 요구사항_1 (테이블) | Step 1 | ✅ 256엔트리 + 생성·대조 방법 명시 |
| 요구사항_2 (게인 적용) | Step 1·4 | ✅ 매트릭스 3상태 모두 대응, U1/U2 미수정 명시 |
| 요구사항_3 (기본값) | 결정_1 + Step 3·6 | ✅ 값 제안 + 상수 단일 출처 |
| 요구사항_4 (런타임 설정) | Step 6 | ✅ Read/Write·응답 5워드·실패 조건 열거 |
| 요구사항_5 (cm3→cfx 전달) | Step 2·3·4 | ✅ 미러 append + 소비 |
| 요구사항_6 (CRADLE 판별) | Step 5 | ✅ |

## 3. 논리 정합성

- **순서 의존성**: Step 4는 Step 1(함수)·2(필드)·3(값)에 의존 — 계획의 Step 번호 순서가 곧 컴파일 가능 순서. 커밋 분할(§4)도 커밋 1→2→3 순으로 각 시점에 빌드 가능(커밋 1 시점의 신규 함수는 미호출 상태이나 컴파일 유효). ✅
- **U3 잔존 판단**: 라이브 호출부만 교체하고 U3 정의를 남기는 이유가 "DEAD `I2S_handle()`이 참조"로 명시됨 — 삭제 시 컴파일 에러라는 근거 타당(`main.c:381`·`:409`). ✅
- **Streaming 무게인 = 유니티 곱**: `x * 65536 >> 16 == x`(x는 정수, 산술 시프트) — 음수 포함 항등. 단 이는 **합산 전 각 항이 아니라 합산 후 시프트**인 계획 수식에서도 성립(`(a*65536 + b*65536) >> 16 = a + b`). ✅
- **기본값 유니티 = 현행 비트 동일**: 현행 U3는 `(mic>>5) + (i2s>>5)`, 신규는 `(65536*(mic>>5) + 65536*(i2s>>5)) >> 16` = 동일. 단 **포화 클램프가 추가**되므로, 현행에서 24비트를 넘겨 랩어라운드하던 극단 입력에서는 동작이 달라진다(포화). 이는 개선이며 agc 관행과 일치 — **의도된 차이로 기록**. ✅
- **실패 시 응답 index 에코**: 앱이 거부 원인을 식별할 수 있어 합리적. 프로토콜 문서가 실패 시 필드 값을 규정하지 않으므로 설계 재량 — 계획에 명시됨. ✅
- **cradle 플래그 잔존**: 클래식 DISCONN 시 0으로 갱신되고, 0x36 미수신 상태에서도 I2S_FLAG 비활성이면 바이패스라 무해([`분석.md §2.3`](분석.md)). ✅

## 4. 근거 검증 (삽입 지점 재확인)

| 계획이 지정한 위치 | 재확인 결과 |
|---|---|
| `audioMixer.h` 매크로·선언 추가 | 현행 `:24`(AUDIO_MIX_NORMALIZE_RSHIFT)·`:27~29`(U1~U3 선언) 존재 ✅ |
| `main.c:440` U3 호출 교체 | 해당 라인이 I2S 활성 분기 내 유일 믹싱 호출 ✅ |
| cm3 구조체 끝 `:240` 뒤 | `pcm_specific_command_read_index`가 마지막 멤버, `:241`이 닫는 중괄호 ✅ |
| cfx 구조체 끝 `:218` 뒤 | 동일 ✅ |
| `initialize.c:344` 직후 삽입 | `CFX_EEPROM_data_is_Loaded = 1` 존재, `:452` `enable_CFX_trigger_for_iteration()`보다 앞 ✅ |
| `ble_communication.c:216` 주석 교체 | "향후 크래들과 I2S 게인 테이블 적용할 수 있게 해야함" 존재 ✅ |
| `ble_commonProtocol.h:115~118` enum | `EN__SND_BT_CMD_80_RANGE { EN__SND_BT_CMD_GENERAL_DEBUG = 0x8F }` ✅ |
| `ble_communication.c:357` 라우팅 옆 | 0x8F else-if 존재 ✅ |
| `remoteControl.c:1353` `#endif` 뒤 케이스 추가 | 0x8F 케이스가 `#if 1`(`:1340`)~`#endif`(`:1353`) 블록, 뒤에 0x59 블록 시작 — 삽입 지점 유효 ✅ |
| 테이블 생성 규칙 일치 | 스크립트 대조 결과 **Q8.16 256엔트리 불일치 0건**, 최대 512,211 ✅ |

> [!NOTE]
> **정정_1 (무해 확인)**: 같은 대조에서 dB 컬럼 12건이 허용오차(0.00005)를 넘었으나, 전부 문서가 4자리로 반올림 표기한 것(`-17.71875` → `-17.7188`)일 뿐이다. 구현이 사용하는 값은 Q8.16 정수 컬럼이며 그쪽은 전수 일치 — **영향 없음**.

## 5. 미해결·이월

- 빌드·실기 검증은 은수님 Eclipse 환경(계획 §2 시스템 행) — Claude 환경에서 수행 불가, 보고 시 명시할 것.
- XMEM 예산(256워드)·ISR 사이클 증가는 링크·실기에서만 확정 가능(분석 위험_3·4).

## 6. 판정

**통과** — 승인 게이트 진입. 승인 시 결정_1(기본 인덱스 128/128)·결정_2(공유메모리 멤버 네이밍) 확정 필요.

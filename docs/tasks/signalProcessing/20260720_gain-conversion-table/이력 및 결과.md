---
name: Gain Conversion Table 기능 이력 및 결과
purpose: 20260720_gain-conversion-table 작업의 시계열 로그 + 완료 요약
type: tasks
maturity: experimental
tags: [gain-table, history, log]
---

# Gain Conversion Table 기능 — 이력 및 결과

**TL;DR**: Q8.16 게인 테이블(cfx audioMixer) + 0x8C 런타임 설정 + CRADLE 판별 전달 구현 작업의 시계열 로그.

## 시계열 로그

- **2026-07-20 (착수)**: 세션 시작. 참고 문서 3종(`Gain Table.md`, `Audio path & Gain setting.png`, `Gain control protocol packet.png`) 확인. `Gain control protocol packet.md`의 참조 이미지 누락 발견 → 은수님이 png 추가.
- **2026-07-20 (요구 확정 인터뷰)**: ① 범위 = cfx 게인 적용 + cm3→cfx 전달까지, 0x8C 패킷 수신·응답 포함 ② 인덱스 = 컴파일 타임 기본값 + 리모컨 앱 런타임 설정 ③ I2S 활성 시 마이크 경로 = 현행(DMIC1 단일) 유지 + Gain_A ④ 작업 폴더 = signalProcessing 신규.
- **2026-07-20 (Git)**: `claude_feature_gain-conversion-table` 브랜치 생성(`claude_develop` 기반). 확인: 대상 파일(audioMixer.c·main.c·ble_communication.c)은 fake-sleep 브랜치와 diff 없음. 워킹트리에 무관 변경 `src/4__eeprom/.cproject`(M) 잔존 — 본 작업에서 커밋하지 않음.
- **2026-07-20 (①②)**: `요구사항.md` 작성 → `요구사항-검증.md` 통과(근거 8건 전수 확인).
- **2026-07-20 (③④)**: `분석.md` 작성(cfx 믹싱 구조·Q8.16 관행·0x8C 충돌검사·공유메모리 미러) → `분석-검증.md` 통과. **정정_1**: 기본값 기록은 `initialize.c:452`(CFX 트리거 활성화) 이전이어야 함.
- **2026-07-20 (⑤⑥)**: `계획.md` 6단계 작성. 게인 테이블 생성 규칙 `round(10^((i-128)*0.140625/20)*65536)`(i=0은 0)을 문서 256엔트리와 스크립트 대조 → **불일치 0건**. `계획-검증.md` 통과, 승인 게이트 대기.
- **2026-07-20 (승인)**: 은수님 결정 — 결정_1 유니티 게인 승인, 결정_3 **절삭(포화)** 지시(랩어라운드 개념 질문에 답변), 결정_4 **테이블은 확장성 고려한 순수 룩업**(인덱스별 임의 값 교체 가능해야 함). 결정_2(네이밍)는 제안대로 진행.
- **2026-07-20 (⑦ 구현)**: 4커밋으로 6단계 구현. `f9b4dfe`(cfx 테이블·게인 믹서) → `1f6ecc0`(공유메모리 3필드·0x8C 핸들러·기본값) → `37242be`(0x8C 라우팅·0x36 cradle) → `ba29f5d`(cfx 디스패치). 11파일 +566/-5.
- **2026-07-20 (⑧ 검증)**: 게인 테이블 256엔트리 문서 대조 **불일치 0**, 공유메모리 미러 35/35 순서 일치, 분기·정적 검증 전수 PASS. **정정_2**: 신규 유닛 번호 U4·U5가 기존 테스트맵과 충돌 → **U13·U14**로 재번호 + 테스트맵 갱신.
- **2026-07-20 (대기)**: 빌드·실기 검증(은수님 Eclipse 환경) 후 `claude_develop` 병합 승인 대기.
- **2026-07-20 (2차 요구 추가)**: 은수님 확장 지시 — 게인 값을 **ISD별 파일로 영속화**하고, ISD 연결 확인 시 해당 ISD의 값을 자동 적용해 "앱이 읽기만 해도 이전 설정 유지"되게 할 것. 기존 저장 구조 조사를 위해 Explore 2노드 병렬 위임([`에이전트-로그/`](에이전트-로그/인덱스.md)).

---
name: Gain Conversion Table 요구사항 검증
purpose: 요구사항.md의 지침 준수·논리 정합성·근거를 자체 검증
type: tasks
maturity: experimental
tags: [gain-table, verification, requirements]
---

# Gain Conversion Table — 요구사항 검증 (②)

**TL;DR**: 요구사항.md 검증 3축 수행. 근거 전수 파일:라인 확인, 논리 모순 없음, 지침 위반 없음. 미확정 1건(컴파일 타임 기본 인덱스 값)은 의도적으로 ⑤계획 제안 사항으로 이월.

## 1. 지침 준수

| 점검 | 판정 | 비고 |
|---|---|---|
| frontmatter + TL;DR | ✅ | name/purpose/type/tags + TL;DR 존재 |
| 한글 라벨(요구사항_N) | ✅ | 알파벳 약어(R1 등) 미사용 |
| 8단계 승인 게이트 명시 | ✅ | §5 제약에 ⑥ 후 승인 명시 |
| tdc_ 접두어·인코딩 규칙 반영 | ✅ | §5 제약 |
| Git 규칙(브랜치·--no-ff) | ✅ | §5, 브랜치 생성 완료(`claude_feature_gain-conversion-table`) |

## 2. 논리 정합성

- **매트릭스 완전성**: I2S 유무 × (Streaming/CRADLE) 3상태가 상호 배타·전체 포괄. I2S 없음 상태에서 classic_type은 미사용이므로 모순 없음. ✅
- **Gain_A 적용 대상**: 다이어그램(빔포밍 합산)과 현행 코드(I2S 시 DMIC1 단일)의 차이를 §1에 명시하고 "현행 유지"로 확정(은수님 답변) — 요구사항 내 상충 없음. ✅
- **응답 패킷**: Read/Write 모두 5바이트 응답으로 통일, 실패 조건 열거(요구사항_4). Gain type=0(Invalid)이 프로토콜에 존재하므로 Failed 처리 규정 타당. ✅
- **인덱스 유효 범위**: 1바이트 필드라 0~255 전 값이 테이블 범위 내 → 인덱스 자체의 범위 오류는 구조적으로 불가(index 0=뮤트는 유효값). ✅
- **미확정 항목**: 기본 인덱스 값 1건만 미정, ⑤계획 제안 + 승인 게이트 확정으로 명시(요구사항_3). ✅

## 3. 근거 검증 (파일:라인)

| 주장 | 근거 | 확인 |
|---|---|---|
| index 128 = 0 dB = 65,536 | `docs/참고/Gain Conversion Table/Gain Table.md:132` | ✅ |
| index 0 = 뮤트(0) | 같은 파일 `:4` (`-999.0000 / 0`) | ✅ |
| 범위 ±17.8594 dB·스텝 0.1406 | 같은 파일 `:5`(index 1), `:259`(index 255) | ✅ |
| I2S 활성 시 DMIC1 단일 믹스 | `src/1__cfx/systemControl/main.c:440` (`tdc_audio_mix_2_buffers(DMIC1, I2S)`) | ✅ |
| I2S 유무 판별 존재 | `main.c:433` `I2S_isStreaming()` 분기 | ✅ |
| CLASSIC_STATE 0x36 수신·classic_type | `src/2__cm3/Cortex-M3-src/BleCommunication/ble_communication.c:198~214` (`classic_type==1` → "CRADLE") | ✅ |
| cfx 전달 경로 부재(주석만) | `ble_communication.c:216` "향후 크래들과 I2S 게인 테이블 적용할 수 있게 해야함" | ✅ |
| 0x8C 패킷 구성 | `docs/참고/Gain Conversion Table/Gain control protocol packet.png` (명령 4B/응답 5B, Control·Gain type·index) | ✅ |

## 4. 판정

**통과** — ③분석 진행 가능. 이월 항목: 기본 인덱스 값(⑤계획), 0x8C 라우팅 위치·cm3→cfx 공유메모리 방식·Q8.16 곱셈 구현 방식(③분석).

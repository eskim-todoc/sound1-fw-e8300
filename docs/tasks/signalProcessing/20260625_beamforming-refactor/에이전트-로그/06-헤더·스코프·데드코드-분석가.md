---
name: 리팩토링 인벤토리 06
purpose: 헤더·스코프·데드코드-분석가
type: tasks
maturity: experimental
tags: [beamforming, refactor, agent-log, analysis]
---

# [분석 06] 헤더·스코프·데드코드 분석가 — 빔포밍 관련 리팩토링 인벤토리 (동작 보존 전제, 할루시네이션 0)

## 결론
6개 파일 실독 후 빔포밍 관련 항목 10건 식별. 핵심: LIB_ADC_DEC_CTRL_VAL_0_9958 헤더 정의만 있고 전 src 내 호출 0건(데드코드). g_enabled_mic_count 선언은 있으나 실질 쓰기/읽기 모두 주석 처리(데드 변수). lib_audio_in.h:55 "IN1" 주석은 현 활성 매크로(IN0_FA0_0)와 불일치(낡은 주석). main.c:87-88 "ADC 0/ADC 1" 주석은 실제 ch 이름(DMIC 0/DMIC 1=EZ/QCC)과 불일치. audioMixer.c 믹서는 16줄 펼침(unroll)으로 df_inputADC_DataBuffLength=16 상수 노출 없음 — 루프 치환 가능(low-risk). PCM_LiveStimulation_Mode 내 `g_enabled_mic_count==1/2` 인라인 주석은 삭제 가능 데드 코드 단편. 모든 제안 동작 보존.

## 인벤토리 항목
- **[deadcode/none]** `src/1__cfx/lib_cfx/lib_audio_in.h:109` (보존=True)
  - 현재: #define LIB_ADC_FRACTIONAL_DELAY_29 (29 << AUDIO_ADC_DEC_CTRL_DELAY_FRACTIONAL_Pos)
#define LIB_ADC_DEC_CTRL_VAL_0_9958 (BAND_SELECT_ADC_0K_8K | ADC_INTEGER_DEL
  - 제안: LIB_ADC_DEC_CTRL_VAL_0_9958 및 그 의존 매크로 LIB_ADC_FRACTIONAL_DELAY_29(line 109)를 삭제. 전체 src 내 사용처 0건(grep 확인). LIB_ADC_FRACTIONAL_DELAY_6(line 112)과 LIB_ADC_DEC_CTRL_VAL_0_0250(line 113)은 system_control.c:88,93에서 실제 사용 중이므로 보존.
  - 동등성: grep 결과: LIB_ADC_DEC_CTRL_VAL_0_9958 정의 1건(lib_audio_in.h:110), 호출 0건. LIB_ADC_FRACTIONAL_DELAY_29도 해당 정의에서만 참조, 다른 곳 0건.
- **[deadcode/none]** `src/1__cfx/systemControl/main.c:13` (보존=True)
  - 현재: volatile int _XMEM g_enabled_mic_count = 0;
  - 제안: g_enabled_mic_count 변수를 제거. 유일한 쓰기 시도(line 96)는 주석 처리됨: `// g_enabled_mic_count = 2;`. 유일한 읽기 시도(line 440, 445)도 주석: `/*g_enabled_mic_count == 1*/`, `/*g_enabled_mic_count == 2*/`. 실제 읽기/쓰기 경로 없음. 제거 전 extern 선언 여부 grep 필요(main.h:206 확인 결과 
  - 동등성: grep 결과: g_enabled_mic_count는 main.c 3곳만 등장, 모두 주석 처리된 코드. extern 선언 없음(main.h:206에는 g_pcm_mode extern만 있음).
- **[comment/none]** `src/1__cfx/lib_cfx/lib_audio_in.h:55` (보존=True)
  - 현재: // U7, DMIC_CLK1/CAL, DMIC_OUT1 : IN1 : FA0_0
// U9, DMIC_CLK2,     DMIC_OUT2 : IN2 : FA0_1
  - 제안: 주석의 IOC 포트 이름을 실제 활성 매크로(line 58)와 일치하도록 수정. line 58은 `IOC_INPUT_CFG_IN0_FA0_0 | IOC_INPUT_CFG_IN2_FA0_1`이므로: U7은 IN0(not IN1), U9는 IN2(맞음). 수정안: `// U7, DMIC_CLK1/CAL, DMIC_OUT1 : IN0 : FA0_0` / `// U9, DMIC_CLK2, DMIC_OUT2 : IN2 : FA0_1`.
  - 동등성: line 58 활성 정의: IOC_INPUT_CFG_IN0_FA0_0(U7=IN0), IOC_INPUT_CFG_IN2_FA0_1(U9=IN2). 주석 line 55 'IN1'은 실제 IN0과 불일치.
- **[comment/none]** `src/1__cfx/systemControl/main.c:87` (보존=True)
  - 현재:     // 오디오 IN 0, ADC 0, DMIC 0   :   FIFO_A0_0   :   DMIC_CLK1/CAL (DIO22) + DMIC_OUT1 (DIO23)   :   EZ
    // 오디오 IN 1, ADC 1, DMIC 1   :   FIFO_A0_1   :   DMI
  - 제안: 빔포밍 ch 식별 측면에서 'ADC 0 / ADC 1'은 현 DMIC 2채널 구성에서 ch0=EZ(DMIC0/ADC ch0), ch1=QCC(DMIC2/ADC ch2)임을 명확히 하도록 보완. 현재 설명 자체는 FIFO 매핑 측면에서 틀리지 않으나(FA0_0=EZ, FA0_1=QCC는 맞음), 'ADC 0/1'이 실제 ADC 채널 번호(0,2)와 혼동될 수 있음. 주석을 `// ch0 (ADC ch0, DMIC0_RE) : F
  - 동등성: lib_audio_in.h:89: LIB_DMIC_AUDIO_MUX_CFG_INPUT_CH_SRC = ADC3_OUT | DMIC2_DATA_FE | ADC1_OUT | DMIC0_DATA_RE. EZ=DMIC0_DATA_RE(ch0), QCC=DMIC2_DATA_FE(ch2). FIF
- **[comment/none]** `src/1__cfx/lib_cfx/lib_audio_in.h:57` (보존=True)
  - 현재: // #define LIB_IOC_ADC_CFG_VAL (IOC_INPUT_CFG_IN0_NONE | IOC_INPUT_CFG_IN1_FA0_0 | IOC_INPUT_CFG_IN2_FA0_1 | IOC_INPUT_CFG_IN3_NONE)
  - 제안: 구 IOC 매핑(IN1=FA0_0)을 기록한 주석 처리 매크로 삭제. 현재 활성 정의(line 58)가 IN0_FA0_0으로 대체 확정. 이력 보존 가치 없음(git log가 이력 역할 수행).
  - 동등성: line 57은 #define 주석 처리이므로 컴파일러 미참조. 삭제 시 바이너리 동일.
- **[comment/none]** `src/1__cfx/systemControl/main.c:95` (보존=True)
  - 현재:     // enable_DMIC();
    // g_enabled_mic_count = 2;
  - 제안: `enable_DMIC()` 주석(line 95)과 `g_enabled_mic_count = 2` 주석(line 96)은 현재 `enable_2_DMICs()`(line 97)로 대체 완료된 구 API 흔적. g_enabled_mic_count 변수 자체가 데드이므로 두 줄 모두 삭제. 단, `// 순서 1. 마이크 입력 켜기`(line 94) 설명 주석은 보존.
  - 동등성: 두 줄 모두 주석 처리 상태이므로 실행 경로 없음. 삭제 시 바이너리 동일.
- **[comment/none]** `src/1__cfx/systemControl/main.c:440` (보존=True)
  - 현재:         if (get_enabled_DMIC_count() == 1 /*g_enabled_mic_count == 1*/)
        {
            ...
        }
        else if (get_enabled_DMIC_count() == 2 /*g_e
  - 제안: 인라인 주석 `/*g_enabled_mic_count == 1*/`(line 440), `/*g_enabled_mic_count == 2*/`(line 445) 제거. 이 주석은 삭제된 g_enabled_mic_count 변수를 참조하는 잔재로 가독성 저하 요소. 삭제 후 조건식: `if (get_enabled_DMIC_count() == 1)` / `else if (get_enabled_DMIC_count() == 2)`.
  - 동등성: 주석은 컴파일러 미참조. 조건식 자체(get_enabled_DMIC_count() 호출)는 변경 없음.
- **[comment/none]** `src/1__cfx/systemControl/main.c:448` (보존=True)
  - 현재:             // delay buffer 0, delay buffer 1, no delay buffer 0
            // audio_mix_2_buffers_for_beamforming((int _XMEM *) &g_lib_dmic_in_buffers[0][1][0
  - 제안: 이전 프로토타입 호출(3인자 모두 동일 버퍼 — 테스트용)을 주석 처리한 코드. 현재 Left/Right 분기별 정확한 호출로 완전 대체됨. 이 2줄 삭제 권고. 단, 직전 `// delay buffer 0, delay buffer 1, no delay buffer 0` 설명 주석은 인자 순서 문서화 역할이 있으므로 활성 호출 위 이동 후 보존 권고.
  - 동등성: 주석 처리 코드이므로 컴파일러 미참조. 삭제 시 바이너리 동일.
- **[loop/medium]** `src/1__cfx/systemControl/audioMixer.c:59` (보존=True)
  - 현재:     p_mix[0]  = ((p_no_delay_buf0[0] >> AUDIO_INPUT_RSHIFT) + (p_delay_buf0[1] >> AUDIO_INPUT_RSHIFT)) >> 1;
    ...(lines 59-74, 16줄 수동 펼침)...
    p_mix[15] = 
  - 제안: i=0..14는 p_mix[i] = ((p_no_delay_buf0[i]>>AUDIO_INPUT_RSHIFT) + (p_delay_buf0[i+1]>>AUDIO_INPUT_RSHIFT))>>1, i=15는 p_delay_buf1[0]으로 루프 경계 처리. chess_loop_range(15,15) 루프(i=0..14) + 별도 p_mix[15] 문장으로 치환 가능. 단, CFX 컴파일러의 chess_loop_range 동작과 
  - 동등성: i=0: p_mix[0]=((no[0]>>5)+(delay0[1]>>5))>>1 … i=14: p_mix[14]=((no[14]>>5)+(delay0[15]>>5))>>1; p_mix[15]=((no[15]>>5)+(delay1[0]>>5))>>1. 루프 치환 후 출력 완전 동일.
- **[naming/low]** `src/1__cfx/systemControl/main.c:454` (보존=True)
  - 현재:                 audio_mix_2_buffers_for_beamforming((int _XMEM *) &g_lib_dmic_in_buffers[1][LIB_AUDIO_IN_BUF_MAX_CNT - 2][0], (int _XMEM *) &g_lib_dmic_in_buffe
  - 제안: LIB_AUDIO_IN_BUF_MAX_CNT=2이므로 LIB_AUDIO_IN_BUF_MAX_CNT-2=0, LIB_AUDIO_IN_BUF_MAX_CNT-1=1로 고정. 인자 해석은 [1][0]=dmic2 현재 블록, [1][1]=dmic2 직전 블록임을 직관적으로 보이지 않음. 아래 주석(dmic2 buffer 0 / buffer 1)이 이를 보완하고 있으나, 매크로 산술 대신 상수 0/1을 직접 쓰는 것이 더 명확(단, BU
  - 동등성: LIB_AUDIO_IN_BUF_MAX_CNT=2(lib_audio_in.h:31). LIB_AUDIO_IN_BUF_MAX_CNT-2=0, LIB_AUDIO_IN_BUF_MAX_CNT-1=1. 리터럴 0/1로 치환 시 값 동일.

---
name: 리팩토링 인벤토리 07
purpose: 인벤토리-종합·우선순위-분석가
type: tasks
maturity: experimental
tags: [beamforming, refactor, agent-log, analysis]
---

# [분석 07] 인벤토리 종합·우선순위 분석가 — 동작 보존 절대 원칙, 실파일 line 근거만 인용, 할루시네이션 0

## 결론
6개 파일을 실제 Read 도구로 전부 확인했습니다. 총 12개 항목을 발굴하고, 저위험(상수화·주석·데드코드 제거) → 중위험(루프화 후보) → 보류(고위험) 순으로 우선순위화했습니다.

적용 권장 순서:
 1단계(저위험·독립적): 항목 01~06 병렬 적용 가능 — 컴파일 결과물 무변경 보장
 2단계(저위험·순차): 항목 07 (g_enabled_mic_count 변수 제거) — 항목 01~06 후 적용, 선언부/주석 동시 정리
 3단계(중위험·단독): 항목 08 (beamforming loop 통합) — 반드시 단독 브랜치, chess HW loop 회귀 측정 필수
 4단계(보류): 항목 09~12 — 사전 조사/합의 후에만 진행

## 인벤토리 항목
- **[constant/none]** `src/1__cfx/systemControl/audioMixer.c:59` (보존=True)
  - 현재: p_mix[0]  = ((p_no_delay_buf0[0] >> AUDIO_INPUT_RSHIFT) + (p_delay_buf0[1] >> AUDIO_INPUT_RSHIFT)) >> 1;
// ... (lines 59-74, i=0..14, delay index = i+1)
p_mix[
  - 제안: p_mix[15] 의 >> 1 을 AUDIO_BEAMFORM_NORM_RSHIFT (값=1) 상수로 명명하고, 기존 >> AUDIO_INPUT_RSHIFT 와 구분 명시. 현재 두 종류의 >>1 (AUDIO_INPUT_RSHIFT=5와 정규화 >>1) 이 동일 표현식 안에 혼재해 의미가 불분명하므로, 정규화 우변만 별도 상수로 분리해 의도를 문서화합니다.
  - 동등성: AUDIO_BEAMFORM_NORM_RSHIFT=1, 기존 >>1 과 비트 동일
- **[constant/none]** `src/1__cfx/systemControl/main.c:454` (보존=True)
  - 현재: audio_mix_2_buffers_for_beamforming((int _XMEM *) &g_lib_dmic_in_buffers[1][LIB_AUDIO_IN_BUF_MAX_CNT - 2][0], (int _XMEM *) &g_lib_dmic_in_buffers[1][LIB_AUDIO_
  - 제안: lib_audio_in.h 에 #define DMIC_BUF_IDX_CURRENT 0 과 #define DMIC_BUF_IDX_PREV 1 을 추가합니다 (LIB_AUDIO_IN_BUF_MAX_CNT=2 고정 전제). 호출부 L454, L461 의 [LIB_AUDIO_IN_BUF_MAX_CNT - 2] → [DMIC_BUF_IDX_CURRENT], [LIB_AUDIO_IN_BUF_MAX_CNT - 1] → [DMIC_BUF_I
  - 동등성: LIB_AUDIO_IN_BUF_MAX_CNT=2 이므로 -2=0=CURRENT, -1=1=PREV 로 값 동일
- **[comment/none]** `src/1__cfx/systemControl/audioMixer.c:28` (보존=True)
  - 현재:             // p_mix[i] = p_external_mic[i];
  - 제안: 이 줄은 게인 시프트 없이 raw 값을 그대로 넣던 이전 실험 코드입니다. 유일한 역할이 비교 참고인데 인접 줄(L27)과 중복 맥락으로 읽기만 어렵게 합니다. 삭제합니다.
  - 동등성: 주석 처리된 코드 — 컴파일 결과 무영향
- **[comment/none]** `src/1__cfx/systemControl/main.c:95` (보존=True)
  - 현재:     // enable_DMIC();
    // g_enabled_mic_count = 2;
    enable_2_DMICs();
  - 제안: 대체된 구버전 호출 2줄(L95-96)은 삭제합니다. enable_2_DMICs() 가 동일 동작을 내포하므로 잔존 주석은 노이즈입니다.
  - 동등성: 주석 처리된 코드 — 컴파일 결과 무영향
- **[comment/none]** `src/1__cfx/systemControl/main.c:440` (보존=True)
  - 현재:         if (get_enabled_DMIC_count() == 1 /*g_enabled_mic_count == 1*/)
        {
            ...
        }
        else if (get_enabled_DMIC_count() == 2 /*g_e
  - 제안: 인라인 주석 /*g_enabled_mic_count == 1*/, /*g_enabled_mic_count == 2*/ 는 현재 API와 다른 이전 접근법 잔재입니다. 삭제합니다. 조건 표현식 자체가 충분히 자명합니다.
  - 동등성: 주석 제거 — 컴파일 결과 무영향
- **[deadcode/none]** `src/1__cfx/systemControl/main.c:512` (보존=True)
  - 현재: #if 0
        find_freq_rep_value(); // 113usec @ 2025.01.10
        logarithmMapping(); // 76 usec 소요 @ 2025.01.20
#endif
  - 제안: #if 0 블록(L512-515)을 제거합니다. 해당 함수 호출들은 주석 내 시간 측정 근거만 담고 있으며, pcmDataOut() 이전 별도 위치(PcmBitStream_Mode_LiveStimulation 세부 설정)로 이동 완료된 상태입니다.
  - 동등성: #if 0 으로 컴파일 제외된 코드 — 제거해도 출력 동일
- **[deadcode/low]** `src/1__cfx/systemControl/main.c:13` (보존=True)
  - 현재: volatile int _XMEM g_enabled_mic_count = 0;
  - 제안: g_enabled_mic_count 는 선언(L13) 외에 오직 주석 처리된 코드에서만 등장하고(L96: // g_enabled_mic_count = 2, L440/445: 인라인 주석), 실제 런타임에서 쓰거나 읽는 경로가 없습니다. volatile이지만 CM3/공유메모리 통신 경로 확인 결과 cross-reference 없음. 선언과 관련 주석을 함께 제거합니다.
  - 동등성: 읽기/쓰기 경로 없는 데드 선언. volatile이므로 제거 전 nm/map으로 링커 단계 symbol 외부 참조 여부 최종 확인 권고
- **[loop/medium]** `src/1__cfx/systemControl/audioMixer.c:59` (보존=True)
  - 현재: p_mix[0]  = ((p_no_delay_buf0[0] >> AUDIO_INPUT_RSHIFT) + (p_delay_buf0[1] >> AUDIO_INPUT_RSHIFT)) >> 1;
...
p_mix[14] = ((p_no_delay_buf0[14] >> AUDIO_INPUT_RS
  - 제안: i=0..14 구간(15줄)을 chess_loop_range(15,15) 루프로 통합하고, i=15(크로스오버) 는 루프 밖에 단독 배치합니다:

for (register int i = 0; i < 15; i++)
    chess_loop_range(15, 15) {
        p_mix[i] = ((p_no_delay_buf0[i] >> AUDIO_INPUT_RSHIFT) + (p_delay_buf0[i + 1] >> 
  - 동등성: i=0..14: p_mix[i]=(no_delay[i]>>5 + delay_buf0[i+1]>>5)>>1 — 기존 수식과 완전 동일. i=15: p_delay_buf1[0] 사용 — 기존 L75 과 동일
- **[deadcode/low]** `src/1__cfx/systemControl/system_control.c:222` (보존=True)
  - 현재: #if 0  // xMin 기능 추가하지 않은 기존 코드
    Normal_PowerMode_event_stimulationVolumeChange();
    if (0 != Addr_SharedMem->userSettingValue.audioVolume) {
        m_aud
  - 제안: #if 0 분기(L222-232)를 제거하고 #else~#endif 도 정리해 유효 코드만 남깁니다. xMin 기능이 확정 채택된 상태로 구버전 분기를 제거합니다. 단, 코드 진화 근거로 보존할 필요가 있다면 git blame/history로 대체 가능하므로 팀 합의 후 결정하세요.
  - 동등성: #if 0 분기는 컴파일 제외 — 제거해도 실행 결과 동일. #else 유효 코드는 그대로 유지
- **[naming/medium]** `src/1__cfx/systemControl/main.h:59` (보존=True)
  - 현재: #define SFCR_16K ADC_MODDIV_BY30
#define SFCR_32K ADC_MODDIV_BY15
#define SFCR_48K ADC_MODDIV_BY10
  - 제안: lib_audio_in.h:18-20 에 LIB_AUDIO_SFCR_16K/32K/48K 가 동일 값으로 이미 존재합니다. main.h 의 중복 정의(SFCR_16K 등)를 제거하고 lib_audio_in.h 의 LIB_AUDIO_SFCR_* 를 단일 정의로 통일합니다. 단, main.h 에서 SFCR_16K 를 실제로 사용하는 호출부가 있는지 전체 grep 선행 필수.
  - 동등성: SFCR_16K=ADC_MODDIV_BY30=LIB_AUDIO_SFCR_16K, SFCR_32K=ADC_MODDIV_BY15=LIB_AUDIO_SFCR_32K, SFCR_48K=ADC_MODDIV_BY10=LIB_AUDIO_SFCR_48K — 값 동일
- **[deadcode/low]** `src/1__cfx/systemControl/main.h:125` (보존=True)
  - 현재: #define ADC_DEC_CTRL_1_VAL ADC_DEC_CTRL_0_VAL
#define ADC_DEC_CTRL_2_VAL ADC_DEC_CTRL_0_VAL
#define ADC_DEC_CTRL_3_VAL ADC_DEC_CTRL_0_VAL
  - 제안: ADC_DEC_CTRL_0_VAL 이 이 파일 어디에도 정의되어 있지 않고(grep 확인), 이 세 매크로를 사용하는 호출부도 없습니다. 세 줄 모두 삭제합니다.
  - 동등성: ADC_DEC_CTRL_0_VAL 미정의 — 이 매크로를 쓰는 코드 존재 시 이미 컴파일 오류. 사용처 없음 확인됨
- **[loop/high]** `src/1__cfx/systemControl/main.c:158` (보존=False)
  - 현재: if (lib_g_i2s_buffer_in_pos == 0) {
    lib_g_i2s_buffer_in_pos = 1;
} else {
    lib_g_i2s_buffer_in_pos = 0;
}
// ... (동일 패턴 L429-436, lib_g_i2s_buffer_out_po
  - 제안: 0↔1 토글 패턴을 lib_g_i2s_buffer_in_pos ^= 1; 및 lib_g_i2s_buffer_out_pos ^= 1; 로 단축할 수 있습니다. 단, lib_i2s.c 에서 동일 변수를 modulo-8(I2S_BUFFER_FULL_READY_CNT=8) 방식으로 증가시키는 경로(L294-298)와 혼용되므로, main.c 의 0/1 토글이 의도적 2-슬롯 ping-pong인지 먼저 확인해야 합니다. 의도가 명확해지
  - 동등성: 

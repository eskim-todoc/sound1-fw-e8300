---
name: 리팩토링 인벤토리 03
purpose: 주석·가독성-분석가.-실파일-Read-후-존재하는-
type: tasks
maturity: experimental
tags: [beamforming, refactor, agent-log, analysis]
---

# [분석 03] 주석·가독성 분석가. 실파일 Read 후 존재하는 line만 인용. 동작 보존 전제.

## 결론
6개 파일 전체 읽기 완료. 버퍼 인덱스 규약, 하이브리드 지연 메커니즘, L/R 조향, 게인 의도 4개 주제에 대해 총 14개 주석 제안을 실파일 line 기준으로 작성. 모든 제안은 코드 변경 없이 주석 추가·교체만이며 동작은 완전 보존.

## 인벤토리 항목
- **[comment/none]** `src/1__cfx/lib_cfx/lib_audio_in.c:9` (보존=True)
  - 현재: /**
 * buffer indexing information
 * dmic1
 * [0] : dmic1 / [0] : buffer0 / [0:15] : 16 samples
 * [0] : dmic1 / [1] : buffer1 / [0:15] : 16 samples
 * dmic2
 
  - 제안: /**
 * g_lib_dmic_in_buffers[mic][block][sample] 인덱스 규약
 *
 * 차원 0 — mic   : 0 = DMIC1(EZ/E8300), 1 = DMIC2(QCC)
 * 차원 1 — block : 0 = 현재 블록(최신 16샘플), 1 = 직전 블록(1 블록 전)
 *                  tdc_copy_DMIC_buffers() 호출마다 [0]←FIFO, [1]←[0] 방향으로
  - 동등성: 주석 교체이므로 런타임 동작 변화 없음
- **[comment/none]** `src/1__cfx/lib_cfx/lib_audio_in.h:112` (보존=True)
  - 현재: #define LIB_ADC_FRACTIONAL_DELAY_6  (6 << AUDIO_ADC_DEC_CTRL_DELAY_FRACTIONAL_Pos)
  - 제안: /* HW 소수점 지연 FRAC=6 → 실제 지연량 = 6/32샘플 = 0.1875샘플 ≈ 0.025샘플(이 매크로명의 _0_0250 근거).
 * 빔포밍에서 SW 지연 1샘플(믹서 i+1 오프셋)과 조합하면
 * 총 지연 ≈ 1.1875샘플(~25mm 음향 경로 차이에 대응).
 * front mic 쪽 마이크를 HW에서 앞당겨(지연 추가) 두 마이크 도달 시간을 정렬한다. */
#define LIB_ADC_FRACTIONA
  - 동등성: 주석 추가만. 매크로 리터럴값 (6 << AUDIO_ADC_DEC_CTRL_DELAY_FRACTIONAL_Pos) 동일
- **[comment/none]** `src/1__cfx/lib_cfx/lib_audio_in.h:113` (보존=True)
  - 현재: #define LIB_ADC_DEC_CTRL_VAL_0_0250 (BAND_SELECT_ADC_0K_8K | ADC_INTEGER_DELAY_0 | LIB_ADC_FRACTIONAL_DELAY_6 | ADC_UNMUTE | ADC_DEC_ENABLE | ADC_DC_REMOVE_CUTO
  - 제안: /* _0_0250 = HW FRAC 지연 0.025샘플(32분율 기준 6/32=0.1875샘플, 명칭은 설계 초기 추정값 기준).
 * front mic가 아닌 쪽 마이크(후방 마이크)에 이 값을 적용하여
 * 빔포밍 SW 지연(i+1 오프셋)에 더해지는 HW 정렬 지연을 추가한다.
 * Left 귀: QCC(ADC2)에 적용 / Right 귀: EZ(ADC0)에 적용 (system_control.c 참조). */
#defi
  - 동등성: 주석 추가만. 매크로 전개값 동일
- **[comment/none]** `src/1__cfx/systemControl/main.c:321` (보존=True)
  - 현재: void tdc_copy_DMIC_buffers(void)
  - 제안: /**
 * tdc_copy_DMIC_buffers — DMIC FIFO → 링 버퍼 shift 복사
 *
 * 매 PCM 인터럽트(16샘플 단위)마다 호출된다.
 * 동작:
 *   1) 기존 block[0] → block[1] 이동 (오래된 방향으로 밀기)
 *   2) FIFO(HCT_A0_x) → block[0] 복사 (최신 16샘플 저장)
 *
 * 결과 레이아웃:
 *   g_lib_dmic_in_buffers[mi
  - 동등성: 함수 선언 시그니처 불변, 주석 추가만
- **[comment/none]** `src/1__cfx/systemControl/audioMixer.c:55` (보존=True)
  - 현재: void audio_mix_2_buffers_for_beamforming(int _XMEM *p_delay_buf0, int _XMEM *p_delay_buf1, int _XMEM *p_no_delay_buf0)
  - 제안: /**
 * audio_mix_2_buffers_for_beamforming — 딜레이-앤-섬(DAS) 빔포밍 믹서
 *
 * 인수:
 *   p_delay_buf0   : 지연 마이크의 현재 블록 (block[0], 샘플 0=최신)
 *   p_delay_buf1   : 지연 마이크의 직전 블록 (block[1])
 *   p_no_delay_buf0: 기준(무지연) 마이크의 현재 블록 (block[0])
 *
 * 하이브리
  - 동등성: 함수 시그니처 불변, 주석 추가만
- **[comment/none]** `src/1__cfx/systemControl/audioMixer.c:59` (보존=True)
  - 현재:     p_mix[0]  = ((p_no_delay_buf0[0] >> AUDIO_INPUT_RSHIFT) + (p_delay_buf0[1] >> AUDIO_INPUT_RSHIFT)) >> 1;
  - 제안:     /* SW 지연: no_delay[i] vs delay_buf0[i+1] — 인덱스 1 오프셋이 1샘플 지연을 의미.
     * sample index 0=최신이므로 delay_buf0[1]은 1샘플 오래된 값. */
    p_mix[0]  = ((p_no_delay_buf0[0] >> AUDIO_INPUT_RSHIFT) + (p_delay_buf0[1] >> AUDIO_INPUT_RSHIFT)) >> 1;
  - 동등성: 연산식 불변, 주석 추가만
- **[comment/none]** `src/1__cfx/systemControl/audioMixer.c:74` (보존=True)
  - 현재:     p_mix[15] = ((p_no_delay_buf0[15] >> AUDIO_INPUT_RSHIFT) + (p_delay_buf1[0] >> AUDIO_INPUT_RSHIFT)) >> 1;
  - 제안:     /* 블록 경계: delay_buf0[16]은 없으므로 직전 블록(p_delay_buf1)의 sample[0](최신)을 사용.
     * delay_buf1[0]은 한 프레임(16샘플) 전 블록의 첫 샘플로, 연속성을 유지한다. */
    p_mix[15] = ((p_no_delay_buf0[15] >> AUDIO_INPUT_RSHIFT) + (p_delay_buf1[0] >> AUDIO_INPUT_RSHIFT)) 
  - 동등성: 연산식 불변, 주석 추가만
- **[comment/none]** `src/1__cfx/systemControl/audioMixer.c:43` (보존=True)
  - 현재: void audio_mix_2_buffers(int _XMEM *p_buf1, int _XMEM *p_buf2)
  - 제안: /**
 * audio_mix_2_buffers — 독립 음원 2채널 단순 합산 믹서 (÷2 없음)
 *
 * 두 버퍼를 AUDIO_INPUT_RSHIFT(>>5)로 각각 스케일한 뒤 더한다.
 * 빔포밍과 달리 ÷2(>>1)를 수행하지 않는다.
 * 이유: p_buf1, p_buf2가 서로 다른 독립 음원(예: DMIC + I2S)이어서
 *       두 신호의 에너지 합이 의도된 혼합 레벨이기 때문.
 * 주의: 두 신호
  - 동등성: 함수 시그니처 불변, 주석 추가만
- **[comment/none]** `src/1__cfx/systemControl/audioMixer.c:50` (보존=True)
  - 현재:             // 두 값을 믹싱 했으므로 반으로 줄여준다. -> 반으로 줄이지 않음
            p_mix[i] = ((p_buf1[i] >> AUDIO_INPUT_RSHIFT) + (p_buf2[i] >> AUDIO_INPUT_RSHIFT));  // >> 1;
  - 제안:             /* 독립 음원 합산 — 의도적으로 ÷2 생략.
             * 빔포밍(동일 음원 2경로)과 달리 여기서는 두 음원이 독립적이므로
             * 합산 레벨이 그대로 최종 출력이 되어야 한다. (주석의 '반으로 줄이지 않음' 확인) */
            p_mix[i] = ((p_buf1[i] >> AUDIO_INPUT_RSHIFT) + (p_buf2[i] >> AUDIO_INP
  - 동등성: 연산식 불변, 주석 내용 보강만
- **[comment/none]** `src/1__cfx/systemControl/system_control.c:85` (보존=True)
  - 현재:                     case Left_Ear:
                        lib_audio_set_front_mic(LIB_AUDIO_FRONT_MIC_LEFT);
                        SYS_SET_ADC_DEC_CTRL(AUDIO
  - 제안:                     case Left_Ear:
                        /* Left 귀: DMIC2(QCC, ADC2)가 앞쪽(front) 마이크.
                         * 빔포밍에서 front mic = 기준(무지연) 경로 → DMIC1(EZ)에 HW 지연 없음.
                         * rear mic = 지연 경로   → DMIC2(QCC)
  - 동등성: SYS_SET_ADC_DEC_CTRL 인수 불변, 주석 추가만
- **[comment/none]** `src/1__cfx/systemControl/system_control.c:91` (보존=True)
  - 현재:                     case Right_Ear:
                        lib_audio_set_front_mic(LIB_AUDIO_FRONT_MIC_RIGHT);
                        SYS_SET_ADC_DEC_CTRL(AUD
  - 제안:                     case Right_Ear:
                        /* Right 귀: DMIC1(EZ, ADC0)이 앞쪽(front) 마이크.
                         * 빔포밍에서 front mic = 기준(무지연) 경로 → DMIC2(QCC)에 HW 지연 없음.
                         * rear mic = 지연 경로   → DMIC1(EZ
  - 동등성: SYS_SET_ADC_DEC_CTRL 인수 불변, 주석 추가만
- **[comment/none]** `src/1__cfx/systemControl/main.c:450` (보존=True)
  - 현재:             if (lib_audio_get_front_mic() == LIB_AUDIO_FRONT_MIC_LEFT)
            {
                // Left front mic : dmic 2
                // audio_mix_2_b
  - 제안:             if (lib_audio_get_front_mic() == LIB_AUDIO_FRONT_MIC_LEFT)
            {
                /* Left 귀 빔포밍 인수 배치:
                 *   p_delay_buf0   = dmic2[block0] (QCC, 현재 블록)  — SW 지연 경로
                 *   p_delay_buf1   = dmi
  - 동등성: 함수 인수 배열 주소 불변, 주석 교체·보강만
- **[comment/none]** `src/1__cfx/systemControl/main.c:457` (보존=True)
  - 현재:             else if (lib_audio_get_front_mic() == LIB_AUDIO_FRONT_MIC_RIGHT)
            {
                // Right front mic : dmic 1
                // audio_
  - 제안:             else if (lib_audio_get_front_mic() == LIB_AUDIO_FRONT_MIC_RIGHT)
            {
                /* Right 귀 빔포밍 인수 배치:
                 *   p_delay_buf0   = dmic1[block0] (EZ,  현재 블록)  — SW 지연 경로
                 *   p_delay_buf1 
  - 동등성: 함수 인수 배열 주소 불변, 주석 교체·보강만
- **[comment/none]** `src/1__cfx/lib_cfx/lib_audio_in.h:31` (보존=True)
  - 현재: #define LIB_AUDIO_IN_BUF_MAX_CNT 2//20
  - 제안: /* 링 버퍼 블록 수: 0=현재 프레임(최신), 1=직전 프레임.
 * 빔포밍 블록 경계 처리(audioMixer.c p_mix[15])가 이 값에 의존한다.
 * 값을 2 초과로 늘리면 tdc_copy_DMIC_buffers()의 shift 루프와
 * audio_mix_2_buffers_for_beamforming() 인수 인덱스(LIB_AUDIO_IN_BUF_MAX_CNT-2/-1)를
 * 함께 검토해야 한다. */
#
  - 동등성: 매크로 리터럴값 2 동일

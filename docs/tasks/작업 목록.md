# 작업 목록 (Sound1 tasks index)

Sound1 프로젝트(`E:\Claude\projects\Sound1`) 작업 레지스트리.

- 신규 작업 시 먼저 이 목록을 조회해 관련 작업 여부 확인 → 기존 폴더에서 이어갈지 신규 분리할지 사용자 확인
- **우선 진행 약속이 등재되어 있을 때, 사용자가 새 작업을 지시하면 응답 첫머리에서 "이것부터 하기로 했습니다."라고 안내한 뒤 사용자 의사를 먼저 확인한다.**
- 컨벤션 상세: 루트 [`지침/일반/문서 작성 규칙.md`](../../../../docs/지침/일반/문서%20작성%20규칙.md) (섹션 3·4·5)

---

## 우선 진행 약속 (사용자 복귀 후 첫 작업)

> [!IMPORTANT]
> 사용자(다) 연차 종료 후 복귀 시점(**2026-05-14, 목**) 부터 다음 작업을 **다른 어떤 작업보다 먼저** 진행하기로 약속됨. 본 약속은 2026-05-08(금) 사용자 명시 지시로 등재됨. 약속 잔존 시 사용자가 새 작업 지시를 하면 응답 첫 줄에서 본 약속을 먼저 안내하고 사용자 의사를 확인한다.

| # | 작업명 | 모듈 | 폴더 | 상태 | 약속일 | 진입 | 비고 |
|---|---|---|---|---|---|---|---|
| (없음) | — | — | — | — | — | — | 우선 약속 모두 이행 완료 (#1 NofM, #2 Sullivan 둘 다 2026-05-14 종결) |

(우선 약속 #1 [`signalProcessing/20260514_nofm-merge`](signalProcessing/20260514_nofm-merge/), #2 [`sync/20260514_sullivan1.5-rel2`](sync/20260514_sullivan1.5-rel2/) 둘 다 2026-05-14 진입·종결 — §완료 참조)

## 작업 목록

| 작업명 | 모듈 | 태그 | 폴더 | 상태 | 시작·완료 | 연계 | 요약 |
|---|---|---|---|---|---|---|---|
| 배터리·LED 코드 리팩토링 (마진 제거·재캡슐화) | main | func_normal, battery, led, hysteresis, margin-free, updateBatteryLevel, table-driven, tdc | [main/20260714_battery-led-refactor](main/20260714_battery-led-refactor/) | 구현 완료(Step1~4·6, 정적검증) — 빌드·실기 대기, Step5 dead삭제 승인대기 | 2026-07-14 ~ | ← [main/20260709_func-normal-refactor](main/20260709_func-normal-refactor/) (동작보존 분해의 배터리→LED 특화 후속, 이번은 동작변경) · 브랜치 `claude_feature_led-battery-refactor` | 은수님 3대 불만(①과거 updateBatteryLevel 미적용 아쉬움 ②하드코딩으로 func_normal 길어짐 ③QCC가 측정하니 변화마진 불필요) 대응. 4페르소나 fan-out+반증검증(할루시네이션 0, 옵션 B 무결함, 마진제거 조건부안전 반증실패). **updateBatteryLevel()**(batteryNPowerControl.c:336)은 CFX 자체측정 기반이라 직접부활 불가·완전 dead code(7단계 enum은 이미 snd_batt_get_level로 포팅)→구조만 신규 tdc_ 함수로. **구현**: LED블록(~118줄)→tdc_led_request_battery/isd/mapping+경계테이블 s_tdc_batt_led_table[](옵션B)로 캡슐화(~35줄), prev_batt_st·s_map_low_active static+진입/이탈 이중임계 제거(**변화마진 제거=의도된 동작변경**: 배터리 sticky band 10~11·78~79 이제 항상 MID, 매핑 pct==21 이제 READY). baseline 미커밋 10/80(은수님 승인). 보존 5항목(override 2단·배터리→ISD→매핑 순서·led_set_isd_conn_state 봉인·pct 1회공유·RESET→IDLE) 전수 유지. 정적검증 PASS(브레이스 136/136·잔여식별자 0·전이등가). 커밋 6cd6bd0. **Step5 dead updateBatteryLevel 삭제**는 빌드안전 확인됐으나 요구사항 §5.2 최종삭제 승인 대기. 빌드·실기·실측게이트(QCC percent·0x34 주기·충전기 바운스)는 은수님 환경. |
| IQS323 전력모드(NP/LP/ULP) **딥다이브** | touch | iqs323, power-mode, np, lp, ulp, auto-prox-cycle, adversarial-verify, watchdog | [touch/20260706_touch-power-mode-np-lp-ulp](touch/20260706_touch-power-mode-np-lp-ulp/) | 딥다이브 완료(권고 확정, 승인 대기) | 2026-07-06 ~ | ← [touch/20260705_touch-event-mode-adoption](touch/20260705_touch-event-mode-adoption/) (유휴폴링연장 권고 후속) · 브랜치 `claude_feature_touch-mutual-cap-review` | 은수님이 NP/LP/ULP 전력비교·"ULP는 터치시에만 통신가능해 평상시 I2C실패"라는 가설 검증·센싱주기 정합성을 질문. INV-CTRL-1~3만 불변으로 Workflow 2단 10에이전트(WF1 그라운드3+후보3 약140만토큰, WF2 검증4 약82만토큰) 딥다이브. **결론(fact-verified): 은수님 가설은 표면적으로 반박**(Force Communication은 파워모드 무관하게 항상 통신창을 강제로 열 수 있음, Halt탈출도 동일기전 — 원문 확인). **그러나 우려의 본질은 다른 메커니즘으로 부활**("틀린 진단, 맞는 걱정") — Auto Prox Cycle Select(0x33, POR 기본값 32배, 코드 미설정)로 ULP 실효 리포트주기가 SW 타임아웃 가정(45ms/100ms)보다 수십 배 느려질 수 있어 "체감상 응답없음" 증상이 재현 가능. 과거 LP/ULP 회귀(PM Timeout=0 전면차단) 인과사슬 재구성(크기충돌+SW폴링·LP리포트레이트 100ms 우연일치 위상표류 신규발견), 단 "피피엠 단위" 정량화는 데이터시트 자체 클럭공차(±5.5%)와 모순돼 정정(실제 맥놀이 초~수십초 규모). **3개 활용후보 전부 미완결**: 후보_1(LP, PM Timeout 0→5000+MAX_WAIT_OPEN_MS 45→150) 조건부해소(전제미확인·재발방지 잠금장치 없음), 후보_2(ULP, Auto Prox Cycle 명시설정+리부트=안전망) 가설판정 정확하나 §8.11.2 할루시네이션 재발(선행라운드에서 이미 정정된 오류!), 후보_3(SW폴링+IC파워모드 국면분리) 보완관계 프레이밍 타당하나 **wake-write가 죽은코드**(func_sleep 탈출은 항상 하드리셋). **완전성 최중요 발견**: 선행 20260705가 확정한 255ms 내장워치독+ResetEvent비트 미점검 공백을 이번 라운드가 재인용 못함 — LP/ULP+절전연장 방향일수록 위험 커짐. **할루시네이션 1건 확정**(후보_2, 선행 라운드 정정오류 재발 — 인용위생 문제). **권고**: 순위1 실측게이트(forcecomm타이밍·AutoProxCycle실효값·클럭공차)→순위2 워치독공백해소 병행→순위3~5 후보1~3 실측후 재설계→비권고 현행유지도 유효선택지. 코드 변경 0. 승인 후 후속 신규작업(④). |
| 이벤트 모드(0xC0 bit7) 채택 **딥다이브** | touch | iqs323, heisenberg, event-mode, interrupt, i2c-lockup, adversarial-verify, force-window-open, watchdog | [touch/20260705_touch-event-mode-adoption](touch/20260705_touch-event-mode-adoption/) | 딥다이브 완료(권고 확정, 승인 대기) | 2026-07-05 ~ | ← [touch/20260703_touch-heisenberg-config-reexplore](touch/20260703_touch-heisenberg-config-reexplore/) (config-level 상쇄불가 확정 후속) · 브랜치 `claude_feature_touch-mutual-cap-review` | 은수님이 이벤트 모드(Streaming→Events, "RDY 매번 안 토글")를 주역 방식으로 승격 지시. INV-CTRL-1~3만 불변, 그 외 자유(force_window_open 프로토콜 재설계 포함)로 **1차** Workflow 2단 14에이전트(WF1 그라운드3+후보5 약184만토큰, WF2 검증6 약159만토큰) 딥다이브. **1차 결론(fact-verified): 원 전제는 3층분해로 좁은의미만 참**(①IC자체 RDY assert빈도 감소=참 ②실질의도인 인터럽트회피=**불성립**, RDY는 애초 busy-wait 폴링전용·EXTI 0건 ③순수트래픽감소=방향유지·크기과대추정 1.3~2.0배→1.07~1.39배). 검증 도중 은수님이 "폴링→인터럽트 전환"을 직접 추가 지시 — 후보_4(`ci_dio.c` 기존 2슬롯 인터럽트 인프라 재사용 SW-only)로 확정. 이벤트모드 자체(후보_1+2)는 단독채택반대(순이득 작음+에라타 리스크). **부수 최중요 발견**: IQS323 내장 255ms 통신 워치독+Reset Event 비트를 런타임이 전혀 점검 안 함(기존 안전장치 3종 전부 미탐지). 그러나 **은수님이 재확인**("결론은 지금이랑 별 차이가 없을것 같다") — 후보_4도 100ms 고정 폴링 주기는 유지한다는 한계 인정, "유휴 시 폴링 자체를 줄이는 결합 시나리오"를 **2차** Workflow 2단 9에이전트(WF3 그라운드3+후보2 약113만토큰, WF4 검증4 약97만토큰)로 추가 딥다이브. **2차 결론(fact-verified)**: 유휴(터치없음·ATI정상) 시 폴링 완전정지+인터럽트 하이브리드(후보_6)로 duty1%에서 트래픽 약 98.6~98.9% 감소 확인 — **실제로 큰 차이가 남**. 단 **핵심 발견**: 인터럽트·이벤트모드 전혀 없이 유휴 폴링주기만 100ms→500~1000ms로 늘리는 훨씬 단순한 변형_1(후보_7)도 트래픽 절감이 대등하고 회귀위험은 더 낮음 — **최종 1순위 권고로 격상**. 검증에서 구현 필수 정정 2건 발견(게이팅 삽입위치는 경과시간게이트 이전 배치 필수·"노말모드 WFI 부재" 서술은 오류로 실제론 이미 존재), duty가정에 이 프로젝트 선행 UX분석(2026-06-15)의 "간접접촉(안경·헤어·베개) 빈도높음" 미반영 발견(반영시 후보_6/7 상대격차 10.9~20.8배→1.9~2.8배로 좁혀짐). **할루시네이션 0**(1·2차 합산, 1차 확정오류 2건만 하류영향 없이 잔존). **최종 권고**: 순위1 후보_7 변형_1(유휴폴링주기연장)→순위2 duty실측(간접접촉포함)→순위3 IC버전실측→순위4(조건부) 후보_6 완전정지+인터럽트→순위5 워치독 공백해소(별도트랙). 코드 변경 0. 승인 후 후속 신규작업(④). |
| 하이젠베르크 회피 **재탐색**(설정·채널·측정 자유) | touch | iqs323, heisenberg, reference-tracking, sw-differential, no-cancellation, adversarial-verify, config-level | [touch/20260703_touch-heisenberg-config-reexplore](touch/20260703_touch-heisenberg-config-reexplore/) | 검토 완료(config-level 상쇄 불가 확정, 승인 대기) | 2026-07-03 ~ | ← [touch/20260703_touch-heisenberg-sw-impl](touch/20260703_touch-heisenberg-sw-impl/) (스코프 정정 후속) · 브랜치 `claude_feature_touch-mutual-cap-review` | 은수님 스코프 정정(제어 3거동만 불변·설정/채널/측정 자유)으로 재탐색. Workflow 2단 14에이전트(WF1 그라운드3+후보5, WF2 6 회의론자 빡센검증, 약287만토큰). **결론(fact-verified): config/channel/measurement 레벨로 코히런트 AC 클럭/SPI 노이즈 상쇄 불가.** 1순위 **reference tracking 데이터시트 기각**(§7.3 p.26 상쇄대상=count drift/온습도뿐·"primary touch/prox 상태"에서만 작동 게이팅→idle 오인식 무관·LTA전용, 반증 4갈래 전부 실패). **C-DUAL(SW차동) 기각**(데이터시트 p.15·21로 CH0-CH1 순차측정 확정→공통노출 전제 붕괴+비대칭결합+co-touch veto). C-MEAS(평균화는 코히런트 바이어스에 무력)·C-SYNC(자유구동 외부동기 불가·에라타)·C-FILT(Threshold만 생존, 헤드룸 k80→85 소진). **V6 완전성비평**: IQS323 전체 레지스터맵+AZD004+AZD125 재탐색→상쇄기능 전무, **Azoteq도 "SNR 마진 확보"만 제시**. **할루시네이션 0**(단 참고문서 오류 3건 발견: 데이터시트md §5.6 "Beta 4비트" 오기·§5.7 Touch Debounce 과잉·main.c TIMER3 주석 모순). **남는 길**: 상쇄 아닌 완화 — 선행 impl 포트폴리오(P0실측→P1 read_debug제거→P2 드라이브→P5 라이드스루)로 수렴. 코드 변경 0. |
| SW-only 하이젠베르크 회피 **구현 계획**(P0~P5) | touch | iqs323, heisenberg, sw-only, implementation-plan, fake-func-sleep, rollout, read-debug, 0xC0-bit7, drive-strength | [touch/20260703_touch-heisenberg-sw-impl](touch/20260703_touch-heisenberg-sw-impl/) | 마스터 계획 완료(승인 대기) | 2026-07-03 ~ | ← [touch/20260703_touch-heisenberg-sw-mitigation](touch/20260703_touch-heisenberg-sw-mitigation/) (조사 후속) · 브랜치 `claude_feature_touch-mutual-cap-review` | 조사 결론(SW-only 회피 가능)+재현경로=fake_func_sleep() 확정 하에 포트폴리오 P0~P5를 **구현-레디 스펙**화(Workflow 7에이전트: P0~P5 스펙6+통합검증, 약130만토큰). 통합검증 **논리충돌 0·확정 불변식 위반 0·fake_func_sleep 누락 0**. **핵심발견: H1(I2C 5배) 재현경로 코드 반증** — fake_func_sleep은 I2C prescale 240(128kHz) 유지 → 애그레서 무게가 H2(read_debug 자체애그레서)·물리결합으로 이동. **스펙**: P0 실측프로토콜(DBG/QCC/NRF/CLK 4스윕, 코드미수정), P1 read_combined()신규 or DEBUG_PRINT_ENABLE=0(iqs323.c:398후·config.h:22), P2 I2C0 8X→4X·SPI1 2X→1X·prescale÷6→÷21(DIO_PIN_Config.h:54/13·main.c:1222, prescale은 재현경로무관), P3 0xC0 bit7 5사이트 0x50→0xD0 Streaming→Events(iqs323.c:318/447/453/465/471, 237제외), P4 0x31 Period5→12(1M→500kHz, 389kHz회피, iqs323.c:316), P5 fake카운터×10 카운터기반(main.c:949/1007). **롤아웃6단계**: P0실측→P2prescale+P4→P2드라이브→P1→P3→P0재실측→P5(최후, 마스킹회피). **전제4**: P0↔P1시간분리·INV④⑦누적분리측정·P5최후·main.c:1011 재부팅복원(은수님확인). 제품소스 변경0. 승인·P0실측 후 롤아웃순 ④구현. |
| SW-only 하이젠베르크(클럭/SPI 결합) 회피 조사 | touch | iqs323, heisenberg, clock-coupling, spi, sleep, sw-only, no-hw-change, fake-func-sleep, drive-strength, robustness | [touch/20260703_touch-heisenberg-sw-mitigation](touch/20260703_touch-heisenberg-sw-mitigation/) | 검토 완료(SW-only 회피 가능, 실측 선결·승인 대기) | 2026-07-03 ~ | ← [touch/20260703_touch-mutual-cap-review](touch/20260703_touch-mutual-cap-review/) (mutual No-go 후속) · 브랜치 `claude_feature_touch-mutual-cap-review` | 절전 프로파일링(QCC on+SPI+e8300 30.72MHz)서 ATI에러·터치오인식→재부팅. 현 셀프캡·**HW 무수정·기존기능 전부 유지, SW 운영만으로** 클럭/SPI 결합(하이젠베르크) 회피법을 특성화2+해법레버8+검증8(Workflow 18에이전트, 세션한도로 7 실패→resumeFromRunId 재개로 전수완료, 약 485만토큰). **결론: SW-only 회피 가능**(회귀안전 레버 다수, mutual No-go와 대조). **특성화 급소**: ①실제 재현경로=**fake_func_sleep()**(main.c:810~1031, BLE General Debug op_mode==1, SYSCLK 30.72MHz 유지·BLE상시·QCC셧다운 주석처리, 재부팅 no-op — 은수님 확인 필요) ②ULP 재부팅 문턱 700ms(ATI에러7연속)/2400ms(터치24연속)로 Normal(무한재시도/30-90s)보다 취약 ③**H2**: 계측 read_debug() 미해결 latch버그(문서39/40)가 CRX0 인접 0xFF 주입=관측이 대상흔듦 ④**H1**: 절전 I2C prescale÷6 재사용시 SCL 5배(5.12MHz)초과. **레버판정**: 채택유력=S5 드라이브하향(I2C0 8X→4X·SPI1 2X→1X, 중~강 물리확립)·S8/R7(0xC0 bit7 자가RDY토글 제거); 조건부=S1 변환주파수(389kHz회피)·S2 필터Beta; 기각/보류=S3 Events(I2C Lock-Up에라타)·S4(H2완화로 수렴)·S7후보1(FSM 100×둔화)·S6-4(카운터화 필수). **권고 포트폴리오**: P0 RTT실측게이트(애그레서·H1/H2·재현경로 확정)→P1 관측탈침습(read_debug 수정)→P2 S5→P3 S8/R7→P4 S1→P5 S6안전망. C2 불변식 위반 0건(S1·S2·S5·S8). 코드 변경 0. 승인·재현경로 확인 후 후속 신규작업. |
| 상호용량 센싱 방식 재검토 (타당성) | touch | iqs323, mutual-capacitance, self-capacitance, heisenberg, reference-tracking, crx0, crx1, feasibility, no-go | [touch/20260703_touch-mutual-cap-review](touch/20260703_touch-mutual-cap-review/) | 검토 완료(조건부 No-Go, 승인 대기) | 2026-07-03 ~ | ← [touch/20260703 이전 관측교란·CRX1-ESD 참고문서] · 브랜치 `claude_feature_touch-mutual-cap-review` | 은수님 가설(상호용량 CRX0↔CRX1 차동이 셀프캡 "하이젠베르크"=클럭 스위칭 관측교란을 회피)을 전문가 6페르소나+검증4(Workflow, 10에이전트·168만토큰·26분) 검토. **결론: 조건부 No-Go — 핵심 4주장 전부 반증.** 근본 오해=**"차동=공통모드 상쇄"는 범주 오류**(그 기능은 mutual이 아니라 별도 채널타입 **reference tracking**). 하이젠베르크 핵심인 경로_2(스위칭 전하주입) 회피 근거 없음(주장_1 refuted 중상, §5.7 인용오류 발견). IQS323 mutual은 레지스터·배선 실재하나 데이터시트 **§3.2 전기특성(Cm 0.2~9pF·Cp≤100pF·Cx/Cm 10~20) 전원 누락**(주장_2 refuted). D패드는 self용 유산·해칭없음·외곽 2겹밴드(주장_3 refuted). CRX1 경로 **R32=0Ω**(권장 470Ω↓, 6노드 미인용) 실전극화 시 ESD 직결(주장_4 refuted high). 권고: **Track A SW-first**(제어재설계 권고 실기검증+§7 완화)·**reference tracking 별도검토**·mutual은 실측게이트4 통과 시 차세대보드. 코드 변경 0. 승인 후 후속 트랙 신규작업. |
| 터치 임계·히스테리시스 파일 영속화 (계획) | touch | iqs323, threshold, hysteresis, nvm, filesystem, persistence, ci_stim_mute, tdc | [touch/20260701_touch-setting-file-persist](touch/20260701_touch-setting-file-persist/) | 계획 완료(구현 보류) | 2026-07-01 ~ | ← [ble/20260701_general-debug-protocol-split](ble/20260701_general-debug-protocol-split/) (BLE 쓰기 트리거 후속 예정) | 터치 threshold/hysteresis를 파일 저장·부팅 로드. **5페르소나 병렬 조사**(①ci_stim_mute 패턴 ②FS API 계층 ③터치 세팅 경로·값 ④Initialize 흐름 ⑤무결성·fallback) → 계획 수립. 설계: 신규 `tdc_touch_setting.c/.h`(Gen1_5/FS), 구조체 `magic_begin+version+size+payload(thr·hys·reserved)+magic_end+crc16`(all-or-nothing 리셋+즉시 재기록), `#define TDC_TOUCH_IQS323_THRESHOLD/HYSTERESIS(80/8)` → 런타임 getter 전환, `apply_settings()`가 캐시 참조, Initialize `tdc_touch_init_begin()`(448) 직전 로드. **핵심 발견**: 값 실제 적용은 Initialize 밖 비동기(auto-ATI 후), ci_stim_mute의 open실패 gap 보완(모든 실패경로 캐시 기본값). 은수님 결정: tdc_ 네이밍·version+size+crc·절전 threshold=255 오버라이드 범위 밖. **BLE 쓰기 트리거·절전 오버라이드 재설계는 범위 밖(후속)**. 구현은 은수님 판단으로 보류. |
| func_sleep() 구조 정리 (변수·로그레벨·주석·함수분리) | touch | sleep, refactor, readability, behavior-preserving | [touch/20260702_func-sleep-refactor](touch/20260702_func-sleep-refactor/) | 구현 완료(정적 검증, 빌드·실기 대기) | 2026-07-02 ~ 2026-07-02 | ← 절전모드 재부팅 원인 분석 세션(비산출물, 대화 내) | `main.c` `func_sleep()`(순수 구조정리, 동작 100% 동일 유지) — ① 지역변수 전부 함수 최상단(영속 6종/임시 3종 구분, `#if` 게이트 변수는 게이트를 선언까지 확장) ② `ci_printe` 금지, printw는 칩 리셋 유발 최종 이벤트 2건만·나머지 전부 printd(핵심 반전 2건: ATI RECOVER 격하·touch REBOOT 승격) ③ 주석 30건 중 유지21·삭제9·축약2(게이트철학·워치독순서·ATI예외조건 3대장 최우선 보존) ④ ULP while 루프만 5개 `tdc_` static 함수로 분리(`tdc_touch_sleep_log_debug`·`_handle_ati_error`·`_handle_notouch_gate`·`_handle_touch_reboot`·`_log_state_transition`, 상태는 포인터 전달, 진입부는 분리 제외 — 05 근거). 페르소나 8노드(분석5·계획1·검증2, Workflow, 약 340K토큰). adversarial 검증 실질 버그 0건·문서완결성 7건 전부 계획.md 반영(else 리셋분기 3곳 명시·st/ok/state 대입 위치 명확화 등) 후 구현 시 전부 반영 확인. `fake_func_sleep()`·LED 표시 공백 수정은 이번 범위 제외(은수님 확인). 정적 검증 전수 PASS(printw 2건·중괄호·tdc_ 접두어·브레이스균형·네이밍충돌0). 빌드·실기는 은수님 환경, 미커밋. |
| 범용 디버깅 프로토콜 파일 분리 (tdc_) | ble | general-debug, remoteControl, tdc, refactor, file-split, behavior-preserving | [ble/20260701_general-debug-protocol-split](ble/20260701_general-debug-protocol-split/) | 완료 (구현·검증, 미커밋) | 2026-07-01 ~ 2026-07-01 | - | `remoteControl.c`(1627줄) `case EN__SND_BT_CMD_GENERAL_DEBUG`(0x8F, 108줄)의 option 1/2/3 처리를 신규 `tdc_remote_general_debug.c/.h`로 분리(동작 동일 리팩토링). 브랜치 `claude_feature_general_debug_protocol`. 조사 3노드(case패턴·빌드통합·심볼헤더 Explore) → 계획 승인 → 구현 → 검증 2노드(회귀·컴파일정합성). **핵심 제약**: `bufferForSPI_tx`(int[20])·`tx_index`는 `remoteControl()` **지역변수** → 인자 전달+반환. 최종 시그니처 `int tdc_remote_general_debug_handle(const ST__REMOTECONTROL_PACKET *packet, int *tx_buf, int tx_index)` — 패킷도 **인자 주입**(은수님 제안, 전역 결합 해소·독립 수행·읽기전용 const). 호출부 `&remoteDataPacket` 전달, 헤더는 타입 위해 `remoteControl.h` include(단방향 DAG, 순환 아님). option별 static 헬퍼 3개(gd_handle_touch_debug 13B·no_backtel 3B·map_init 3B) 분리, case는 108→8줄 축약(함수호출+writeDataToSpiTxBuff+clearRemoteColtrolCommand 잔류). **네이밍**: `tdc_remote_general_debug`(remoteControl 계보 우선, BLE는 전송매체) — 은수님 선택. **검증 전수 PASS**: 회귀 노드 바이트 순서·마스킹(>>8&0xFF)·부수효과(OTA DFU CONN/DISCONN·ci_map_init 4회·SYS_WATCHDOG 4회)·로그 100% 동일, 컴파일 노드 include 경로·심볼 선언·시그니처·타입정합·중복정의 0. `.cproject` 폴더단위 등록이라 수정불필요(Eclipse Refresh 자동 픽업). 빌드·실기는 은수님 환경. 미커밋. |
| 터치 임계·계수 레지스터 동작 레퍼런스 (조사) | touch | iqs323, register, threshold, hysteresis, prox, beta, ati, reference, docs | [touch/20260623_threshold-register-reference](touch/20260623_threshold-register-reference/) | 완료 | 2026-06-23 ~ 2026-06-23 | ← [touch/20260622_touch-instrumentation-sleep-ati](touch/20260622_touch-instrumentation-sleep-ati/) (실측 튜닝 맥락) | 코드 상수↔레지스터↔IC 내부계산↔계수출처 전수 정리. 페르소나 4(코드매핑·DS공식·계수출처·데이터흐름)+adversarial 검증 1(할루시네이션 0). **절대임계=계수×LTA/256**, 분류(그대로/코드변환/IC계산), **Beta AZD004(α=1/2^β) 실측확정**, ATI는 baseline(Cp)만 정규화·grounding ΔC 미보정, 실측게이트 4종. 산출 `docs/참고/touch/터치 임계·계수 레지스터 동작/`(상세 md + Claude스타일 html). 코드 수정 0. 주석 stale 3건 발견(기록만). |
| QCC가 NRF_On_OFF 역할 커버하는지 분석 | ble | qcc, nrf, on-off, coverage, architecture-shift, analysis | [ble/20260629_qcc-nrf-onoff-coverage](ble/20260629_qcc-nrf-onoff-coverage/) | 완료 (분석) | 2026-06-29 ~ 2026-06-29 | - | 폐기예정 nRF `NRF_On_OFF(isd_state,global_BLE_Off,mappingConnection,Mapping_BLE_Off)`의 BLE on/off 결정 로직을 현 QCC 제어(`snd_qcc.c`)가 커버하는지 다페르소나 비교. 8노드(분석5·통합1·검증2, sonnet/high, 35만토큰, 1차 중단→resume 완주). **평결: `architecture_shift`** — "전부 커버"가 아님. ①NRF 출력부(`NRF_Off/On_Command`)가 이미 **데드코드**(GPIO 전부 주석, systemControl.c:37~45·104·108) → 기능 이전 아님. ②결정 주체 **E8300(CM3)→QCC 펌웨어 위임**(E8300은 ISD_CHECK·QCC_CTRL 핀만 출력, snd_qcc.c는 결정로직 0). 5조건 판정: 조건_2(ISD미연결)=covered / 조건_1(0.6초지연)·3(mappingConnection)=partial / **조건_4(Mapping_BLE_Off)=uncovered 유일 실질 gap**(BLE_Off_Command가 QCC경로 전무, 데드코드 main.c:660에만 전달) / 조건_5(충전기off)=**의도적 정책반전**(충전중 BLE유지→뚜껑감지, main.c:799~824). QCC고유 신규 3종(절전·OTA·뚜껑 SHUTDOWN). `set_mode(SHUTDOWN)`=칩전원차단+재부팅 ≠ NRF 광고off. **검증: 할루시네이션 0건(22인용 전수)·반증 8축 생존.** 부수발견: dead macro `BLE_OffTimeAfterISD_Disconnected`(미사용)·main.c:508 #else 데드코드. 권고 4건(조건4 확인·조건5 문서화·데드코드 정리). 코드변경 0(순수 분석)·미커밋. |
| I2S·빔포밍 모듈화 (유닛/모듈/테스트 계층) | signalProcessing | modularize, unit-module, validation, i2s, beamforming, tdc-naming | [signalProcessing/20260626_i2s-beamforming-modularize](signalProcessing/20260626_i2s-beamforming-modularize/) | 완료 (구현·검증, 미커밋) | 2026-06-26 ~ 2026-06-26 | ← [signalProcessing/20260625_beamforming-impl-review](signalProcessing/20260625_beamforming-impl-review/)·[20260625_beamforming-refactor](signalProcessing/20260625_beamforming-refactor/); → [`docs/지침/SW 밸리데이션 계층화 방법론.md`](../지침/SW%20밸리데이션%20계층화%20방법론.md)(신설 방법론) | I2S+빔포밍+입력설정+모드전환을 의료기기 밸리데이션 관점 유닛/모듈로 재구조화(현 체계 특화·경량). **인터뷰로 방법론 정립**(유닛 자동승격 금지·통합테스트=모듈내 전유닛 동시활성화·합성 유닛→모듈→시스템·의존성="A는 B 전제") → **재사용 상위 지침 신설**. ②분석 12페르소나(유닛후보 136→수렴 12유닛/6모듈 DAG, 믹서 비순수 정정) → ③계획 승인 → ④구현(경량: tdc_ 리네임 9심볼·dead 정리·[UNIT]12/[MODULE]7 배너·유닛-모듈-테스트맵.md) → 단계_4(3페르소나). **동작보존·빌드안전 PASS, 정합 4건 정정.** 기존 `lib_i2s.c:82 #else` 오타 발견(제 변경 무관·보고). 전체 시스템 리팩토링 템플릿. 미커밋. |
| CFX 빔포밍 코드 리팩토링 (상수화·주석·데드코드) | signalProcessing | beamforming, cfx, refactoring, behavior-preserving, constants, comments | [signalProcessing/20260625_beamforming-refactor](signalProcessing/20260625_beamforming-refactor/) | 완료 (구현·검증, 미커밋) | 2026-06-25 ~ 2026-06-25 | ← [signalProcessing/20260625_beamforming-impl-review](signalProcessing/20260625_beamforming-impl-review/) (대상 코드) | 기능 정확성 검증된 빔포밍 구현을 **동작 보존** 리팩토링. ②분석 9페르소나(인벤토리76, 할루시네이션 all_real·동작보존 unsafe3 적발→제외) + ③계획 승인 + ④구현 + 단계_4 검증 5페르소나. **적용**: 신규 상수(`LIB_AUDIO_DMIC_COUNT_SINGLE/DUAL`·`LIB_DMIC_IDX_DMIC1/2`·`AUDIO_MIX_NORMALIZE_RSHIFT`·배열차원), 데드코드 제거(`_0_9958`·`g_enabled_mic_count`·구주석), doxygen/버퍼규약/÷2의도/하이브리드지연(FRAC=6=0.025샘플) 주석, 낡은 주석 정정(IN1→IN0·ADC1→ADC2). **제외(위험)**: 믹서 언롤→루프·I2S XOR·mic복사 삼항. tdc_ rename 보류. **검증: preserved 4/5, regression 1은 git diff HEAD 베이스라인 오류로 은수님 구현을 오귀속한 오탐(라인증거 반증).** 6파일 변경 미커밋. 빌드는 은수님 Eclipse. |
| CFX 빔포밍 실구현 리뷰 (분석·이론대조·개선) | signalProcessing | beamforming, cfx, implementation-review, delay-and-sum, gain-staging | [signalProcessing/20260625_beamforming-impl-review](signalProcessing/20260625_beamforming-impl-review/) | 완료 (리뷰) | 2026-06-25 ~ 2026-06-25 | ← [signalProcessing/20260623_beam-forming](signalProcessing/20260623_beam-forming/)·[20260623_dmic-sel-edge-match](signalProcessing/20260623_dmic-sel-edge-match/) (이론·정합 베이스), ← 브랜치 `claude_feature_cfx-beamforming-wip`(877a18d) | 은수님 CFX 빔포밍 실구현(화이트노이즈 L/R 동작 확인) 정독 후 17 페르소나(분석12·검증5, 98만토큰). **결론: 기능적으로 정확, 재작성 불필요** — 하이브리드 지연(SW 1샘플+HW FRAC=6=1.025샘플≈22mm 1.026), 동일패리티 ch0/ch2, SEL엣지 정합, L/R 재조향 모두 검증 confirmed(반증0). critical 3건은 버퍼 인덱스 가정(0=최신)이 근원이나 검증상 "참"→fragility(미문서화). "이론 FRAC=14≠구현6"은 오탐(2cm-allHW vs 22mm-하이브리드 혼동). **개선 Tier1: 게인 경로 통일(빔포밍÷2 vs I2S 비÷2 +6dB 불연속)·버퍼가정 명문화·전환 글리치 mute. Tier2~4: fractional 재적용·QCC AND·데드코드·전후비 정량.** 코드 변경은 후속. |
| 마이크 SEL핀 ↔ E8300 DMIC 캡처엣지 정합 분석 | signalProcessing | dmic, sel-pin, clock-edge, pdm, sph0641, e8300 | [signalProcessing/20260623_dmic-sel-edge-match](signalProcessing/20260623_dmic-sel-edge-match/) | 완료 (이론 분석) | 2026-06-23 ~ 2026-06-23 | ← [signalProcessing/20260623_beam-forming](signalProcessing/20260623_beam-forming/) (포인트_3 해소) | Knowles SPH0641LM4H-1 마이크 SELECT 핀(데이터시트 §5·§6 p.7)과 E8300 DMIC 캡처 엣지(HW §18.2 p.563) 대조. 11 페르소나(분석7·검증4, 45만토큰). **올바른 조합 확정: SEL=GND↔`DMIC*_DATA_RE`(rising)·SEL=VDD↔`DMIC*_DATA_FE`(falling) 2가지뿐**(역조합은 마이크 High-Z 반주기 캡처→손상). 현 펌웨어(DMIC1=`_RE`/DMIC2=`_FE`)는 **U7 SEL=GND(내부 풀다운 기본과 정합)·U9 SEL=VDD(능동 스트랩 필수)** 요구. **급소=U9 SEL=VDD** — 미스트랩 시 Left 채널 손상→빔포밍 불성립. 검증 confirmed 4/0/0. **회로도 확인(2026-06-23): U7 SEL=GND·U9 SEL=VDD → 현재 펌웨어 두 채널 정합 ✅ 확정**(빔포밍 2채널 켜도 엣지 정합 유지). 잔여=부품번호 불일치(회로도 SPH8690 vs 데이터시트 SPH0641) SEL표 확인 권고. 코드 변경 없음. |
| DMIC 2채널 빔포밍 타당성 분석 | signalProcessing | beamforming, dmic, decimation, sampling-delay, end-fire, e8300 | [signalProcessing/20260623_beam-forming](signalProcessing/20260623_beam-forming/) | 완료 (이론 분석) | 2026-06-23 ~ 2026-06-23 | - | cfx 현재 DMIC 1개(U7 Right/U9 Left, 2cm) → 빔포밍 요구. 34 페르소나 3단계(분석12·토론10·검증12, 160만토큰). **은수님 두 가설 데이터시트 확인**: ① 입력 패스 채널 고유 지연 존재(decimation 시분할 짝수 ch0·2가 홀수 ch1·3보다 1/8 샘플=7.81µs 선행, HW p.222·445), ② SW 시프트 없이 HW `AUDIO_ADC_DEC_CTRL` `DELAY_INTEGER`(1/8 단위 0~7/8)+`DELAY_FRACTIONAL`(1/240 해상도)로 채널별 지연 부여(HW p.450·563). end-fire 필요지연 0.933샘플(58.3µs). **핵심전략: front mic을 홀수 채널 배치→스큐가 음향선행 자동보정→필요지연 0.808샘플(INT=6/FRAC=14) HW 단독구현**. 지연대상=front mic(착용 귀별 DMIC1/2). 검증 confirmed 11/refuted 0/uncertain 1. 코드변경은 후속(계획.md 후속_1~5). |
| 터치 계측·절전 Full ATI 정합 (계측·디바운스·메시지) | touch | iqs323, full-ati, sleep, instrumentation, lta, debounce, logging | [touch/20260622_touch-instrumentation-sleep-ati](touch/20260622_touch-instrumentation-sleep-ati/) | 구현 완료(빌드·실기 검증 대기) | 2026-06-22 ~ | ← [touch/20260619_touch-fullati-control](touch/20260619_touch-fullati-control/) (후속), ← [touch/20260622_touch-behavior-spec](touch/20260622_touch-behavior-spec/) (스펙 근거) | Full ATI 리팩토링 후속 4건. 페르소나 7노드(분석4·계획1·검증2, 458K토큰). **절전 threshold=255 무감(Fixed 잔재) 제거→운용임계 16 유지로 절전 중 감지·롱터치 깨움 복원**(ATI Full 보존), 신규 `read_debug()`(0x13 Counts+0x14 LTA 연속 4바이트)+200ms `ci_printd` 계측(LTA·CNT·delta·절대임계·BAND, DEBUG_PRINT_ENABLE 빌드가드), boot_ignore 해제 400ms(2폴링) 디바운스(순수 FSM), ATI=printw·Re-ATI=printi 원인분기(기존 printe 보존). 검증 치명 1건(`i2c_read` int buf[2] 고정→4바이트 read 불가·LTA 쓰레기) 수정(buf[4]+len가드+루프). 정적 전수 PASS. Beta는 값유지(계측만). 빌드·실기 RTT는 은수님 환경 대기. 미커밋. |
| Full ATI 터치 제어 재설계 (구현) | touch | iqs323, full-ati, re-ati, sw-timeout, redesign, implementation | [touch/20260619_touch-fullati-control](touch/20260619_touch-fullati-control/) | 구현 완료(빌드·실기 검증 대기) | 2026-06-19 ~ | ← [touch/20260618_touch-review](touch/20260618_touch-review/) (브레인스토밍 수렴 승계), ← [touch/20260619_external-reference-integration](touch/20260619_external-reference-integration/) (앱노트 근거) | 브레인스토밍 수렴안(FullATI 타임아웃 단순안)을 펌웨어로 구현. 11노드 분석(effort xhigh)→Phase P0~P6 빌드가드 단계 구현(fork, 5파일 +458/−25)→5노드 adversarial 검증. **Full ATI 전환**(0x36 0x0C·FIXED MULT/COMP 폐기·CRX1 더미·CRX0 방전 폐기·`(void)ati_error` 제거), ATI 에러 폴링→Re-ATI 게이트(터치 중 보류=급소), SW 타임아웃 30초 3단계(노말 전용), Beta 비대칭·Power Automatic No ULP·1MHz, **임계 계수 100→16(검증 09 단위오류 정정: Threshold=계수×LTA/256, 부등식 계수<32)**. 신규 API 3종(reseed_only·re_ati_trigger 공개·read_status_full)·빌드가드 9종. 검증 급소 [치명] 1건(read 실패 stale 캐시 Re-ATI 누출)→수정·재검증 PASS. 빌드·실기는 은수님 Eclipse 환경, RTT로 실측값(t_ati·Beta 해석A/B·계수↔raw LTA) 확정 대기. 미커밋. |
| IQS323 앱노트 md 정리 | touch | iqs323, azd004, azd125, appnote, docs, ssot | [touch/20260619_appnote-md-reference](touch/20260619_appnote-md-reference/) | 완료 | 2026-06-19 ~ 2026-06-19 | ↔ [touch/20260619_external-reference-integration](touch/20260619_external-reference-integration/) | AZD004·AZD125·IQS323 Arduino 예제코드를 데이터시트 폴더처럼 원문 충실 1차 정리(SSOT). `참고/touch/{AZD004 센싱 일반 가이드·AZD125 정전용량 설계 가이드·IQS323 예제코드}/` 신설(각 README+다파일). 상위 README 인덱스 갱신. |
| 외부 레퍼런스 통합 분석 | touch | iqs323, azd004, azd125, user-guide, reference, 적용분석 | [touch/20260619_external-reference-integration](touch/20260619_external-reference-integration/) | 완료 | 2026-06-19 ~ 2026-06-19 | ← [touch/20260618_touch-review](touch/20260618_touch-review/) | Azoteq 외부 자료 4종(예제코드·AZD004·AZD125·User Guide)을 제어 방향에 정독·매핑·adversarial 검증(10노드 Workflow + 검증 2노드). **온도 드리프트 정량 발견(AZD125 §8.2.4, −18cnt/°C 감소 방향)**·Follow UI 적용불가 확정·v3 D1~D5 전부 보강. 영속 문서 `참고/touch/적용가이드/IQS323-제어운영-적용가이드.md` 신설. 한자 0·원문대조 PASS(인용 19건). |
| 터치센서 재검토 (분석 결론 → 구현 수렴) | touch | iqs323, auto-ati, reseed, convergence, implementation, review | [touch/20260618_touch-review](touch/20260618_touch-review/) | 완료 | 2026-06-18 ~ 2026-06-19 | 중단 흡수: [crx1-esd-guard](touch/20260609_crx1-esd-guard/)·[touch-rtt-tuning](touch/20260610_touch-rtt-tuning/)·[eunsu-consensus-review](touch/20260615_eunsu-consensus-review/); ← [control-redesign-study](touch/20260611_control-redesign-study/)·[eunsu-opinion-review](touch/20260612_eunsu-opinion-review/) (권고 베이스); → 구현 신규 작업 승계 | 누적 분석 9건 결론(만장일치 "Reseed 시점 baseline 오염" + 하이브리드 권고)을 브레인스토밍으로 수렴. **최종 수렴: FullATI 타임아웃 단순안**(Full ATI·ATI 에러 시 Re-ATI·SW 타임아웃 30초·드리프트 Re-ATI 자동커버·Fixed/공장캘리/CalCap 폐기) — `브레인스토밍_FullATI타임아웃단순안.md`. 외부 레퍼런스 통합·앱노트 정리(20260619 2건) 동반. 구현은 신규 작업으로 승계. |
| 크래들 뚜껑 열림/닫힘 감지 LED 활성화 판단 | LED | cradle, lid-detection, system-info, power, ble-cmd | [LED/20260615_cradle-lid-detection](LED/20260615_cradle-lid-detection/) | 완료 | 2026-06-15 ~ 2026-06-17 | - | BLE 0x34 data[2] 파싱 → tdc_charger_set_cradle_cover_state() 신설 → 첫 닫힘 감지 시 func_cradle_lid_closed_loop() 약 절전 진입(SYS_WAIT_FOR_INTERRUPT 기반) → 뚜껑 열림 패킷 수신 시 워치독 리셋 재부팅. CFX 유지, ISD 연결 시 차단. |
| C51 DC블로킹→J3 전하누적 가설 검증 (개선 스터디) | touch | iqs323, c51, esd, charge, hypothesis, multi-agent | [touch/20260612_j3-charge-accumulation](touch/20260612_j3-charge-accumulation/) | 완료 | 2026-06-12 ~ 2026-06-12 | ← [touch/20260612_discharge-autoati-coexist](touch/20260612_discharge-autoati-coexist/) (방전·ESD 후속) | 은수님 가설(CRX0-J3 직렬 C51 100pF DC블로킹 + 전극 GND 부재 → 전하 누적 → 일정 횟수 후 먹통)을 5페르소나(아날로그 회로이론 포함)+4 adversarial 검증(워크플로우 9에이전트·508K토큰). `개선/J3 전하누적 가설 검토/` 8문서 + **`개선/J3 전하누적 가설 평결.md`**. **평결: 회로 토폴로지(H1 DC블로킹·H2 전극 IC측 격리·H5 C51 너머 못뺌) 정확, 단 "C51 전하누적 단독 먹통"은 약함**(H3 영구누적 반증 성향—충전전달 전하회수·전극 누설 τ 수초·R_leak이 가름; H4 C51≫Cx 분압비·충전전달 양방향). **진짜 그림 = SW 드리프트(주, FIXED+LTA 고착·이슈해결 §7 "counts drift" IC 진단) + C51 물리누적(부) 가산 모델**(GND 쇼트 복귀가 물리누적 부차기여 살림). **방전 무력 1차 = B2(0x30=0x02 Linearise, 핀 VSS 아님)**, C51(H5)은 0x34 교정 후 2차. **정정: C51 100pF 직렬 비표준 아님**(WLCSP11 슬라이더 Fig 2.3에 C6 100pF 표준 — 내 00 입력 Fig 2.1 비교는 패키지 불일치 오류). 판별 실측: **C51 단락 시 먹통 소멸 여부가 단일 결정 게이트**. 해결: 물리누적→전극 GND 블리더(1~10MΩ) / SW→ATI Full+노터치 게이트 / 가산→병행. 정량(Z_C51 1.59kΩ@1MHz·τ·C_eff) 근거. 커밋 미진행. |
| auto-ATI 중 CRX0 방전 공존 검토 (개선 스터디) | touch | iqs323, auto-ati, discharge, calcap, esd, coexist | [touch/20260612_discharge-autoati-coexist](touch/20260612_discharge-autoati-coexist/) | 완료 | 2026-06-12 ~ 2026-06-12 | ← [touch/20260612_eunsu-opinion-review](touch/20260612_eunsu-opinion-review/) (하이브리드 권고 후속) | 하이브리드 권고에서 ESD 물리누적 미지수 시 수동방전(CRX0 VSS+CRX1 CalCap)을 auto-ATI와 공존 가능한지 5페르소나+4 adversarial 검증(워크플로우 9에이전트·550K토큰). `개선/방전·autoATI 공존 검토/` 8문서 + **`개선/방전·autoATI 공존 권고.md`**. **평결: auto-ATI Full 상시+200ms 방전 직접공존 조건부불가**(0x30 비트공유·Re-ATI 진동·ATI 수렴교란·I²C 무응답; I²C hang 시 방전복원 막혀 CH0 영구 disable 치명). **충돌 근원은 자율 Re-ATI 하나 → auto Re-ATI만 끄면(부팅 1회 ATI+Disabled 고정+LTA 추적) 공존 가능.** **🔴 라이브 발견: 현재 방전 `0x30 0x02`는 데이터시트상 Linearise(VSS 아님, VSS는 0x34·핀enable 0x33) → 방전 물리효력 미검증, 진위 실측 최우선.** 레이어 분리 참(방전=핀 물리전하 고유기능, ESD 물리누적 시). 대안: 통신관리→방전진위·ESD 실측→Re-ATI 억제→드리프트면 방전폐기/물리누적이면 안전화(RMW·verify·빈도)→HW 방전. CRX1 CalCap은 방전 유지 시 그대로. 코드 grep 라이브 확정(942/947/1142/h:89). 커밋 미진행. |
| 은수님 제안(auto-ATI+빠른BETA) 비교·검증 (개선 스터디) | touch | iqs323, auto-ati, beta, review, multi-agent, hybrid | [touch/20260612_eunsu-opinion-review](touch/20260612_eunsu-opinion-review/) | 완료 | 2026-06-12 ~ 2026-06-12 | ← [touch/20260611_control-redesign-study](touch/20260611_control-redesign-study/) (이전 zero-base 탐구) | 은수님이 직접 제시한 제어안(auto-ATI 복귀 + LTA 자기정상화 + 빠른 BETA, 10단계)을 6 페르소나 + 4 adversarial 검증(워크플로우 10에이전트·695K토큰)으로 검증. `개선/은수님 의견 비교 분석/` 9문서 + **`개선/은수님 안 평결·하이브리드 권고.md`**(결과). **평결: 방향 타당·통찰 가치**(잘못 시딩돼도 자기정상화→B1 시점의존 우회). 수정점: ①7번 메커니즘 명명 반대(frozen 덕분✗→부재 덕분○, 결과는 성립) ②손 떼야 정상화 100% 의존→노터치 게이트 안전망 ③빠른 BETA 단일상향 위험(약터치 흡수)→**Fast Filter Band 비대칭이 정답** ④ATI Full=I²C 무응답 재현(현 펌웨어가 끈 이유) ⑤M7·M8 반증(절전 세팅 실재·방전≠ATI, 코드 grep 확정). **결론: 은수님 안 ⊕ 이전 결론=상보, 하이브리드(ATI Full+노터치 게이트+Fast Filter Band 비대칭 BETA+터치중 차단+I²C 통신관리) 최적.** 명제 M1~M8 개별 판정. 커밋 미진행. |
| IQS323 터치 제어 zero-base 재설계 탐구 (개선 스터디) | touch | iqs323, redesign, ideation, multi-agent, study, reseed | [touch/20260611_control-redesign-study](touch/20260611_control-redesign-study/) | 완료 | 2026-06-11 ~ 2026-06-11 | ← [touch/20260611_circuit-analysis](touch/20260611_circuit-analysis/)·[touch/20260611_fw-implementation-analysis](touch/20260611_fw-implementation-analysis/) (HW·SW 분석 기반) | HW·SW·데이터시트 분석 위에서 **현재 구현에 얽매이지 않는 zero-base 제어 재설계**를 다중 에이전트 토론으로 탐구. `참고/touch/개선/생각정리/`에 과정 13문서 + **`개선/제어 재설계 분석·권고.md`**(정식). 6관점 발산→18비판→6 blocking 검증→4해법 토론·교차검증(워크플로우 발산·비판 24 + 검증·해법 14 에이전트, 누적 ~3.3M 토큰, schema 한글키 오류 1회→영문화 resume 복구). **만장일치 수렴: 진짜 문제는 `Reseed` 시점 baseline 오염**(autoATI·Release UI 아님, 검증 B1). 권고: Reseed 노터치 게이트(Phase1·SW only·데이터시트 비의존) + FIXED→ATI Full 복귀(Phase2, 이슈6·8·9 드리프트·카운트변동), 전면 재작성 불요. **결정적 반증 B4**: 웜부트도 `mclr_reset()` 무조건 실행→IC POR(상태 보존 거짓)→펌웨어 분석 §11 정정. 실측 게이트 3종(오염 실채널·self-cap 1ch 전류·0x30 LSB Linearise). 라인 참조 6/6 검증·할루시네이션 0. 다중 에이전트 발산-비판-검증-수렴 방식. 커밋 미진행. |
| IQS323 터치 펌웨어 구현·시퀀스 분석 (개선 스터디) | touch | iqs323, firmware, sequence, driver, study, analysis | [touch/20260611_fw-implementation-analysis](touch/20260611_fw-implementation-analysis/) | 완료 | 2026-06-11 ~ 2026-06-11 | ← [touch/20260611_circuit-analysis](touch/20260611_circuit-analysis/) (HW 짝), ← [touch/20260611_iqs323-datasheet-md](touch/20260611_iqs323-datasheet-md/) (레지스터 SSOT) | SW 관점 현재 펌웨어 구현·설정 시퀀스를 **코드 ground truth**로 분석. `참고/touch/개선/펌웨어 구현·시퀀스 분석.md`(14절+부록). 2계층(`tdc_touch`↔`tdc_drv_iqs323`), 초기화(MCLR→auto-ATI 폴링 2.5s→`apply_settings` 9단계), CH1 CalCap 더미, ATI FIXED(보드변종, PACKAGE 0x5C82/0x63EF, Mode Disabled), 200ms CRX0 VSS 방전, 노말 2.4s/절전 2.2s 롱터치(웜부트·MCLR 미수행). Explore 3개 매핑 후 **핵심 구간 직접 Read 확정**(라인 환각 배제) → 서브에이전트 누락 포착(**절전 255/255→reseed 100/80 2단계**, PACKAGE ATI define↔주석표 0x5E/0xE4 불일치, prox no-op, `write_and_verify` 값 미검증, 코드 주석↔상수 불일치). 데이터시트 대조 쟁점 §8(코드 0x30 INACTIVE_RXS↔데이터시트 A.5 비트정의 불일치 — 실측 확인 필요) **독립 검증 타당 확인**. 서브에이전트 5개 fan-out(touch/drv/main 코드정합+데이터시트+추정·구조) 전부 PASS·치명 0·할루시네이션 0. SW 쟁점 9건 제기(개선안은 후속). 커밋 미진행. |
| IQS323 터치 회로 구성·분석 (개선 스터디) | touch | iqs323, circuit, schematic, study, improvement | [touch/20260611_circuit-analysis](touch/20260611_circuit-analysis/) | 완료 | 2026-06-11 ~ 2026-06-11 | ← [touch/20260611_iqs323-datasheet-md](touch/20260611_iqs323-datasheet-md/) (데이터시트 SSOT 참조), ↔ 기존 `참고/touch/레퍼런스/회로-구성`(C54 동반 정정) | IQS323 터치(전원 스위치 겸용) 대규모 개선 스터디 착수 — 첫 단계 회로 정리. `참고/touch/개선/` 신설(README 로드맵 + `회로 구성·분석.md` 12절). 실제 회로도 JPG를 ground truth로 회로 전수 정리 + 데이터시트 §2.5/§3.4 권장 대조 + 현재 구현(self-cap·FIXED MULT/COMP·200ms CRX0→VSS 방전·CRX1 CalCap·스트리밍)·이슈 9건의 회로적 해석 + 개선 검토 포인트. **C54 오기 발견·정정(4.7nF→4.7µF, JPG+데이터시트 양쪽 근거)** — 기존 레퍼런스/회로-구성.md 동반 정정. 서브에이전트 4개 fan-out 검증(회로사실 JPG·데이터시트 인용·구현/이슈 정합·구조) 전부 PASS·치명 0·할루시네이션 0(추정 표기 적절). 데이터시트 대비 차이 포착(I²C 풀업 회로측 부재·CRX0 직렬 100Ω<권장 470Ω·전원경로 무필터). 펌웨어 개선은 후속 스터디(autoATI vs FIXED·드리프트·채터링·ESD/guard). 커밋 미진행(승인 대기). |
| IQS323 데이터시트 원문 md 정리 | touch | iqs323, datasheet, docs, reference, ssot | [touch/20260611_iqs323-datasheet-md](touch/20260611_iqs323-datasheet-md/) | 완료 | 2026-06-11 ~ 2026-06-11 | ↔ 기존 `참고/touch/레퍼런스/`(2차 해설, 보완 관계) | 68p PDF 데이터시트 재독 컨텍스트 소모 절감 위해 `참고/touch/데이터시트/`에 README+01~08(9파일) 원문 1차 정리(SSOT). 기존 6개 md는 전부 2차 해설이라 원문 정리물 부재 → 신설(중복 아닌 보완). 분할=논리 그룹 8파일(§1~4 개요·전기·타이밍 / §5 proxfusion동작 / §6 하드웨어설정 / §7 부가기능·UI / §8 i2c / §9+부록A 레지스터레퍼런스 / §10~11 오더링·패키지 / 부록B·C 개정이력·이슈). 전 68p 직접 재독 후 작성(수치·비트 PDF 대조, 본인 추론 배제). 서브에이전트 **12개 fan-out 교차검증**(PDF=ground truth, 06은 §9/A.1~19/A.20~37/핵심값 삼중) → 치명·할루시네이션 0, 중 7건(ball pitch 표기·TL;DR 250자 초과) 수정 후 재검증 PASS. 상위 README 2→3축 확장. `레지스터-맵.md` 무변경(슬림화는 별도 후속). 커밋 미진행(사용자 승인 대기). |
| init 콜트리 errorCodeUpdate 일괄 디바운스 | internalDevice | isd, fpga, errorcode, debounce, led | [internalDevice/20260610_init-errorcode-debounce](internalDevice/20260610_init-errorcode-debounce/) | 구현 완료(실기 검증 대기) | 2026-06-10 ~ | ← FPGA 에러 LED 무한 깜빡임 분석(별도 세션) | `isd_interface()`의 `init_txPowerIC`/`init_FPGA`/`init_ISD` 콜트리 내 `errorCodeUpdate()` 호출 구간을 일괄 디바운스로 일원화하려는 작업. 현행은 함수 레벨 `error_cnt` 8개(`>3`×7·`read_FPGA_FIFO_counter`만 `>10`) + init 본문 명명 카운터 5개(`no_reset_value`·`no_disabled_rf`·`outer/inner_rf_tx`·`pulse_width`)로 **산발 적용**, 임계 혼재·다수 누락(case 210·260 경로에 누락 집중). errorCodeUpdate 전수 호출 직접 grep으로 ground truth 확정(서브에이전트 라인 환각 배제). 설계 권장: 컨텍스트 플래그(A)+COMM 단일 카운터+정상 사이클 리셋+`#define` 임계(기본 10). 결정 5건은 은수님 검토 후 구현 진입(H-2.5). 빌드·실기는 은수님 환경 필수. |
| QCC 배터리 타임아웃 시 절전 진입 | power | sleep, qcc, battery, timeout, led, main | [power/20260609_qcc-batt-timeout-sleep](power/20260609_qcc-batt-timeout-sleep/) | 완료 | 2026-06-09 ~ 2026-06-09 | - | main.c fake_0x34 블록 QCC 0x34 수신 타임아웃 `2000→3000`ms + 타임아웃 시 fake 배터리 9% 세팅 제거 → 파란 파워오프 패턴(`LED_ST_POWER_OFF`) 재생 후 절전(`func_sleep`) 진입(방법 E). `df_Default` 게이트로 systemControl 자체 파워오프 미도달 → systemControl 직후 전용 트리거 블록 추가. main.c 단일 3곳 변경(타임아웃·플래그 2·전용 블록). 정적 검증 fan-out 2 PASS + 실기 검증 통과(은수님 "잘 동작한다"). `claude_develop` 커밋·푸시 완료 |
| ISD 연결/해제 LED 표시 제어 | LED | isd, arbiter, user-setting, verification | [LED/20260602_isd-conn-led-arbitration](LED/20260602_isd-conn-led-arbitration/) | 완료 | 2026-06-02 ~ 2026-06-02 | - | 내부기 연결 중=사용자 설정 LED 표시(1=켜기/2=끄기), 연결 해제 시=배터리 레벨 복귀. `s_isd_conn` 게이트(`compute_best_state`)로 연결 중에만 user_off 억제 → 해제 시 잔존 설정값 무시. 정적 분석 7/7 PASS(13에이전트 워크플로) + 실기 검증 통과(사용자 "잘 동작하네"). 적용: B-1 `main.c:524 ==1`(dead branch 제거)·B-2 `led_set_isd_conn_state` 분기 뒤 이동(disconnect 1-tick 백색 잔상 race 제거). 배터리 임계 테스트값(pct<40)은 전기기계적안정성 시험 중이라 유지(LED 검증은 배터리 40%↑). 커밋은 별개 변경(ble 0x36·passkey 우회) 분리 후 진행 예정. |
| LED 운용 방식 | LED | operation, scheme | [LED/20260421_operation-scheme](LED/20260421_operation-scheme/) | 완료 | ~2026-04-21 | - | 분석·구현·이력 완결 (이력 문서 존재) |
| LED Fade-Off 비차단 변환 | LED | non-blocking, isr, fade, refactor | [LED/20260427_non-blocking-fade-off](LED/20260427_non-blocking-fade-off/) | 완료 | 2026-04-27 ~ 2026-04-27 | ← [LED/20260427_bootloader-power-on-indicator](LED/20260427_bootloader-power-on-indicator/) §4 (발견 경위) | 폴링 fade-off (`turnOffLED` 30ms · `led_force_fade_off` 40ms) → ISR 상태머신 변환. 사용자 검증 통과 (30 ms 절약 확인). |
| 부트로더 단계 LED 조기 점등 | LED | bootloader, startup, indicator | [LED/20260427_bootloader-power-on-indicator](LED/20260427_bootloader-power-on-indicator/) | 완료 | 2026-04-27 ~ 2026-04-30 | ← [LED/20260427_non-blocking-fade-off](LED/20260427_non-blocking-fade-off/) (A·B 단계), ↔ [LED/20260423_power-on-early-lighting](LED/20260423_power-on-early-lighting/) (별개 진행) | 부트로더 단계 SKYBLUE fade 점등 — 첫 LED 인지 1670 ms → ~540 ms (~68% 단축). Timer2 SW PWM (40 kHz tick, PWM 100 bin / 400 Hz, fade 30+300+30+180=540 ms/cycle), G:B=30:40 (k_led_mix[SKYBLUE]). 빌드 가드 `TDC_BOOT_LED_ENABLE` (디폴트 1) + 런타임 UART 검증 핀 2 단 분기. Step 3 SLOWCLK_DIV32=40kHz 보정 후 정상 동작 확인. |
| POWER_ON LED 조기 점등 및 초기화 병렬화 | LED | init, startup, parallelization, fsm, isr | [LED/20260423_power-on-early-lighting](LED/20260423_power-on-early-lighting/) | 완료 | 2026-04-23 ~ 2026-05-06 | ← [LED/20260427_bootloader-power-on-indicator](LED/20260427_bootloader-power-on-indicator/) (별개 진행, 보완 관계) | CM3 단계 LED_ST_POWER_ON 조기 점등 (P3-Early = `ci_dio_configure_normal()` 직후, Initialize 단계 5 후) + Initialize 나머지 LED 버스트 병렬 진행. TIMER3 ISR 책임 분리 (LED 전담) + `g_tdc_timer_t3_tick` 신규 카운터 (LED·터치 공유) + tdc_touch FSM 명시화. POWER_OFF burst pending set/clear 책임 분리 (Fix B-LED-3 — `s_tdc_burst_pending`, `tdc_led_is_burst_pending()`). V1~V9 통과. V10 (ULP wakeup SPI race) = 본 작업 외부 (QCC 시퀀스 영역) 로 분리, 이후 필요 시 재개. 16 commit + 마무리 1, 머지 사용자 승인 대기. |
| 저전력 모드 전환 | power | sleep, low-power | [power/20260422_low-power-mode](power/20260422_low-power-mode/) | 완료 | ~2026-04-22 | - | ULP 모드 SYSCLK 30.72 MHz → 2.56 MHz, `ci_power_sleep()` 활성화. 코드 적용 확인 ([main.c:707](../../src/2__cm3/Cortex-M3-src/main.c)). |
| 터치 레이어 분리 | touch | layer, refactor | [touch/20260421_layer-separation](touch/20260421_layer-separation/) | 완료 | ~2026-04-21 | → [touch/20260421_init-split](touch/20260421_init-split/) (Step A) | 기능 레이어(`tdc_touch.c`) ↔ 드라이버 레이어(`tdc_drv_iqs323.c`) 분리. IC 교체 시 수정 범위 한정. 코드 적용 확인. |
| 터치 센서 초기화 분할 | touch | init, sequencing | [touch/20260421_init-split](touch/20260421_init-split/) | 완료 | ~2026-04-21 | → [touch/20260421_layer-separation](touch/20260421_layer-separation/) (Step B) | Auto-ATI 대기 1.5 s를 POWER_ON LED 버스트와 병렬 진행 (`tdc_touch_init_begin()` / `tdc_touch_process()` 분리). 코드 적용 확인. |
| Sound1 docs 폴더 구조 세분화 | meta | docs, convention, migration | [meta/20260424_docs-restructure](meta/20260424_docs-restructure/) | 완료 | 2026-04-24 ~ 2026-04-24 | → 루트 [meta/20260424_docs-restructure](../../../../docs/tasks/meta/20260424_docs-restructure/), → [meta/20260507_internal-links-fix](meta/20260507_internal-links-fix/) | flat `[프리픽스]` 22건 → 폴더 기반 + tasks 레지스트리. 22 파일 이동 + `tasks/작업 목록.md` 신설 + CLAUDE.md 갱신. 후속 tech debt = 깨진 markdown 링크 25+ 건 (internal-links-fix). |
| 내부 링크 경로 정정 | meta | docs, links, tech-debt | [meta/20260507_internal-links-fix](meta/20260507_internal-links-fix/) | 완료 | 2026-05-07 ~ 2026-05-07 | ← [meta/20260424_docs-restructure](meta/20260424_docs-restructure/) | docs-restructure 후 깨진 markdown 링크 25 건 정정. 검증 grep 0 건. src 코드 (`../src/...`) 상대경로 깊이 오류는 별도 후속 작업 (본 작업 범위 외). |
| docs 폴더별 README 추가 | meta | docs, readme, lazy-loading | [meta/20260507_folder-readmes](meta/20260507_folder-readmes/) | 완료 | 2026-05-07 ~ 2026-05-07 | ← 루트 [meta/docs-consistency-audit](../../../../docs/tasks/meta/docs-consistency-audit/) (후속 분기 1번) | `docs/지침/`·`사용방법/`·`참고/`·`tasks/` 4개에 README.md 인덱스 신규 추가 (루트 `문서 작성 규칙 §9.3` 충족). 인덱스 자료는 Explore 서브에이전트로 일괄 추출. `참고/LED/` 서브폴더 README는 후속 검토. |
| 영속 문서 frontmatter+TL;DR 일괄 소급 | meta | docs, frontmatter, retrofit, lazy-loading | [meta/20260507_frontmatter-retrofit](meta/20260507_frontmatter-retrofit/) | 완료 | 2026-05-07 ~ 2026-05-07 | ← [meta/20260507_folder-readmes](meta/20260507_folder-readmes/) (점검 빈틈 후속) | 사용자 명시 요청으로 일괄 소급. `docs/{지침·사용방법·참고}/` 영속 9 파일에 frontmatter 5필드 + `**TL;DR**:` 한 줄 in-place 추가. general-purpose 서브에이전트 1회 위임 + spot check 2 파일. 본문 무변경. |
| Sound1 CLAUDE.md 슬림화 + 루트 강제 강화 | meta | docs, claude-md, root-enforcement, slim | [meta/20260508_claude-md-slim](meta/20260508_claude-md-slim/) | 완료 | 2026-05-08 ~ 2026-05-08 | - | 외부 의존 3섹션(대외/외부의존/완성본) 제거, > [!IMPORTANT] Alert로 루트 절대 우선 강제 + 충돌 정책 명시(CLAUDE.md=루트 우선, docs/지침/=프로젝트 우선), 루트 지침 참조 표·docs 레이아웃 다이어그램 제거, 작업 목록.md 진입점만 §작업 라우팅으로 보존, frontmatter+TL;DR 추가, 절대 경로→상대 경로. 99줄/9섹션 → 60줄/6섹션 |
| service 모드 RTT 디버그 콘솔 | bootloader | rtt, debug-console, service, refactor, sound1-fw-extractor | [bootloader/20260507_service-rtt-debug-console](bootloader/20260507_service-rtt-debug-console/) | 완료 | 2026-05-07 ~ 2026-05-08 | ↔ [`sound1-fw-extractor`](../../../sound1-fw-extractor) (외부 도구 명세 협업) | 부트로더 UART 디버그 모드 4 함수 폐기 + storage init 시퀀스를 `tdc_boot_storage_init()` 헬퍼로 추출 (`tdc_boot_get_ohdl()` getter 포함) + service 모드에 RTT 디버그 콘솔 신설. 출력 형식은 외부 Python 도구 [`sound1-fw-extractor`](../../../../sound1-fw-extractor) 명세 준수 — `dump` 단일 명령으로 4 파일 (`MANIFEST.TXT` + `APP000~002.FEZ`) 일괄 hex 덤프 → fw.txt 캡처 → 자동 추출. RTT up 채널은 service 모드 한정 BLOCK_IF_FIFO_FULL 전환 (drop 0 보장). `reboot` = 500 ms RTT flush + `SYS_WATCHDOG_RESET()`. 사용자 extractor 추출 통과 (45 + 178336 + 9476 + 1280 B + 경고 0). |
| OTA DFU FW 이미지 변종별 LED 색상 정리 | LED | ota-dfu, fw-variant, build-config, cleanup | [LED/20260508_ota-dfu-fw-variant](LED/20260508_ota-dfu-fw-variant/) | 완료 | 2026-05-08 ~ 2026-05-08 | ↔ [touch/20260508_board-variant-iqs323-ati](touch/20260508_board-variant-iqs323-ati/) (동일 패턴 짝 작업, 같은 브랜치·사이클) | LedOutput.c 디스크립터 표 안의 `en__LED_PURPLE/*en__LED_WHITE*/` 코멘트-스왑 형태를 빌드 매크로 `TDC_FW_VARIANT` (APP / FACTORY_RESET) 분기로 정리. 디폴트 = APP, `#error` 로 잘못된 값 즉시 빌드 실패. LED_ST_IN_USE: APP=WHITE / FACT=PURPLE, LED_ST_POWER_ON: APP=SKYBLUE / FACT=WHITE. 사용자 빌드 + 실기 검증 통과 ("다 잘 빌드 된다. 동작도 정상이야"). LedOutput.c +38/-2 단일 commit. |
| IQS323 ATI 보상값 보드 변종 매크로 정리 | touch | iqs323, board-variant, build-config, cleanup | [touch/20260508_board-variant-iqs323-ati](touch/20260508_board-variant-iqs323-ati/) | 완료 | 2026-05-08 ~ 2026-05-08 | ↔ [LED/20260508_ota-dfu-fw-variant](LED/20260508_ota-dfu-fw-variant/) (동일 패턴 짝 작업, 같은 브랜치·사이클) | tdc_drv_iqs323.c L502~517 의 `#if 1 // Mini Board` / `#else // Develop Board` 형태를 빌드 매크로 `TDC_BOARD_VARIANT` (MINI / DEVELOP) 분기로 정리. 디폴트 = Mini (현 활성 변종 보존), `#error` 잘못된 값 빌드 실패. ATI 6 매크로 이름·사용처 무변경 → `write_ati_compensation()` 동작 동등. `Board_is_*` 와는 의도적 분리 (핀 매핑 vs 캘리브레이션, 의미 레이어 다름). 사용자 빌드 + 실기 터치 검증 통과. tdc_drv_iqs323.c +45/-7 단일 commit. |
| Sullivan1.5 rel.2 동기화 | sync | sullivan, baseline, sync, comparison | [sync/20260514_sullivan1.5-rel2](sync/20260514_sullivan1.5-rel2/) | 완료 (흡수 0건) | 2026-05-14 ~ 2026-05-14 | ↔ 우선 진행 약속 #2 → [signalProcessing/20260514_nofm-merge](signalProcessing/20260514_nofm-merge/) (Ble 영역 인벤토리 인계) | 베이스 `Sullivan1_5__CM3` ↔ Sound1 `2__cm3` 의 BleCommunication(13)·internalDevice(32)·signalProcessing(4) 합 **49 파일** 의미적 비교. 차이 9 파일(Ble 4 + iDev 5), 동등 40 파일. Sullivan 후속 패치 후보 4 묶음 (A. fatfs 마운트 2회 패치, B. DFU 디버그 로그 7 줄, C. FPGA I²C 실패 진단 로그 4 줄, D. ISD FIFO COUNT 실패 진단 로그 1 줄) + Sound1 잠재 의미 변경 1 건 (E. FPGA_version MAJOR mask `0x07`→`0x0F`) 식별. 사용자 판단 = "이 정도 차이는 의미 없다, 흡수 안 함" → 코드 변경 0 건, 임시 베이스 폴더 삭제, `src/.gitignore` 정리. NofM 작업 재참조용으로 산출물 5 종 영구 보존. |
| NofM 기능 병합 | signalProcessing | nofm, cfx, analysis, daniel, merge | [signalProcessing/20260514_nofm-merge](signalProcessing/20260514_nofm-merge/) | 완료 (분석 종결) | 2026-05-14 ~ 2026-05-14 | ← 우선 진행 약속 #1 (`ble/nofm-merge` placeholder → 모듈 재분류 commit `549d30b` → 본 분석 진입), ↔ [sync/20260514_sullivan1.5-rel2](sync/20260514_sullivan1.5-rel2/) (Ble 인벤토리 재참조 가능) | 다른 팀원(다니엘) 작성 CFX NofM 신호처리 기능을 사용자(다) 본인 직접 working tree 병합 (빌드 OK, 9 파일 unstaged). 본 task = 분석 문서화만 (구현은 본 task 외). 분석.md Rev.0 (8 절, 함수별 walk-through 심화) — NofM 본체 6 신규 함수 (`stimulationStrategy_NofM` 마스터 + `Init_Buffers` + `Peak_Pick` (packed score + bitmask sort) + `Interleaving` (half_ch stride) + `Packet` (loop peeling + 거리 기반 시작점) + `PCM_Write` (NOP pre-fill + Case A/B 1ms 한도)) 라인 단위 분석. 부수 최적화 (`nonlinearMapping.c` loop unswitching + branchless math, `FrequencyAnalysis.c` 포인터 후증가 + branchless max) + 통합 (`system_control.c:75` Phase 초기화 트리거, `main.c:428-435` CIS 호출 dispatch 이동) 분석. CM3 측 2 파일 (`ci_ble_control_boot.c` OTA 후 QCC 절전 딜레이 2200×1ms, `LedOutput.c` 팩토리 리셋 LED color default) 은 NofM 무관 — 사용자 명시 별도 task 분리 권고 (`ble/20260514_qcc-shutdown-delay`, `LED/factory-reset-default`). NofM 코드 staging/commit 은 사용자 본인 의도로 별도 진행 (본 task 외 보존). |
| OTA Factory Reset LED default 전환 | LED | ota, factory-reset, default, fw-variant | (단일 파일, 별도 폴더 없음) | 완료 | 2026-05-14 ~ 2026-05-14 | ← [LED/20260508_ota-dfu-fw-variant](LED/20260508_ota-dfu-fw-variant/) (default 후속), ← [signalProcessing/20260514_nofm-merge](signalProcessing/20260514_nofm-merge/) (분리 권고) | `LedOutput.c:208` `TDC_FW_VARIANT` default 를 `APP` → `FACTORY_RESET` 으로 전환. 사용자(다) 명시 "처음 펌웨어 다운로드 받은 상태가 팩토리 리셋 상태라는 것을 표시하기위한 LED 색상 버전이야. 앞으로는 이 설정 상태를 기본으로 할거야". 단일 라인 default 토글 — 별도 task 폴더 없이 단순 commit + 작업 목록.md 등재. NofM 분석에서 분리된 CM3 측 부수 변경 짝 작업 ([`ble/20260514_qcc-shutdown-delay`](ble/20260514_qcc-shutdown-delay/) 와 같은 사이클). |
| QCC OTA 후 셧다운 진입 딜레이 | ble | qcc, shutdown, delay, ota, dfu, reboot | [ble/20260514_qcc-shutdown-delay](ble/20260514_qcc-shutdown-delay/) | 완료 | 2026-05-14 ~ 2026-05-14 | ← [signalProcessing/20260514_nofm-merge](signalProcessing/20260514_nofm-merge/) (부수 발견·분리), ↔ [bootloader/20260507_service-rtt-debug-console](bootloader/20260507_service-rtt-debug-console/) (OTA DFU 인접) | `ci_ble_control_boot.c` `_fetch_packet_boot_select()` +15/-15. OTA DFU 후 Ezairo 재부팅 전 QCC 절전 진입 시퀀스 명시화 — 응답 송신 20 ms 대기 + `snd_qcc_set_mode(SND_QCC_MODE_SHUTDOWN)` 호출 + 2.2 초 대기 (워치독 리프레시 1 ms) + `SYS_WATCHDOG_RESET()` 재부팅. 사용자(다) 명시 의도 — "ble의 OTA DFU 기능으로 펌웨어 업데이트 후 Ezairo를 재부팅 하기 전에 QCC를 절전 모드로 진입 시킨 후 재부팅". 산출물 3 종 (요구사항 Rev.1 + 분석 Rev.0 + 이력 Rev.0). |
| UI 명령어로 프로그램(맵) 변경 | ui | command, rtt, map, program, guard | [ui/20260514_program-change-command](ui/20260514_program-change-command/) | 완료 | 2026-05-14 ~ 2026-05-14 | - | `tdc_ui_command.c` RTT 디버그 콘솔에 `--prog <1~4>` 명령어 신설. 입력 1~`MaxNumMap` → `changeProgramMapNum(N)` 호출. 가드 3 단 분리 메시지 — ISD 연결 (`snd_isd_interface_get_state().conneded_ISD`) + 자극 10V 정상 (`isd_controlState == en__isdStatus_stimul_10V_Ok`) + 매핑 앱 미연결 (= LiveStimulationStandAlone 운용). 설계 A 채택 — `mappingProgramConnected` 가 `mappingControl()` 내부 static 으로 외부 직접 접근 불가, `isMappingProgramConnected()` 헤더 선언만 있고 본문 없는 스텁 → 기존 `tdc_ui_command_override_*` 와 동일한 main → tdc setter 한 방향 의존 패턴 차용 (`tdc_ui_command_set_mapping_connected()` 신설, main.c `tdc_ui_command_poll()` 직전에 매 cycle 호출). 코드 변경 = `tdc_ui_command.{h,c}` +3/+60, `main.c` +1. 사용자(다) AC-1~8 실기 통과. 진행 중 절차 위반 1 건 (ExitPlanMode 승인 직후 정식 산출물 검토 게이트 없이 코드 commit 까지 진행 — 사용자 경고 후 둘 다 보존·재검토 결정). 신규 모듈 `ui` 정의 추가. |
| CFX·CM3 분석 3편 신 컨벤션 이전 | signalProcessing | cfx, cm3, migration, legacy, docs | [signalProcessing/20260514_cfx-cm3-analysis](signalProcessing/20260514_cfx-cm3-analysis/) | 완료 | 2026-05-14 ~ 2026-05-14 | ↔ [signalProcessing/20260514_nofm-merge](signalProcessing/20260514_nofm-merge/) (참조 인벤토리), ← `/recall-rules` 후속 사용자 회상 문의 | 2026-04-18 작성·미머지 `origin/claude_docs_cfx-cm3-analysis` 원격 브랜치의 분석 3편 (CFX 프로젝트 구조 Rev.0 797줄 + CFX-CM3 공유메모리 시퀀스·타이밍 Rev.1 743줄 + CFX 주파수밴드-전극-채널 매핑 Rev.2 753줄, 총 2293줄) → `tasks/signalProcessing/20260514_cfx-cm3-analysis/`로 이전. `git show origin/<브랜치>:<원본> > <신경로>` 방식 추출 (cherry-pick 회피 — 옛 Co-Auth 트레일러 + src/ 173 files 회귀 동시 차단). 옛 `[분석]`·`by 김은수` prefix/suffix 제거 + frontmatter 5필드 + TL;DR 80~250자 추가, 본문 1글자도 무변경. 본 task 산출물 5종 (요구사항·분석·계획·진행상황·이력) 동반, 즉시 archive. 원본 `origin/claude_docs_cfx-cm3-analysis` 브랜치 보존 (force-push 회피, Co-Auth 잔존 5 commit 정책과 일관). |
| NofM 튜닝 (프레임 3·백텔 강제·16ch 조건) | signalProcessing | nofm, frame-count, backtel, channel-config, tuning | [signalProcessing/20260514_nofm-tuning](signalProcessing/20260514_nofm-tuning/) | 완료 | 2026-05-14 ~ 2026-05-14 | ← [signalProcessing/20260514_nofm-merge](signalProcessing/20260514_nofm-merge/) (본체 통합), ↔ [signalProcessing/20260514_cfx-cm3-analysis](signalProcessing/20260514_cfx-cm3-analysis/) (Rev.1 1ms PCM 틱 동기 모델), ↔ [ui/20260514_program-change-command](ui/20260514_program-change-command/) (`--prog 3` NofM 진입) | NofM 본체 통합 (efbe067) 후 사용자(다) 직접 적용 5 변경 정리. 영구 3건 — ① `driver_PCM_liveStimulation.c` `frame_3_perChannel()` connectionCheck 직후 2 슬롯 NopStandby → NopBacktel (FPGA COLA TX 1 프레임 race 회피, 전력 +2 프레임 무시 가능), ② `stimulationStrategy_NofM_PCM_Write()` stride 정책 명시 (`#if 1` 공유메모리 `g_pcmFrameNum_per_channel` 활성 + `#else` backup `stride=3` 보존), ⑤ `calculationStimulParaN_cfxShare()` NofM 시 `numFrequencyBand<16` → `ci_printe` 차단 · ≥16 → `frameNumPerOneChannel=3`·`transferableChannelNum=8` + `ci_printw` 알림. 임시 2건 (NOTE 후속 삭제 명시) — ③ `stimulationStrategy_NofM()` Phase 0 `g_pcm_amplitude_level[0..15]` 결정론 벡터 (255~60), ④ `ci_map_init_map_data()` Map3 `stimulationStrategy=2` 강제. clang-format 부수 (`_XMEM*` → `_XMEM *` 등) 의미 변경과 통합. NofM frame 3 고정 경로 = CM3 `stimulationParaCal.c` → 공유메모리 → CFX `stimulationStrategy_NofM_PCM_Write()` 양방향 핸드셰이크. src 단일 commit `6216b21` (+188/-112) + docs commit 별도. |

---

## 상태 정의

| 상태 | 의미 |
|---|---|
| `진행` | 작업 진행 중 |
| `대기` | 선행 조건 충족 대기 |
| `차단` | 외부 요인으로 진행 불가 |
| `완료` | 병합·릴리즈 완료 |
| `중단` | 진행 중이던 작업을 후속 작업으로 대체하며 중단 (산출물은 후속으로 승계) |
| `취소` | 작업 취소 |

## 모듈 정의 (Sound1 도메인)

| 모듈 | 범위 |
|---|---|
| `LED` | LED 운용·밝기·dimming·패턴·표시 상태 |
| `touch` | 터치 센서·레이어·초기화·이벤트 처리 |
| `power` | 저전력 모드·전원 관리 |
| `bootloader` | 부트로더·service 모드·storage init·디버그 콘솔 |
| `ble` | BLE 통신 (`BleCommunication/`) — 페어링·서비스 등 |
| `signalProcessing` | CFX 신호처리 (`src/1__cfx/signalProcessing/`) — NofM, AGC, FrequencyAnalysis, nonlinearMapping, stimulationStrategy 등 |
| `sync` | 외부 baseline·release(예: Sullivan1.5 rel.2) 동기화 — 다중 영역에 걸치는 비교·맞추기 작업 분류용 |
| `meta` | 문서·워크플로우 등 프로젝트 메타 작업 |
| `ui` | RTT 디버그 콘솔(`tdc_ui_command`)·사용자 인터페이스 명령어 (`--led`/`--prog`/`--vol` 등) |
| (추가 예정) | `internalDevice`, `audio`, `calibration` 등 필요 시 |

## 갱신 이력

| 날짜 | 이벤트 |
|---|---|
| 2026-04-24 | 레지스트리 생성. 기존 Sound1 작업 5건 등재 (활성 2, 완료(추정) 3 + 완료 1). `meta/20260424_docs-restructure` (migration 작업) 활성으로 등재. |
| 2026-04-24 | 구조 정정: `기준/` → `지침/` 통합 (1건 이동). `참고/` 모듈 분할 — `칩/`·`LED/`·`터치/` 서브폴더 생성 (9건 이동). `참고/LED/` 내 3 파일 프리픽스 "LED 시스템 —" 제거 후 상호참조 링크 갱신. `참고/칩/Ezairo 클럭·타이머·I2C 스펙 정리.md` PDF 링크 연결. `참고/터치/터치센서 운용 방식.md`의 IQS323 링크 정정 (malformed 이스케이프 시퀀스 교체). Sound1 CLAUDE.md layout 섹션 갱신. |
| 2026-04-27 | 활성 작업 2건 등재: `LED/20260427_bootloader-power-on-indicator` (조사 단계, 진행상황 인계 commit `e1630e9`) · `LED/20260427_non-blocking-fade-off` (요구사항.md 작성, 사용자 승인 대기). |
| 2026-04-27 | `LED/20260427_non-blocking-fade-off` 활성 → 완료. 사용자 검증 통과 (30 ms 절약 확인, 회귀 없음). 잔존 이슈는 LED G 미스터리 (별도 작업). |
| 2026-04-27 | `LED/20260427_non-blocking-fade-off` 사후 fix (commit `85c250f`) — fade-off cancel 시 cross-fade Phase A 잔존 색 가동 anomaly 해소. 부수 발견: LED G 미스터리(B 작업) 도 동일 원인이라 함께 해소. 이력 및 결과.md §8 추가. |
| 2026-04-27 | `LED/20260427_bootloader-power-on-indicator` 진행상황.md 갱신 (B 자연 해소·C 진입 준비). 회고 신규 카테고리 [`코딩.md`](../../../../docs/회고/코딩.md) 생성 — "비차단/ISR 시스템에서 동기 폴링 회피". CLAUDE.md / 회고 README 카테고리 목록 갱신. 다음 세션 = 2026-04-30 (목) C 작업 진입. |
| 2026-04-30 | `LED/20260427_bootloader-power-on-indicator` C 작업 진입 — 작업 브랜치 `claude_feature_bootloader-power-on-indicator` 생성, `요구사항.md` Rev.0 작성 (사용자 승인 대기). |
| 2026-04-30 | `LED/20260427_bootloader-power-on-indicator` `요구사항.md` Rev.1 갱신 — 사용자 피드백 반영: §6 매핑 C4(SW PWM) 채택, AC-2 강화 (CM3 와 동일 mix/PWM/dimming), 인접 작업과 별개 진행 가능, 빌드 가드 매크로 `BOOTLOADER_LED_INDICATOR_ENABLE` 정식 채택. |
| 2026-04-30 | `LED/20260427_bootloader-power-on-indicator` `요구사항.md` Rev.2 갱신 — 사용자 피드백 2차 반영: ① **R-11/Q-PWM-0 (CM3 이미지 로드 중 부트로더 ISR 가용성)** 을 분석 1순위 / 본 작업 선결 조건으로 격상, ② SW PWM 종료 시점 = `bootloader_boot_cm3()` 호출 직전 ([`bootloader.c:1374`](LED/20260427_bootloader-power-on-indicator/) 부근, AC-7 신설), ③ 점등 시작 시점을 T-A(`main()` 직후, 클럭 7.68 MHz) / T-B(`Sys_Trims_SetOperatingFrequency()` 30.72 MHz 전환 후) 두 후보로 분기 — 클럭 변경까지 시간이 짧으면 T-B 채택 (Q-PWM-3, R-12). |
| 2026-04-30 | `LED/20260427_bootloader-power-on-indicator` `요구사항.md` Rev.3 갱신 — 사용자 피드백 3차 반영: ① **R-11 영향 시 본 작업 폐기** 분기 명시 (우회 방안 미검토) + 가치 이전 대상 = `LED/20260423_power-on-early-lighting` (CM3 부팅 직후 즉시 LED 점등 형태로 흡수). 분석 단계를 Phase 1(R-11 단독 선결) / Phase 2(나머지) 로 분리. ② AC-3 / AC-7 무점등 시간 제약 완화 — CM3 측이 OFF 에서 fade-in 으로 자연스럽게 시작하므로 부트로더 OFF → CM3 OFF → 정식 LED 사이의 무점등 구간은 끊김으로 보지 않음 (§1.4 보강). |
| 2026-04-30 | `LED/20260427_bootloader-power-on-indicator` `분석.md` Rev.0 — Phase 1 (R-11) **가능 ✅** 판정. 근거: ① 부트로더 PRAM5(0x60000) 와 CM3 적재 영역(PRAM0~6) 의 운용 분리 검증 (실 운용 사실), ② [main.c:39-42](LED/20260427_bootloader-power-on-indicator/) 에서 NVIC 전체 disable + pending clear 로 단일 상태, ③ 부트로더는 polling-only — 활성 ISR 핸들러 없음, ④ 부트로더에 타이머 사용 흔적 전무. 본 작업 진행 결정. Phase 2 진입 사용자 승인 대기. |
| 2026-04-30 | `LED/20260427_bootloader-power-on-indicator` `분석.md` Rev.1 — Phase 2 정밀 분석 추가. 결정 D-1~D-9: ① 타이머 = Timer3 외 (CM3 점유 회피, ci_timer.c:18), ② LED 엔진 = 자체 구현 (사용자 결정 — fade-in 30 + peak 300 + fade-out 30 + off 180 = 540 ms/cycle, peak 만 정식 LED_ST_POWER_ON 의 2 배), ③ 점등 시작 = **T-B** (`Sys_Trims_SetOperatingFrequency()` 직후, bootloader.c:643) — T-A 채택 시 클럭 변경으로 fade 시간 1/4 단축 회피, ④ 초기 디버그 LED 빌드 가드 분기, ⑤ `bootloader_boot_cm3()` 직전 540 ms wait, ⑥ UART RX 와 G LED (DIO15) 충돌 → 빌드 가드 분기, ⑦ active-low 가정 + 구현계획 단계 검증. 구현계획.md 진입 사용자 승인 대기. |
| 2026-04-30 | `LED/20260427_bootloader-power-on-indicator` `분석.md` Rev.2 — 사용자 2 차 피드백 반영. ① **D-1 = Timer2** (CM3 미점유 확인 — F-10), ② D-3 = T-B 확정, ③ **D-4 변경** = main.c 디버그 GPIO 블록을 빌드 가드로 무력화 → GPIO `DIO_MODE_DISABLE` (hi-Z) 유지 → 풀다운 저항으로 자연 OFF (사용자 명시 "물리적으로 LED 가 OFF 상태로 전원이 켜짐") → T-B 시 SW PWM fade-in 자연 점등, ④ **D-7 변경** = 빌드 가드 + 런타임 UART 검증 핀 (`Sys_GPIO_Read(DIO_NUM_UART_ENABLE) == DIO_ACTIVE_LEVEL_UART_ENABLE`, [main.c:59](LED/20260427_bootloader-power-on-indicator/)) 2 단 분기 — 가드 ON + UART 활성=LED 비활성, 가드 ON + UART 비활성=LED 활성, ⑤ **D-8 = active HIGH** 확정 (non-blocking-fade-off 이력 §1 의 "active LOW" 정보 갱신, `LED_IS_ACTIVELOW` 미정의 상태와 일관). 구현계획.md 진입 사용자 승인 대기. |
| 2026-04-30 | `LED/20260427_bootloader-power-on-indicator` `구현계획.md` Rev.0 작성. 신규 모듈 `boot_led` (헤더 + .c) — Timer2 10 us ISR, PWM 100 bin/1 kHz, phase 머신 IDLE/FADE_IN/PEAK/FADE_OUT/OFF/DONE, k_led_mix[en__LED_SKYBLUE] G:B=30:40 비율 차용. 통합 4 지점: ① main.h 매크로 디폴트 정의, ② main.c:16-24 `#if !BOOTLOADER_LED_INDICATOR_ENABLE` 가드, ③ bootloader.c:1496 직후 (T-B) → 검증 핀 read + init/start, ④ bootloader.c:1374 직전 → 540 ms wait + stop. 단계별 commit 7-step (가드 OFF 회귀 → SDK 함수 확정 → ISR PWM → phase tick → wait/stop → UART 분기 → 종합). 위험·롤백 명시 (Timer SDK fallback, reload 보정, 풀다운 보드별 차이). 사용자 승인 → 구현 진입 대기. |
| 2026-04-30 | `LED/20260427_bootloader-power-on-indicator` `구현계획.md` Rev.1 — 사용자 피드백 2 건 반영. ① **네이밍 지침** ([`docs/지침/코딩/네이밍 컨벤션.md`](../../지침/코딩/네이밍%20컨벤션.md)) 의 `tdc_` prefix 일관 적용 — 모듈 = `tdc_boot_led`, 파일 = `tdc_boot_led.h/.c`, 함수 = `tdc_boot_led_init/start/stop/is_done/elapsed_ms`, 매크로 = `TDC_BOOT_LED_*`, 빌드 가드 = `TDC_BOOT_LED_ENABLE` (Rev.0 의 `bli_*` / `BOOTLOADER_LED_INDICATOR_ENABLE` 폐기), 타입 = `tdc_boot_led_phase_t`, static 전역 = `s_tdc_boot_led_*`. ② **Cortex-M3 ISR IRQ Clear 제거** — NVIC 가 ISR entry/exit 시 active bit 자동 set/clear, peripheral pending bit 도 RSL10 Timer 는 hardware auto-clear 또는 read-to-clear 가정 (Step 2 측정에서 재확인, 필요 시 ack 추가). init 시점의 `NVIC_ClearPendingIRQ` 는 잔여 pending 보호 차원으로 유지. |
| 2026-04-30 | `LED/20260427_bootloader-power-on-indicator` 구현 단계 진입 — **Step 1 commit `4099af7`**: `TDC_BOOT_LED_ENABLE` 가드 도입 + `tdc_boot_led` 빈 골격 (가드 OFF 회귀 검증용). **Step 2 commit `75fa380`**: `tdc_boot_led.c` 본체 구현 — `TIMER_2_IRQHandler` (10 us, 100 kHz), PWM 100 bin/1 kHz, phase 머신 5 단계 (FADE_IN 30 → PEAK 300 → FADE_OUT 30 → OFF 180 → DONE = 540 ms), G/B duty 30:40, Timer2 reload 307 (측정 후 보정), SDK API 는 `ci_timer.c` 패턴 (`Sys_Timer_Config` + `TIMER_PRESCALE_1` + `TIMER_FREE_RUN`). 사용자 Eclipse 빌드 + 오실로 측정 대기 — 회귀 (가드 OFF) 와 동작 (가드 ON × UART 검증 핀 두 케이스). 진행상황.md §6 측정 절차 안내. |
| 2026-04-30 | `LED/20260427_bootloader-power-on-indicator` 사용자 1 차 측정 → 오동작. 사용자 진단: "타이머 클럭에 문제. CM3 시스템 클럭 설정 후 클럭 div 설정을 부트로더에서도 동일하게". 발견 — RSL10 General Timer = **SLOWCLK / 32 = 40 kHz** ([`ci_timer.h:56`](LED/20260427_bootloader-power-on-indicator/) `T = 2^prescale × (tick+1) / 40 kHz`). Step 2 의 reload=307 (30.72 MHz 가정) 잘못. 또한 부트로더는 [`ci_power.c:154-156`](LED/20260427_bootloader-power-on-indicator/) 의 D_CLK 분주 설정 자체가 없어 SLOWCLK 가 디폴트 — 이중 문제. **Step 3 commit `27f2c28`**: ① `tdc_boot_led_init()` 안에 D_CLK->CFG_1/CFG_2 설정 추가 (CM3 와 동일, `SLOWCLK_PRESCALE_24 → SLOWCLK = 1.28 MHz → Timer = 40 kHz`) + `tdc_delay_ms(5)` 안정화 대기. ② PWM 매개변수 재계산: ISR 25 us (Timer 분해능 한계, reload=0), PWM 분해능 100 → 주기 2.5 ms (400 Hz), 1 ms 단위 phase tick 은 ISR 40 회 마다 별도 카운터 (s_tdc_boot_led_isr_in_ms). duty G:B=30:40 그대로. 사용자 재측정 대기. |
| 2026-04-30 | `LED/20260427_bootloader-power-on-indicator` 활성 → **완료**. 사용자 재측정 정상 동작 확인. **Step 4 commit `9bc5163`** = 정식 채택 정리 (헤더 위치 `include/led/`, `.cproject` include path, 가드 디폴트 0→1). 이력 및 결과.md 작성. 머지 (`claude_feature_bootloader-power-on-indicator` → `claude_develop`) 사용자 승인 대기. |
| 2026-05-06 | `LED/20260423_power-on-early-lighting` 활성 → **완료**. LED·터치 V1~V9 통과 + POWER_OFF (Fix B-LED-3) 검증 OK. V10 (ULP wakeup SPI race) 은 회로 단서 (DIO24 풀-다운 10K) + QCC 담당자 피드백 cross-check 결과 QCC 펌웨어 시퀀스 영역으로 본 작업 외부 분리, 별도 task 신설 없이 종결 (이후 필요 시 진행상황·이력 §V10 시발점으로 재개). 이력 및 결과.md 작성. 머지 (`claude_feature_early-power-on-led` → `claude_develop`) 사용자 승인 대기. |
| 2026-05-07 | 일괄 정리 — ① 완료 _(추정)_ 3건 (`power/20260422_low-power-mode`, `touch/20260421_layer-separation`, `touch/20260421_init-split`) 코드베이스 검증 후 정식 완료 확정 + 각 폴더 `이력 및 결과.md` 작성. ② `meta/20260424_docs-restructure` 활성 → 완료 (실작업은 2026-04-24 commit `129212e` 머지로 종결, 진행상황.md 만 stale 상태였음). ③ `meta/20260507_internal-links-fix` 작업 완료 — 깨진 markdown 링크 25건 정정 (작업 대상 8 파일 + 추가 발견 2 파일). src 코드 상대경로 깊이 오류는 별도 후속. ④ 미사용 feature 브랜치 3개 삭제 (`led-spec-and-sleep-guard`, `non-blocking-fade-off` 머지 완료, `touch-log-level` 폐기). |
| 2026-05-07 | `meta/20260507_folder-readmes` 등재·완료 (루트 `meta/docs-consistency-audit`의 후속 분기 1번). `docs/지침/·사용방법/·참고/·tasks/` 4개 README.md 신규 추가 (루트 §9.3 충족). Sound1 archive 컨벤션상 폴더 이동 없이 위치(`_archive/meta/20260507_folder-readmes/`) 유지·list.md 완료 등재. |
| 2026-05-07 | `meta/20260507_frontmatter-retrofit` 등재·완료 (folder-readmes 점검 빈틈 후속). 사용자 (다) 명시 요청으로 일괄 소급. `docs/{지침·사용방법·참고}/` 영속 9 파일에 frontmatter+TL;DR 일괄 추가, general-purpose 서브에이전트 1회 위임 + spot check 통과. 본문 무변경. CLAUDE.md·tasks/_archive 산출물은 본 task 외(권고로 이력에 명시). |
| 2026-05-07 | task 산출물 frontmatter 일괄 소급 — 사용자 명시 트리거 "전부다 빼지말고 다 적용"로 frontmatter-retrofit 후속 영향에서 권고했던 항목 처리. `tasks/{LED·touch·power·meta}/<>/` 활성 위치 산출물 34 파일 점검 후 신규 23 / TL;DR만 11 / skip 12. general-purpose 서브에이전트 1회 위임. 본문 무변경, 신 §9.2 정책(80~250자) 준수. |
| 2026-05-07 | `bootloader/20260507_service-rtt-debug-console` 활성 등재. 부트로더 UART 디버그 모드(`tdc_boot_debug_mode` 등 4개 함수) 폐기 + service 모드에 RTT 입력 디버그 콘솔(`manifest`/`app0~2`/`reboot`) 신설 + `bootloader_boot()` storage init 시퀀스를 `tdc_boot_storage_init()` 헬퍼로 추출. `reboot`는 워치독 리셋으로 재부팅. 신규 모듈 `bootloader` 추가. 산출물 3종 Rev.0 작성 후 사용자 피드백 반영(명령어 이름 + reboot 동작) Rev.1 갱신, 사용자 재검토 대기. |
| 2026-05-07 | `bootloader/20260507_service-rtt-debug-console` 산출물 Rev.2 갱신 — 사용자 피드백 2차 반영. ① `reboot` 처리 = `SYS_WATCHDOG_RESET()` 직접 호출 + 500 ms 사전 delay (RTT 출력 버퍼 flush 보장 — 출력 중이던 hex 덤프 메시지 손실 방지). ② line_terminator `\n` 확정 → 위험_2 해소. CM3 코드 4곳에서 `SYS_WATCHDOG_RESET()` 사용 사례 확인 (강제 리셋 패턴), 부트로더 빌드 가용성은 Step 3에서 검증. 사용자 재검토 대기. |
| 2026-05-07 | `bootloader/20260507_service-rtt-debug-console` 산출물 Rev.3 갱신 — 사용자 피드백 3차 반영. 헬퍼 함수 책임 축소: `snd_boot_set_fp` / `snd_boot_handle_file` / `tdc_boot_print_boot_file` 은 OTA DFU 이미지 슬롯 선택 영역이라 `tdc_boot_storage_init()` 외부에서 호출 (`bootloader_boot()` 은 `bootloader_load_app_file()` 직전, `service.c` 는 storage init 직후). ohdl 접근은 신규 `tdc_boot_get_ohdl()` getter 경유. 헬퍼 내부 변수(`buf[128]`/`options`/`boot_info` cast) 모두 로컬, `out_boot_info` 만 외부 인터페이스. `ci_boot.h` 헤더 의존 정리 (`bootloader_internal.h`/`ff.h` 추가 검토). 사용자 재검토 대기. |
| 2026-05-07 | `bootloader/20260507_service-rtt-debug-console` 산출물 Rev.4 갱신 — 사용자 피드백 4차 반영. service 는 OTA 슬롯 처리 자체 불필요라 `snd_boot_set_fp`/`snd_boot_handle_file`/`tdc_boot_print_boot_file` 호출 안 함 (Rev.3에서 service 도 호출하던 디자인 폐기). `tdc_boot_get_ohdl()` getter 는 service 의 `_print_fw_file` 이 부트로더 정적 변수 `ohdl` 을 그대로 재사용용으로만 사용 — 별도 `s_tdc_service_fp` 변수 폐기 (사이즈 절약). 부트로더는 같은 파일 내 정적 변수라 `snd_boot_set_fp(&ohdl)` 직접 사용 (getter 불필요). 사용자 재검토 대기. |
| 2026-05-08 | `meta/20260508_claude-md-slim` 등재·완료 (claude_develop 머지 `4fc3700`). Sound1 CLAUDE.md 슬림화 + 루트 절대 우선 강제 강화 — 외부 의존 3섹션 제거, > [!IMPORTANT] Alert로 충돌 정책 명시, 중복 섹션 제거, frontmatter 추가, 절대 경로 통일. 99줄/9섹션 → 60줄/6섹션. 루트의 4단계 프로세스 첫 Sound1 적용 사례. `_archive/meta/20260508_claude-md-slim/`로 이동, `이력 및 결과.md` 작성. |
| 2026-05-08 | `bootloader/20260507_service-rtt-debug-console` 활성 → **완료**. Step 1~3 구현 commit (`bf1d269` storage init 헬퍼 추출, `4984f49` UART 디버그 폐기, `89c6969` service RTT 콘솔). 압축 후 진행 = Step 4 fix commit (`9d8b42f` doxygen `*/` 빌드 에러 + ci_boot.h transitive 의존 분리, 회고 [`코딩.md`](../../../../docs/회고/코딩.md) "doxygen 주석 안 */ 시퀀스 금지" 추가) + Step 5 commit (`0f3b174` 출력 형식 sound1-fw-extractor 명세 준수 + RTT BLOCK 모드, 명령 5→2 통합). 사용자 검증 — BLOCK 모드 동작 OK + extractor 4 파일 추출 정상 (`MANIFEST.TXT` 45 B + `APP000~002.FEZ` 178336/9476/1280 B + 경고 0) + 모두 OK. 이력 및 결과.md 작성. 머지 (`claude_feature_service-rtt-debug-console` → `claude_develop`) 진행. |
| 2026-05-08 | `LED/20260508_ota-dfu-fw-variant` 활성 등재. OTA DFU 테스트용 LedOutput.c 코멘트-스왑 정리 — `LED_ST_IN_USE` (App=WHITE / FactRst=PURPLE), `LED_ST_POWER_ON` (App=SKYBLUE / FactRst=WHITE) 빌드 매크로 `TDC_FW_VARIANT` 분기로 통합. 디폴트 App. 4 단계 산출물 작성 + 단일 코드 commit. 사용자 빌드 검증 대기. 부수 발견: `ci_ble_control_boot.c` 의 QCC SHUTDOWN 시퀀스 변경은 별도 작업으로 분리 (working tree 그대로 유지). |
| 2026-05-08 | `touch/20260508_board-variant-iqs323-ati` 활성 등재. LED 작업과 동일 패턴 — `tdc_drv_iqs323.c` L502~517 의 `#if 1 // Mini Board` / `#else // Develop Board` 를 `TDC_BOARD_VARIANT` (MINI / DEVELOP) 매크로 분기로 정리. 디폴트 Mini (현 활성과 동등). ATI 6 매크로 이름·사용처 무변경. `Board_is_*` 와는 의도적 분리. 4 단계 산출물 + 단일 코드 commit, LED 작업과 동일 브랜치 (`claude_feature_ota-dfu-fw-variant-led`) 에서 한 번의 빌드/실기 사이클로 함께 검증 예정. |
| 2026-05-08 | `LED/20260508_ota-dfu-fw-variant` + `touch/20260508_board-variant-iqs323-ati` 활성 → **완료** 동시 처리. 사용자 빌드 + 실기 검증 통과 ("다 잘 빌드 된다. 동작도 정상이야"). 두 작업 각각 이력 및 결과.md 작성 + 작업 목록.md 활성→완료 이동. 머지 (`claude_feature_ota-dfu-fw-variant-led` → `claude_develop`, --no-ff) 진행 — LED commit `356a1e5` + 보드 commit `103ff17` + 정리 docs commit 함께. ci_ble_control_boot.c (QCC SHUTDOWN 시퀀스) 와 LedOutput.c default 토글(사용자 OTA 테스트 진행분) 은 별도 흐름으로 working tree 보존. |
| 2026-05-08 | **우선 진행 약속** 신설 — 사용자(다) 연차 종료 후 복귀(2026-05-14, 목) 첫 작업으로 두 건 등재. ① `ble/nofm-merge` (NofM 기능 병합), ② `sync/20260514_sullivan1.5-rel2` (Sullivan1.5 rel.2 baseline 비교·맞추기 — `BleCommunication`/`internalDevice`/`signalProcessing` 3 폴더). 두 작업 모두 placeholder 요구사항.md (Rev.0) 만 작성, 자세한 내용은 사용자 복귀 시 설명 예정. 신규 모듈 `ble`·`sync` 정의 추가. **본 작업 목록.md 안내 라인에 "우선 진행 약속이 있으면 새 작업 지시 시 그것부터 안내" 조항 신설**. |
| 2026-05-14 | `sync/20260514_sullivan1.5-rel2` 활성 등재 — 사용자(다) 연차 복귀 당일 직접 지시로 진입. 베이스 = `src/Sullivan1_5__CM3/` (임시, `src/.gitignore` 등록). 요구사항 Rev.0 → Rev.1 갱신, `첨부_분석_BleCommunication.md` Rev.0 작성 — 13 파일 중 4 파일 차이(`ble_commonProtocol.h`/`ble_communication.c`/`ci_ble_control_boot.c`/`ci_ble_control_ota.c`), 9 파일 의미적 동등. Sullivan 측 후속 패치 후보 2 건 식별: ① `snd_fatfs_remount_twice()` 8 곳 (마운트 2회 패치), ② DFU 디버그 로그 7 줄. Sound1 측 추가는 QCC 0x33~0x35 시스템 정보 패킷·`snd_boot_*` 타입 마이그레이션·QCC SHUTDOWN 시퀀스·RTT 로그 전환. 다음 단계 = `internalDevice/` / `signalProcessing/` (사용자 트리거 대기). |
| 2026-05-14 | `sync/20260514_sullivan1.5-rel2` `첨부_분석_internalDevice.md` + `첨부_분석_signalProcessing.md` + `요약.md` Rev.0 작성. internalDevice 32 파일 중 5 파일 차이 — Sullivan 후속 패치 후보 = I²C 실패 진단 로그 5 줄 (`isd_interface_init_FPGA.c` 4 + `isd_interface_init_ISD.c` 1). 잠재 의미 변경 1 건 = FPGA_version MAJOR 비트 mask `0x07` → `0x0F`. Sound1 측 정리는 QCC ISD 통보·getter 신설·`s_` prefix·로그 매크로(`ci_link_print*`/`ci_ifc_init_fpga_print*`) 전체 폐기·로그 메시지/레벨 조정. signalProcessing 4 파일 **모두 의미적 동등** (변경 없음). 3 영역 종합 = Sullivan 후속 패치 후보 **총 4 묶음** (A~D) + Sound1 잠재 의미 변경 1 건 (E). 본 task 활성 → "사용자 판단 대기" 상태 전환. |
| 2026-05-14 | `sync/20260514_sullivan1.5-rel2` 활성 → **완료** (흡수 0 건). 사용자(다) 판단 = "내 판단에 딱히 할 게 없다. 이 정도 차이는 의미 없다. 비교는 더 안 해도 될 것 같다. 마무리 하자. 나중에 NofM 작업할 때 다시 얘기할게." 후보 A~E 전부 기각, 코드 변경 0 건. `이력 및 결과.md` Rev.0 작성, `src/Sullivan1_5__CM3/` 임시 폴더 삭제, `src/.gitignore` Sullivan 항목 제거. 우선 진행 약속 #2 이행 완료. 산출물 5 종(요구사항/분석 ×3/요약/이력) 영구 보존 — NofM 작업([`signalProcessing/20260514_nofm-merge`](signalProcessing/20260514_nofm-merge/)) 진입 시 Ble 영역 Sound1 후속 작업 인벤토리(분석 BleCommunication §4) 재참조 가능. |
| 2026-05-14 | `ble/nofm-merge` placeholder 모듈 재분류 — `ble` → `signalProcessing`. 사용자(다) 지적 ("nofm-merge는 ble관련 작업이 아니고 CFX의 신호처리 관련 작업이야") 으로 NofM 병합 본질이 CFX 신호처리 영역임이 확정. 폴더 `git mv` (`docs/tasks/ble/nofm-merge` → `docs/tasks/signalProcessing/20260514_nofm-merge`) + 요구사항.md 6곳 + 갱신 이력 1행. 작업 목록.md = 우선 진행 약속 행 모듈/링크 + Sullivan 완료 행 cross-link + Sullivan 종결 갱신 이력 cross-link + 모듈 정의 `signalProcessing` 정식 등재 + `ble` 정의에서 NofM 토큰 제거 + `(추가 예정)` 토큰 정리. `_archive/sync/20260514_sullivan1.5-rel2/이력 및 결과.md` NofM 인계 cross-link 경로. `ble` 모듈 정의는 보존 (Sullivan 산출 BleCommunication 인벤토리 잔존, 향후 재진입 가능성). |
| 2026-05-14 | `signalProcessing/20260514_nofm-merge` 활성 → **완료** (분석 종결). 다른 팀원 다니엘 작성 CFX NofM 기능을 사용자(다) 본인 직접 working tree 병합 (빌드 OK, 9 파일 unstaged) → 본 task 분석 문서화 진입·종결. 산출물 = 요구사항.md Rev.1 + 분석.md Rev.0 (8 절, 함수별 walk-through 심화, `stimulationStrategy.c` 의 NofM 본체 6 신규 함수 라인 단위) + 이력 및 결과.md Rev.0. NofM 본체 알고리즘 = packed score `(amp<<5)|(31-idx)` + bitmask sort (Peak_Pick), `half_ch` stride (Interleaving), 거리 기반 시작점 + loop peeling (Packet), NOP pre-fill + Case A/B 1ms 한도 (PCM_Write). 원본 (`nOFm`) 대비 ① 매크로/심볼명 정규화, ② Peak selection brute-force → packed score + bitmask 단일 변수 (RAM 32× 절약), ③ Interleaving 16 줄 명시 → stride 일반화, ④ Packet modulo wrap → loop peeling, ⑤ Phase0/Phase1 별도 함수 → 통합 `PCM_Write`. 부수 최적화 (nonlinearMapping/FrequencyAnalysis) 와 통합 (system_control/main) 포함. CM3 측 2 파일 (`ci_ble_control_boot.c` OTA 후 QCC 절전 딜레이, `LedOutput.c` 팩토리 리셋 LED default) 은 NofM 무관 — 별도 task 등재 권고 (`ble/20260514_qcc-shutdown-delay`, `LED/factory-reset-default`). NofM 코드 9 파일 staging/commit 은 사용자 본인 의도로 별도 진행 (본 task 외 보존). 우선 진행 약속 #1 이행 완료 — 활성 우선 약속 없음. |
| 2026-05-14 | `ble/20260514_qcc-shutdown-delay` 활성 등재 — `signalProcessing/20260514_nofm-merge` 분석에서 분리 권고된 CM3 측 부수 변경 중 하나. 사용자(다) 명시 별도 task 분리 — "CM3 측 2 파일은 별도로 ... 진행하자". placeholder Rev.0 요구사항.md 작성 (사용자 설명 대기). `ci_ble_control_boot.c` +15/-15 (`snd_qcc_set_mode(SND_QCC_MODE_SHUTDOWN)` + 딜레이 2.2 초). 짝 작업 = `LedOutput.c` default 토글 (단순 commit, 별도 task 폴더 없음). |
| 2026-05-14 | `LedOutput.c` `TDC_FW_VARIANT` default 전환 — `APP` → `FACTORY_RESET`. 사용자(다) 명시 "처음 펌웨어 다운로드 받은 상태가 팩토리 리셋 상태라는 것을 표시하기위한 LED 색상 버전이야. 앞으로는 이 설정 상태를 기본으로 할거야". NofM 분석에서 분리된 CM3 측 부수 변경 짝 작업 (`ble/20260514_qcc-shutdown-delay` 와 같은 사이클). 단일 라인 변경이라 별도 task 폴더 없이 작업 목록.md §완료 행만 등재 + LedOutput.c 코드 + 작업 목록.md 1 commit. |
| 2026-05-14 | **(루트 지침 시정 적용)** 사용자(다) 명시 지적 — 본 세션 commit 들이 Git Identity §AI 표시 절대 금지 위반 (`Co-Authored-By: Claude Opus 4.7` 트레일러 포함) + claude_feature_* 분기 누락 + claude_main 머지 누락. 시정 사항: ① 새 commit 부터 Co-Authored-By 제거, ② 새 작업부터 `claude_feature_*` 분기 + `--no-ff` 머지, ③ claude_develop → claude_main 안정 머지 + push 양쪽. 본 세션 코드 작업 ([`signalProcessing/20260514_nofm-merge`](signalProcessing/20260514_nofm-merge/) NofM 7 파일, [`ble/20260514_qcc-shutdown-delay`](ble/20260514_qcc-shutdown-delay/) ci_ble_control_boot.c) 마무리. 이미 push 된 5 commit (21d69d8/549d30b/6f3ed98/176c36f/7c1b08b) 의 트레일러는 force-push 위험으로 그대로 보존 — 사용자 사후 판단 대상. |
| 2026-05-14 | `claude_feature_nofm-merge` 분기 → NofM 코드 7 파일 commit (`efbe067`, Co-Authored-By 없음) → `claude_develop` --no-ff 머지 (`79557e6`) → `claude_main` --no-ff 머지 (`b86ac85`) → push origin claude_develop + claude_main. NofM 마무리 완료 (커밋·머지·푸쉬·원격). |
| 2026-05-14 | `ble/20260514_qcc-shutdown-delay` 활성 → **완료**. 사용자(다) 명시 설명 — "ble의 OTA DFU 기능으로 펌웨어 업데이트 후 Ezairo를 재부팅 하기 전에 QCC를 절전 모드로 진입 시킨 후 재부팅" 반영. 요구사항.md Rev.0 → Rev.1 + 분석.md Rev.0 (변경 hunk + 시퀀스 다이어그램 Mermaid) + 이력 및 결과.md Rev.0. 코드 변경 = `ci_ble_control_boot.c` `_fetch_packet_boot_select()` +15/-15 (응답 20 ms → QCC SHUTDOWN → 2.2 초 → 재부팅). `claude_feature_ble-qcc-shutdown-delay` 브랜치 분기 → 코드 + docs commit → `claude_develop` --no-ff 머지 → `claude_main` --no-ff 머지 → push origin claude_develop + claude_main. |
| 2026-05-14 | `ui/20260514_program-change-command` 신설·완료. 신규 모듈 `ui` 정의 추가 (RTT 디버그 콘솔·UI 명령어 도메인). `tdc_ui_command.c` 에 `--prog <1~4>` 명령어 추가 — 입력 1~`MaxNumMap` 에서 `changeProgramMapNum(N)` 호출, ISD 연결 + 자극 10V 정상 + 매핑 앱 미연결 (LiveStimulationStandAlone) 3 단 가드. 설계 A 채택 — main → tdc setter 한 방향 의존, 기존 `tdc_ui_command_override_*` 패턴 일관. 코드 변경 = `tdc_ui_command.{h,c}` +3/+60 + `main.c` +1. 산출물 5 종 (요구사항/분석/계획/진행상황/이력). 사용자 AC-1~8 실기 통과. **절차 위반 1 건 발생** — ExitPlanMode 승인 직후 정식 산출물 검토 게이트 없이 코드 commit 까지 진행하여 사용자(다) 경고 → "둘 다 보존, 산출물 먼저 검토" 결정 후 재검토 통과 → 이력 및 결과.md §절차 회고 기록. `claude_feature_ui-prog-command` 브랜치 분기 → 코드 + docs commit → 머지·푸시 사용자 승인 대기. |
| 2026-05-14 | **(/recall-rules 후속 일괄 정리)** 사용자(다) trigger 4건 (A. tasks 폴더 정리, B. 지침 점검, C. Co-Authored-By 잔존 유지, D. 작업 마무리) 통합 처리. ① **A. tasks 폴더 정리** — 활성 폴더 17건 (LED×5 / ble×1 / bootloader×1 / meta×4 / power×1 / signalProcessing×1 / sync×1 / touch×3) 일괄 `git mv` → `_archive/<모듈>/<작업>/`. 작업 목록.md §완료 17행 폴더 컬럼 + 본문 cross-link sed 17 패턴 일괄 치환 (이중 `_archive` 회귀 0, 잔존 모듈 접두 link 0). ② **B. 지침 점검** — `claude_main` 워킹트리 직접 변경 2건 식별 (`src/1__cfx/signalProcessing/stimulationStrategy.c` +132/-84, `src/2__cm3/Gen1_5/FS/ci_map.c` +10/-2). `/* NOTE: NofM 테스트를 위한 코드. 테스트 후 삭제할 것. */` 마킹된 사용자(다) 본인 NofM 테스트 코드(`g_pcm_amplitude_level[0..15] = 250..100` 강제값 + Map3 `stimulationStrategy=2` 강제)로 분류 — 본 task 외 보존 (Claude 손대지 않음, [`signalProcessing/20260514_nofm-merge`](signalProcessing/20260514_nofm-merge/) 갱신 이력의 "NofM staging/commit 사용자 본인 의도" 정책과 일관). ③ **C. Co-Authored-By 잔존 5 commit** (`21d69d8`/`549d30b`/`6f3ed98`/`176c36f`/`7c1b08b`) 그대로 유지 확정 — 이미 push 완료, force-push 위험 회피, 직전 동일일자 갱신 이력(루트 지침 시정 적용)의 보존 결정과 일관. ④ **D. 마무리** — 본 정리 작업 자체는 별도 task 폴더 신설 없이 본 갱신 이력 행으로 종결 (정형 archive + 사용자 trigger 명시 일괄 위임, 작업 진행 규칙 §8 단순 작업 면제 적용). 브랜치 `claude_feature_recall-rules-cleanup` → `claude_develop` --no-ff → `claude_main` --no-ff → push origin claude_develop + claude_main 진행 (사용자 승인 후). |
| 2026-05-14 | `signalProcessing/20260514_cfx-cm3-analysis` 신설·완료. 사용자(다) 회상 문의 "전에 CM3·CFX 초기화 분석 했던 거 있지 않아? 다른 PC였는지 기억 안 난다" → `git branch -a` 점검으로 `origin/claude_docs_cfx-cm3-analysis` 원격 브랜치 (2026-04-18 작성·약 한 달 묵힘·main 미머지) 발견. 분석 3편 (797+743+753=2293줄) 을 `git show 'origin/<브랜치>:<원본>' > <신경로>` 방식으로 추출 (cherry-pick 회피로 옛 `Co-Authored-By: Claude Opus 4.7` 트레일러 + src/ 173 files 옛 코드 회귀 동시 차단) + 옛 `[분석]`·`by 김은수` prefix/suffix 제거 + frontmatter 5필드 (`type: tasks/분석`) + TL;DR 80~250자 추가 (본문 1글자도 무변경). 본 task 산출물 5종 (요구사항·분석·계획·진행상황·이력) 동반, 즉시 `_archive/signalProcessing/20260514_cfx-cm3-analysis/`로 이동. `claude_feature_cfx-cm3-analysis-migration` 분기 → `claude_develop` --no-ff → `claude_main` --no-ff → push origin 양쪽 + feature 브랜치 삭제 (사용자 승인 후). 원본 `origin/claude_docs_cfx-cm3-analysis` 브랜치는 보존 (force-push 회피, Co-Auth 잔존 5 commit 정책과 일관). 회상 비용 0 보장. |
| 2026-06-15 | `LED/20260615_cradle-lid-detection` 신설. `EN__SND_BT_CMD_SYSTEM_INFO_POWER` 처리 수정 + 크래들 뚜껑 열림/닫힘 감지 후 LED 활성화 판단 기능 추가 작업 착수. 요구사항.md 빈 템플릿 생성(사용자 작성 대기). |
| 2026-05-14 | `signalProcessing/20260514_nofm-tuning` 신설·완료. 사용자(다) NofM 본체 통합 (efbe067) 이후 실 동작 검증 중 직접 적용한 5 변경 (CFX 2 + CM3 2 파일, 워킹트리 잔존) 정리·문서화·git 사이클 마무리. 영구 3건 — ① CFX `driver_PCM_liveStimulation.c` `frame_3_perChannel()` connectionCheck 직후 NopStandby 2 슬롯 → NopBacktel (펄스 폭 따라 FPGA COLA TX 1 프레임 종료 race 시 Backtel 수신 실패 회피, 전력 +2 프레임 분 사용자 판단 무시 가능), ② CFX `stimulationStrategy.c` `stimulationStrategy_NofM_PCM_Write()` stride 정책 — `#if 1` 활성 `stride = g_pcmFrameNum_per_channel` (공유메모리 사용) + `#else` backup `stride=3` 보존, ⑤ CM3 `stimulationParaCal.c` `calculationStimulParaN_cfxShare()` NofM 시 `numFrequencyBand < 16` → `ci_printe "NOT AVAILABLE"` 차단 (자동 fallback 없음, 사용자 맵 재구성 유도) · ≥ 16 → `frameNumPerOneChannel = 3` + `transferableChannelNum = 8` 강제 + `ci_printw` 알림. 임시 2건 (NOTE `테스트 후 삭제할 것` 코멘트, 후속 삭제 task 사용자 검증 후 등재) — ③ CFX `stimulationStrategy_NofM()` Phase 0 진입 직후 `g_pcm_amplitude_level[0..15]` 결정론 벡터 강제 (Phase 0 255~140 / Phase 1 130~60), ④ CM3 `ci_map_init_map_data()` Map3 (`CI_MAP_NUM_3_INDEX`) `stimulationStrategy=2` (nOFm) 강제 (`--prog 3` RTT 진입 트리거). 부수 = clang-format 적용 (`_XMEM*` → `_XMEM *` 포인터 공백, brace 스타일 다수) 의미 변경 hunk 와 같은 영역 혼재라 통합 commit. NofM frame 3 고정 핵심 경로 = CM3 `stimulationParaCal.c` 가 공유메모리 `g_pcmFrameNum_per_channel = 3` 을 쓰고 CFX `stimulationStrategy_NofM_PCM_Write()` 의 stride 가 그 값을 읽는 양방향 핸드셰이크. src 4 파일 단일 commit `6216b21` (+188/-112) + docs commit 별도 (산출물 5종 + archive + 작업 목록.md). `claude_feature_signalProcessing-nofm-tuning` 분기 → `claude_develop` --no-ff → `claude_main` --no-ff → push origin 양쪽 + feature 브랜치 삭제. 사용자 일괄 위임 ("이상 없으면 마무리까지") 으로 단계 사이 yes/no 게이트 면제, 종료 보고만. 모든 commit AI 트레일러 절대 부재. |
| 2026-05-18 | **(루트 Package C 패턴 자율 적용)** Sound1 docs 명명·구조 일괄 갱신. ① `_modules.md` → `모듈 현황.md`, `list.md` → `작업 목록.md`, 21 작업 폴더의 `이력.md` → `이력 및 결과.md` (`git mv` 23건). ② 작업 폴더 21개 `<slug>` → `YYYYMMDD_<slug>` prefix 추가 — 시작일은 본 §작업 목록 "시작·완료" 컬럼 우선, 없으면 첫 commit 날짜. ③ cross-link 81 .md + `initialize.c` 코멘트 2건 일괄 갱신 (Python UTF-8 in-place + 단어 boundary). 잔존 검증 0건 (`_modules\.md`·`이력\.md`·옛 폴더 slug 모두). README.md 운용 가이드 라벨도 신 명명으로 정합. **본 작업 범위 외** — 거대 파일(300+) 분할 · 프로젝트 CLAUDE.md `maturity:core` 인라인. `claude_feature_docs-rebuild` 분기, 사용자 검토 후 머지 진행. |
| 2026-06-18 | `touch/20260618_touch-review` 신설 — 「터치센서 재검토」(분석 결론 → 구현 수렴) 착수. 착수 정리: 미커밋 변경의 em-dash 인코딩 손상 신규 6곳(main.c 744·839·859, iqs323 657·671·685) 복원 + SLEEP_ATI 매크로 주석을 코드 사실(보드 변종별 normal ATI 동일값)에 맞춰 정정. 기존 커밋된 손상 ~40곳은 별도 커밋 예정. 미완료 터치 3건(`crx1-esd-guard`·`touch-rtt-tuning`·`eunsu-consensus-review`) 중단 처리(은수님 지시) — 각 이력 및 결과.md에 중단 사유·승계 방침 기록, §상태 정의에 `중단` 추가. 누적 권고 2종(제어 재설계·하이브리드) 정독 서브에이전트 기동. ①요구사항 진행 중. |
| 2026-06-23 | `signalProcessing/20260623_dmic-sel-edge-match` 신설·완료 (이론 분석). Knowles SPH0641LM4H-1 마이크 SELECT 핀↔E8300 DMIC 캡처 엣지 정합 분석 — 빔포밍 포인트_3 후속. 마이크 데이터시트(14p) §5·§6 p.7 SELECT↔엣지 표 추출 + E8300 §18.2 p.563 대조 그라운딩. 11 페르소나(분석7·검증4, 45만 토큰) — confirmed 4/0/0. **올바른 조합: SEL=GND↔`_RE`·SEL=VDD↔`_FE`(역조합은 High-Z 캡처→손상). 현 펌웨어는 U7 SEL=GND·U9 SEL=VDD 전제, U9 SEL=VDD 능동 스트랩이 급소.** 최우선 후속=회로도 SEL 스트랩 실측. beam-forming 포인트_3 cross-link 해소. 산출물 6 + 에이전트-로그 13(00+01~11+인덱스). 코드 변경 없음. |
| 2026-06-23 | `signalProcessing/20260623_beam-forming` 신설·완료 (이론 분석). E8300 DMIC 2채널 end-fire 빔포밍 타당성 — 은수님 두 가설(입력 패스 채널 고유 지연·SW 시프트 없는 HW 내부 샘플링 지연)을 HW/FW Reference 데이터시트로 검증. pdftotext 색인 후 핵심 페이지(p.222 채널쌍 1/8 샘플 스큐·p.445 decimation 시분할·p.450 `AUDIO_ADC_DEC_CTRL` DELAY 레지스터·p.563 DMIC fractional) 정밀 추출 그라운딩. 34 페르소나 3단계(분석12·토론10·검증12, Workflow 160만 토큰, 12분) — confirmed 11/refuted 0/uncertain 1. 핵심 통찰: front mic을 홀수 채널 배치 시 시분할 스큐가 음향 선행(0.933샘플) 자동 보정 → 필요 레지스터 지연 0.808샘플(INT=6/FRAC=14) HW 단독 구현. 지연 대상=front mic(착용 귀별 DMIC1/2). 산출물 8 + 에이전트-로그 36(00+01~34+인덱스). 코드 변경 없음(계획.md 후속_1~5로 별도 작업·승인 대상). |

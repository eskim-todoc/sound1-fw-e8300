---
name: isd 모듈 작업 목록
purpose: ISD(내부기) 제어 · 매핑 측정 · 자극 파라미터 설정 관련 작업 인덱스
type: tasks/인덱스
applies_to: [Sound1]
tags: [isd, ecap, impedance, stim-para, specific-stim, tasks, index]
---

# 작업 목록 - isd

**TL;DR**: `src/2__cm3/source/isd/`(11파일 8,849줄) 관련 작업 인덱스. 2026-08-06 신설. **이 모듈은 호스트 러너가 컴파일하지 않는다** — `tests/unit/run.sh` 의 `TEST_TARGETS` 에 `isd/` 의 `.c` 가 하나도 없어 431 PASS 는 이 모듈을 한 줄도 검증하지 않는다. 따라서 여기 작업은 **실기 아니면 검증 수단이 없다.**

> [!NOTE]
> 루트 [`작업 목록.md`](../작업%20목록.md) 는 2026-07-14 부로 DEPRECATED·동결됐다. 신규 작업은 이 모듈 인덱스에 등재한다.

> [!CAUTION]
> **`-I "$SRC/isd"`(`run.sh:109`)를 커버리지로 오해하지 말 것.** include 경로일 뿐 `.c` 는 링크되지 않는다.

| 작업명 | 태그 | 폴더 | 상태 | 시작·완료 | 연계 | 요약 |
|---|---|---|---|---|---|---|
| **isd 죽은 코드 일괄 제거 (단계_1 마무리)** | 죽은코드, 조건부컴파일, 독립검증, 도구결함, thumb2인코딩 | [20260806_1530_isd-dead-code-sweep](20260806_1530_isd-dead-code-sweep/) | **완료** (①~⑧) | 2026-08-06 | <- 2026-08-06 은수님 *"전부 지워"* | 남은 7파일에서 **149줄** 제거. **착수 전 계량을 정정했다** - 직전 보고의 «191줄» 은 **결정적 죽음 116 + 조건부 비활성 85** 를 섞은 수치였다. `#if 0`/`#if 1` 은 소스에 답이 박혀 있어 지워도 되지만, `#ifdef CM3_I2C_controls_FPAG`·`#ifndef DisalbedBackTel` 의 비활성 가지는 **빌드 설정을 바꾸면 살아난다**. `tdc_isd_fpga.c` 는 15줄 전부가, `stim_para_setting.c` 는 10줄 전부가 조건부라 **한 줄도 안 건드렸다**. **핵심 - 1차 도구가 조건부 `#else` 가지를 지웠고 검증이 그걸 통과시켰다**: `#ifdef CM3_I2C_controls_FPAG` 의 `tdc_hal_i2c_cfx_read(...)` 15줄이 지워졌는데, **검증이 도구와 같은 가정(조건부의 첫 가지가 활성)을 써서** `#else` 가지가 양쪽 계산에서 똑같이 빠져 «완전 일치» 로 통과했다. **ecap·live 에서 이 검증이 통했던 것은 그 두 파일에 조건부 `#if` 가 0개였기 때문**이지 검증이 강해서가 아니었다. **독립 검증기를 만들었다** - 활성/비활성을 계산하지 않고 «지워진 줄이 원본에서 어떤 블록 안이었나» 구조만 본다. **버그본으로 먼저 시험해 15줄 전부 지목하는 것을 확인한 뒤** 본 작업에 썼다. 검증: 4파일 전부 «조건부 영역 삭제 0» · 호스트 514 PASS · ARM 에러 0 경고 0 · 크기 불변 · 실기 대기 불변(11건). **`.text` 예측 정정** - 41바이트 차이가 예측(18)과 어긋나 디스어셈블하니 **명령어는 정확히 17개**(예측한 밀린 운반체 수)였다. **Thumb-2 는 `MOV.W`(modified immediate)와 `MOVW`(imm16) 두 형식**을 쓰고 값이 형식 경계를 넘으면 4바이트가 통째로 바뀐다(실제 8건). **예측은 바이트가 아니라 명령어 수로 해야 한다** |
| **map_live 죽은 코드 제거 (단계_1 일부)** | live, 죽은코드, 바이트등가, 디스어셈블, 자산정정 | [20260806_1450_live-dead-code-removal](20260806_1450_live-dead-code-removal/) | **완료** (①~⑧) | 2026-08-06 | <- ecap 제거에 이은 단계_1 두 번째 · 2026-08-06 은수님 *"map_live 죽은 코드도 제거해"* | `tdc_isd_map_live.c` 의 **155줄**(죽은 145 + 지시자 10)을 걷어냈다(911 -> 756). 죽은 것 중 **106줄이 `#if 1` 의 `#else` 가지**였다. **검증을 셋으로 늘렸다** - ecap 은 차이가 2바이트라 값 대조로 충분했지만 여기는 많아져 «우연히 값이 맞는» 경우를 배제할 수 없어 **차이 주소를 전부 디스어셈블**했다. 8개 전부 `movs`/`mov.w`/`movw r3, #<줄번호>` 로 `tdc_sys_error_send_to_app(..., __LINE__)` 의 인자였다. 나머지 186,508바이트 동일. **핵심 - 예측 12바이트인데 실제 8이었고, 파고들어 자산_1 의 오류를 찾았다**: «모든 `TDC_PRINTF_*` 가 `__LINE__` 을 품는다» 는 틀렸고 **`TDC_PRINTF_E` 하나뿐**이다(`W`·`I`·`D`·`V` 는 평범한 `SEGGER_RTT_printf`). 진짜 운반체만 세니 8개 -> 8바이트로 정확히 맞았다. **«적게 나왔으니 다행» 으로 넘겼다면 틀린 모델을 계속 썼을 것이다.** 인수인계 자산_1 을 정정했다. **부수 성과 - `HoldOn` 285 -> 143줄**: 죽은 `#else` 106줄이 그 안에 있었다. 계획이 «150줄 이하면 추가 분해 불필요» 를 미리 정해 뒀고 143줄이라 **미뤄 뒀던 재분해가 «안 해도 되는 것» 으로 결론났다**. 순서를 지킨 덕이다 - 먼저 나눴다면 죽은 코드를 담은 함수 둘이 생겼다가 다시 합쳐야 했다. 실기 대기 불변(11건) |
| **ecap 죽은 코드 제거 (단계_1 일부)** | ecap, 죽은코드, 전처리기, 바이트등가, cmp, 실기불필요 | [20260806_1420_ecap-dead-code-removal](20260806_1420_ecap-dead-code-removal/) | **완료** (①~⑧) | 2026-08-06 | <- [분해 분석](20260806_0834_isd-대형함수-분해-분석/) §6.1 단계_1 · 2026-08-06 은수님 *"ecap 죽은 코드 제거해"* | `tdc_isd_map_ecap.c` 의 **컴파일되지 않던 267줄**(죽은 235 + 지시자 32)을 걷어냈다(1,455 -> 1,188). 전처리기 조건 분기가 하나도 남지 않았다. **검증을 두 겹으로 했다**: ①**살아 있는 줄만 뽑아 전후 대조 - 1,188개 완전 일치**(스크립트에 내장해 어긋나면 파일을 쓰지 않는다) ②**`.text` 바이트 전수 비교**(자산_1 준수) - 186,516바이트 중 **다른 바이트 2개**. 둘 다 밀린 `__LINE__` 즉치의 하위바이트로 정확히 설명된다(1244->1089 `0x4DC`->`0x441` 220->65, 1260->1105 `0x4EC`->`0x451` 236->81). 첫 제거 지점(395줄) 앞의 `__LINE__` 4개가 **안 밀린 것이 제거 범위가 의도대로였다는 방증**이다. **결과 - 코드를 267줄 지웠는데 실기 대기가 늘지 않는다**(소스 동일 -> 바이너리 동일 -> 동작 동일 3단 사슬). **자산 - 죽은 코드 제거에는 구조 지표를 쓸 수 없다**: `metrics.sh` 가 `#if 0` 안의 분기도 세므로 `if` 46->35 처럼 줄어드는 것이 정상이고 옳은지 판정할 기준이 없다. 불변량이 «분기 보존» 이 아니라 «살아 있는 코드 보존» 이므로 다른 자를 쓴다. **주의 - 다른 파일은 전제가 다르다**: ecap 은 `TDC_PRINTF` 0건이라 소스에 보이는 `__LINE__` 6개가 전부였다. `map_live` 등은 매크로 안에 숨은 `__LINE__`(자산_1)까지 세어야 한다 |
| **impedance · live 분해** | impedance, live, 분해, 전처리기, 기계추출, 상태기계 | [20260806_1330_impedance-live-decompose](20260806_1330_impedance-live-decompose/) | **완료** (①~⑧) | 2026-08-06 | <- 단계_3·4 완료 후 «같은 맥락» 재량 위임(2026-08-06 은수님) | `isd/` 에 남은 **단일 함수 둘**을 각각 11함수로 나눴다 - `map_impedance` 664줄 · `map_live` 809줄. `map_ecap`(1,454줄)은 **검증 불가로 제외 유지**(앱이 0x63·0x64 미사용, 은수님 확인). **세 번 틀렸고 세 번 다 다른 장치가 잡았다**: ①**전처리기가 중괄호를 쪼개** `case 0` 이 switch 전체를 삼켰다(`#if 1` 가지의 `{` 를 `#endif` 뒤 `}` 가 닫고 `#else` 에도 `{` 가 있다). **컴파일러는 못 잡는다** - 중첩 case 로 문법이 성립할 수 있다. **구조 지표만이 잡았다.** ②이름 치환이 **구조체 멤버까지** 바꿨다(`impedanceCheck.iterationNum`) - 컴파일러가 잡았다. ③블록 경계에 **여는 중괄호를 포함**해 함수가 안 닫혔다(`invalid storage class`) - 컴파일러가 잡았다. 검증: **예측치와 정확히 일치** - impedance **전 지표 불변**(병합 없고 void 라 return 도 안 는다), live **`return` +10 외 불변**. 호스트 514 PASS · ARM 에러 0 경고 0 · text +276/+304B · data·bss 불변. **`HoldOn` 285줄은 일부러 남겼다** - 상당수가 죽은 `#else` 안이라 지금 나누면 **죽은 코드에 이름을 붙이는 꼴**이다. 단계_1 후 재검토 |
| **stim_para_setting 분해 (단계_4)** | stim-para, 분해, 상태기계, static충돌, 중복병합, 기계추출, 심볼대조 | [20260806_1215_stim-para-decompose](20260806_1215_stim-para-decompose/) | **완료** (①~⑧) | 2026-08-06 | <- [분해 분석](20260806_0834_isd-대형함수-분해-분석/) §6.4 단계_4 · 분석이 «자극 본체 최고 위험» 으로 분류 | 모노폴라 320줄 · 바이폴라 708줄 두 상태 기계를 case 별 **16함수**로 분해(최대 106줄). **1,111줄을 손으로 옮기지 않고 기계 추출**했다 - 본문은 원문 그대로 옮기고 껍데기만 생성, case 집합이 기대와 다르면 중단. **핵심 함정 셋**: ①`bi` 의 6개 case 가 **전부 29줄이라 같아 보였지만 md5 대조하니 `case 24` 만 달랐다**(루프 상한 6 vs 2). 인자 없이 합쳤으면 마지막 두 전극에서 FIFO 를 넘겨 읽는다. **6×5+2=32=df_MaxNumOfElectrode** 였다. ②유사도 80%+ 다섯 쌍 중 **둘은 병합 불가**(`disable_backtel` 한 줄 차이 · 에러 처리 차이) - 유사도는 후보를 좁히는 도구이지 판단이 아니다. ③**두 함수가 같은 이름의 독립 static 을 각자 가짐**(`flowControlCounter` 등) - 그대로 승격하면 하나로 합쳐져 서로를 밟는다. `s_mono_`·`s_bi_` 로 갈랐고 **심볼 13:13 1:1 대응**으로 확인했다. 검증: 구조 지표가 **예측치와 정확히 일치**(`for` **-12** = 병합 산술, `return` **+16** = 신규 함수 수, 나머지 전부 불변) · 호스트 514 PASS 유지 · ARM 에러 0 경고 0 · text 186,484->**185,944(-540B)** · data 불변 · bss -4(정렬 패딩, 심볼 대조로 손실 아님 확인). **자산 - `Copy-Item` 은 타임스탬프를 보존해 make 가 재컴파일을 건너뛴다**(원본 대조 시 `LastWriteTime` 갱신 필수) |
| **specific_stim 분해 (단계_3)** | specific-stim, 분해, flowCounter, 상태기계, static승격, metrics | [20260806_1130_specific-stim-decompose](20260806_1130_specific-stim-decompose/) | **완료** (①~⑧) | 2026-08-06 | <- [분해 분석](20260806_0834_isd-대형함수-분해-분석/) §6.3 단계_3 | `tdc_isd_map_specific_stim_step()` **576줄 단일 함수**를 flowCounter 상태별 **12함수**로 나눴다(최대 70줄). **`switch` 밖 124줄 블록**도 함께 분해했다 - 자산_5 «기계 추출이 놓치는 예외» 의 **네 번째** 유형이다. 함수-지역 `static` **15개**를 파일 범위로 승격했고(15개 전부가 두 상태 이상에 걸쳐 있어 선택지가 없었다), **`bss` 불변이 그 안전성의 증거**다 - 함수-지역과 파일 범위 static 은 같은 섹션에 놓인다. `case 5`(전극 0~23)·`case 7`(24~31)이 **경계값만 다른 같은 코드**여서 `_send_reference_range(first, last)` 하나로 합쳤다. 검증: 구조 지표 `for` **정확히 -2**(병합분) 나머지 **전부 불변** · 호스트 **514 PASS 유지** · ARM 에러 0 경고 0 · text +124B(호출 오버헤드) · **data·bss 불변**. **자산 - metrics.sh 가 제 역할을 하면서 자기 결함도 드러냈다**: 첫 대조가 -1 을 보고했는데 예상은 -2 였다. 원본 `for(;pcm_index` 에 공백이 없어 패턴 `^[[:space:]]*for \(` 이 못 셌던 것이다. 도구의 제어문 패턴 4곳을 `[[:space:]]*\(` 로 고쳤다. **«변했지만 의도한 것» 으로 넘겼으면 못 찾았다** |
| **자극 모드 인코딩 중복 통합 (단계_2)** | stim-mode, 중복통합, 순수함수, 호스트게이트, isd | [20260806_1046_stim-mode-encode-dedup](20260806_1046_stim-mode-encode-dedup/) | **완료** (①~⑧) | 2026-08-06 | <- [분해 분석](20260806_0834_isd-대형함수-분해-분석/) §6.2 단계_2 | `stimulationMode` -> 내부기 레지스터 비트 변환이 **3파일 5쌍(스위치 10블록)** 중복이었다. 순수 함수 2개(`tdc_isd_stim_mode_reference_bits` 비트[3:2] · `tdc_isd_stim_mode_output_bits` 비트[1:0])로 모았다. **시프트는 호출부에 남겼다** - 레지스터를 한 필드씩 쌓아 올리는 흐름이 보여야 어느 비트가 어디 놓이는지 읽힌다. **핵심 성과 - `isd/` 로직에 처음으로 호스트 테스트(31케이스)가 붙었다.** 그전 483 PASS 는 `isd/` 를 한 줄도 검증하지 않았다. 순수 함수라 스텁 없이 단독 링크된다(위험_4 조치 자산_D 의 두 번째 실증, 이번엔 `isd/` 안에서). **동작 불변 보존** - 출력모드 스위치에 원래 `default` 가 없어 범위 밖 값이 하위 2비트 0(모노폴라)으로 남던 것을, `default`->0 으로 명시해 결과를 같게 뒀다(더 나은 값을 고르면 동작 변경이라 별건으로 남김). 검증: 잔여 0 · **165줄 순감소** · 483->**514 PASS** · ARM 에러 0 경고 0 · text 186,600->**186,360(-240B)** · data·bss 불변. **자산 3건** - ①다파일 기계 치환은 **전부 검증 후 일괄 쓰기**(중간 실패가 부분 적용을 남긴다) ②**정규식으로 중첩 블록 끝을 못 잡는다**(`flowCounter` 스위치가 `stimulationMode` 스위치를 삼킴 -> 중괄호 짝맞추기) ③**헤더 의존은 추측 말고 컴파일**(SDK `hw.h` 포함에도 호스트 통과 -> 열거형 타입 유지) |
| **isd 대형 함수 분해 분석** | ecap, stim-para, specific-stim, 분해, 중복, 죽은코드, 게이트, 분석전용 | [20260806_0834_isd-대형함수-분해-분석](20260806_0834_isd-대형함수-분해-분석/) | **완료** (①~④, 코드 무변경) | 2026-08-06 | <- BLE 이월 전량 소진 후 다음 대상 선정 · 실기 대기 5건이라 코드 변경 보류 | 은수님이 «분석만 선행» 을 선택해 **코드 한 글자도 대지 않고** 조사만 했다. **후보 지표는 줄 수가 아니라 «줄 수 / 함수 수»** — `main.c`(1,121줄)는 44함수로 이미 분해돼 있고, 실제 덩치는 `isd/` 세 파일이다(`map_ecap` 1,519줄/**2함수**, `stim_para_setting` 1,178줄/6함수, `map_specific_stim` 629줄/**1함수**). **핵심 발견 - `ecap` 은 두 게이트가 모두 닫혀 있다**: 호스트는 `isd/` 미컴파일, 실기는 앱이 0x63·0x64 를 안 보낸다(`tdc_ble_mapping.c:389` 호출 · `:403` 주석). **분해해도 확인할 방법이 없어 위험이 사라지는 게 아니라 eCAP 을 켜는 날로 미뤄진다.** 부수 발견 - 같은 파일 **15.5%(235줄)가 컴파일되지 않는 죽은 코드**이고, `stimulationMode` -> 레지스터 인코딩이 **3파일 5쌍(스위치 10블록)** 중복이며, `switch` **밖**에 275줄 블록이 또 있다(자산_5 «기계 추출이 놓치는 예외» 의 **세 번째** 사례). **권장 순서**: 죽은 코드 264줄 제거 -> 중복 5쌍 통합(**새 파일로 떼면 순수 함수라 호스트 게이트가 열린다**) -> `specific_stim` 605줄 분해(0x65 실기 가능·폭발 반경 작음) -> `stim_para_setting`(자극 본체·최고 위험) -> `ecap`(권하지 않음). **④ 검증에서 내 오류 3건을 잡았다** — `specific_stim` 을 착수 순서에서 누락, `grep` 이 낸 `case` 줄을 `switch` 줄로 오기, 감소량 과대(200 -> 150줄). ⑤계획 이후는 은수님 판단 |

## 참고 - 이 모듈의 검증 수단 (2026-08-06 확인)

| 파일 | 실기 트리거 | 호스트 | 비고 |
|---|---|---|---|
| `tdc_isd_map_ecap.c` | **없음** | 없음 | 0x63 호출 · 0x64 주석. 앱이 둘 다 미사용 |
| `tdc_isd_map_specific_stim.c` | **0x65 특정자극** | 없음 | 2026-07-30 실기 확인 이력(대장 §4) |
| `tdc_isd_stim_para_setting.c` | **라이브 자극** | 없음 | `map_live.c:161`·`:549`·`:588`·`:731` · `stim_standalone.c:104` |
| `tdc_isd_map_impedance.c` | **0x62 임피던스** | 없음 | 위험_3 마스크 추가(2026-08-06, 실기 대기) |

**호스트 게이트를 여는 유일한 통로는 «의존 사슬에 닿지 않는 순수 함수를 새 파일로 떼는 것»** 이다(분석 §6.2).

---
name: fake sleep mode 제거 - 구현 검증
purpose: ⑦ 구현(cc5e4e5) 및 연계 커밋(4a0f272)의 검증 라벨 대조 - 실기 게이트 대기 기록
type: tasks/검증
applies_to: [Sound1]
tags: [fake-sleep, removal, verification, implementation, heartbeat]
---

# 구현 검증 — fake sleep mode 제거

**TL;DR**: 계획 §7 검증 라벨 9개 중 정적 검증 가능한 7개 전부 통과(커밋 `cc5e4e5`). 잔여 2개(검증_7 BLE 실기 · 검증_9 Eclipse 빌드)는 은수님 환경 게이트로 대기. 연계 커밋 `4a0f272`(heartbeat 명명 정리)의 검증 결과를 병기한다.

---

## 1. 검증 라벨 대조 (커밋 `cc5e4e5`)

| 라벨 | 항목 | 결과 | 근거 |
|---|---|---|---|
| 검증_1 | fake 심볼 잔존 0 | ✅ PASS | `fake_func_sleep` / `tdc_get_fake_op_mode` / `tdc_set_fake_op_mode` / `s_tdc_fake_op_mode` / `ci_fake_power_sleep` / `gd_handle_op_mode_setting` 전부 0 (주석 제외, 전 프로젝트 grep) |
| 검증_2 | `func_sleep()` 불변식 | ✅ PASS | 정의 1 · ULP 헬퍼 6(`#if` 2벌 포함) · `ci_power_sleep()` 호출 1 · `func_sleep()` 호출 1 유지 |
| 검증_3 | BLE option 1 공급 유지 | ✅ PASS | `tdc_touch.c:337~343` 직접 대입 7 · getter 호출 7 · `tdc_touch_process()` 호출 유지 (**회귀 1순위**) |
| 검증_4 | setter 제거 / getter 보존 | ✅ PASS | `set_recent_*` 잔존 0 / `get_recent_*` 선언 7 보존 |
| 검증_5 | 브레이스 균형 | ✅ PASS | `main.c` 117/117 · `ci_power.c` 25/25 · `tdc_remote_general_debug.c` 12/12 · `tdc_touch.c` 55/55 |
| 검증_6 | 전처리기 균형 | ✅ PASS | `main.c` · `ci_power.c` · `tdc_touch.c` depth 0 |
| 검증_7 | BLE option 1/2/3 실기 | ⏳ 대기 | 은수님 환경 |
| 검증_8 | 인코딩 | ✅ PASS | pre-commit 훅 통과 (커밋 성립 자체가 증거) |
| 검증_9 | Eclipse 빌드 + 실기 | ⏳ 대기 | 은수님 환경 |

규모: `main.c` 1397 → 1161줄, `tdc_touch.c` 446 → 416줄 (총 약 300줄 감소).

---

## 2. 연계 커밋 `4a0f272` (heartbeat 명명 정리) 검증

`main_counter` 실사용 여부 질의에서 파생된 정리. 같은 브랜치에 연계 커밋으로 수행.

| 항목 | 결과 | 근거 |
|---|---|---|
| 구 심볼 잔존 0 | ✅ | `main_counter` / `update_CM3Status_toCFX` / `update_CM3tempValue1_toCFX` / `update_CM3tempValue2_toCFX` (주석 제외 0) |
| 신규 심볼 정합 | ✅ | `s_cm3_heartbeat` 선언 1 · 사용 3 / `tdc_shared_publish_cm3_heartbeat` 선언 1 · 정의 1 · 호출 1 |
| **공유 메모리 ABI 무변경** | ✅ | `1__cfx` · `5__calibration` 헤더 diff 없음, `2__cm3` 헤더는 함수 선언 구간만 변경. 구조체 필드 3종(`CM3_status` / `CM3_tempValue1` / `CM3_tempValue2`) 존치 |
| CFX AGC 무영향 | ✅ | `agc.c` 의 `CM3_tempValue` 참조 7건 그대로 |
| 브레이스 균형 | ✅ | 3파일 OK |

---

## 3. 종결 조건

검증_7 · 검증_9 통과(은수님 빌드 · 실기 확인) 시 본 문서에 결과를 추기하고 `claude_develop` 병합으로 종결한다.

---
name: systemControl FSM 분해 - 이력 및 결과
purpose: systemControl() 281줄 암묵 상태머신의 명시적 FSM 분해 작업 시계열 로그 및 완료 요약
type: tasks/이력
applies_to: [Sound1]
tags: [systemControl, fsm, refactor, cm3, main]
---

# 이력 및 결과 — systemControl() FSM 분해

**TL;DR**: `systemControl()` 281줄 / `static` 상태 10개의 암묵 상태머신을 명시적 FSM 구조로 분해한다. 선행 작업(func_normal 분해 · 레거시 LED 제거)에서 호출부와 죽은 경로를 정리한 뒤의 마지막 단계. 동작 보존이 대전제.

---

## 시계열 로그

| 시각 | 단계 | 내용 |
|---|---|---|
| 2026-07-15 | 착수 | 은수님 지시: "event_collect -> handler 구조로 개혁, 불필요한 과정 생략, 안 쓰이는건 없애기". 범위 A+B+C 중 **C** 에 해당 |
| 2026-07-15 | 선행 완료 | A(레거시 LED 제거) · B(event collect/handle 분리) 병합 완료 (`a96b589`). 빌드 · 실기 동작 확인됨 |
| 2026-07-15 | ① | `요구사항.md` 작성 |
| 2026-07-15 | ② | `요구사항-검증.md` 작성 |
| 2026-07-15 | ③ | `분석.md` 작성 |
| 2026-07-15 | ④ | `분석-검증.md` 작성 |
| 2026-07-15 | ⑤ | `계획.md` 작성 |
| 2026-07-15 | ⑥ | `계획-검증.md` 작성 |
| — | 승인 | 사용자 승인 대기 (⑦ 구현 진입 게이트) |
| — | ⑦ | 구현 |
| — | ⑧ | `구현-검증.md` |

---

## 선행 맥락 (이번 세션)

| 커밋 | 내용 | 상태 |
|---|---|---|
| `adf984c` | CP949 인코딩 손상 해결 + 방어 3중 | 병합 · push |
| `f871604` | func_normal() while 본문 함수 분리 (245 -> 168줄) | 병합 · push |
| `23d8c65` | func_normal() 블록 5개 추출 + `tdc_` 접두어 | 병합 · push |
| `df737d2` | func_normal() 컨텍스트 구조체 도입 (168 -> 83줄) | 병합 · push |
| `a96b589` | 레거시 LED 경로 제거 + event collect/handle 분리 | 병합 (push 대기) |

이번 작업(C)은 위 사슬의 마지막 단계다. 호출부(`func_normal`)와 죽은 LED 경로가 이미 정리됐으므로, 남은 것은 `systemControl()` 내부다.

---

## 결정 사항

| 결정 | 내용 | 근거 |
|---|---|---|
| 1-tick 지연 유지 | `conneded_ISD` / `mappingConnected` 는 지난 tick 값으로 유지 | 소비처 4곳 전부 시간 누적 · 인간 조작 스케일이라 무해. 순환 구조상 제거하려면 함수를 반으로 갈라야 하는데 이득 없음 (조사 완료, `systemControl.c` 주석에 명시) |
| 동작 보존 대전제 | FSM 분해는 구조 변경만, 동작 변경 금지 | 실기 검증 수단이 은수님 환경뿐. 동작 변경은 별도 작업으로 분리 |

---

## 완료 요약

(작업 종료 시 작성)

---
name: 이력 및 결과 — 배터리·LED 코드 리팩토링
purpose: func_normal() 배터리→LED 로직을 updateBatteryLevel() 정신으로 재캡슐화 + 변화 마진 제거 작업의 시계열 로그·완료 요약
type: tasks/이력
applies_to: [projects/Sound1]
maturity: experimental
tags: [main, func_normal, battery, LED, refactor, hysteresis, updateBatteryLevel, qcc]
---

# 이력 및 결과: 배터리·LED 코드 리팩토링

**TL;DR**: `main.c` `func_normal()`의 배터리→LED 결정 로직을 (1) 과거 `updateBatteryLevel()`의 캡슐화 정신을 QCC 기반 구조에 맞게 되살리고, (2) 인라인 하드코딩을 함수로 분해하며, (3) QCC가 이미 측정·필터링하므로 불필요해진 배터리 변화 마진(히스테리시스)을 제거하는 방향의 분석·계획 작업. 8단계 프로세스로 진행하며 구현 착수는 은수님 승인 후.

## 개요

| 항목 | 내용 |
|---|---|
| 브랜치 | `claude_feature_led-battery-refactor` (`claude_develop` 기준 분기) |
| 작업 폴더 | `docs/tasks/main/20260714_battery-led-refactor/` |
| 선행 작업 | [`main/20260709_func-normal-refactor`](../20260709_func-normal-refactor/) (동작 보존 분해 계획 — 이번은 그 위에서 **동작 변경**을 추가) |
| 현재 단계 | ⑥ 계획 검증 완료 → **승인 게이트**(구현 미착수) |

## 시계열 로그

### 2026-07-14

- **작업 착수**: 은수님 지시 — "`main.c` `func_normal()`의 배터리 레벨→LED 설정 코드가 (1) 과거 `updateBatteryLevel()`을 현 구조에 맞게 못 살린 게 아쉽고, (2) 하드코딩이라 함수가 길고, (3) 배터리 변화 마진을 뒀는데 QCC가 배터리 레벨을 입력·측정하므로 마진이 불필요하다. 잘 분석·계획하고 수정 여부는 그다음 결정. 서브에이전트 잘 쓰고 문서 정리."
- **선행 조사 (직접)**:
  - 기존 `20260709_func-normal-refactor` 작업 발견 — `func_normal()` 5구역 **동작 보존** 분해 계획(7-Step) 완료·미구현 상태. 이번 작업은 그와 달리 **동작을 변경**(마진 제거)하므로 별도 신규 작업으로 분리(은수님 "새 작업" 명시).
  - `updateBatteryLevel()` 실체 확인: `batteryNPowerControl.c:336` 정의 **생존**, `main.c:448` 호출은 **주석 처리**. 배터리 레벨을 7단계 `EN__BATTERY_LEVEL` enum으로 매핑 + "노이즈 추세 반전 방지" 마진 로직 내장. `readBatteryLevel_FromCFX()`로 **자체 측정**(현 구조는 QCC가 측정, 이 경로가 낡음).
  - 현 구조: `main.c:451` `batteryLevel = snd_batt_get_level()` — "직접 측정 않고 QCC에서 배터리 정보 받으면 업데이트". LED 블록(538-655)은 `snd_batt_get_percent()`를 직접 재사용해 인라인 히스테리시스(40/41·65/64·20/22) 수행.
  - **미커밋 변경 발견**: `main.c` 배터리 임계값이 작업트리에서 `40/41/65/64` → `10/12/80/78`로 되돌려져 있음(주석 옛값을 실제 값으로). 은수님 WIP로 추정 — 보존하고 분석 baseline 판단에 반영, 승인 게이트에서 확인.
- **환경 세팅**: 배경 세션 격리 가드를 `.claude/settings.local.json`(gitignored, 로컬)에서 해제 — 루트 헌법 "워크트리는 사용자 명시 시만" + 은수님 Eclipse 워크스페이스 `src/` import 정합 위해 워크트리 대신 인플레이스 브랜치 사용.
- **브랜치·폴더 세팅**: `claude_feature_led-battery-refactor` 생성, 작업 폴더 신설, 본 이력·요구사항 작성.
- **분석 fan-out(③)**: 4개 페르소나 병렬(model sonnet) + 반증 검증 1노드. 로그 `에이전트-로그/01~04·06` + `인덱스.md`. 인용 전수 재대조 **할루시네이션 0건**, 마진 제거 안전성 반박 실패(조건부 안전 유지), 옵션 B(테이블 구동) 무결함 확인. 사실 정정 2건(veryLowBattery 40%p·`ci_battery_init` 미호출) 분석에 반영.
- **분석·계획 작성(③~⑥)**: `분석.md`·`계획.md` 작성(각 자체 검토 포함). 옵션 B 1순위, 마진 제거 전후 전이표 명시, 보존 필수 5항목 도출. 승인 전 확정 필요 결정 6건(§0) 정리.
- **승인 게이트 도달**: 코드 변경 0. 은수님께 결정 6건 확인 요청 예정. 미커밋 src 변경(main.c 임계값·microcode·cproject)은 커밋하지 않고 docs만 커밋.

## 완료 요약

(구현 승인·완료 시 작성) — 현재 분석·계획까지 완료, 구현 미착수.

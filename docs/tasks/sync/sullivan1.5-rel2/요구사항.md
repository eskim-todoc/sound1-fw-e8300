---
name: Sullivan1.5 rel.2 동기화 요구사항
purpose: Sullivan1.5 베이스 프로젝트 대비 Sound1 코드 비교 분석 — 베이스에 추가된 후속 업데이트 식별·반영 검토
type: 요구사항
revision: Rev.1
status: 진행
applies_to: [Sound1, sync, ble, internalDevice, signalProcessing]
tags: [sullivan, baseline, sync, comparison]
---

# Sullivan1.5 rel.2 동기화 — 요구사항 (Rev.1)

**TL;DR**: `src/2__cm3/` 는 Sullivan1.5 베이스 프로젝트의 fork. 사용자(다) 는 "Sound1 으로 병합한 이후 Sullivan 측에 추가 업데이트가 있던 것으로 기억"하며, 본 작업은 그 가설을 검증·반영 검토하는 비교 분석이다. 베이스를 `src/Sullivan1_5__CM3/` (임시, git 미추적) 로 복사 받았으며, **공백·엔터·중괄호·주석 변경은 무시하고 실제 코드 변경만** 식별해 문서로 정리한다. 진행 순서는 `BleCommunication` → `internalDevice` → `signalProcessing` 폴더 차례.

> [!IMPORTANT]
> **비교 대상 = `src/Sullivan1_5__CM3/Cortex-M3-src/`** 는 분석 종료 후 삭제 예정인 임시 폴더. `src/.gitignore` 에 추가하여 git 추적 차단됨. 분석 산출물(`docs/tasks/sync/sullivan1.5-rel2/`) 만 영구 보존.

## 1. 배경

- Sullivan1.5 (베이스 프로젝트) 가 `src/2__cm3/` 의 원본. Sound1 작업으로 분기 이후 양쪽이 각자 발전했을 가능성.
- 사용자(다) 기억: **"Sound1 으로 병합한 이후 Sullivan 측에 추가 업데이트가 있었던 것 같다"** → 검증·반영 후보 식별이 본 작업 목적.
- 사용자 연차 복귀 당일(2026-05-14, 목) 본 작업 진입 — 우선 진행 약속 2 번. 1 번([`signalProcessing/nofm-merge`](../../signalProcessing/nofm-merge/)) 은 별도 진행.

## 2. 목적·범위

- **대상 영역 (Sound1 측)** — `src/2__cm3/Cortex-M3-src/` 하위 3 폴더:
  1. `BleCommunication/` — **1 단계** (본 Rev.1 시점 분석 진행 중)
  2. `internalDevice/` — 2 단계
  3. `signalProcessing/` — 3 단계
- **베이스** — `src/Sullivan1_5__CM3/Cortex-M3-src/` 의 동명 폴더
- **작업 성격**: 두 라인의 의미적 diff 추출 → 어디가 어떻게 다른지 문서화 → 차이 해석(어느 쪽이 후속인가) → Sound1 미반영 항목 후속 반영 여부 검토
- **모듈 분류**: `sync` (다중 영역에 걸치므로 단일 도메인 모듈 분류 회피)

## 3. 요구사항

### R-1. 비교 기준 — 의미적 변경만

**무시할 차이**:
- 공백 추가/제거 (들여쓰기, 줄 끝 공백 등)
- 빈 줄 추가/제거
- 중괄호 위치 변경 (K&R / Allman 등 스타일 차이)
- 줄바꿈 위치 변경 (clang-format 결과 차이)
- 단순 주석 텍스트 추가/제거 (의미 변경이 없는 경우)

**잡아낼 차이**:
- 함수/변수/타입 이름 변경
- 로직 추가/삭제/변경 (조건문, 분기, 루프)
- 함수 호출 변경 (다른 함수, 다른 인자)
- include / 매크로 변경
- 상수값 변경
- 주석이라도 의미를 바꾸는 IMPORTANT/NOTE 코멘트는 표시

→ 방법: `git diff --no-index -w --ignore-blank-lines` 1 차 스크리닝 → hunk 직접 검토로 주석/스타일 제외.

### R-2. 산출물 형식

각 폴더별 별도 `분석 <폴더이름>.md` 작성 (예: `분석 BleCommunication.md`, `분석 internalDevice.md`, `분석 signalProcessing.md`).

각 분석 문서는 다음 구조:
- 요약표 (파일 단위 동등/차이/방향)
- §Sullivan에만 있는 변경 (Sound1 미반영 — **검토 대상**)
- §Sound1에만 있는 변경 (Sullivan 미반영 — 단순 기록)
- §양방향 분기 (양쪽 다 다른 부분)
- §결론·후속 권고
- §방법론 메모 (재현 명령)

### R-3. 베이스 폴더 정리

- `src/Sullivan1_5__CM3/` 는 **분석에만 사용**, git 추적 차단 (`src/.gitignore` 등록 완료).
- 3 영역 분석 완료 후 사용자 승인하에 폴더 삭제 (또는 사용자가 직접 삭제).

### R-4. 반영(머지) 작업은 별도

본 task 는 **분석·식별·문서화** 까지. 실제 Sound1 측에 Sullivan 측 패치를 흡수하는 코드 변경은 항목별로 별도 task 로 분리 (필요시 모듈별 task — 예: `ble/fatfs-remount-twice-from-sullivan/`).

## 4. 진행 단계

- [x] 요구사항.md Rev.1 — 사용자 설명 반영
- [x] 분석 BleCommunication.md Rev.0 — 13 파일 비교 (2026-05-14)
- [x] 분석 internalDevice.md Rev.0 — 동일 방법
- [x] 분석 signalProcessing.md Rev.0 — 동일 방법
- [x] 종합 결론 — 3 영역 통합 후속 권고 정리 (필요시 `요약.md` 또는 요구사항.md Rev.2 본문 §결론)
- [x] Sullivan 임시 폴더 삭제 (사용자 승인 후)
- [x] 이력.md — 본 task 완료 후

(반영 작업은 본 task 외 — §R-4)

## 5. 갱신 이력

| 날짜 | 변경 |
|---|---|
| 2026-05-08 | Rev.0 placeholder 등록 — 사용자(다) 연차 종료 후(2026-05-14, 목) 설명 예정. |
| 2026-05-14 | Rev.1 작성 — 사용자(다) 복귀 당일 직접 설명 반영. 베이스 = `Sullivan1_5__CM3` (임시), 진행 순서 BleCommunication → internalDevice → signalProcessing, 비교 기준은 의미적 변경만, 각 폴더별 별도 분석 산출물. BleCommunication 분석 Rev.0 동시 작성·완료. |

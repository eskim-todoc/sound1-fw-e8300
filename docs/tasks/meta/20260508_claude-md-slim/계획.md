---
name: 계획 — Sound1 CLAUDE.md 슬림화 + 루트 강제 강화
purpose: 신규 CLAUDE.md 본문 초안과 적용 절차
type: tasks/계획
applies_to: [Sound1]
maturity: stable
tags: [meta, docs, claude-md, root-enforcement, slim]
---

# 계획: Sound1 CLAUDE.md 슬림화 + 루트 강제 강화

**TL;DR**: 현행 99줄·9섹션 → 신규 약 50~60줄·6섹션. frontmatter+TL;DR 추가, 헤더 Alert 강화, 루트 지침 참조 표·외부 의존 3섹션·문서 작성 다이어그램 제거, 작업 목록.md 진입점 별도 §작업 라우팅으로, 절대 경로 → 상대 통일.

## 1. 적용 절차

`Write` 도구로 CLAUDE.md를 통째 재작성한다(편집 영역이 광범위해 in-place Edit 비효율). 단일 호출로 안전.

```mermaid
flowchart LR
    A["① CLAUDE.md Write<br/>(신규 본문)"] --> B["② 진행상황·이력 및 결과.md 갱신"]
    B --> C["③ git add + commit"]
    C --> D["④ claude_develop 체크아웃<br/>--no-ff 머지"]
    D --> E["⑤ feature 삭제 + archive 이동<br/>(이력 및 결과.md 병합 해시 갱신)"]
    E --> F["⑥ 후속 커밋 + 원 브랜치 복귀<br/>+ stash pop"]
```

## 2. 신규 CLAUDE.md 본문 초안

### 2.1 구조 (6섹션)

```
0. frontmatter + TL;DR (신규)
1. # Sound1
2. > [!IMPORTANT] 루트 강제 + 충돌 정책 (강화)
3. 한 줄 소개 — E8300 임베디드 펌웨어 프로젝트
4. ## 메타 (보존)
5. ## 원본 기여 흐름 (보존)
6. ## Layout (보존, docs 줄 요약)
7. ## 작업 라우팅 (신규 — 작업 목록.md 진입점)
```

### 2.2 본문 초안

```markdown
---
name: Sound1 프로젝트 컨텍스트
purpose: E8300 임베디드 펌웨어 프로젝트 진입점 — 루트 지침 절대 우선
type: 메타
applies_to: [Sound1]
tags: [project, e8300, firmware, embedded]
---

# Sound1

**TL;DR**: E8300 임베디드 펌웨어 프로젝트. 루트 [`../../CLAUDE.md`](../../CLAUDE.md)와 그 지침 체계를 **절대 우선**으로 따른다. 본 파일은 Sound1 고유 정보(repo·기여 흐름·폴더 구조·작업 라우팅)만 담는다.

> [!IMPORTANT]
> **Sound1은 루트 [`../../CLAUDE.md`](../../CLAUDE.md)와 그 지침 체계를 절대 우선으로 따른다.** 본 파일은 Sound1 고유 정보(repo·메타·기여 흐름·폴더 구조·작업 라우팅)만 담고, 일반 규칙은 모두 루트에서 상속한다.
>
> **세션 시작 시**: ① 루트 `CLAUDE.md` 명시 Read → "루트 지침 확인 완료: <한 줄 요약>" 보고, ② 루트 [`docs/지침/작업 규칙.md`](../../docs/지침/작업%20규칙.md) 확인.
>
> **충돌 정책**:
> - 본 CLAUDE.md ↔ 루트 CLAUDE.md 충돌 시 → **루트 우선**. 본 파일이 잘못된 것으로 간주, 사용자에게 즉시 보고.
> - 본 프로젝트 `docs/지침/` ↔ 루트 `docs/지침/` 충돌 시 → 루트 [`문서 작성 규칙.md §2.1`](../../docs/지침/일반/문서%20작성%20규칙.md)에 따라 **프로젝트 우선** (의도적 도메인 특화 예외).

E8300 임베디드 펌웨어 프로젝트.

## 메타

- Repository (`origin`, 본인): `eskim-todoc/sound1-fw-e8300` (평소 push 대상)
- Repository (`upstream`, 원본): `todoc-dev/sound1-fw-e8300` (PR 기여 대상)
- Main branch: `claude_main` (origin 기준, 일상 작업 베이스) · `Develop` (upstream 동기화용 baseline)
- 담당 범위: SW/펌웨어 코드 + 문서 작성

## 원본 기여 흐름 (Fork-and-PR)

본인 저장소(`origin`)를 중간 단계로 사용. upstream 직접 push 권한 제한 대응.

- 일상 작업: `claude_<설명>` 브랜치 → `claude_main` 머지 → `git push origin claude_main`
- upstream 변경 받아오기: `git fetch upstream && git checkout Develop && git merge upstream/Develop && git push origin Develop`
- 원본 기여: GitHub 웹에서 `eskim-todoc/sound1-fw-e8300:claude_main` → `todoc-dev/sound1-fw-e8300:Develop` cross-repo PR (빈 repo로 만든 저장소라 공식 fork 관계는 없지만 공통 커밋 조상이 있어 PR 가능)

## Layout

```
Sound1/
├── CLAUDE.md       이 파일
├── src/            Eclipse 임베디드 프로젝트 (펌웨어 소스)
│   ├── 0__bootloader/
│   ├── 1__cfx/
│   ├── 2__cm3/
│   ├── 3__hear/
│   ├── 4__eeprom/
│   ├── 5__calibration/
│   ├── .metadata/          Eclipse workspace (경로 이동 시 재-import 필요)
│   ├── .clang-format
│   ├── .gitignore
│   └── CODEOWNERS
├── tests/          단위 · 통합 테스트 (초기: 빈 폴더)
└── docs/           SW 문서 — 루트 컨벤션 적용 (지침/·참고/·사용방법/·tasks/<모듈>/<작업>/)
```

## 작업 라우팅

활성·완료 작업 목록은 [`docs/tasks/작업 목록.md`](docs/tasks/작업 목록.md) 참조. 신규 작업 시 먼저 조회해 기존 폴더 이어가기 또는 신규 분리 결정.
```

(끝)

### 2.3 줄 수 비교

| 항목 | 현재 | 목표 |
|---|---|---|
| 총 줄 | 약 99 | 약 60 |
| 섹션 | 9 | 6 |
| 외부 의존 정보 | 3섹션 (대외·외부 의존·완성본 공유) | 0 |
| 절대 경로(`E:\Claude\`) | 1건 | 0 |

## 3. 검증 매핑

| 검증 | 본문 위치 |
|---|---|
| 검증_1 (외부 의존 3섹션 제거) | 신규 본문에 부재 |
| 검증_2 (Alert + 충돌 정책) | `> [!IMPORTANT]` 블록 |
| 검증_3 (루트 지침 참조 표 제거) | 신규 본문에 부재 |
| 검증_4 (list 진입점) | §작업 라우팅 |
| 검증_5 (상대 경로) | 모든 링크 `../../...` |
| 검증_6 (메타·기여·Layout 보존) | 해당 섹션 그대로 |
| 검증_7 (frontmatter+TL;DR) | 본문 최상단 |

## 4. 자체 검토 (단계 3)

### 4.1 지침 준수 여부

| 지침 | 준수 |
|---|---|
| 루트 `문서 작성 규칙.md` §9 frontmatter+TL;DR | ✅ 본 계획·신규 CLAUDE.md 모두 |
| 루트 §6.5 의미 한글 라벨 | ✅ `검증_1`~`검증_7` 매핑 |
| 루트 §6.3 Mermaid | ✅ §1 적용 절차 |
| 분석.md 결정 사항 반영 | ✅ 결정_1·2·3·4 모두 본문에 |

### 4.2 논리적 정합성

| 점검 | 결과 |
|---|---|
| 신규 본문이 요구사항 모두 만족하는가 | ✅ 검증 매핑 완전 |
| 줄 수 감소 vs 정보 손실 | ✅ 운영 정보(외부 의존)만 제거, 작업 컨텍스트는 보존 |
| 머지 절차 누락 없는가 | ✅ 적용 절차 ①~⑥ 완비 (stash pop·원 브랜치 복귀까지) |
| 루트 우선/프로젝트 우선 분리 모순 없는가 | ✅ CLAUDE.md ↔ CLAUDE.md = 루트 우선, `docs/지침/` ↔ `docs/지침/` = 프로젝트 우선 |

### 4.3 미해결 이슈

| 이슈 | 처리 |
|---|---|
| 없음 | 단계 4 진입 가능 |

## 자체 검토

### 지침 준수 여부
| 지침 | 준수 |
|---|---|
| [`작업 진행 규칙.md`](../../../../../../docs/지침/일반/작업%20진행%20규칙.md) | ✅ |
| [`문서 작성 규칙.md`](../../../../../../docs/지침/일반/문서%20작성%20규칙.md) | ✅ |

### 논리적 정합성
| 점검 | 결과 |
|---|---|
| 본문 기존 내용 보존 (양식 정합화 시 무손실) | ✅ |
| frontmatter·TL;DR 기준 충족 | ✅ |
| 관련 산출물(요구사항·분석·계획·이력) 상호 일관 | ✅ |

### 미해결 이슈
| 이슈 | 처리 |
|---|---|
| 해당 없음 | — |

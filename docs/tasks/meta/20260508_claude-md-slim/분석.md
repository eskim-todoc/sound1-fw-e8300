---
name: 분석 — Sound1 CLAUDE.md 슬림화 + 루트 강제 강화
purpose: 현행 CLAUDE.md 구조 매핑 + 변경 분류·결정
type: tasks/분석
applies_to: [Sound1]
tags: [meta, docs, claude-md, root-enforcement, slim]
---

# 분석: Sound1 CLAUDE.md 슬림화 + 루트 강제 강화

**TL;DR**: 현 Sound1 CLAUDE.md는 9개 섹션·약 99줄. 보존 4(헤더 강화·메타·기여 흐름·Layout), 제거 4(루트 지침 참조 표·대외 문서·외부 의존·완성본 공유), 슬림화 1(문서 작성 — 작업 목록.md 진입점만 보존). frontmatter 부재 → 추가. 절대 경로 1건 → 상대 통일.

## 1. 현행 구조 매핑

| 섹션 (현재 파일 라인) | 처리 | 사유 |
|---|---|---|
| 헤더 + 루트 참조 (L1~6) | **강화 (Alert)** | `> [!IMPORTANT]` + 충돌 시 루트 우선 명시 |
| `## 메타` (L8~13) | **보존** | Sound1 고유 repo·branch·담당 |
| `## 원본 기여 흐름` (L15~20) | **보존** | Sound1 고유 fork-and-PR 워크플로우 |
| `## Layout` (L22~40) | **보존 + 통합** | Eclipse 폴더 구조 보존, docs 한 줄 요약 추가 |
| `## 루트 지침 참조` 표 (L42~51) | **제거** | 루트 인덱스가 권위, 동기화 부담만 |
| `## 문서 작성 (프로젝트 고유)` (L53~98) | **슬림화** | tasks/작업 목록.md 진입점만 1~2줄 |
| └ §내부 작업 문서 (L55~72) | **슬림화** | 레이아웃 다이어그램 제거, list 링크만 |
| └ §대외 제출용 공식 문서 (L74~86) | **제거** | 회사 운영 규칙, 외부 의존 |
| §외부 의존 문서 (L88~92) | **제거** | 본인 작성 아님 |
| §완성본 공유 (L94~98) | **제거** | 운영 절차 |

## 2. 변경 분류

### 분석_1 — 추가

| 항목 | 내용 |
|---|---|
| frontmatter | name·purpose·type=메타·applies_to=[Sound1]·tags |
| TL;DR | "Sound1은 E8300 펌웨어 프로젝트. 루트 CLAUDE.md를 절대 우선으로 따름." 류 1줄 |
| `> [!IMPORTANT]` Alert | 강한 루트 강제 + 충돌 시 루트 우선 + `docs/지침/`은 분리 |

### 분석_2 — 보존

| 항목 | 위치 |
|---|---|
| §메타 | repo origin/upstream·branch·담당 (Sound1 고유 — fork-and-PR 사전 정보) |
| §원본 기여 흐름 | upstream PR 절차 (Sound1만의 흐름) |
| §Layout | Eclipse 폴더 구조 + src 모듈 구조 (Sound1 고유) |
| `tasks/작업 목록.md` 진입점 한 줄 | 작업 라우팅 첫 동작 |

### 분석_3 — 제거

| 항목 | 사유 |
|---|---|
| §루트 지침 참조 표 (4행) | 루트 CLAUDE.md 인덱스가 권위, 두 군데 동기화 부담 |
| §문서 작성 §내부 작업 문서 docs 다이어그램 | 표준 인스턴스, 폴더 ls로 확인 |
| §대외 제출용 공식 문서 (Dropbox) | 회사 운영 규칙, 펌웨어 작업 컨텍스트와 직교 |
| §외부 의존 문서 (IRS·IOVM·EPTC·EPTR) | 본인 작성 아님 |
| §완성본 공유 (Dropbox 수동 복사) | 운영 절차 |

### 분석_4 — 절대 경로 점검

`E:\Claude\` 또는 `E:\Claude\CLAUDE.md` 패턴 점검 결과: 헤더의 링크 텍스트(L3) **1건**. 본문 외 발생은 없음(URL은 모두 `../../...` 상대).

## 3. 결정 사항

### 결정_1 — `> [!IMPORTANT]` 본문 표현

```markdown
> [!IMPORTANT]
> **Sound1은 루트 [`../../CLAUDE.md`](../../CLAUDE.md)와 그 지침 체계를 절대 우선으로 따른다.** 본 파일은 Sound1 고유 정보(repo·메타·기여 흐름·폴더 구조)만 담고, 일반 규칙은 모두 루트에서 상속한다.
>
> **세션 시작 시**: ① 루트 `CLAUDE.md` 명시 Read → "루트 지침 확인 완료: <한 줄 요약>" 보고, ② 루트 [`docs/지침/작업 규칙.md`](../../docs/지침/작업%20규칙.md) 확인.
>
> **충돌 정책**:
> - 본 CLAUDE.md ↔ 루트 CLAUDE.md 충돌 시 → **루트 우선**. 본 파일이 잘못된 것으로 간주, 사용자에게 즉시 보고.
> - 본 프로젝트 `docs/지침/` ↔ 루트 `docs/지침/` 충돌 시 → 루트 `문서 작성 규칙.md §2.1`에 따라 **프로젝트 우선** (의도적 도메인 특화 예외).
```

### 결정_2 — §문서 작성 슬림화 형태

`tasks/작업 목록.md` 진입점 한 줄만 §Layout 또는 별도 섹션으로 흡수:

```markdown
## 작업 라우팅

활성·완료 작업 목록은 [`docs/tasks/작업 목록.md`](docs/tasks/작업 목록.md) 참조. 신규 작업 시 먼저 조회.
```

또는 §Layout 안 docs 줄에 같이 표기. **별도 §작업 라우팅** 으로 두는 게 발견성 ↑. 결정 = 별도 짧은 섹션.

### 결정_3 — Layout 갱신

현재 Layout의 `docs/` 줄을 다음으로 단순화 (서브폴더는 루트 지침과 ls로 확인):

```
└── docs/           SW 문서 — 루트 컨벤션 적용 (지침/·참고/·사용방법/·tasks/<모듈>/<작업>/)
```

### 결정_4 — frontmatter type 값

루트 `문서 작성 규칙.md §9.1` "type 값" 표에 `메타` 명시. CLAUDE.md(루트 진입점)는 `메타` 사용. Sound1 CLAUDE.md도 동일하게 `메타`.

## 4. 자체 검토 (단계 2)

### 4.1 지침 준수 여부

| 지침 | 준수 |
|---|---|
| 루트 `문서 작성 규칙.md` §9 frontmatter+TL;DR | ✅ |
| 루트 §6.5 의미 한글 라벨 | ✅ `분석_1`~`분석_4`, `결정_1`~`결정_4` |
| `요구사항.md` §4 검증 항목 매핑 | ✅ 검증_1(제거 3건)·검증_2(Alert)·검증_3(루트 지침 참조 표)·검증_4(list 진입점)·검증_5(상대 경로)·검증_6(보존)·검증_7(frontmatter) 모두 결정에 반영 |

### 4.2 논리적 정합성

| 점검 | 결과 |
|---|---|
| 보존·제거·슬림화 분류가 명확한가 | ✅ 분석_1·2·3 분리 |
| 결정이 미해결 이슈를 닫는가 | ✅ 요구사항 §5.3의 두 이슈 — 메타 안 Git 한 줄(보존, 결정 별개), frontmatter 추가(결정_4) |
| 사용자 의견과 정합 | ✅ "어때?" 질문에 yes 받은 보정 사항(작업 목록.md 진입점·CLAUDE.md 루트 우선·상대 경로) 모두 반영 |
| 모순·중복 | ✅ 없음 |

### 4.3 미해결 이슈

| 이슈 | 처리 |
|---|---|
| 없음 | 단계 3 진입 |

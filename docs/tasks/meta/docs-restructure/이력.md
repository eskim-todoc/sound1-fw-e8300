---
name: docs 폴더 구조 세분화 이력
purpose: Sound1 docs flat 구조 → 폴더 기반 + tasks 레지스트리 migration 결과 기록
type: 이력
tags: [meta, docs, convention, migration]
---

# Sound1 docs 폴더 구조 세분화 — 이력

**TL;DR**: 루트 폴더 기반+tasks 레지스트리 컨벤션을 Sound1에 동일 적용. flat+`[프리픽스]` 22파일(+이미지)을 참고/·기준/·사용방법/·tasks/<모듈>/<작업>/로 재편하고 머지 완료(`129212e`). 후속 작업들이 모두 본 구조를 따라 진행 중이며, 잔존 깨진 링크는 `meta/internal-links-fix`로 분리.

작업 슬러그: `meta/docs-restructure`
기간: 2026-04-24 (당일 완료, 사후 정리 2026-05-07)
상태: **완료**

---

## 1. 결과 요약

루트 repo의 폴더 기반 + tasks 레지스트리 컨벤션을 Sound1 에 동일 적용. 22 파일 (+ `.gitkeep` + 이미지) 이동 완료, 후속 작업들이 모두 이 구조를 따라 진행 중.

## 2. 변경 사항

### 2.1 구조 (Before / After)

**Before** (flat + `[카테고리]` 프리픽스):
- `[구현계획]` × 3, `[분석]` × 4, `[요구사항]` × 1, `[이력]` × 1
- `[참고]` × 7 (PDF 2 + .md 5), `[기준]` × 1, `[로그]` × 3 (.txt), 이미지 1

**After** (폴더 기반):
```
docs/
├── 지침/     프로젝트 특화 지침
├── 참고/     외부 스펙·도메인 (칩/, LED/, 터치/ 모듈 분할)
├── 사용방법/ UI 명령어 등
└── tasks/
    ├── list.md
    └── <모듈>/<작업슬러그>/{요구사항,분석,구현계획,진행상황,이력}.md
```

### 2.2 산출물

- 영속 문서 10건: `참고/`·`지침/`·`사용방법/` 으로 이동, 프리픽스·저자 suffix 제거
- 작업 문서 7건 (tracked) + 5건 (untracked): `tasks/<모듈>/<작업>/` 구조로 재편, 고정 파일명 적용
- `tasks/list.md` 신설 — 활성/완료/완료(추정) 분류 + 갱신 이력 누적
- `CLAUDE.md` 링크 갱신 (6 건) + 참고/LED 시스템 3 파일 상호참조 (8 건)

### 2.3 머지

`claude_feature_docs-restructure` → `claude_develop` 머지 완료 (commit `129212e`, 2026-04-24).

---

## 3. 알려진 tech debt

기존 작업 문서들이 다른 문서를 참조하는 markdown 링크 25+ 건이 구 경로·프리픽스 기반 URL 로 남아 있음. 콘텐츠는 정상이나 markdown preview 클릭 시 404. 후속 작업으로 분리: [`meta/internal-links-fix`](../internal-links-fix/).

---

## 4. 사후 정리 (2026-05-07)

작업 자체는 2026-04-24 commit `129212e` 머지로 종결됐으나 [`진행상황.md`](진행상황.md) 의 체크리스트 (`[ ] 커밋`, `[ ] 병합`) 가 미갱신 상태로 남아 [`tasks/list.md`](../../list.md) 활성 섹션에 잔존. 2026-05-07 정리 단계에서 진행상황.md 갱신 + 본 이력.md 작성 + list.md 활성→완료 이동.


---

## 시계열 로그 (구 진행상황.md 흡수, Task β 2026-05-15)

<details>
<summary>구 진행상황.md 본문 (frontmatter·헤더 포함)</summary>

---
name: 진행상황 — docs 폴더 구조 세분화
purpose: 폴더 migration 단계별 완료·잔여 항목·후속 작업 추적
type: tasks/진행상황
applies_to: [Sound1]
tags: [meta, docs, migration, progress]
---

# 진행상황: Sound1 docs 폴더 구조 세분화

**TL;DR**: 2026-04-24 당일 종결, 머지 commit `129212e`. 영속 10건+tracked 7건+untracked 5건 이동 완료, CLAUDE.md 링크 6건·LED 3개 파일 상호참조 8건 갱신. 후속 tech debt(구 경로 링크 25+건)는 `meta/internal-links-fix`로 분리 등재.

## 현재 상태 (2026-04-24 종결, 2026-05-07 정리 마무리)

**완료** — 파일 이동·재편·커밋·병합 모두 종결. 자세한 내용은 [`이력.md`](이력.md) 참조.

- 작업 위치: `E:\Claude\projects\Sound1` (메인 디렉토리, 브랜치 전환 방식)
- 브랜치: `claude_feature_docs-restructure` (base: `claude_develop`) → 머지 후 삭제됨
- 머지 commit: `129212e` (`claude_develop`)

## 완료 항목

### 구조 변경
- [x] 브랜치 `claude_feature_docs-restructure` 생성 (from `claude_develop`)
- [x] 새 폴더 구조 생성 (`참고/`, `기준/`, `사용방법/`, `tasks/<모듈>/<작업>/`)
- [x] **영속 문서 10건 이동**: 참고/·기준/·사용방법/ 폴더로 이동, 프리픽스·저자 suffix 제거
- [x] **작업 문서 7건 이동 (tracked)**: `tasks/<모듈>/<작업>/` 구조로 재편, 고정 파일명 (`요구사항.md`·`분석.md`·`구현계획.md`·`이력.md`) 적용
- [x] **작업 문서 5건 이동 (untracked)**: POWER_ON LED 요구사항·분석 (2건), 터치 로그 3건 — `mv` + `git add`로 staging

### 기획·레지스트리
- [x] `tasks/meta/docs-restructure/` 기획 문서 3종 작성 (요구사항·분석·구현계획)
- [x] `tasks/list.md` 생성 — 활성 2건 (본 작업, LED 조기 점등), 완료 1건 + 완료(추정) 3건
- [x] `진행상황.md` 작성 (이 문서)

## 남은 항목

- [x] Sound1 `CLAUDE.md` 링크 6건 업데이트
- [x] 참고/ 폴더 내 LED 시스템 3개 파일 상호참조 링크 8건 수정
- [x] 참고/LED 시스템 — 아키텍처 · 운용.md 내 `led_ind_state.JPG` 링크 경로 갱신
- [x] 커밋
- [x] `claude_develop` 병합 (`--no-ff`, commit `129212e`)

## 알려진 tech debt (follow-up 작업 `meta/internal-links-fix`로 등재)

기존 Sound1 task 문서들이 **다른 문서를 참조하는 상호 링크**가 **구 경로·프리픽스 기반 URL**로 남아 있다. 이번 migration에서 이동·재분류된 영향으로 다수 링크가 깨진 상태. 목록:

| 파일 | 깨진 링크 수 | 주요 참조 대상 |
|---|---|---|
| `tasks/LED/operation-scheme/분석.md` | 2 | `[이력]` LED 구현 결과, `[참고]` UI 명령어 |
| `tasks/LED/operation-scheme/이력.md` | 1+ | `[분석]` LED 운용 방식, Rev.5 변경 이력 텍스트 |
| `tasks/LED/power-on-early-lighting/분석.md` | 2 | `[요구사항]` POWER_ON LED, 루트 `[지침]` 분석·디버깅 규칙 |
| `tasks/LED/power-on-early-lighting/요구사항.md` | 3 | `[구현계획]` 터치 초기화, `[참고]` LED 아키텍처·Ezairo |
| `tasks/power/low-power-mode/구현계획.md` | 5 | `[분석]` 저전력 영향, `[참고]` Ezairo, `[기준]` 레지스터 검증 |
| `tasks/power/low-power-mode/분석.md` | 1 | `[참고]` Ezairo 클럭·타이머 |
| `tasks/touch/init-split/구현계획.md` | 4 | `[분석]` 터치·LED, `[참고]` IQS323, `[기준]` 레지스터 검증 |
| `tasks/touch/layer-separation/구현계획.md` | 5 | `[구현계획]` 터치 초기화, `[분석]` 터치, `[참고]` IQS323, 루트 `[지침]` 네이밍 |
| `참고/Ezairo 8300 클럭·타이머·I2C 스펙 정리.md` | 2 (text) | `[참고]` FW/HW Reference PDF (본문 내 언급) |
| `참고/터치센서 운용 방식.md` | 1 | `[참고]` IQS323 레지스터 레퍼런스 |

**총 25+ 건.**

이 링크들은 **파일 내용 손실 없이 URL 경로만 구 컨벤션 사용**하는 상태. 콘텐츠는 정상이지만 markdown preview 에서 클릭 시 404 가능. 후속 작업 `tasks/meta/internal-links-fix/`로 분리해 처리 예정.

## 기존 `claude_feature_early-power-on-led` 브랜치 처리

## 완료 상태 판정 추정 근거

기존 작업 중 `이력.md`가 없는 것들은 `list.md`에 **"완료(추정)"** 로 표기:

| 작업 | 이력 여부 | mtime | 판정 |
|---|---|---|---|
| LED operation-scheme | 有 | 2026-04-21 | 완료 (확정) |
| LED power-on-early-lighting | 無 | 2026-04-23 | 진행 (untracked + 최신) |
| power low-power-mode | 無 | 2026-04-22 | 완료(추정) |
| touch layer-separation | 無 | 2026-04-21 | 완료(추정) |
| touch init-split | 無 | 2026-04-21 | 완료(추정) — 로그 존재로 테스트까지 진행 추정 |

→ 사용자 확인 후 `list.md` 수동 정정 가능.

## 기존 `claude_feature_early-power-on-led` 브랜치 처리

현재 Sound1에 `claude_feature_early-power-on-led` 브랜치가 존재함. POWER_ON LED 작업을 위해 사용자님이 만든 브랜치인데, 본 migration에서 해당 untracked 파일들을 `tasks/LED/power-on-early-lighting/`로 이동했음.

**권고 처리:**
1. 본 migration이 `claude_develop`에 병합되면, `claude_feature_early-power-on-led` 브랜치는 사용하지 않아도 됨
2. 해당 브랜치는 사용자님이 나중에 삭제하거나, rebase/merge를 통해 `claude_develop` 최신 내용을 받아 사용
3. POWER_ON LED 작업 재개 시 `tasks/LED/power-on-early-lighting/` 폴더에서 `분석.md` 마무리 → `구현계획.md` 작성 → `이력.md` 기록 순으로 진행

## 완료 시그널 이후 처리

1. `git commit` — 브랜치 `claude_feature_docs-restructure`에 커밋
2. `git checkout claude_develop` 후 `git merge --no-ff ...`로 병합
3. 루트 repo의 `docs/tasks/list.md`에서 **Sound1 migration 상태 대기 → 완료**로 업데이트, 루트도 별도 커밋
4. (선택) `claude_feature_docs-restructure` 브랜치 삭제

## 체크리스트

- [x] 브랜치 생성
- [x] 파일 이동 22건 (17 tracked + 5 untracked)
- [x] 기획 문서 3종 + 레지스트리 + 진행상황
- [x] CLAUDE.md 링크 갱신
- [x] 검증
- [x] 커밋 + 병합 (commit `129212e`)


</details>


---

## 시계열 로그 (구 진행상황.md 흡수, Task β 2026-05-15)

<details>
<summary>구 진행상황.md 본문 (frontmatter·헤더 포함)</summary>

---
name: 진행상황 — docs 폴더 구조 세분화
purpose: 폴더 migration 단계별 완료·잔여 항목·후속 작업 추적
type: tasks/진행상황
applies_to: [Sound1]
tags: [meta, docs, migration, progress]
---

# 진행상황: Sound1 docs 폴더 구조 세분화

**TL;DR**: 2026-04-24 당일 종결, 머지 commit `129212e`. 영속 10건+tracked 7건+untracked 5건 이동 완료, CLAUDE.md 링크 6건·LED 3개 파일 상호참조 8건 갱신. 후속 tech debt(구 경로 링크 25+건)는 `meta/internal-links-fix`로 분리 등재.

## 현재 상태 (2026-04-24 종결, 2026-05-07 정리 마무리)

**완료** — 파일 이동·재편·커밋·병합 모두 종결. 자세한 내용은 [`이력.md`](이력.md) 참조.

- 작업 위치: `E:\Claude\projects\Sound1` (메인 디렉토리, 브랜치 전환 방식)
- 브랜치: `claude_feature_docs-restructure` (base: `claude_develop`) → 머지 후 삭제됨
- 머지 commit: `129212e` (`claude_develop`)

## 완료 항목

### 구조 변경
- [x] 브랜치 `claude_feature_docs-restructure` 생성 (from `claude_develop`)
- [x] 새 폴더 구조 생성 (`참고/`, `기준/`, `사용방법/`, `tasks/<모듈>/<작업>/`)
- [x] **영속 문서 10건 이동**: 참고/·기준/·사용방법/ 폴더로 이동, 프리픽스·저자 suffix 제거
- [x] **작업 문서 7건 이동 (tracked)**: `tasks/<모듈>/<작업>/` 구조로 재편, 고정 파일명 (`요구사항.md`·`분석.md`·`구현계획.md`·`이력.md`) 적용
- [x] **작업 문서 5건 이동 (untracked)**: POWER_ON LED 요구사항·분석 (2건), 터치 로그 3건 — `mv` + `git add`로 staging

### 기획·레지스트리
- [x] `tasks/meta/docs-restructure/` 기획 문서 3종 작성 (요구사항·분석·구현계획)
- [x] `tasks/list.md` 생성 — 활성 2건 (본 작업, LED 조기 점등), 완료 1건 + 완료(추정) 3건
- [x] `진행상황.md` 작성 (이 문서)

## 남은 항목

- [x] Sound1 `CLAUDE.md` 링크 6건 업데이트
- [x] 참고/ 폴더 내 LED 시스템 3개 파일 상호참조 링크 8건 수정
- [x] 참고/LED 시스템 — 아키텍처 · 운용.md 내 `led_ind_state.JPG` 링크 경로 갱신
- [x] 커밋
- [x] `claude_develop` 병합 (`--no-ff`, commit `129212e`)

## 알려진 tech debt (follow-up 작업 `meta/internal-links-fix`로 등재)

기존 Sound1 task 문서들이 **다른 문서를 참조하는 상호 링크**가 **구 경로·프리픽스 기반 URL**로 남아 있다. 이번 migration에서 이동·재분류된 영향으로 다수 링크가 깨진 상태. 목록:

| 파일 | 깨진 링크 수 | 주요 참조 대상 |
|---|---|---|
| `tasks/LED/operation-scheme/분석.md` | 2 | `[이력]` LED 구현 결과, `[참고]` UI 명령어 |
| `tasks/LED/operation-scheme/이력.md` | 1+ | `[분석]` LED 운용 방식, Rev.5 변경 이력 텍스트 |
| `tasks/LED/power-on-early-lighting/분석.md` | 2 | `[요구사항]` POWER_ON LED, 루트 `[지침]` 분석·디버깅 규칙 |
| `tasks/LED/power-on-early-lighting/요구사항.md` | 3 | `[구현계획]` 터치 초기화, `[참고]` LED 아키텍처·Ezairo |
| `tasks/power/low-power-mode/구현계획.md` | 5 | `[분석]` 저전력 영향, `[참고]` Ezairo, `[기준]` 레지스터 검증 |
| `tasks/power/low-power-mode/분석.md` | 1 | `[참고]` Ezairo 클럭·타이머 |
| `tasks/touch/init-split/구현계획.md` | 4 | `[분석]` 터치·LED, `[참고]` IQS323, `[기준]` 레지스터 검증 |
| `tasks/touch/layer-separation/구현계획.md` | 5 | `[구현계획]` 터치 초기화, `[분석]` 터치, `[참고]` IQS323, 루트 `[지침]` 네이밍 |
| `참고/Ezairo 8300 클럭·타이머·I2C 스펙 정리.md` | 2 (text) | `[참고]` FW/HW Reference PDF (본문 내 언급) |
| `참고/터치센서 운용 방식.md` | 1 | `[참고]` IQS323 레지스터 레퍼런스 |

**총 25+ 건.**

이 링크들은 **파일 내용 손실 없이 URL 경로만 구 컨벤션 사용**하는 상태. 콘텐츠는 정상이지만 markdown preview 에서 클릭 시 404 가능. 후속 작업 `tasks/meta/internal-links-fix/`로 분리해 처리 예정.

## 기존 `claude_feature_early-power-on-led` 브랜치 처리

## 완료 상태 판정 추정 근거

기존 작업 중 `이력.md`가 없는 것들은 `list.md`에 **"완료(추정)"** 로 표기:

| 작업 | 이력 여부 | mtime | 판정 |
|---|---|---|---|
| LED operation-scheme | 有 | 2026-04-21 | 완료 (확정) |
| LED power-on-early-lighting | 無 | 2026-04-23 | 진행 (untracked + 최신) |
| power low-power-mode | 無 | 2026-04-22 | 완료(추정) |
| touch layer-separation | 無 | 2026-04-21 | 완료(추정) |
| touch init-split | 無 | 2026-04-21 | 완료(추정) — 로그 존재로 테스트까지 진행 추정 |

→ 사용자 확인 후 `list.md` 수동 정정 가능.

## 기존 `claude_feature_early-power-on-led` 브랜치 처리

현재 Sound1에 `claude_feature_early-power-on-led` 브랜치가 존재함. POWER_ON LED 작업을 위해 사용자님이 만든 브랜치인데, 본 migration에서 해당 untracked 파일들을 `tasks/LED/power-on-early-lighting/`로 이동했음.

**권고 처리:**
1. 본 migration이 `claude_develop`에 병합되면, `claude_feature_early-power-on-led` 브랜치는 사용하지 않아도 됨
2. 해당 브랜치는 사용자님이 나중에 삭제하거나, rebase/merge를 통해 `claude_develop` 최신 내용을 받아 사용
3. POWER_ON LED 작업 재개 시 `tasks/LED/power-on-early-lighting/` 폴더에서 `분석.md` 마무리 → `구현계획.md` 작성 → `이력.md` 기록 순으로 진행

## 완료 시그널 이후 처리

1. `git commit` — 브랜치 `claude_feature_docs-restructure`에 커밋
2. `git checkout claude_develop` 후 `git merge --no-ff ...`로 병합
3. 루트 repo의 `docs/tasks/list.md`에서 **Sound1 migration 상태 대기 → 완료**로 업데이트, 루트도 별도 커밋
4. (선택) `claude_feature_docs-restructure` 브랜치 삭제

## 체크리스트

- [x] 브랜치 생성
- [x] 파일 이동 22건 (17 tracked + 5 untracked)
- [x] 기획 문서 3종 + 레지스트리 + 진행상황
- [x] CLAUDE.md 링크 갱신
- [x] 검증
- [x] 커밋 + 병합 (commit `129212e`)


</details>


---

## 시계열 로그 (구 진행상황.md 흡수, Task β 2026-05-15)

<details>
<summary>구 진행상황.md 본문 (frontmatter·헤더 포함)</summary>

---
name: 진행상황 — docs 폴더 구조 세분화
purpose: 폴더 migration 단계별 완료·잔여 항목·후속 작업 추적
type: tasks/진행상황
applies_to: [Sound1]
tags: [meta, docs, migration, progress]
---

# 진행상황: Sound1 docs 폴더 구조 세분화

**TL;DR**: 2026-04-24 당일 종결, 머지 commit `129212e`. 영속 10건+tracked 7건+untracked 5건 이동 완료, CLAUDE.md 링크 6건·LED 3개 파일 상호참조 8건 갱신. 후속 tech debt(구 경로 링크 25+건)는 `meta/internal-links-fix`로 분리 등재.

## 현재 상태 (2026-04-24 종결, 2026-05-07 정리 마무리)

**완료** — 파일 이동·재편·커밋·병합 모두 종결. 자세한 내용은 [`이력.md`](이력.md) 참조.

- 작업 위치: `E:\Claude\projects\Sound1` (메인 디렉토리, 브랜치 전환 방식)
- 브랜치: `claude_feature_docs-restructure` (base: `claude_develop`) → 머지 후 삭제됨
- 머지 commit: `129212e` (`claude_develop`)

## 완료 항목

### 구조 변경
- [x] 브랜치 `claude_feature_docs-restructure` 생성 (from `claude_develop`)
- [x] 새 폴더 구조 생성 (`참고/`, `기준/`, `사용방법/`, `tasks/<모듈>/<작업>/`)
- [x] **영속 문서 10건 이동**: 참고/·기준/·사용방법/ 폴더로 이동, 프리픽스·저자 suffix 제거
- [x] **작업 문서 7건 이동 (tracked)**: `tasks/<모듈>/<작업>/` 구조로 재편, 고정 파일명 (`요구사항.md`·`분석.md`·`구현계획.md`·`이력.md`) 적용
- [x] **작업 문서 5건 이동 (untracked)**: POWER_ON LED 요구사항·분석 (2건), 터치 로그 3건 — `mv` + `git add`로 staging

### 기획·레지스트리
- [x] `tasks/meta/docs-restructure/` 기획 문서 3종 작성 (요구사항·분석·구현계획)
- [x] `tasks/list.md` 생성 — 활성 2건 (본 작업, LED 조기 점등), 완료 1건 + 완료(추정) 3건
- [x] `진행상황.md` 작성 (이 문서)

## 남은 항목

- [x] Sound1 `CLAUDE.md` 링크 6건 업데이트
- [x] 참고/ 폴더 내 LED 시스템 3개 파일 상호참조 링크 8건 수정
- [x] 참고/LED 시스템 — 아키텍처 · 운용.md 내 `led_ind_state.JPG` 링크 경로 갱신
- [x] 커밋
- [x] `claude_develop` 병합 (`--no-ff`, commit `129212e`)

## 알려진 tech debt (follow-up 작업 `meta/internal-links-fix`로 등재)

기존 Sound1 task 문서들이 **다른 문서를 참조하는 상호 링크**가 **구 경로·프리픽스 기반 URL**로 남아 있다. 이번 migration에서 이동·재분류된 영향으로 다수 링크가 깨진 상태. 목록:

| 파일 | 깨진 링크 수 | 주요 참조 대상 |
|---|---|---|
| `tasks/LED/operation-scheme/분석.md` | 2 | `[이력]` LED 구현 결과, `[참고]` UI 명령어 |
| `tasks/LED/operation-scheme/이력.md` | 1+ | `[분석]` LED 운용 방식, Rev.5 변경 이력 텍스트 |
| `tasks/LED/power-on-early-lighting/분석.md` | 2 | `[요구사항]` POWER_ON LED, 루트 `[지침]` 분석·디버깅 규칙 |
| `tasks/LED/power-on-early-lighting/요구사항.md` | 3 | `[구현계획]` 터치 초기화, `[참고]` LED 아키텍처·Ezairo |
| `tasks/power/low-power-mode/구현계획.md` | 5 | `[분석]` 저전력 영향, `[참고]` Ezairo, `[기준]` 레지스터 검증 |
| `tasks/power/low-power-mode/분석.md` | 1 | `[참고]` Ezairo 클럭·타이머 |
| `tasks/touch/init-split/구현계획.md` | 4 | `[분석]` 터치·LED, `[참고]` IQS323, `[기준]` 레지스터 검증 |
| `tasks/touch/layer-separation/구현계획.md` | 5 | `[구현계획]` 터치 초기화, `[분석]` 터치, `[참고]` IQS323, 루트 `[지침]` 네이밍 |
| `참고/Ezairo 8300 클럭·타이머·I2C 스펙 정리.md` | 2 (text) | `[참고]` FW/HW Reference PDF (본문 내 언급) |
| `참고/터치센서 운용 방식.md` | 1 | `[참고]` IQS323 레지스터 레퍼런스 |

**총 25+ 건.**

이 링크들은 **파일 내용 손실 없이 URL 경로만 구 컨벤션 사용**하는 상태. 콘텐츠는 정상이지만 markdown preview 에서 클릭 시 404 가능. 후속 작업 `tasks/meta/internal-links-fix/`로 분리해 처리 예정.

## 기존 `claude_feature_early-power-on-led` 브랜치 처리

## 완료 상태 판정 추정 근거

기존 작업 중 `이력.md`가 없는 것들은 `list.md`에 **"완료(추정)"** 로 표기:

| 작업 | 이력 여부 | mtime | 판정 |
|---|---|---|---|
| LED operation-scheme | 有 | 2026-04-21 | 완료 (확정) |
| LED power-on-early-lighting | 無 | 2026-04-23 | 진행 (untracked + 최신) |
| power low-power-mode | 無 | 2026-04-22 | 완료(추정) |
| touch layer-separation | 無 | 2026-04-21 | 완료(추정) |
| touch init-split | 無 | 2026-04-21 | 완료(추정) — 로그 존재로 테스트까지 진행 추정 |

→ 사용자 확인 후 `list.md` 수동 정정 가능.

## 기존 `claude_feature_early-power-on-led` 브랜치 처리

현재 Sound1에 `claude_feature_early-power-on-led` 브랜치가 존재함. POWER_ON LED 작업을 위해 사용자님이 만든 브랜치인데, 본 migration에서 해당 untracked 파일들을 `tasks/LED/power-on-early-lighting/`로 이동했음.

**권고 처리:**
1. 본 migration이 `claude_develop`에 병합되면, `claude_feature_early-power-on-led` 브랜치는 사용하지 않아도 됨
2. 해당 브랜치는 사용자님이 나중에 삭제하거나, rebase/merge를 통해 `claude_develop` 최신 내용을 받아 사용
3. POWER_ON LED 작업 재개 시 `tasks/LED/power-on-early-lighting/` 폴더에서 `분석.md` 마무리 → `구현계획.md` 작성 → `이력.md` 기록 순으로 진행

## 완료 시그널 이후 처리

1. `git commit` — 브랜치 `claude_feature_docs-restructure`에 커밋
2. `git checkout claude_develop` 후 `git merge --no-ff ...`로 병합
3. 루트 repo의 `docs/tasks/list.md`에서 **Sound1 migration 상태 대기 → 완료**로 업데이트, 루트도 별도 커밋
4. (선택) `claude_feature_docs-restructure` 브랜치 삭제

## 체크리스트

- [x] 브랜치 생성
- [x] 파일 이동 22건 (17 tracked + 5 untracked)
- [x] 기획 문서 3종 + 레지스트리 + 진행상황
- [x] CLAUDE.md 링크 갱신
- [x] 검증
- [x] 커밋 + 병합 (commit `129212e`)


</details>

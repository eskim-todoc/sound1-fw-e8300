---
name: Sound1 docs 폴더별 README 추가 — 계획
purpose: README 4개 작성 순서·내용 골격·완료 처리
type: tasks/계획
applies_to: [Sound1]
status: 진행
---

# 계획

**TL;DR**: README 4개 (지침/·사용방법/·참고/·tasks/) 작성 → 커밋 → `claude_develop` 병합 → 본 task archive·list.md 갱신. 모든 README는 frontmatter+TL;DR+인덱스 표 형식 통일. 약 4 파일 신규 작성 + 5 task 산출물.

## 1. 작성 순서

| # | 파일 | 본문 골격 |
|---|---|---|
| 계획_1 | `docs/지침/README.md` | frontmatter + TL;DR + 폴더 목적 + "전역 지침은 루트 참조" 안내 + 1행 인덱스 표 |
| 계획_2 | `docs/사용방법/README.md` | frontmatter + TL;DR + 폴더 목적 + 1행 인덱스 표 |
| 계획_3 | `docs/참고/README.md` | frontmatter + TL;DR + 모듈 서브폴더(칩/·LED/·터치/) 인덱스 표 + 각 서브폴더 진입 안내 |
| 계획_4 | `docs/tasks/README.md` | frontmatter + TL;DR + 폴더 구조(`list.md`·활성·`_archive/`) + 새 작업 절차 + 완료 절차 (루트 `tasks/README.md` 정신 따름, Sound1 특화) |

## 2. 본문 작성 원칙

- 모든 표는 GFM pipe table
- 한글 경로 링크는 `%20` 인코딩
- "사용 시점" 컬럼은 명시적 트리거 위주(예: "I2C 레지스터 변경 시", "ENABLE_UI_CMD 빌드 시")
- TL;DR은 폴더 한 줄 요약 + 인덱스 진입 안내

## 3. 완료 처리 절차

1. `이력 및 결과.md` 작성
2. `tasks/meta/20260507_folder-readmes/` → `tasks/_archive/meta/20260507_folder-readmes/` 이동
3. `list.md` 활성 → 완료 이동, 폴더 링크 갱신, 갱신 이력 1줄
4. 커밋 → `claude_develop` 체크아웃 + `--no-ff` 병합

## 4. 자체 검토

| 점검 | 결과 |
|---|---|
| 작성 4개로 §9.3 필수 충족 | ✅ |
| 사용자 일괄 승인 적용 (루트 task의 yes) | ✅ — 단계 ④ 즉시 진입 |
| 다른 문서 링크 영향 | ✅ 없음 (신규 파일만 추가) |

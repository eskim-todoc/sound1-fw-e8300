---
name: 이력 — service 모드 RTT 디버그 콘솔
purpose: storage init 헬퍼 추출·UART 디버그 폐기·service RTT 콘솔·sound1-fw-extractor 명세 출력 적용 결과 기록
type: tasks/이력
applies_to: [Sound1]
maturity: stable
tags: [bootloader, service, rtt, debug-console, sound1-fw-extractor, history]
---

# service 모드 RTT 디버그 콘솔 — 이력

**TL;DR**: 2026-05-07 요구사항 작성 시작 ~ 2026-05-08 검증·머지 완료. 부트로더 UART 디버그 모드(`tdc_boot_debug_mode` 등 4 함수, ~140 줄) 폐기 + storage init 시퀀스를 `tdc_boot_storage_init()` 헬퍼로 추출 + service 모드에 RTT 디버그 콘솔 신설. 출력 형식은 외부 도구 [`sound1-fw-extractor`](../../../../../sound1-fw-extractor) 명세 준수 — `dump` 단일 명령으로 4 파일 (`MANIFEST.TXT` + `APP000~002.FEZ`) 일괄 hex 덤프 → fw.txt 캡처 → Python 도구가 4 바이너리 자동 추출. RTT up 채널은 service 모드 한정 BLOCK_IF_FIFO_FULL 전환 (drop 0 보장). 사용자 extractor 추출 통과 (`MANIFEST.TXT` 45 B + `APP000~002.FEZ` 178336/9476/1280 B + 경고 0).

작업 슬러그: `bootloader/20260507_service-rtt-debug-console`
브랜치: `claude_feature_service-rtt-debug-console` (base: `claude_develop`)
기간: 2026-05-07 (요구사항 작성) ~ 2026-05-08 (검증·머지)
상태: **완료** (사용자 검증 통과)

---

## 1. 결과 요약

- **사용자 검증 통과** (2026-05-08):
  - 검증_1: 결정_6 BLOCK 모드 동작 정상 — "블록 방식으로 동작 잘 하는거 같아"
  - 검증_2: 결정_5 출력 형식 + sound1-fw-extractor 추출 정상 — `dump` → fw.txt → extractor 실행:
    ```
    [생성] out/MANIFEST.TXT (45 바이트)
    [생성] out/APP000.FEZ (178336 바이트)
    [생성] out/APP001.FEZ (9476 바이트)
    [생성] out/APP002.FEZ (1280 바이트)
    [완료] 4개 파일 추출.
    ```
    `[경고]` 0
  - 일반 부팅 / LED tick / reboot / 사이즈 모두 OK
- **수용 기준 통과**: 수용_1 (`dump` 4 파일 일괄 hex), 수용_2 (extractor 4 파일 추출), 수용_3 (`reboot` 워치독 리셋), 수용_4 (LED tick), 수용_5 (부트로더 회귀 없음), 수용_6 (사이즈 ≤ +100 B), 수용_7 (auto-rtt-viewer 호환), 수용_8 (BLOCK 모드 drop 0)
- **외부 인터페이스 합의**: [`sound1-fw-extractor` 명세](../../../../../sound1-fw-extractor/docs/사용방법/Sound1%20측%20fw.txt%20출력%20명세.md) §4·§5·§6 형식 준수 — Sound1 측 dump 함수 = 외부 도구 입력 명세 그대로 구현

---

## 2. 변경 사항

### 2.1 파일

| 파일 | 분류 | 변경 |
|---|---|---|
| [`bootloader.c`](../../../../src/0__bootloader/source/bootloader.c) | 수정 | storage init 시퀀스 헬퍼 추출 + `tdc_boot_get_ohdl()` getter + UART 디버그 호출 1줄 삭제 + L1404 doxygen 주석 fix |
| [`ci_boot.c`](../../../../src/0__bootloader/source/bootmgr/ci_boot.c) | 수정 | UART 디버그 모드 4 함수 + 정적 선언 삭제 (~140 줄) |
| [`ci_boot.h`](../../../../src/0__bootloader/include/bootmgr/ci_boot.h) | 수정 | `tdc_boot_storage_init` + `tdc_boot_get_ohdl` 선언 추가, `tdc_boot_debug_mode` 선언 삭제, `<bootloader_internal.h>` include → forward declaration 으로 단순화 |
| [`service.c`](../../../../src/0__bootloader/source/service/service.c) | 수정 (대규모) | LED 8색 순회 폐기 + `tdc_boot_storage_init(NULL)` 호출 + RTT 디버그 콘솔 + LED tick. 출력 형식은 sound1-fw-extractor 명세 준수 (마커 + 16 B/라인 + LF only). RTT BLOCK 모드 전환. 명령 = `dump` + `reboot` 2 개 |

### 2.2 Commit (시간 순)

| Hash | 분류 | 내용 |
|---|---|---|
| `6e95ad5` | docs | task 산출물 (요구사항 / 분석 / 구현계획 / 진행상황) Rev.4 + 작업 목록.md 갱신 |
| `bf1d269` | refactor | Step 1 — storage init 시퀀스를 `tdc_boot_storage_init()` 헬퍼로 추출 + `tdc_boot_get_ohdl()` getter 추가 |
| `4984f49` | refactor | Step 2 — UART 디버그 모드 4 함수 폐기 (`tdc_boot_debug_mode` / `_handle_debug_mode` / `_print_fw_file` / `_count_down`) |
| `89c6969` | feat | Step 3 — service.c RTT 디버그 콘솔 + LED tick (Rev.4 시점, `manifest`/`app0~2`/`reboot` 5 명령) |
| `40294e4` | docs | compact 전 진행상황 + 테스트 항목 + 머지 절차 정리 |
| `9d8b42f` | fix | Step 4 — doxygen 주석 안 `*/` 시퀀스 빌드 에러 (bootloader.c L1404) + ci_boot.h transitive 헤더 의존 분리 |
| `0f3b174` | feat | Step 5 — 출력 형식 sound1-fw-extractor 명세 준수 (4 파일 일괄 dump, 마커 형식, 16 B/라인, LF only) + service 모드 RTT BLOCK_IF_FIFO_FULL 전환. 명령 5 → 2 개 통합 (`dump` + `reboot`). 문서 Rev.5 |

병합: `--no-ff` (Merge commit 별도)

---

## 3. 주요 설계 결정

| ID | 결정 | 근거 |
|---|---|---|
| 결정_1 | 부트로더 UART 디버그 모드 코드 폐기 | 사용자 미사용. 사이즈 절약 (~-300 B) |
| 결정_2 | `bootloader_boot()` storage init 부분 (NVMInit ~ NVMSync) 을 `tdc_boot_storage_init()` 헬퍼로 추출, 부트로더·service 양쪽 호출 | 사이즈 우선, 1 벌만 존재. service 가 NVM/FATFS init 누락 해소 |
| 결정_3 | service LED 8 색 순회 루프 폐기. 디버그 명령 처리 루프 안에서 백그라운드 tick (380 iter ≈ 380 ms) | 정밀 무관, 단순 카운트로 코드 사이즈 최소 |
| 결정_4 | 헬퍼 책임 = NVM/FATFS init 만. `snd_boot_*` / `tdc_boot_print_boot_file` (OTA DFU 이미지 슬롯 선택) 은 부트로더만 호출 (service 미해당). `tdc_boot_get_ohdl()` getter 로 service 의 `_print_fw_file` 이 부트로더 정적 변수 `ohdl` 재사용 (별도 fp 변수 폐기) | service 는 슬롯 처리 자체 불필요. 사이즈 절약 |
| 결정_5 | 출력 형식 = [`sound1-fw-extractor` 명세](../../../../../sound1-fw-extractor/docs/사용방법/Sound1%20측%20fw.txt%20출력%20명세.md) 준수. 5 명령 → 단일 `dump` (4 파일 일괄). 마커 = 데이터 영역 (`========== FW DUMP BEGIN/END ==========`) + 파일 (`:::FILE:<NAME>:BEGIN/END:::`). 데이터 라인 = `%02X ` × 16 B + `\n`. 데이터 영역 안 LF only | 외부 Python 도구 자동 파싱 → 4 바이너리 자동 추출. 명령 5 → 2 통합으로 사이즈 절약 |
| 결정_6 | service 모드 한정 RTT up 채널 0 모드 = `SEGGER_RTT_MODE_BLOCK_IF_FIFO_FULL` (`SEGGER_RTT_SetFlagsUpBuffer(0, ...)` 진입 첫 줄). 부트로더 본체 무관 | 기본 NO_BLOCK_SKIP 은 1024 B buffer 가득 시 호출 통째 drop. dump (~52 B / 16 B 청크) 가 host (auto-rtt-viewer 10 ms polling, ~100 B/ms drain) 보다 빨리 누적 → drop. BLOCK 모드는 자동 backpressure (인터랙티브 콘솔이라 stall 허용) |

`reboot` 동작: `[SERVICE] reboot requested. Resetting in 500ms...` 출력 → 500 ms LED tick + watchdog refresh 유지 (RTT flush 보장) → `SYS_WATCHDOG_RESET()` 직접 호출 → 즉시 HW reset → `main()` 재진입.

---

## 4. 외부 도구 협업 패턴 (학습)

본 작업은 **외부 Python 도구 ([`sound1-fw-extractor`](../../../../../sound1-fw-extractor)) 와 인터페이스 명세를 공유**하는 형태로 진행. 학습 포인트:

- **명세 우선**: 도구 측이 먼저 입력 명세 (`docs/사용방법/Sound1 측 fw.txt 출력 명세.md`) 를 작성하고, Sound1 측이 그 명세를 참조해 출력 함수 구현
- **마커 충돌 검증**: 명세가 `:::` / `==========` 패턴을 제어 마커로 정의하므로, Sound1 코드베이스 grep 으로 충돌 가능성 사전 검증 — 결과: `==========` 6 곳 사용 중이지만 모두 `tdc_uart_printf` (UART) 라 RTT 무관 ✅
- **검증 자동화**: extractor 가 `[경고]` 메시지로 마커 짝맞춤 / 누락 파일 자동 검증 → 사용자가 `python src/main.py` 한 번으로 수용_2 통과 확인
- **줄바꿈 정책 분리**: 데이터 영역 안 LF only (extractor 정규식 호환) / 데이터 영역 밖 CRLF (viewer 가독성) — 영역별 분리 정책으로 양쪽 모두 만족

---

## 5. 빌드 에러 fix — doxygen 주석 안 `*/` 시퀀스 (학습 큰 사건)

### 5.1 현상

Step 1~3 commit 후 사용자 빌드: 빌드 에러. 사용자 진단:
> "1398라인부터 보면 문법에 어긋나"

### 5.2 원인

[`bootloader.c:1404`](../../../../src/0__bootloader/source/bootloader.c) doxygen 주석 안에:

```c
/**
 * ...
 * 책임 외: LED 가드, app_name 결정, OTA 슬롯 선택(snd_boot_*/tdc_boot_print_boot_file),
 *                                                            ^^^
 *                                          이 '*/' 가 주석을 일찍 닫음
 * ...
 */
```

`snd_boot_*/tdc_boot_print_boot_file` 의 **`*/` 시퀀스가 주석 종료 토큰** 으로 해석 → 이후 한글 + 함수 시그니처가 모두 코드로 파싱 → 줄줄이 syntax error.

### 5.3 fix

`*` 와 `/` 사이 공백 1 칸 삽입: `snd_boot_* / tdc_boot_print_boot_file`. 가독성 동일, 컴파일러 토큰 분리.

부수 fix: `ci_boot.h` 의 `<bootloader_internal.h>` include → `struct _bootloader_boot_information;` forward declaration 으로 대체. transitive 헤더 의존 단순화 — ci_boot.c / service.c 다른 컴파일 단위에서 aes.h / bootloader_application_file.h 등 부수 의존 충돌 회피.

### 5.4 학습 (회고 등재 완료)

루트 [`docs/회고/코딩.md`](../../../../../../docs/회고/코딩.md) "doxygen 주석 안 `*/` 시퀀스 금지" 패턴 추가 (별도 commit `93778ac`). 다음 같은 실수 재발 방지.

---

## 6. RTT NO_BLOCK_SKIP drop 동작 (학습)

큰 파일 dump 중 데이터 누락 우려 → SEGGER RTT 동작 정밀 분석 결과:

- **NO_BLOCK_SKIP 모드 (기본)**: buffer 가득 시 **write 호출 통째 drop** ([`SEGGER_RTT.c:1144~1148`](../../../../src/0__bootloader/source/segger_rtt/SEGGER_RTT.c) `Avail < NumBytes → Status = 0`). 부분 drop 아님
- **버퍼 1024 B**, **호출당 최대 64 B** (vsnprintf), **호스트 polling 10 ms** (auto-rtt-viewer)
- 청크 당 194 B (64 hex) 또는 52 B (16 hex) 가 polling 사이 누적 → buffer 임계값 초과 시 drop

**대안 비교**:

| 옵션 | 평가 |
|---|---|
| chunk 당 5 ms delay | 출력 시간 ~1.6 s 증가 (10 KB 기준), 마진 60% |
| **선택**: BLOCK_IF_FIFO_FULL 전환 | drop 자체 원천 차단 (CPU stall 자동 backpressure). 코드 1 줄. 부트로더 본체 무관 |
| 전역 BLOCK 모드 | 부트로더 본체 RTT 출력도 stall — 부팅 시간 영향 우려 → 반려 |

`SEGGER_RTT_SetFlagsUpBuffer(0, SEGGER_RTT_MODE_BLOCK_IF_FIFO_FULL)` 한 줄로 service 모드 한정 적용. 사용자 검증 통과.

---

## 7. 호출자 영향

| 위치 | 영향 |
|---|---|
| `bootloader_main()` → `bootloader_boot()` | storage init 부분이 `tdc_boot_storage_init(&boot_info)` 호출로 단순화. app_name 결정 / LED 가드 / OTA 슬롯 선택 (`snd_boot_*` + `tdc_boot_print_boot_file`) 은 그대로 부트로더에 남음. 동작 동일 |
| `bootloader_main()` 직전 `tdc_boot_debug_mode()` 호출 1 줄 | 삭제. `CI_BOOT_COUNT_DOWN_SEC=0` 이라 카운트다운 즉시 통과 + `\n` 미입력 시 디버그 모드 미진입 → 기존에도 사실상 미사용. 영향 없음 |
| `service_main()` | LED 8 색 순회 루프 폐기 → RTT BLOCK 모드 전환 + `tdc_boot_storage_init(NULL)` + RTT 콘솔 + LED tick 으로 전체 재작성 |
| `ci_boot.h` 사용처 (ci_boot.c, service.c) | 헤더 의존 충돌 해소 (forward declaration). 다른 호출처 영향 없음 |
| 부트로더 부팅 시퀀스 (CFX/CM3 boot, FATFS, NVM) | 영향 없음 — 코드 이동만 (수용_5 통과) |

---

## 8. 후속 작업

- `claude_main` 머지 — 사용자 추가 승인 시. 본 시점은 `claude_develop` 머지까지 완료
- service 모드 추가 명령 (예: 메모리 dump, NVM register read 등) — 필요 시 별도 task

---

## 9. 참고

- [요구사항.md](요구사항.md) — Rev.0 ~ Rev.5
- [분석.md](분석.md) — Rev.0 ~ Rev.5
- [구현계획.md](구현계획.md) — Rev.0 ~ Rev.5
- [진행상황.md](진행상황.md) — 단계 진척 / 결정 누적 / compact 전·후 진행 사항 / 사용자 검증 결과
- 외부 인터페이스 명세: [`sound1-fw-extractor` Sound1 측 fw.txt 출력 명세.md](../../../../../sound1-fw-extractor/docs/사용방법/Sound1%20측%20fw.txt%20출력%20명세.md)
- 외부 추출 도구: [`sound1-fw-extractor`](../../../../../sound1-fw-extractor) (Python)
- 호스트 측 RTT viewer: [`auto-rtt-viewer`](../../../../../auto-rtt-viewer)
- 회고: [`docs/회고/코딩.md`](../../../../../../docs/회고/코딩.md) "doxygen 주석 안 `*/` 시퀀스 금지" 패턴


---

## 시계열 로그 (구 진행상황.md 흡수, Task β 2026-05-15)

<details>
<summary>구 진행상황.md 본문 (frontmatter·헤더 포함)</summary>

---
name: 진행상황 — service 모드 RTT 디버그 콘솔
purpose: 단계 진행·이슈·다음 동작 추적
type: tasks/진행상황
applies_to: [Sound1]
tags: [bootloader, service, rtt, debug-console, progress]
---

# 진행상황: service 모드 RTT 디버그 콘솔

**TL;DR**: 단계 ④ 구현 진행 중 — Step 1~3 commit 완료, BLOCK 모드 검증 통과. **Step 5 (파일 출력 형식 변경) 코드 적용 완료** (working tree, 미커밋): sound1-fw-extractor 명세 (`projects/sound1-fw-extractor/docs/사용방법/Sound1 측 fw.txt 출력 명세.md`) 기반. 5 명령 → 2 명령 (`dump`/`reboot`) + 마커 형식 (`========== FW DUMP BEGIN/END ==========` + `:::FILE:<NAME>:BEGIN/END:::`) + 16 B/라인 LF only. 사용자 빌드 + 동작 검증 (auto-rtt-viewer 캡처 → fw.txt → sound1-fw-extractor 추출) 대기.

## 단계 진척

| 단계 | 산출물 | 상태 | 일시 |
|---|---|---|---|
| ① 요구사항 | [요구사항.md](요구사항.md) Rev.4 | 사용자 피드백 4차 반영, 재검토 대기 | 2026-05-07 |
| ② 분석 | [분석.md](분석.md) Rev.4 | 사용자 피드백 4차 반영, 재검토 대기 | 2026-05-07 |
| ③ 구현계획 | [구현계획.md](구현계획.md) Rev.4 | 사용자 피드백 4차 반영, 재검토 대기 | 2026-05-07 |
| ④ 구현 | Step 1 commit `bf1d269`, Step 2 `4984f49`, Step 3 `89c6969`, fix + Step 5 (working tree, 예정) | Step 1~3 빌드 OK, BLOCK 검증 통과. Step 5 (출력 형식) 코드 적용 — 사용자 빌드 + extractor 검증 대기 | 2026-05-08 |

## 결정 누적

- (사용자 승인) 결정_1: 부트로더 UART 디버그 모드 코드 폐기. service에서만 RTT 디버그 콘솔 운영
- (사용자 승인) 결정_2: 코드 사이즈 우선 — `bootloader_boot()`의 storage init 부분을 헬퍼로 추출, 부트로더·service 양쪽이 호출
- (자동) 결정_3: `tdc_boot_print_boot_file()`은 storage init 헬퍼 안에 포함 (양쪽 자동 출력)
- (사용자 피드백 2026-05-07 1차) 결정_4: 명령어 이름 `--quit`/`--print=mf`/`--print=a0~2` → `reboot`/`manifest`/`app0~2`로 변경 (요구사항.md §4 수용_1~3)
- (사용자 피드백 2026-05-07 2차) 결정_5: `reboot` 명령 후 동작 = `SYS_WATCHDOG_RESET()` 직접 호출 + 500 ms 사전 delay (RTT 출력 버퍼 flush 보장 — 출력 중이던 hex 덤프 메시지 손실 방지) (분석.md §2.5)
- (사용자 확정 2026-05-07 2차) 결정_6: auto-rtt-viewer `write.line_terminator` = `\n` 확정 → 위험_2 해소 (분석.md §2.7)
- (사용자 피드백 2026-05-07 3차) 결정_7: 헬퍼 함수 책임 축소. `snd_boot_set_fp` / `snd_boot_handle_file` / `tdc_boot_print_boot_file` 은 OTA DFU 이미지 슬롯 선택 영역이라 `tdc_boot_storage_init()` 외부에서 호출 (`bootloader_boot()` 은 `bootloader_load_app_file()` 직전, `service.c` 는 storage init 직후). ohdl 접근은 신규 `tdc_boot_get_ohdl()` getter 경유 (분석.md §2.2)
- (사용자 피드백 2026-05-07 4차) 결정_8: 결정_7 수정 — service 는 슬롯 처리 자체 불필요라 `snd_boot_set_fp` / `snd_boot_handle_file` / `tdc_boot_print_boot_file` 모두 호출 안 함. `tdc_boot_get_ohdl()` getter 는 service 의 `_print_fw_file` 이 부트로더 정적 변수 `ohdl` 을 재사용용 (별도 `s_tdc_service_fp` 변수 폐기, 사이즈 절약). 부트로더는 같은 파일 내 정적 변수라 `&ohdl` 직접 사용 (분석.md §2.2)
- (구현 단계 2026-05-07) 구현_1: Step 1 (storage init 헬퍼 추출 + ohdl getter), Step 2 (UART 디버그 모드 4 함수 폐기), Step 3 (service.c RTT 디버그 콘솔) commit. LED 가드 위치가 `bootloader_load_manu_table` 직후 → `NVMSync` + app_name 결정 후로 ~수십 ms 늦춰짐 (LED/20260427_bootloader-power-on-indicator AC-1 측정 검증 필요)
- (fix 단계 2026-05-07) 구현_2: 빌드 에러 fix — bootloader.c L1404 doxygen 주석 안 `snd_boot_*/...` 의 `*/` 가 주석을 일찍 닫음. `*` 와 `/` 사이 공백 1 칸 추가. + ci_boot.h 의 `<bootloader_internal.h>` include 를 `struct _bootloader_boot_information;` forward declaration 으로 대체 (transitive 헤더 의존 단순화). 회고 [`docs/회고/코딩.md`](../../../../../docs/회고/코딩.md) "doxygen 주석 안 `*/` 시퀀스 금지" 추가
- (사용자 승인 2026-05-08) 결정_9: service 모드 한정 RTT up 채널 0 모드를 `SEGGER_RTT_MODE_BLOCK_IF_FIFO_FULL` 로 전환. 근거 — 기본 `NO_BLOCK_SKIP` 은 buffer (1024 B) 가득 시 write 호출 통째 drop. 파일 hex 덤프 (`%02X ` × 64 + `\r\n` = 194 B / 64 B 청크) 가 host drain (auto-rtt-viewer 10 ms polling, 약 100 B/ms) 보다 빨라 chunk 누락 가능. BLOCK 모드는 CPU stall 로 자동 backpressure (인터랙티브 콘솔이라 stall 허용). 적용 위치 = `service_main()` 진입 첫 줄 (LED init 전). 부트로더 본체는 service_main 진입 안 하므로 영향 없음. delay 추가 방식 대비 코드 단순 + drop 자체 원천 차단
- (사용자 검증 2026-05-08) 검증_1: 결정_9 BLOCK 모드 동작 정상 확인 — "블록 방식으로 동작 잘 하는거 같아"
- (사용자 검증 2026-05-08) 검증_2: 결정_10/11 출력 형식 + sound1-fw-extractor 추출 정상 확인 — `dump` 명령 → fw.txt 캡처 → extractor 실행 결과 4 파일 정상 추출 (`MANIFEST.TXT` 45 B, `APP000.FEZ` 178336 B, `APP001.FEZ` 9476 B, `APP002.FEZ` 1280 B), `[완료] 4개 파일 추출.` 메시지 출력, `[경고]` 0. 수용_1·수용_2·수용_8 통과
- (사용자 확정 2026-05-08) 결정_10: 파일 출력 방식 변경 — 4 파일 (MANIFEST.TXT + APP000~002.FEZ) 단일 명령 일괄 출력. 형식 = [`sound1-fw-extractor` 명세](../../../../../sound1-fw-extractor/docs/사용방법/Sound1%20측%20fw.txt%20출력%20명세.md) 준수: 데이터 영역 마커 `========== FW DUMP BEGIN/END ==========`, 파일 마커 `:::FILE:<NAME>:BEGIN/END:::`, 데이터 영역 안 줄바꿈 LF only. 외부 프로그램 (sound1-fw-extractor Python 도구) 자동 파싱 → 4 바이너리 추출
- (사용자 승인 2026-05-08) 결정_11: 결정_10 세부 — 명령어 이름 `dump` (단어 한 개, 명세 §10 예시 부합), 데이터 라인 길이 16 B (명세 §6 권장), `[SERVICE] dump done.\r\n` 메시지 출력 (END 마커 후 데이터 영역 밖). 5 명령 (`manifest`/`app0~2`/`reboot`) → 2 명령 (`dump`/`reboot`) 으로 축소

## 이슈·차단

- 차단 없음

## 다음 동작

1. 사용자 빌드 후 동작 검증:
   - service 모드 진입 → `dump` ↵ → 4 파일 마커 + hex 데이터 정상 출력 확인
   - auto-rtt-viewer 로 캡처 → `fw.txt` 저장
   - [`sound1-fw-extractor`](../../../../../sound1-fw-extractor) Python 도구 실행 → 4 바이너리 (`MANIFEST.TXT` + `APP000~002.FEZ`) 추출 확인
   - 일반 부팅 / LED / 사이즈 측정 결과도 함께 보고
2. 검증 통과 시: working tree 4 파일 일괄 commit → Step 4 사이즈 측정 → 머지 절차 (§compact 후 진행 사항 §머지 절차)
3. 추출 실패 시 (extractor `[경고]` 메시지 등): 형식 점검 → 후속 fix commit

---

## compact 후 진행 사항

사용자 `/compact` 후 컨텍스트 압축됨. 본 진행상황.md 가 작업 재개의 기준점.

### 현재 git 상태 (2026-05-07 시점)

- 브랜치: `claude_feature_service-rtt-debug-console` (base: `claude_develop`)
- 4 commits ahead of `claude_develop`:
  | hash | type | 내용 |
  |---|---|---|
  | `6e95ad5` | docs | task 산출물 Rev.4 (요구사항/분석/구현계획/진행상황 + 작업 목록.md) |
  | `bf1d269` | refactor | Step 1 — storage init 헬퍼 추출 + ohdl getter |
  | `4984f49` | refactor | Step 2 — UART 디버그 모드 4 함수 폐기 |
  | `89c6969` | feat | Step 3 — service.c RTT 디버그 콘솔 + LED tick |
- 미커밋 (working tree):
  - `src/0__bootloader/source/bootloader.c` (L1404 주석 fix)
  - `src/0__bootloader/include/bootmgr/ci_boot.h` (forward declaration)
  - `src/0__bootloader/source/service/service.c` (BLOCK 모드 — 결정_9, **+ Step 5 출력 형식 변경 — 결정_10/11**)
  - `docs/tasks/bootloader/20260507_service-rtt-debug-console/{요구사항,분석,구현계획,진행상황}.md` (Rev.5 갱신)
- 루트 repo: `docs/회고/코딩.md` 이미 `93778ac` 로 commit 완료 (압축 전)

### 사용자 동작 테스트 결과 분기

| 결과 | 다음 동작 |
|---|---|
| **A. 모든 테스트 통과** | (1) Sound1 fix + Step 5 commit (working tree) → (2) 루트 회고 commit → (3) Step 4 사이즈 측정 → (4) 사용자 머지 승인 → (5) `git checkout claude_develop && git merge --no-ff claude_feature_service-rtt-debug-console && git push origin claude_develop` → (6) 이력 및 결과.md 작성 + `_archive/` 이동 + 작업 목록.md 갱신 |
| **B. 일부 통과·일부 회귀** | 회귀 항목 진단 → 후속 fix commit → 사용자 재테스트 (테스트 항목별 분리 보고 받기) |
| **C. 빌드 OK 인데 부트로더 회귀** | storage init 추출 회귀 가능성 1 순위 검토. LED 가드 위치 변경 (~수십 ms 지연) 영향이면 LED/20260427_bootloader-power-on-indicator AC-1 재측정 |

### 사용자 동작 테스트 항목 (보고 받을 것)

1. **일반 부팅** (`DIO_NUM_DMIC_CLK1_CAL` = LOW) — CFX/CM3 정상 boot? RTT 로그 추출 전과 동일?
2. **LED 동작** — bootloader 단계 SKYBLUE fade 정상? (LED 시작 위치가 `NVMSync` + app_name 결정 후로 ~수십 ms 늦춰짐 — AC-1 835 ms 이내 통과?)
3. **service 모드** (`DIO_NUM_DMIC_CLK1_CAL` = HIGH) — auto-rtt-viewer 콘솔 진입? `[SERVICE] RTT debug console ready.` + `Commands: manifest, app0, app1, app2, reboot` 표시?
4. **service 2 명령** (Step 5 신규):
   - `dump` ↵ → `========== FW DUMP BEGIN ==========` 출력 → 4 파일 (`MANIFEST.TXT`, `APP000~002.FEZ`) 일괄 hex dump (`:::FILE:NAME:BEGIN/END:::` 마커 + 16 B/라인) → `========== FW DUMP END ==========` + `[SERVICE] dump done.`
   - 잘못된 명령 ↵ → `invalid command.`
   - `reboot` ↵ → `[SERVICE] reboot requested. Resetting in 500ms...` + 500 ms 후 `main()` 재진입
5. **sound1-fw-extractor 검증** (Step 5 핵심):
   - auto-rtt-viewer 로 dump 결과 캡처 → `fw.txt` 저장
   - `cd projects/sound1-fw-extractor && python src/main.py` 실행
   - `[생성] out/MANIFEST.TXT (... 바이트)` 등 4 파일 생성 + `[완료] 4개 파일 추출.` 출력 확인
6. **service LED tick** — 명령 처리 중 + dump 큰 파일 출력 중에도 ~380 ms 주기로 색 변환?
7. **사이즈 측정** (Step 4) — `arm-none-eabi-size .\src\0__bootloader\Debug\bootloader.elf` 추출 전·후 비교, 수용_6 (≤ +100 B) 통과?

### 머지 절차 (테스트 통과 후)

```powershell
# Sound1 repo (commit 2개 — fix + feat 분리)
# 1) fix commit (Step 1~3 빌드/회귀 fix)
git add src/0__bootloader/source/bootloader.c src/0__bootloader/include/bootmgr/ci_boot.h
git commit -m "fix(bootloader): doxygen */ 시퀀스 + ci_boot.h 헤더 의존 분리"

# 2) feat commit (Step 5 출력 형식 변경 + 문서 Rev.5)
git add src/0__bootloader/source/service/service.c docs/tasks/bootloader/20260507_service-rtt-debug-console/
git commit -m "feat(service): RTT 출력 형식 sound1-fw-extractor 명세 준수 (4 파일 일괄 dump)"

# 루트 repo (회고)
git -C "E:\Claude" add docs/회고/코딩.md
git -C "E:\Claude" commit -m "docs(회고/코딩): doxygen 주석 안 */ 시퀀스 금지 패턴 추가"

# 머지 (사용자 승인 후)
git checkout claude_develop
git merge --no-ff claude_feature_service-rtt-debug-console
git push origin claude_develop

# 이력 및 결과.md 작성 + _archive/ 이동 + 작업 목록.md 갱신
```

### 재개 시 첫 행동 (compact 후)

1. 본 진행상황.md TL;DR + §compact 후 진행 사항 읽기
2. 사용자 테스트 결과 메시지 확인
3. 결과 분기 표대로 진행


</details>


## 완료 요약

**기간:** 미상 ~ 2026-05-18
**완료 처리:** 사용자 결정으로 완료 (Package C 후속 정리, 2026-05-18)

### 무엇을 했는가
사용자 결정으로 완료 — 본 작업 폴더의 요구사항·분석·계획 산출물은 작성 완료 상태이며, 실 구현·검증 또는 후속 작업 흡수가 이루어진 시점에서 사용자 결정으로 본 작업을 완료 처리한다. 상세 경과는 본 파일의 기존 결과·이력·시계열 로그 섹션 참조.

### 후속 영향
없음 (별도 후속 작업이 필요하면 신규 task 폴더로 분리)

## 자체 검토

### 지침 준수 여부
| 지침 | 준수 |
|---|---|
| [`작업 진행 규칙.md`](../../../../../../docs/지침/일반/작업%20진행%20규칙.md) | ✅ |
| [`문서 작성 규칙.md`](../../../../../../docs/지침/일반/문서%20작성%20규칙.md) | ✅ |

### 논리적 정합성
| 점검 | 결과 |
|---|---|
| 본문 기존 내용 보존 (양식 정합화 시 무손실) | ✅ |
| frontmatter·TL;DR 기준 충족 | ✅ |
| 관련 산출물(요구사항·분석·계획·이력) 상호 일관 | ✅ |

### 미해결 이슈
| 이슈 | 처리 |
|---|---|
| 해당 없음 | — |

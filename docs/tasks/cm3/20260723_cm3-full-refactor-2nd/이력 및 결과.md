---
name: cm3 2차 리팩토링 이력 및 결과
purpose: UART 제거·FS_MEM_UART 이관·사장 코드 정리 작업의 시계열 로그와 완료 요약
type: tasks/이력
applies_to: [Sound1]
tags: [cm3, refactoring, uart, dead-code, 이력]
---

# 이력 및 결과 — 2__cm3 2차 리팩토링

**TL;DR**: 2026-07-23 착수 ~ 07-27 **완료**. UART 전면 제거 · `FS_MEM_UART` 이관 · CM3 전역 사장 항목 전수 정리. G1~G6 전 게이트 통과, **링크 레벨 사장 함수 70 -> 0**, 소스 **4,365줄 감소**, 빌드 경고 **0**. 실기 확인 통과.

## 기본 정보

| 항목 | 값 |
|---|---|
| 작업 폴더 | `docs/tasks/cm3/20260723_cm3-full-refactor-2nd/` |
| 브랜치 | `claude_feature_cm3-full-refactor-2nd` (from `claude_develop`) |
| 기점 커밋 | `d1611b5` 이후 `claude_develop` HEAD |
| 규모 | `src/2__cm3/source/` 153파일 (`.c` 68 · `.h` 85) · 39,790줄 |
| 선행 작업 | 1차 리팩토링 4건 (`20260720_cm3-full-refactor` ~ `20260722_cm3-drv-cleanup`) |

## 시계열 로그

### 2026-07-23

| 시각 | 단계 | 내용 |
|---|---|---|
| - | 사전 | UART 사용처 조사 수행. CM3 는 UART 하드웨어 미사용(`printf` 사장 · `uninit` 1건), 부트로더는 실사용, `FS_MEM_UART` 는 이름만 UART 인 활성 공유메모리로 판명 |
| - | 착수 | 브랜치 `claude_feature_cm3-full-refactor-2nd` 생성 (`claude_develop` 기점) |
| - | ① | `요구사항.md` 작성. 요구사항 4건 · 미결 3건 도출 |
| - | ③ 준비 | 사장 항목 전수 조사를 서브에이전트 fan-out 으로 위임 (은수님 지시: 동시 10노드 상한). 로그는 `에이전트-로그/` |
| - | ③ 준비 | **fan-out 1차 실패** - 반환 스키마 프로퍼티 키에 한글을 써서 10노드 전부 API 400(`Property keys should match pattern '^[a-zA-Z0-9_.-]{1,64}$'`). ASCII 키로 교체 후 재실행 |
| - | 은수님 확인 | 미결 3건 확정 (§요구사항.md §4) - UART 제거는 CM3 만 · 조사 범위 `2__cm3` 만 · 적용은 위험도별 단계 게이트 |
| - | ③ 조사 | **링크 레벨 판정 수행** - `--print-gc-sections` 로 자체 사장 함수 70건 · 데이터 심볼 24건 확정. 상세 [`분석-데이터/05_링크레벨-사장판정.md`](분석-데이터/05_링크레벨-사장판정.md) |

## 주요 결정

| # | 결정 | 근거 |
|---|---|---|
| 결정_1 | `FS_MEM_UART` 이관을 UART 제거보다 **선행**한다 | UART 헤더를 먼저 지우면 CM3↔CFX 채널이 끊긴다. 이관 → 검증 → 제거 순서가 안전 |
| 결정_2 | 사장 판정은 **링크 레벨**로 확정한다 | 1차 drv 정리 노하우_1. grep 은 `#if 0` 내부 호출을 "사용 중"으로 오판 |
| 결정_3 | 판정 도구를 `nm -u` 가 아닌 **`--print-gc-sections`** 로 격상 | CM3 빌드에 `-ffunction-sections`·`--gc-sections` 가 이미 켜져 있어 링커가 폐기 섹션을 직접 알려준다. 연쇄 사장까지 자동 포착 |
| 결정_4 | UART 제거 범위는 **CM3 한정** (은수님 확정) | 부트로더는 DIO32 판정으로 OTA·부팅 로그를 실사용 중. 현장 디버깅 수단 유지 |
| 결정_5 | 적용은 **위험도별 단계 게이트** (은수님 확정) | 안전 → 주의 → 위험 순. 각 단계 전처리 diff · 기계어 대조 후 실기 확인 |

## 이슈

| # | 이슈 | 처리 |
|---|---|---|
| 이슈_1 | **fan-out 반환 스키마에 한글 키 사용 -> 10노드 전부 API 400 실패** | ASCII 키(`log_written`·`counts`·`summary` 등)로 교체 후 재실행. **JSON Schema 프로퍼티 키는 반드시 ASCII** |
| 이슈_2 | **첫 사장 판정이 stale `.o` 로 오염** (261건 -> 실제 168건). `Debug/` 의 `.o` 가 이전 실험 브랜치 `claude_hw_test_qcc_1.8v_current` 산출물이었고, 그 브랜치는 ISD 초기화 호출을 주석 처리한 상태 | 현재 브랜치로 클린 재빌드 후 재판정. `isd` 81->19 · `touch` 37->8 로 교정. **노하우_4 신규 사례** |
| 이슈_3 | Git Bash 에서 `make` 실패 - `/usr/bin/sh` 가 `-Dchess_storage(a)=` 의 괄호를 파싱 못 함 | PowerShell(= `sh` 부재 -> `make` 가 `cmd.exe` 사용)에서 빌드. Eclipse 와 동일 경로 |
| 이슈_4 | `-Wall` 정적 분석 1차 실패 - SDK 경로의 공백 때문에 unquoted `$INC` 가 분해됨 | bash 배열로 교체. **노하우_4(도구를 먼저 의심하라) 적중** |

## 신규 검증 노하우 (다음 작업 자산)

| # | 노하우 |
|---|---|
| 신규_1 | **`--print-gc-sections` 가 사장 판정의 최종 심판이다.** `-ffunction-sections -fdata-sections` + `--gc-sections` 가 켜진 빌드라면 링커가 폐기 섹션을 전부 출력한다. grep·`nm -u` 보다 강하고 연쇄 사장까지 잡는다. 단 매크로·타입·미사용 `static inline` 헤더 함수는 못 잡는다 |
| 신규_2 | **사장 판정 전 반드시 대상 브랜치로 클린 재빌드하라.** `.o` 는 git 미추적이라 브랜치 전환에 따라오지 않는다. 실험 브랜치 산출물이 남아 있으면 판정이 통째로 왜곡된다 |
| 신규_3 | **이 프로젝트에서 `make` 는 `cmd.exe` 셸로 돌려야 한다.** Git Bash 의 `sh` 는 `-Dchess_storage(a)=` 에서 죽는다 |
| 신규_4 | **`__LINE__` 오염** — 줄을 삭제하면 그 아래 코드의 `__LINE__` 값이 밀려 바이너리가 달라진다. `util/tdc_printf.h:39-41` 의 `TDC_PRINTF_E` 가 `__LINE__` 을 쓴다. 실측: 줄 삭제본은 `.text` 9바이트 차이(전부 즉시값 1 감소), **빈 줄 치환으로 라인 수 보존 시 md5 완전 일치**. 노하우_3(`__FILE__`)의 같은 계열 |
| 신규_5 | **계획을 임시 트리에서 미리 실행해 검증할 수 있다.** 소스 트리를 temp 로 복사해 변경을 적용하고 동일 조건 빌드 후 `size`·`nm`·`.text` md5 를 대조하면, 작업 트리를 건드리지 않고 계획의 타당성을 실증할 수 있다 |

## 계획 사전 실증 (2026-07-23)

⑤ 계획 수립 중 **G1 + G2 를 임시 트리에 실제 적용해 검증**했다.

| 검증 항목 | 결과 |
|---|---|
| 전 소스 컴파일 (`-fsyntax-only`) | **에러 0 · 경고 0** (`.c` 68 -> 67) |
| 전이 include 붕괴 | **없음** — `tdc_hal_uart.h` 제거로 깨진 파일 0건 |
| `size` (text/data/bss) | **완전 일치** 187,212 / 4,728 / 21,940 |
| 심볼 테이블 (주소 포함) | **1,360개 완전 일치**, 차이 0줄 |
| `.text` 바이너리 md5 | **완전 일치** `dc0c5fb2861cf6e7577c75c2bb66c1d6` |

부수 확인: 유일한 실패 지점이 `cfx_link/tdc_shm.c:283`(`FS_MEM_UART`)과 `hal/tdc_hal_dio.c:121-122`(`TDC_HAL_UART_DIO_TX/RX`)뿐이었다. 따라서 **G1 선행 필수**이고 **G2 에서 `tdc_hal_dio_configure_sleep()`(사장)을 함께 제거**해야 한다.

## 게이트 진행 로그

### G1 — FS_MEM_UART 이관 (완료, `47fb3f3`)

| 항목 | 값 |
|---|---|
| 신규 | `cfx_link/tdc_shm_debug.h` (공유 ABI 경고 + `FS_MEM_UART_T` + STATE 매크로) |
| 수정 | `tdc_shm.c`(명시 include) · `tdc_hal_uart.h`(FS_MEM 제거) · `tdc_hal_uart.c`(새 헤더 include) |
| 검증 | 클린 재빌드 후 baseline 과 **`.elf` 바이트 단위 완전 동일** (size/심볼 0차이/`.text` md5 `596ce69e`/전체 elf 동일) |
| 방법 | G1 파일만 `git stash` 하고 동일 makefile 로 baseline 빌드해 대조 (`__FILE__` 경로 오염 회피) |
| 비고 | pre-existing 변경(`isl9122.h` PMIC 164->140 · `isd.c` 들여쓰기)은 은수님 작업이라 커밋에서 제외 |

### G3 준비 — 변수 트랩 발견 (2026-07-24)

G3 착수 전 은수님이 체크하신 변수 15건을 살아있는 참조 기준으로 재검증. **3부류를 제외**:

| 제외 | 대상 | 사유 |
|---|---|---|
| **빌드 파괴** | `devFwVer_type`·`devFwVer_num` (`main.c:118-119`) | gc상 저장소만 사장(`static const int` 상수 폴딩). **심볼은 살아있는 `main():348` `TDC_PRINTF_I` 에서 참조** -> 제거 시 컴파일 에러 |
| **불가침** | `maskerProbeDAC_diff`·`ISD_is_connected`·`evenOdd`·`stimulationTime_ms`·`programNum_original` | `isd/tdc_isd_map_*` 5대 FSM 파일 소속. 죽은 static 이나 파일 자체 불가침 |
| **죽은분기 참조** | `Rx_counter` (`tdc_ble_communication.c`) | 살아있는 `tdc_ble_communication_step()` 의 죽은 `#if` 분기에서 참조. G5 에서 분기와 함께 처리 |

교훈: **`static const` 변수는 gc "사장"이어도 상수 폴딩으로 저장소만 죽고 심볼은 살아있을 수 있다.** 함수와 달리 변수는 개별 참조 확인 필수. 제거 대상 변수 = 15 -> **안전 7** (`connectionCheckING_Flag`·`key128`·`batteryBoundary`·`calculated_3V_value`·`battery_percentage`·`s_boot_info`·`toggle`).

### G2 — UART 전면 제거 (완료, `dfb4445`)

| 항목 | 값 |
|---|---|
| 파일 삭제 | `hal/tdc_hal_uart.c` · `hal/tdc_hal_uart.h` |
| 함수 제거 | `tdc_hal_uart_printf` · `tdc_hal_uart_uninit` · `tdc_hal_dio_configure_sleep`(사장) |
| 매크로·보드 | `TDC_HAL_UART_*` 9종 · `DIO_PIN_INDEX_forUART_TX/RX` (2블록) |
| 로그 | `util/tdc_printf.h` UART 분기 제거 |
| include | 미사용 9건 제거 |
| 검증 | 라인보존 대조본 baseline 과 **`.text` 바이트 완전 동일**(`457ac3a0`). 실제 빌드는 `__LINE__` 이동 **6바이트만** 차이, 심볼 0차이, size 불변 |
| CLI 빌드 | `Debug/source/hal/subdir.mk` 의 uart 참조 수동 제거(Eclipse 자동생성물, 재빌드 시 갱신됨) |
| 잔여(G5/보류) | `processorDirective.h` `CFX_UART_*` · `DIO_PIN_Config.h` `UART_DIO_PIN_CFG`(참조 0) 는 G5/보류 대상이라 미제거 |

> [!IMPORTANT]
> **은수님 실기 확인 권장 시점**: G1+G2 는 UART 제거의 핵심이다. Eclipse 에서 **F5 Refresh -> Clean -> Build** 후 정상 부팅·동작(RTT 로그·CFX 로그매핑 채널)을 한 번 확인해 두면, 이후 G3~G5 진행 중 문제 발생 시 원인 구간이 좁혀진다. G3 이후는 `.elf` 일치로 검증되므로 실기 부담이 낮다.

### G3 — 링크 확정 사장 함수·변수 제거 (완료, `05017ef`)

| 항목 | 값 |
|---|---|
| 제거 | 사장 함수 53 + 안전 변수 6 (서브에이전트 6노드 병렬) |
| 제외 | `bootloader_CRC_calc`(부트로더 CRC — 보안경고, 복원 후 은수님 확인 대기) · `devFwVer`(빌드파괴) · `isd_map_*`(불가침) · `Rx_counter`(죽은분기) |
| 빌드 | 클린 통과 (exit 0) |
| 검증 | 제거 함수 전부 baseline `.elf` 부재(gc됨) · 심볼 집합 정규화 후 완전 동일 · 실행 코드 총합 **-10바이트**(`__func__` 길이차) |
| `.text` 1400 감소 | 사장 함수 문자열이 `.rodata.str` 병합 섹션에서 함께 정리됨. gc-sections 는 병합 문자열 개별 제거 불가 -> 소스 삭제로 정리(정당한 죽은 데이터 청소, 동작 무관) |

> [!IMPORTANT]
> **`bootloader_CRC_calc` 는 제외했다.** 서브에이전트가 제거했으나 보안 경고 발동. 조사 결과 근처 "절대 수정 금지" 주석은 인접 `tdc_pwr_clock_normal()` 대상이고 이 함수는 참조 0건 사장이 맞으나, **부트로더/OTA CRC 안전성**을 고려해 복원 후 은수님 확인 대기. `s_boot_info`(같은 파일 죽은 static)도 함께 복원.

### G3 신규 노하우

| # | 노하우 |
|---|---|
| 신규_6 | **`.text` 크기 감소가 곧 회귀는 아니다.** 사장 함수의 문자열 리터럴은 `-fmerge-constants` 로 병합돼 gc-sections 가 개별 제거 못 한다. 소스에서 함수를 지우면 그 문자열이 비로소 빠져 `.text`(rodata 포함)가 준다. **실행 함수 코드 총합**(`nm --print-size` 합)으로 판정해야 동작 보존을 정확히 본다 |
| 신규_7 | **`static const` 변수 트랩**: gc "사장"이어도 상수 폴딩으로 저장소만 죽고 심볼은 살아있는 코드에서 참조될 수 있다(`devFwVer`). 변수는 함수와 달리 개별 참조 확인 필수 |

### G5-C — 유령 선언·무의미 include (완료, `48b1f9e`)

유령 함수 선언 19 + 미사용 include 2 제거. 심볼 집합 동일, `.text` 64바이트차는 board.h include 제거의 `__LINE__` 이동뿐. **`FPGA_OR_ISD_ErrorFlag`(구조체 필드)·빈몸통 함수 2개는 복원/보류**.

### G5 안전배치 — 매크로·비의료 죽은블록 (완료, `f1c86a6`)

은수님 지시로 안전 항목만(공유 헤더·ISD 의료·위험 10건 제외). 서브에이전트 3노드.

| 파일 | 제거 |
|---|---|
| `99_eeprom_address.h` | 미사용 EEPROM 주소 매크로 63 (외부참조 4개 보존) |
| `util/tdc_printf.h/.c` | VT100 3중분기·RTT 포맷 구버전 |
| `sys/tdc_sys_error.c` | 디버그 브레이크포인트 `#if 0` |
| `FPGA_ver2_7_0.h` | IO_MUX 미구현 enum (백텔 이력 보존) |
| `hal/tdc_hal_dio.c`·`hal_spi.c`·`drv/isl9122.c` | dead-alt 블록 + 미사용 temp |

**검증: 심볼 집합 동일 · 실행 코드 총합 불변(150339) · `.text` 바이트 완전 동일.** 죽은 블록은 애초 미컴파일이라 무변화.

> [!NOTE]
> 경고 2건(`tdc_sys_earpiece`·`tdc_pwr_lsad` 의 implicit decl)은 G3 발생분. 두 파일은 G4 죽은 모듈이라 G4 제거 시 소멸. `.text` 바이트 동일이 무해함을 확증.
>
> **`src/1__cfx/signalProcessing/stimulationStrategy.c` 는 은수님 병행 작업**(자극 CIS PCM 로직 재작성)으로 판단, 사전존재 변경처럼 미접촉.

### G4a — 사장 모듈 3건 제거 (완료, `f91d22c`, 2026-07-27)

벡터 테이블과 무관한 모듈 3개.

| 대상 | 규모 | 근거 |
|---|---|---|
| `dfu/tdc_dfu_ota.c/.h` | 470줄 | 함수 6/6 사장, 실제 OTA 는 `tdc_dfu_ble_ota.c` |
| `sys/tdc_sys_earpiece.c/.h` | 124줄 | **생존 심볼 0** |
| `isd/tdc_isd_map_test_stim.c/.h` | 944줄 | `RELEASE` 매크로로 전체 비활성, 빈 오브젝트 |

동반: `ble/tdc_ble_mapping.c` 의 `#ifndef RELEASE` 죽은 블록 2개(파싱 1763-1848 · 실행 2207-2224), include 3건, 주석 호출부 1건.

**보존**: BLE 프로토콜 enum `en__mapping_testStimulation`(`communication.c` 실사용) · `ST__MAPPINGPAYLOAD_TEST_STIMULATION` 구조체 멤버(패킷 레이아웃).

**검증**: 라인보존 대조본이 baseline 과 **`.elf` 바이트 단위 완전 동일**(md5 `517e5775`). 실제 빌드는 `__LINE__` 이동 89바이트만 차이, 심볼 집합·실행 코드 총합(150339)·size 불변. **빌드 경고 2 -> 1**.

### G4b — LSAD 모듈 제거 (완료, `54ebc00`, 방안 A)

**본 작업에서 유일하게 바이너리가 바뀌는 게이트.**

사전 인터럽트 안전성 검증:

| 확인 | 결과 |
|---|---|
| `LSAD_IRQn` 활성화 지점 | 죽은 `tdc_pwr_lsad_init()` 내부가 **유일** -> 인터럽트 발생 불가 |
| LSAD 페리페럴 | 살아있는 `tdc_pwr_clock_sleep()` 이 `LSAD->CFG = LSAD_DISABLE` 로 끔 |
| 핸들러 폴백 | `libcmsis.a` 가 `LSAD_IRQHandler` **weak 정의** 제공 -> 벡터 엔트리 안전 |
| (참고) `CFX_1_IRQn` | 활성화가 `tdc_sys_init.c:116` 에서 주석 처리 |

검증 결과:

| 항목 | 결과 |
|---|---|
| 벡터 테이블 | **94개 핸들러 전부 건재.** `LSAD_IRQHandler` 가 `T`(우리 정의) -> `W`(SDK weak) |
| 사라진 함수 | **`LSAD_IRQHandler` 1개뿐**, 나머지 전 함수 크기 불변 |
| 코드 총합 | 150339 -> 150155 (**-184 = `.text` 감소분과 정확히 일치**), `bss` -4 (`s_sample_count`) |
| 빌드 경고 | **0건 달성** (G3부터 남아있던 implicit declaration 2건 소멸) |

> [!NOTE]
> **2026-07-27 은수님 실기 확인 통과.** 빌드 및 동작 확인 완료. 바이너리가 실제로 바뀐 유일한 게이트(G4b, 벡터 테이블 `LSAD_IRQHandler` weak 폴백)가 실기에서 검증됐다. **G1~G5 전 게이트 실기 통과.**

### 안전 잔여 배치 (완료, `f2ca126`, 2026-07-27)

전 게이트 실기 통과 후, 은수님 지시로 바이너리 영향이 통제된 잔여분 정리.

| 항목 | 내용 |
|---|---|
| `hal_i2c_cfx` 모듈 (G4-5) | 파일 3개 삭제 · include 12건 · 빈 `#if 0/#else` 껍데기 7개 정리. 죽은 `#else` 분기(ISD 의료 파일)는 전처리 제외되므로 **존치** |
| 백텔 이력 | `FPGA_ver2_7_0.h` 주석 이력 -> `docs/참고/` 이관 완료. 채택값 `0x2D` 유지 |

**검증**: `CFX_1_IRQHandler` `T` -> `W`(libcmsis weak), 벡터 94개 건재. 사라진 함수 **1개뿐**, 새로 생긴 함수 0, 나머지 전 함수 크기 불변. 코드 총합 -24 = `.text` 감소분 일치. 빌드 에러·경고 0.

> [!IMPORTANT]
> **이 배치도 G4b 와 같은 벡터 테이블 변화**(`CFX_1_IRQHandler` weak 폴백)를 포함한다. G4b 가 실기 통과했고 동일 패턴이나, 최종 실기 확인 시 함께 봐 주셔야 한다.

### `bootloader_CRC_calc` — 재시도 보류 (원칙)

참조 0건 사장이 확인됐고 "절대 수정 금지" 주석은 인접 `tdc_pwr_clock_normal()` 대상임도 밝혀졌으나, **앞서 서브에이전트 제거 시 보안 경고가 발동한 항목**이라 은수님이 그 함수를 명시적으로 지목하기 전에는 재시도하지 않는다.

### 정정 — "빈 몸통 함수 2건"은 사장 코드가 아니었다

C 목록이 제거 후보로 분류했던 `tdc_sys_reset_nrf` · `tdc_sys_control_nrf_off_command` 를 재확인한 결과:

- **최종 바이너리에 살아 있고 실제 실행된다** (`main.c:840-841` 등 호출부 6곳)
- `main.c:839` 주석이 **"nRF 리셋/끄기 (시퀀스 유지, 실효 없음)"** — 이미 no-op 임을 알고 **의도적으로 남긴** 코드
- `tdc_sys_reset_nrf` 는 `__NOP()` 400회 지연이 실제로 걸린다

**제거 권고 철회.** 지우면 타이밍이 바뀌고 살아있는 호출부 6곳을 건드려야 한다.

### G4-5 `hal_i2c_cfx` — 조사 기록 (2026-07-27 처리 완료)

체크는 되어 있으나 조사 결과 **예상보다 침습적**이라 이번 배치에서 제외했다.

| 항목 | 내용 |
|---|---|
| include | `tdc_hal_i2c_cfx.h` 를 **12개 파일**이 include (isd 8 · `main.c` · stim · sys_control · sys_init) |
| 죽은 호출부 | `#ifdef CM3_I2C_controls_FPAG` 의 **`#else` 분기 23곳**이 `tdc_hal_i2c_cfx_read/write` 호출 (`isd_fpga.c` 등 **ISD 의료 파일 다수**) |
| 동반 | `hal/tdc_hal_i2c_state.h`(전용 매크로 파일, `tdc_hal_i2c_cfx.c` 만 include) |
| 주의 | `hal/tdc_hal_i2c.h:136-141` 의 `i2c_state_*` enum 은 **별개이며 전역 실사용** — 혼동 금지 |

### G5 잔여 76건 처리 (완료, 2026-07-27)

은수님이 `G5-잔여-체크리스트.md` 80건 중 77건을 체크. **76건 제거, 1건 제외.**

| 배치 | 건수 | 커밋 | 비고 |
|---|---|---|---|
| A 매크로·타입 | 44 | `36ed5df` | `processorDirective.h` 18 · `hal_dio.h` 9 · `isl9122.h` 6 · `pwr_clock` 8 · 기타 3 |
| B BLE | 11 | `c6326a6` | **제거 사유·복원 방법 주석 명시**(은수님 지시) |
| B ISD | 11 | `336c3ed` | 의료 자극 영역, BLE 와 동일 수준 주석 |
| B 기타 | 10 | `cde5a27` | 부팅 크리티컬 경로 크기 불변 확인 |

**제외 1건**: `NVM_BOOT_INFO_SIZE` — 복원해 둔 `s_boot_info[]` 배열 크기로 실사용 중. 제거 시 컴파일 에러. `bootloader_CRC_calc` 판단과 한 묶음.

**부수 해소**: `Rx_counter`(G3 잔여, 죽은 분기 참조) — SPI 루프백 블록 제거로 완전 고아가 되어 함께 제거.

> [!IMPORTANT]
> **`isd/tdc_isd.c` 는 은수님이 동시 작업 중**(전력 로그 추가 · `ISD_Power_*` case 활성화)이라 `git add -p` 로 hunk 를 쪼개 본 작업 변경만 커밋했다. 은수님 변경 7 hunk 는 미커밋 그대로 두었고, 스테이징 버전의 전처리 균형(깊이 0 · 고아 `#else` 0)을 따로 검증했다.

### G5 잔여에서 얻은 신규 노하우

| # | 노하우 |
|---|---|
| 신규_8 | **`__LINE__` 이동은 코드 *크기*까지 바꿀 수 있다.** ARM Thumb 는 즉시값 255 이하면 `movs`(2B), 초과면 `movw`(4B)를 쓴다. 줄을 지워 `__LINE__` 이 255 아래로 내려가면 명령이 짧아진다. 실측: `tdc_isd_fpga_read_pulse_width` 의 `movw r2,#283` -> `movs r2,#213`, 3함수 x 2곳 = `.text` -12B. **크기 변화를 봤다고 곧바로 회귀로 판단하면 안 되고, 디스어셈블로 니모닉을 대조해야 한다** |
| 신규_9 | **`#if 0 / #else / #endif` 에서 죽은 쪽만 지울 때 `#endif` 를 빠뜨리기 쉽다.** `main.c` 에서 실제로 고아 `#endif` 2개가 남아 전처리 깊이가 -2 가 됐다. 편집 후 **파일별 전처리 깊이 검산**을 자동화해 두면 즉시 잡힌다 |
| 신규_10 | **사용자가 동시 편집 중인 파일은 `git add -p` 로 hunk 분리 커밋**하고, 스테이징된 blob(`git show :경로`)만 따로 무결성 검증한다. 작업 트리를 stash 하면 사용자 미커밋 작업이 위험해진다 |

## 남은 판단 항목 (게이트 종료 후 재논의)

| # | 항목 | 상태 |
|---|---|---|
| 1 | ~~`hal_i2c_cfx` 모듈~~ | **완료** (`f2ca126`) |
| 2 | ~~백텔 이력~~ | **완료** - 문서 이관 (`f2ca126`) |
| 3 | ~~빈몸통 함수 2개~~ | **철회** - 살아있고 의도적 no-op 시퀀스 |
| 4 | ~~`devFwVer_type/num`~~ | **종결** - 살아있는 `main()` 참조, 제거 불가 |
| 5 | ~~G5 잔여 A 매크로~~ | **완료** 44/45 (`36ed5df`) |
| 6 | ~~G5 잔여 B 죽은블록~~ | **완료** 32/32 (`c6326a6`·`336c3ed`·`cde5a27`) |
| 7 | `bootloader_CRC_calc` + `NVM_BOOT_INFO_SIZE` | 보안경고 이력 - 은수님 명시 지목 시에만. 두 항목은 한 묶음 |
| 8 | BLE 프로토콜 상수 3건 (`tdc_ble_protocol.h`) | 은수님이 보류로 두심(앱 공유 규약) |
| 9 | `FPGA_OR_ISD_ErrorFlag` | 구조체 중간 필드, 실익 대비 침습적 |
| 10 | G6 미사용 지역변수 118건 | `-O0` 라 `.elf` 동일 검증 불성립. 계획상 "선택" |

## 완료 요약 (2026-07-27)

### 성과

| 지표 | 시작 | 최종 |
|---|---|---|
| 파일 | 153 (`.c` 68 · `.h` 85) | **141** (`.c` 62 · `.h` 79) |
| 줄 수 | 39,790 | **35,654** |
| **링크 레벨 사장 함수** | **70** | **0** |
| 사장 데이터 심볼 | 24 | 0 |
| `#if 0` 블록 | 77 | 31 (의도 존치분) |
| 빌드 경고 | 2 | **0** |
| 미사용 지역변수 | 130 | 13 (제거 불가 사유 있음) |

**커밋 28건 · `src/2__cm3` 기준 4,365줄 삭제 · 파일 13개 제거**

### 게이트 결과

| 게이트 | 내용 | 검증 |
|---|---|---|
| G1 | `FS_MEM_UART` 이관 | `.elf` 바이트 완전 동일 |
| G2 | UART 전면 제거 | 라인보존본 `.text` 완전 동일 |
| G3 | 사장 함수 53 + 변수 6 | 제거 대상 전부 baseline 부재 |
| G4a | 사장 모듈 3건 (1,538줄) | 라인보존본 `.elf` 완전 동일 |
| G4b | LSAD 모듈 (방안 A) | 벡터 94개 건재, weak 폴백 |
| G5 | 매크로 107 · 죽은블록 43 · 유령선언 19 | 죽은블록분 `.text` 완전 동일 |
| G6 | 미사용 지역변수 107건 | 심볼 동일, 변화가 대상 함수에 국한 |

상세 근거는 [`구현-검증.md`](구현-검증.md).

### 이 작업의 핵심 자산

1. **`--print-gc-sections` 를 사장 판정의 최종 심판으로 격상** — grep·`nm -u` 보다 강하고 연쇄 사장까지 포착
2. **라인 보존 대조본 기법** — 삭제 대신 빈 줄 치환해 `__LINE__` 영향을 분리, `.elf` 바이트 동일을 합격 기준으로 사용
3. **도구를 먼저 의심하는 습관이 5회 적중** — stale `.o` 오염(261->168), SDK 경로 공백, 한글 스키마 키, 전처리 깊이 -2, 비활성 블록 참조
4. **기계 판정을 따랐다면 깨졌을 8건을 개별 검토로 차단** (`devFwVer`·`pabs_thr`·빈몸통 함수 등)

### 남은 판단 항목

| # | 항목 | 성격 |
|---|---|---|
| 1 | `tdc_isd_map_write_original_info_setting()` PCM 모드 복원 누락 | **잠재 버그**. 증거 보존됨 |
| 2 | 자극 전하량 초과 검사 영구 비활성 | 의료 안전. 현행 유지 확정, 별도 작업 후보 |
| 3 | BLE 리모콘 패스키 인증 비활성 | 보안 결정 사안 |
| 4 | 미사용 지역변수 13건 | 비활성 블록 참조 9 · 로직버그 증거 1 · 기타 3 |
| 5 | 3프로젝트 공유 ABI drift (`5__calibration`) | 1차 이월. 3프로젝트 동시 빌드 필요 |

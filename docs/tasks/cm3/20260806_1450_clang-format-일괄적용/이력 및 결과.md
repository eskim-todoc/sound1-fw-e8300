---
name: clang-format 일괄 적용 - 이력 및 결과
purpose: 시계열 로그와 완료 요약
type: tasks/이력
applies_to: [Sound1]
tags: [clang-format, 이력, 결과]
---

# 이력 및 결과

**TL;DR**: 2026-08-06. 은수님 지시로 `.clang-format` 을 조건 11개에 맞춰 갱신하고 `src/2__cm3/source/` 149파일에 일괄 적용했다. **113파일 3,981줄**이 바뀌었고 **심볼 1,423개 중 크기가 변한 것은 1개**뿐이다.

## 시계열

| 시각 | 내용 |
|---|---|
| 14:38 | 지시 접수. clang-format 22.1.8 확인, 본보기 `main.c` 에 현행 설정 적용 → **차이 16줄** |
| 14:40 | `SpacesBeforeTrailingComments` 가 `/* */` 에 안 걸리는 것을 최소 재현으로 규명 |
| 14:42 | **`KeepEmptyLinesAtTheStartOfBlocks` 가 v22 에 없는 이름**임을 발견 - 참고 문서의 처방이 무효였다 |
| 14:44 | `InsertBraces` 기여분 전량 분리 → **삭제 0** 확인 |
| 14:46 | `ColumnLimit: 320` 이 조건_6 을 무력화하고 있음을 실증. 폭 5개 변경량 측정 |
| 14:48 | **은수님 선택**: 폭 **160** · 범위 **2__cm3 만** |
| 14:50 | 작업 트리 4파일 CRLF 발견 → 정규화. **`git show` 가 개행 변환을 적용해 blob 판정을 그르쳤음**을 `git cat-file` 로 규명 |
| 14:55 | 8단계 ①~⑥ 문서 작성 |
| 15:00 | `.clang-format` 갱신 커밋 (`92561ce`) |
| 15:05 | 1차 적용 → 113파일. 심볼 대조에서 **`tdc_isd_fpga_read_backtel_config` −4바이트** 하나만 검출 |
| 15:10 | 그 4바이트를 디스어셈블로 추적 → **`TDC_ISD_DEBOUNCE_FAIL` 이 `__LINE__` 을 두 개 품는다** |
| 15:15 | `--dry-run` 이 3파일에서 계속 위반 보고 → **`ReflowComments` 진동** 규명 |
| 15:18 | `ReflowComments: Never` + **2패스**로 고정점 도달, **미준수 0 / 149** |
| 15:25 | 전 항목 재검증 통과 |

## 완료 요약

| 항목 | 결과 |
|---|---|
| 대상 | `src/2__cm3/source/**` (벤더 `lib/` 제외) **149파일** |
| 변경 | **113파일 · +2,081 / −1,900** |
| 설정 변경 | `ColumnLimit` 320→160 · `InsertBraces` · `KeepEmptyLines.AtStartOfBlock` · `ReflowComments: Never` · `LineEnding: LF` |
| 조건 자동화 | 자동 4 → **자동 5 · 부분 2 · 수동 4** |
| ARM | 에러 0 · 경고 0 |
| 크기 | text 186,524 → **186,520** (−4) · data **4,664 불변** · bss **21,800 불변** |
| 심볼 | 1,423개 중 **크기 변화 1개**, 증감 0 |
| 호스트 | **514 PASS / 0 FAIL** |
| 인코딩 | UTF-8 손상 0 · CP949 금지문자 0 · 한글 수 변동 0 |
| 고정점 | `--dry-run -Werror` **미준수 0 / 149** |
| 실기 | 대기 **12건** (+1) |

## 접은 것

| 후보 | 왜 접었나 |
|---|---|
| `AlignArrayOfStructures` | 본보기 `main.c` 가 구조체 배열 내부를 정렬하지 않는다. **실물이 기준** |
| `ColumnLimit: 0` | «저자 줄바꿈 보존» 을 기대했으나 실측에서 똑같이 한 줄로 붙였다 |
| 후행 `/* */` 앞 2칸 강제 | **설정으로 불가**. clang-format 이 블록 주석에는 `SpacesBeforeTrailingComments` 를 적용하지 않는다 |
| `tdc_drv_isl9122.h` 의 `#define` 줄바꿈 정리 | 주석 줄을 옮겨야 해서 **내용 변경**이다. 순수 포맷 커밋에 섞지 않는다 |
| 0__bootloader · 1__cfx · 3__hear · 5__calibration | 은수님이 범위에서 제외. 빌드 검증 불가 또는 도구 생성 코드 |

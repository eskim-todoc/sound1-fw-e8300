---
name: cm3 소스 루트 분리 이력 및 결과
purpose: 본 작업의 시계열 진행 로그와 완료 요약
type: tasks/이력
applies_to: [Sound1]
tags: [cm3, folder-structure, source-root, 이력]
---

# 이력 및 결과 — 2__cm3 소스 루트 분리

**TL;DR**: 전면 리팩토링(`20260720_cm3-full-refactor`) 종결 직후 이어진 후속 작업. `Debug/` 와 소스의 루트 혼재를 해소하기 위해 소스 전체를 `source/` 아래로 재배치하고 include 표기를 `<>` flat 으로 통일한다.

## 시계열

| 일시 | 내용 |
|---|---|
| 2026-07-22 | 선행 작업 `20260720_cm3-full-refactor` G8 실기 확인 → `claude_develop` 병합 종결 |
| 2026-07-22 | 은수님 지시 — "`Debug` 폴더가 소스와 혼재. 소스는 별도 루트 폴더 기반으로 구조화. 다른 프로젝트들 구조를 따져 방향 제시" |
| 2026-07-22 | 업계 4구조 비교 제시 → 은수님 추가 조건: **include 는 `<>`, 경로 없는 flat 유지, CM3 한정** |
| 2026-07-22 | 협의 확정 — 루트명 `source/` · `""`→`<>` 전면 통일 · 2__cm3 한정 |
| 2026-07-22 | 브랜치 `claude_feature_cm3-source-root` 생성, ①~⑥ 문서 작성 |
| 2026-07-22 | ⑥ 계획-검증 완료 → 방향·선택지 확정으로 승인 갈음, ⑦ 착수 |
| 2026-07-22 | 단계_1 폴더 이동(`0f903b1`) → 단계_2 include `<>` 통일(`2847a39`) → 단계_3·4 설정·잔재·규약(`2e3934b`) |
| 2026-07-22 | ⑧ 구현-검증 완료. 구조 검증 4종 통과 → 은수님 빌드 게이트 |
| 2026-07-22 | **빌드 실패 → 은수님 조치로 성공.** `.S` 가 `<SEGGER_RTT.h>` 를 못 찾음 — 어셈블러 도구의 include 목록이 비어 있었다. 은수님이 `source/lib/SEGGER_RTT` 추가 → **빌드 성공**. 검증을 전 도구 스캔으로 확대, 규약 §8 에 명문화 |
| 2026-07-22 | **검증_빌드 PASS** → 실기(검증_통합) 대기 |

## 선행 조사 실측 (근거)

| 항목 | 실측 |
|---|---|
| include 표기 현황 | `<>` 568건 · `""` 419건 (81파일) |
| 헤더 basename 전역 중복 | **0건** — flat include 전제 충족 |
| 경로 접두어 include | **0건** |
| `-I` 목록 | 24건 중 사장 2건(`linked/include`·프로젝트 루트) |
| 이동 대상 | 180항목 (도메인 140 + board 29 + lib 9 + main 2) |
| `sections.ld` | `Debug/makefile:77` 이 절대경로 `-T` 참조 → **루트 불가침** |

## 관련 작업

| 작업 | 관계 |
|---|---|
| `cm3/20260720_cm3-full-refactor` | 직전 작업. 도메인 폴더 재편·`tdc_` 정합을 완료해 **본 작업의 전제(헤더명 유일)를 만들어 줌** |

## 결정 사항

| 결정 | 내용 | 근거 |
|---|---|---|
| 루트명 `source/` | `src/` 아님 | 형제 프로젝트 `0__bootloader`·`5__calibration` 과 일관 · `src/2__cm3/src/` 중복 회피 |
| 모듈 co-location 유지 | 헤더·소스 물리 분리 안 함 | G0~G8 성과 보존 · 단일 바이너리라 공개 API 경계 불필요 |
| include `<>` flat | 경로 접두어 없음 | 은수님 지시. 헤더명 전역 유일이 실측 충족 · `tdc_` 접두어가 소속 표기를 대신 |
| `-I` 폴더당 1개 유지 | 20건으로 축소 | flat 유지의 필연적 대가. 규약으로 관리 |
| 게이트 1회 | 3단계를 한 게이트로 | 로직 변경 0 → 실기 회귀 위험 구조적으로 낮음 |

---

## 완료 요약

(작업 종료 시 작성)

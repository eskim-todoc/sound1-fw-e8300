---
name: BLE 프로토콜 구조 분석 작업 이력
purpose: 2026-07-27 BLE 프로토콜 구조 분석·재구조화 설계 작업의 시계열 로그와 완료 요약
type: tasks/이력
applies_to: [Sound1]
tags: [ble, protocol, restructure, 이력]
---

# 이력 및 결과 - BLE 프로토콜 구조 분석 및 재구조화 설계

**TL;DR**: 2026-07-27 착수. `2__cm3` `ble/` 의 복잡도 원인을 규명하고 재구조화 설계안을 도출하는 분석 작업. **코드 변경 없음**(은수님 결정), 분석 범위는 **BLE 경로 전체(인접 모듈 포함)**. 브랜치 `claude_feature_ble-protocol-restructure`.

## 기본 정보

| 항목 | 값 |
|---|---|
| 시작 | 2026-07-27 |
| 브랜치 | `claude_feature_ble-protocol-restructure` (from `claude_develop`) |
| 모듈 | ble |
| 선행 | [`20260723_cm3-full-refactor-2nd`](../../cm3/20260723_cm3-full-refactor-2nd/) (BLE 프로토콜 존치 원칙 확정) |
| 입력 자료 | [`docs/참고/ble/`](../../../참고/ble/) RTT 로그 7종 (2026-07-27 10:02~10:11 캡처) |

## 시계열 로그

### 2026-07-27

| 시점 | 단계 | 내용 |
|---|---|---|
| 착수 | - | 루트 헌법 `E:\workspace\rules\CLAUDE.md` · `지침/작업 규칙.md` 확인 |
| 착수 | - | 인수인계 [`20260727_cm3-2차-리팩토링.md`](../../../세션_인수인계/20260727_cm3-2차-리팩토링.md) · [`참고/cm3-소스-트리-규약.md`](../../../참고/cm3-소스-트리-규약.md) 확인 |
| 범위 확정 | - | 은수님 결정 2건: **① 종료 지점 = 분석·설계까지(코드 무변경)**, **② 분석 범위 = BLE 경로 전체(인접 모듈 포함)** |
| 브랜치 | - | `claude_feature_ble-protocol-restructure` 생성 |
| 착수 관측 | - | `ble/` 규모 1차 측정 (뒤에 오측으로 판명 - 아래 정정 참조) |
| 측정 정정 | - | **줄 수 오측 발견·정정.** `Measure-Object -Line` 이 빈 줄을 세지 않아 실제보다 적게 나왔다. LF 기준 재실측 확정: `tdc_ble_mapping.c` **2,196** · `tdc_ble_remote.c` **1,389** · `.c` 6개 합계 **4,637**, 상위 2파일이 **77.3%**. 노드 03·05 가 독립 실측으로 오케스트레이터 브리핑을 반증한 건이다 |
| 착수 관측 | - | **[중요] 매핑 명령이 0x60~0x71 · 0x90~0x91 대역**(`EN__MAPPING_COMMAND`, `tdc_ble_protocol.h:84-113`)임을 확인. 은수님 제시 6개 그룹(0x30/0x40/0x80/0xA0/0xC0/0xE0) 어디에도 속하지 않음 -> 그룹 체계 공백으로 보고 |
| ① | 요구사항 | [`요구사항.md`](요구사항.md) 작성 - 요구사항 8건 · 제약 5건 · 완료조건 4건 |
| ② | 요구사항-검증 | [`요구사항-검증.md`](요구사항-검증.md) 작성. 결함 0 · 보완 2 · 정정 1(비율 84% -> 78.6%) |
| 은수님 정정 | - | **0x60~0x7F = 매핑 프로토콜 범위** 확인(누락분 보완). §4 그룹표에 `매핑 프로토콜_60` 등재 -> 미결_1 해소. **0x90~0x91 2건은 여전히 그룹 밖**이라 미결_2 로 이관 |
| ③ | 분석 | 오케스트레이터 사전 파악: **fetch/step 2단 구조** · **헤더 범위 if-else 디스패치** · `ST__MAPPING_PACKET` 이 8종 페이로드를 union 없이 보유 · 상태 변수 이중화(`fetched_command`/`command`) 확인 |
| ③ | 분석 | 7노드 병렬 fan-out 착수. 축: 프로토콜 카탈로그 · 디스패치 경로 · 매핑 골격 · **라이브모드 심층** · 리모콘/기타 · 로그 디코딩 · 인접모듈 결합도. 위임 설계는 [`에이전트-로그/00_오케스트레이터-입력.md`](에이전트-로그/00_오케스트레이터-입력.md) |
| ③ | 분석 | 7노드 전부 완료. 오케스트레이터 **직접 재검증 8건** 수행 후 [`분석.md`](분석.md) 종합. 교차 검증 5건 성립 |
| ④ | 분석-검증 | [`분석-검증.md`](분석-검증.md) 작성. 논리 결함 0 · **근거 등급 오표기 3건 자체 적발·정정** · 반증 4건 시도(1건 부분 성립 -> 설계 조건 도출) · 표본 재확인 8/8 일치 |
| 은수님 확인 | - | **0x91(`en__mapping_read_Connected_ISD_id`)을 매핑 프로토콜_60 에 편입.** 예외적으로 사용되는 헤더이나 실사용되므로 그룹에 포함. 노드 01 분석(fetch·step 양쪽 정상 처리)과 부합 -> 미결_2 해소. **0x90 은 핸들러 부재라 미편입**(결함_1 유지, 향후 처리는 미결_3) |
| ⑤ | 계획 | [`계획.md`](계획.md) 작성. 재구조화 **5단계 로드맵**(결함트랙 -> 파일분할 -> 응답공통화 -> 디스크립터테이블 -> 상태계약 -> FSM분리) + ⑦ 문서 저술 계획 |
| ⑥ | 계획-검증 | [`계획-검증.md`](계획-검증.md) 작성. **중대 논리 결함 1건 자체 적발** - 단계_1 "순수 이동 + 바이트 동일 검증"이 자기모순(거대 단일 함수라 함수 추출 필수) -> 1a/1b 분리 및 검증 재설계. 반증 3건 시도 |
| 문서 수정 | - | **Mermaid 문법 오류 수정** (은수님 지적). 분석.md §5.3 + `에이전트-로그/04` 4개 다이어그램. 원인 3종 - ① `stateDiagram-v2` 전이 레이블 안의 **두 번째 콜론**(`(:29)`·`(파일.c:710)`)이 파서를 깨뜨림 ② **`\n` 이스케이프**는 Mermaid 미지원(`<br/>` 사용, flowchart 한정) ③ 레이블 내 화살표 문자(`->`). 근거는 다이어그램 밖 표로 분리해 해결 |
| 승인 | - | **승인 완료** (2026-07-27 은수님) - ⑦ 구현(참고 문서 저술) 착수 |
| ⑦ | 구현 | `docs/참고/ble/` 하위 4폴더 정비 - `프로토콜/`(3) · `코드구조/`(4) · `라이브모드/`(3) · `재구조화/`(3). 저술 3건은 서브에이전트 위임, 재구조화·인덱스는 직접 작성. `README.md` 폴더 인덱스로 갱신, `docs/tasks/ble/작업 목록.md` 신규, `모듈 현황.md` 갱신 |
| **분류 오류 정정** | ⑦ 중 | **0x90 을 결함으로 분류한 것은 오류.** [`cm3-소스-트리-규약.md:41`](../../../참고/cm3-소스-트리-규약.md) 에 "`en__mapping_testStimulation`**(핸들러는 제거)**" 로 2차 리팩토링의 확정 결정이 이미 기록돼 있었다. **의도적 미지원**이며 조치 불필요. 저술 노드가 원본 규약을 대조하다 적발. 결함 13건 -> **12건 + 취소 1건**. 교훈: "핸들러가 없다"는 사실과 "결함이다"라는 해석은 별개이며, 문서화된 설계 결정을 먼저 확인해야 한다 |
| 문서 수정 | ⑦ 중 | **Mermaid 문법 오류 추가 수정** (은수님 지적, `에이전트-로그/05` §6). 원인 - 노드 레이블 안의 **이스케이프 큰따옴표(`\"`)** 와 **대괄호(`[PASSKEY]`)**. 같은 파일의 다른 차트 2개도 위험 요소 제거(레이블 내 `[]`, 점선 화살표 레이블의 괄호). 전 문서 재검사 결과 잔존 0건 |
| ⑦ | 구현 | 참고 문서 저술 (`docs/참고/ble/<세부폴더>/`) |
| ⑧ | 구현-검증 | 진행 예정 |

## 주요 결정

| # | 결정 | 시점 | 근거 |
|---|---|---|---|
| 결정_1 | 이번 작업은 **분석·설계까지, 코드 무변경** | 2026-07-27 | 은수님 선택. "리팩토링의 단초" 라는 목적에 부합하며 설계안을 보고 방향 결정 |
| 결정_2 | 분석 범위는 **BLE 경로 전체**(QCC/SPI 수신 ~ isd/stim 호출) | 2026-07-27 | 은수님 선택. 라이브모드 복잡도의 실제 근거가 isd 맵 제어·자극 파라미터에 있어 경계 안쪽만 봐서는 설계 근거가 약해짐 |

## 이슈

(없음)

## 완료 요약

**2026-07-27 완료.** 코드 변경 0건, 분석·설계 문서 산출.

### 결론

| # | 결론 |
|---|---|
| 결론_1 | **은수님 가설(데이터 인덱스 분기가 복잡도 주원인)은 참.** 라이브를 배제해도 비-라이브 파싱 결정 포인트의 약 59% 차지. 단 20바이트 MTU 제약의 필연적 귀결이라 **제거 아닌 흡수 대상**이며, 매핑만이 아닌 **BLE 전역 패턴**(리모콘 0x4C·0x4D 동일) |
| 결론_2 | **진짜 고칠 수 있는 근인 3건** - ① 함수 분해 부재(`fetch_packet` 단일 함수 1,724줄) ② 라이브 상태 4층 분산 ③ 프로토콜 파서가 자극 FSM 의 tick 드라이버를 겸함 |
| 결론_3 | **라이브모드가 어려운 이유 규명** - 상태가 4층에 독립 존재하고 **층간 관계가 코드 어디에도 명시돼 있지 않다**. 이해하려면 2,196줄을 다 읽고 재구성해야 한다 |
| 결론_4 | **폴더 분할 최대 걸림돌은 `ble/` 내부가 아니라 `isd/` 와의 역결합** - `isd/` 6파일이 `ble/` 상태를 56회 참조·클리어. 읽기가 아닌 **쓰기 결합**이라 헤더 분리로 해소되지 않는다 |
| 결론_5 | **검증된 재구조화 템플릿이 사내에 있다** - `tdc_ble_general_debug.c`(2026-07-01)의 "패킷 포인터 주입 + `tx_index` 반환" 패턴을 `tdc_ble_gain_control.c` 가 재사용 중 |

### 산출물

| 위치 | 내용 |
|---|---|
| `docs/tasks/ble/20260727_ble-protocol-restructure/` | 8단계 산출물 8건 + `에이전트-로그/` 9건 |
| `docs/참고/ble/프로토콜/` | 명령 카탈로그(63개 SSOT) · 패킷 구조와 응답 규약 + README |
| `docs/참고/ble/코드구조/` | 아키텍처와 디스패치 · 복잡도 실측 · 결합도와 계층 경계 + README |
| `docs/참고/ble/라이브모드/` | **상태머신 사양** · 실측 로그 대조 + README |
| `docs/참고/ble/재구조화/` | 재구조화 설계안(6단계) · 알려진 결함 목록 + README |
| `docs/참고/ble/README.md` | 폴더 전체 인덱스로 확장 + 로그 해석 함정 경고 |
| `docs/tasks/ble/작업 목록.md` | 신규 (모듈 인덱스 부재 해소) |

### 발견한 결함

12건 기록 + 1건 취소. 상세는 [`참고/ble/재구조화/알려진 결함 목록.md`](../../../참고/ble/재구조화/알려진%20결함%20목록.md).

- **중**: 리모콘 0x51·0x53·0x58 핸들러 부재로 명령 무응답 (주석이 이미 증상 증언)
- **조사 필요**: 라이브 재진입 가드가 **상태 전이 구간에서 `en__Stop` 을 거부**. 정상 운용 중(HoldOn)에는 정상 동작하며 위험 창 길이는 미정량
- **취소**: 0x90 은 결함이 아니라 2차 리팩토링의 의도된 미지원

### 자체 적발한 오류 4건

| # | 오류 | 적발 경로 |
|---|---|---|
| 오류_1 | 줄 수 오측 (`Measure-Object -Line` 이 빈 줄 미포함) | 노드 03·05 독립 실측이 오케스트레이터 브리핑을 반증 |
| 오류_2 | 근거 등급 오표기 3건 (단일 노드 산출을 "확정"으로 표기) | ④ 분석-검증 자체 점검 |
| 오류_3 | ⑤ 계획 단계_1 자기모순 ("순수 이동 + 바이트 동일 검증"이 거대 단일 함수에서 성립 불가) | ⑥ 계획-검증 자체 점검 |
| 오류_4 | **0x90 을 결함으로 분류** (문서화된 설계 결정 미조회) | ⑦ 저술 노드가 원본 규약 대조 중 적발 |

### 이월

[`docs/tasks/ble/작업 목록.md`](../작업%20목록.md) 이월 항목 참조. 핵심은 ① 라이브 Stop 거부 위험 창 정량화 ② 0x51·0x53·0x58 처리 방향 ③ 재구조화 로드맵 단계 선택 ④ 패스키 인증 활성화 여부.

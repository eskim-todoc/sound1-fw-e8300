---
name: ble 모듈 작업 목록
purpose: BLE 통신·프로토콜 관련 작업 인덱스
type: tasks/인덱스
applies_to: [Sound1]
tags: [ble, tasks, index, protocol, qcc]
---

# 작업 목록 - ble

**TL;DR**: BLE 통신 계통(QCC 연동 · 매핑/리모콘 프로토콜 · OTA 경로) 작업 인덱스. 2026-07-27 에 **BLE 프로토콜 구조 전면 분석**이 수행되어, 이후 모든 BLE 리팩토링의 기준 문서가 [`docs/참고/ble/`](../../참고/ble/README.md) 에 정비됐다.

| 작업명 | 태그 | 폴더 | 상태 | 시작·완료 | 연계 | 요약 |
|---|---|---|---|---|---|---|
| QCC OTA 후 셧다운 진입 딜레이 | qcc, shutdown, delay, ota, dfu | [20260514_qcc-shutdown-delay](20260514_qcc-shutdown-delay/) | **완료** | 2026-05-14 ~ 05-18 | <- signalProcessing/20260514_nofm-merge (부수 발견) | OTA DFU 후 Ezairo 재부팅 전 QCC 절전 진입. `_fetch_packet_boot_select()` 에 응답 송신 20ms 대기 + SHUTDOWN + 2.2초 대기 + 재부팅 |
| QCC가 NRF_On_OFF 역할 커버하는지 분석 | qcc, nrf, on-off, coverage, architecture-shift | [20260629_qcc-nrf-onoff-coverage](20260629_qcc-nrf-onoff-coverage/) | **완료** (분석) | 2026-06-29 | - | 8노드 fan-out. 평결 `architecture_shift` - 결정 주체가 E8300 에서 QCC 펌웨어로 위임됨. 5조건 중 조건_4(Mapping_BLE_Off)만 실질 gap, 조건_5 는 의도적 정책 반전. 할루시네이션 0. 코드 변경 0 |
| 범용 디버깅 프로토콜 파일 분리 | general-debug, tdc, refactor, file-split, behavior-preserving | [20260701_general-debug-protocol-split](20260701_general-debug-protocol-split/) | **완료** (구현·검증) | 2026-07-01 | - | `remoteControl.c` 의 0x8F 처리 108줄을 `tdc_ble_general_debug.c/.h` 로 분리. **패킷 포인터 주입 + `tx_index` 반환** 순수함수형 패턴 확립 - 이후 `tdc_ble_gain_control.c` 가 재사용. 회귀·컴파일 정합성 검증 전수 PASS |
| BLE 패킷 버퍼 8비트 전환 및 구조 재설계 | spi, dma, uint8, word-size, unit-module, descriptor-table, hw-5gate | [20260727_ble-8bit-packet-redesign](20260727_ble-8bit-packet-redesign/) | **완료** (B1~B3c 전환·실기 확인) | 2026-07-27 ~ 07-28 | <- 20260727_ble-protocol-restructure (전제 재검토) | 은수님 제안 - SPI1 은 이미 8비트 워드인데 DMA 만 32비트라 패킷 버퍼가 `int[]`(84바이트)이 됐다는 지적. 5게이트 검증 후 `WORD_SIZE_8BITS_TO_8BITS` 전환. **`transferLength`(21)·주소 증가는 손댈 필요 없음**을 부트로더 `dma_len_mul` 로 실증. 최초 실기 게이트로 잡았던 니블 재배치는 원문 조건절(`If byte ordering is enabled`)이 현 설정에서 불성립이라 폐지. **어댑터 병행으로 파일 단위 이행** - B1(HAL) -> B2(RX) -> B3a(ble) -> B3b(isd) -> B3c(dfu·sys+어댑터제거). 전 단계 빌드 무결·실기 정상. **정적 메모리 186B·코드 1,448B 절감**, 잔여 `int` 조립 버퍼 26->0. 전환 중 기존 결함 2건 발견·수정(바이폴라 경계·EEPROM 슬롯0) |
| **BLE 응답 조립 공통화 (단계_2)** | reply-builder, response-assembly, stateless-api, refactor | [20260728_ble-reply-builder](20260728_ble-reply-builder/) | **완료** (구현·실기 확인) | 2026-07-28 | <- 20260728_ble-folder-restructure (적용 지점 명확화) | 에러 응답은 공통화돼 있었으나 정상 응답은 명령마다 손으로 조립하던 것을 빌더로 흡수. **핵심은 API 형태 결정** - `tx_index` 가 함수 진입 시 한 번만 0 이 되고 각 응답 후 리셋되지 않는 구조(switch case 가 배타적이라 성립)라, 빌더가 인덱스를 소유하면 리셋 시점을 새로 정해야 해 동작 변경 위험이 생긴다. **호출자가 인덱스를 계속 관리하는 stateless API** 로 원천 회피. 16비트 분해 표기가 4변형(마스크 생략·`(char)` 캐스팅·`0x00FF`·`0xff & (v>>8)`)이었으나 `uint8_t` 대입의 하위 8비트 절단으로 **전부 등가**(음수 포함)임을 확정해 하나로 통일. `ble/` **133곳 전환**(header 41 · u8 81 · u16 8 · u32 3). 검증은 **조건 분기 총수 `+0`** - 빌더 전환은 분기를 만들지도 없애지도 않으므로 필요조건이자 사실상 충분조건. 5개 전환 커밋 전부 `+0`, 설명 불가 차이 0건. **크기 -568B**(계획 상한 +300B의 반대 방향). 조사 누락 2건(32비트 분해 3곳 · `sound1/` 25곳)은 전환 중 발견해 처리. `isd/` 21곳은 역결합 우려로 의도적 제외 |
| **BLE `ble/` 폴더 개편 (단계_1a·1b)** | refactor, folder-split, function-extraction, elf-identical, file-prefix-map, disassembly | [20260728_ble-folder-restructure](20260728_ble-folder-restructure/) | **완료** (구현·실기 확인) | 2026-07-28 | <- 20260727_ble-protocol-restructure (설계안 단계_1a·1b) | 설계안의 전제 3건을 실측으로 뒤집고 진행. ① **"`.elf` 바이트 동일"이 그대로는 불성립** - `TDC_PRINTF_E` 가 `__FILE__` 을 써서 소스 경로가 `.rodata` 에 박힌다(실측 +8B). `-ffile-prefix-map` 으로 정규화해 **바이트 동일 확증**(md5 `BB7F5AF6…`). 1차 대조 FAIL 의 원인은 **링크 순서**였고 순서를 맞추자 일치. ② **설계안이 함수 지역 static 4개를 인지 못함** - `prev_subCommandData_Num_index` 가 잔류·stim·flash 3그룹에 23곳 공유. 접근자 2개로 노출하고 읽기/쓰기 분류표를 어셈블리 호출 수와 1:1 대조. ③ `mapping/` 3분할 분류가 명령 5개(0x6B·0x6E·0x6F·0x70·0x71) 누락 -> flash 로 재분류. `system/` 은 넣을 파일이 없어 미생성(0x30~0x36 은 `tdc_ble_communication.c` 내부). **`tdc_ble_mapping.c` 2,196 -> 664줄, `fetch_packet` 1,724줄 -> 디스패치만.** 5커밋(`e5cd6a4`·`3eabdb0`·`373bffc`·`9cbb6c8`·`684096d`). 검증은 **조건 분기 총수 보존**(265/254/143/112 전부 `+0`) + 호출 대상 집합 + 범위 검사 상수 + 브레이스, 설명 불가 차이 0건 |
| **BLE 라이브 상태 계약 명시 (단계_4)** | state-contract, hold-on, guard, live-stimulation, layered-state | [20260729_ble-live-state-contract](20260729_ble-live-state-contract/) | **완료** (구현·실기 확인) | 2026-07-29 ~ 07-30 | <- 20260729_ble-descriptor-table (단계_3 계층 네이밍 위에서 진행) | 설계안 단계_4. 라이브 상태 4층의 층간 규칙이 각 함수 안 조건문에만 존재하는 것을 코드 계약으로 승격. **핵심 판단 - 설계안이 제안한 "서브커맨드별 허용 선행 상태 테이블"을 채택하지 않는다**: 단계_3 에서 같은 성격의 테이블이 가독성 문제로 기각됐고 게이트가 4개뿐이라 규모 이점도 없다. 대신 **전제를 함수 이름으로 선언**(`tdc_ble_cmd_0x66_require_hold_on()`)하고 조기 반환으로 본문 들여쓰기를 걷는다. **실측** - 층_2(`subCommand`) 접근 43곳(`cmd_0x66_live.c` 22 · `mapping.c` 2 · `isd_map_live.c` 19), `en__HoldOn` 게이트 4곳(하위 3·4·5·6)이 **완벽 균일**(차이는 포맷상 줄바꿈뿐), 네 함수 모두 `else` 가 마지막 블록이라 조기 반환 안전. **`lineNumber` 는 어떤 설계든 값이 바뀐다** — 계약이 아님을 근거 3건으로 확정(`stub_ble.h` 명시 · 단계_1b 에서 이미 전량 변경 · 카탈로그도 라인 참조 폐기)하고, 보존할 성질("명령마다 다른 값")은 `__LINE__` 인자 전달로 유지. **현재 테스트가 게이트 4곳 중 2곳만 덮음**(하위 5·6 커버리지 0) → 가드 수렴 시 한 곳이 틀리면 4곳이 동시에 깨지므로 테스트 선행이 단계_3 보다 중요. 그룹_3(조건 없는 하위 2·7·8 이 어느 상태에서도 수락되는지)이 가드 오부착 검출용 핵심 안전장치. `isd/` 19곳과 재진입 가드 구조 변경은 범위 밖(단계_5·결함_4 정량화 선행). **구현 결과** — 커밋_1(테스트 26케이스, 기존 코드로 통과) → 커밋_2(가드 수렴). **게이트 수렴 검증**: `subCommand == en__HoldOn` 5->1 · `en__Command_Order` 5->1 · `else` 14->10, 반면 **본문 지표는 전부 불변**(`if (` 45 · `for (` 18 · `en__OutOfDataRange` 7) — 분기를 없앤 게 아니라 이름 있는 자리로 옮긴 것. 수락 조건을 9개 하위 명령 머리에 전부 명시(조건 없는 것도 "없음"이라 적어 가드 누락과 구분). **301 PASS · ARM 에러 0 경고 0**. **실기 통과**(2026-07-30) — 게이트 **통과**(라이브 중 4개 명령) · **거부**(라이브 아닐 때 거부 + 명령 순서 에러) · **무조건 수락**(시작 · 종료 · 상태 읽기) 세 경로 전부. 정적 검증은 게이트 **오부착**과 조건 **반전**을 잡지 못하므로, 항목 3·4·5 동시 통과가 세 오류 유형을 배제한 실질 근거다 |
| **BLE 0x66 라이브 자극 계층 네이밍 재구조화 (단계_3)** | command-scoped-file, layered-naming, live-stimulation, tests-first, short-enums, readability | [20260729_ble-descriptor-table](20260729_ble-descriptor-table/) | **완료** (구현·실기 확인) | 2026-07-29 ~ 07-30 | <- 20260728_ble-reply-builder (단계_2 후속) · [tests/20260728_tests-infra](../tests/20260728_tests-infra/) (안전망 전제) | 설계안 단계_3. 409줄짜리 `tdc_ble_map_stim_live_all_parameter()` 의 데이터 인덱스 1~15 를 분해. **두 설계를 시도해 1차가 기각됐다.** ① **디스크립터 테이블**(23엔트리 + 순회 루프) — 지표는 전부 우수(`text` **-1,276B**, 함수 409->101줄, 검증 6종 통과)했으나 **은수님 검토에서 기각**: 매크로 인자 `(f, 0, 17, 1, 1, 100)` 이 정의를 찾아가야 뜻을 알고, 필드 배열 + 슬롯 `first` 오프셋의 **이중 관리**라 인덱스 1 에 필드 하나만 추가해도 슬롯 14줄이 연쇄로 밀렸다. `05c3a81` 로 revert. ② **확정 — 프로토콜 계층을 함수 이름에**(`tdc_ble_cmd_0x66_subNN_idxKK_<이름>`) + **명령 단위 파일**(`tdc_ble_cmd_0x66_live.c`). 데이터 인덱스 15개가 각각 18~21줄 독립 함수가 되고 남은 `switch` 는 **15줄 목차**. 하위 명령 2~8 은 단계_1b 에서 이미 분리돼 있었고 **하위 1 만 비대칭**이었던 것을 해소. **크기는 +588B 로 늘었으나 의도된 교환**(`-O0` 호출 오버헤드, 판단 기준이 지표가 아니라 가독성). **발견_1: "페이로드 필드는 전부 `int`" 전제가 거짓** — `stimulationMode` 만 enum 이고 ARM EABI `-fshort-enums` 기본이라 **1바이트**, 호스트 컴파일러는 4바이트 → 폭 기반 접근 시 **호스트 테스트는 통과하고 실기에서만 깨진다**. **발견_2: `default:` 는 죽은 코드가 아님** — 데이터 인덱스 카운터를 `map_flash`(최대 34)와 공유해 16~35 가 실제 도달. **커밋_1 을 테스트 선행으로 배치**(인덱스 2~15 는 커버리지 0 이었다) 후 기존 코드로 통과시켜 안전망을 성립시키고 뮤테이션 2건으로 검출력 확인 — 이 89케이스가 두 설계를 모두 검증했다. 재검증은 **조건 분기 총수 6종 원본과 동일** + 범위 상수 다중집합 일치. **275 PASS / 0 FAIL · ARM 에러 0 경고 0**. 0x65 는 확대 단계로 이월. **후속 — `명령 카탈로그.md` 소스 참조 73건을 라인 번호에서 함수 이름 기준으로 전환**(은수님 확정). 라인 참조는 리팩토링마다 통째로 깨져(단계_1b 로 73건 일괄 무효, 단계_3 으로 또 변경) 기준 자체를 바꿨다. 라인 참조 **0건**, 함수 참조 114건. **발견_3** — 카탈로그를 함수 기준으로 재작성하니 미분해 그룹이 드러났다: 리모콘 0x40~0x59 는 전부 `tdc_ble_remote_fetch_packet()`(약 1,340줄) 안, Ezairo 0x33~0x36 은 전부 `setting_nrf_ble_adv_info()`(183줄, 이름과 달리 배터리·전원·LED·클래식 상태 응답을 모두 담음) 안 — **다음 분해 후보**. **실기 통과**(2026-07-30) — Eclipse F5 Refresh 후 Clean 재빌드로 신규 파일 인식(text **184,908**), 라이브 전체 파라미터 **15단계 완주** + 자극 반영 동일. 정적 검증이 못 덮던 **파싱 순서·`prev_subCommandData_Num_index` 공유 상태**가 이로써 확증됐다 |
| **BLE 프로토콜 구조 분석 및 재구조화 설계** | protocol, mapping, live-stimulation, complexity, coupling, analysis, restructure | [20260727_ble-protocol-restructure](20260727_ble-protocol-restructure/) | **완료** (분석·설계, 코드 무변경) | 2026-07-27 | <- cm3/20260723_cm3-full-refactor-2nd (프로토콜 존치 원칙) | 7노드 fan-out + 직접 재검증 8건. **은수님 가설(데이터 인덱스 분기가 주원인) 참으로 확정**(비-라이브 결정 포인트 약 59%)하되 20바이트 MTU 제약의 필연이라 **제거 아닌 흡수 대상**으로 규정. 근인 3건 규명(함수 분해 부재 `fetch_packet` 1,724줄 · 라이브 상태 **4층 분산** · 파서가 자극 FSM 구동). **`ble/`-`isd/` 가 하나의 상태머신을 두 폴더에 쪼개 보유**(역호출 56회)가 분할 최대 걸림돌. 결함 13건 기록(0x90 상시 에러 · 0x51/0x53/0x58 핸들러 부재 · 라이브 전이 구간 Stop 거부). 6단계 로드맵 설계. 참고 문서 [`docs/참고/ble/`](../../참고/ble/README.md) 정비 |

## 이월 항목

| 항목 | 출처 | 상태 |
|---|---|---|
| BLE 리모콘 패스키 인증 비활성 | cm3 2차 리팩토링 | **이월 유지** - `#if 0` 이 아니라 `remocon_passkey_Match = true;` 무조건 대입(`tdc_ble_remote.c:698`). 원 비교 루프의 입력이던 `tdc_shm_read_remocon_passkey_connected_isd()` 는 호출부 없는 고아 함수. 보안 결정 사안 |
| ~~`en__mapping_testStimulation`(0x90) 처리 방향~~ | 20260727 구조 분석 | **해소** - 미결이 아니었다. [`cm3-소스-트리-규약.md:41`](../../참고/cm3-소스-트리-규약.md) 에 "핸들러는 제거"로 확정 기록돼 있는 **의도적 미지원**. 조치 불필요 |
| 라이브 재진입 가드의 Stop 거부 위험 창 정량화 | 20260727 구조 분석 | **미결** - 정량화가 조치의 선행 조건. [`알려진 결함 목록.md`](../../참고/ble/재구조화/알려진%20결함%20목록.md) 결함_4 |
| **바이폴라 전극번호 경계 결함 정밀 재측정** | 20260727 실기 | **수정 적용·재측정 필요** - `electrodeMap[99-1]` 범위 밖 읽기를 범위 검사로 방어했고 1차 실기에서 증상 소멸 확인. 단 **쓰레기값이 백텔 COUNT 0 의 직접 원인인지 미확정.** 재측정 절차는 [`바이폴라 전극번호 경계 결함.md §5`](../../참고/ble/재구조화/바이폴라%20전극번호%20경계%20결함.md) |
| `specific_stim` 바이폴라+기준전극99 조합 미방어 | 20260727 실기 | **미결** - 파싱이 99 를 허용하므로(`tdc_ble_mapping.c:277`) 비정상 조합 시 같은 범위 밖 읽기. 정상 앱은 보내지 않는 조합이라 보류. `:258` 레지스터 조합에 `0x1F` 마스크 부재도 함께 |
| 리모콘 0x51·0x53·0x58 핸들러 부재 | 20260727 구조 분석 | **미결** - 실사용 여부 확인 후 구현 또는 미지원 명문화 |
| 재구조화 로드맵 단계 선택 | 20260727 구조 분석 | **미결** - 6단계 중 어디까지 적용할지 은수님 판단 |

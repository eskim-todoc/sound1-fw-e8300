---
name: ble 모듈 작업 목록
purpose: BLE 통신·프로토콜 관련 작업 인덱스
type: tasks/인덱스
applies_to: [Sound1]
tags: [ble, tasks, index, protocol, qcc]
---

# 작업 목록 - ble

**TL;DR**: BLE 통신 계통(QCC 연동 · 매핑/리모콘 프로토콜 · OTA 경로) 작업 인덱스. 2026-07-27 에 **BLE 프로토콜 구조 전면 분석**이 수행되어, 이후 모든 BLE 리팩토링의 기준 문서가 [`docs/참고/ble/`](../../참고/ble/README.md) 에 정비됐다.

| 작업명 | 태그 | 폴더 | 상태 | 시작·완료 | 연계 | 요약 |
|---|---|---|---|---|---|---|
| QCC OTA 후 셧다운 진입 딜레이 | qcc, shutdown, delay, ota, dfu | [20260514_qcc-shutdown-delay](20260514_qcc-shutdown-delay/) | **완료** | 2026-05-14 ~ 05-18 | <- signalProcessing/20260514_nofm-merge (부수 발견) | OTA DFU 후 Ezairo 재부팅 전 QCC 절전 진입. `_fetch_packet_boot_select()` 에 응답 송신 20ms 대기 + SHUTDOWN + 2.2초 대기 + 재부팅 |
| QCC가 NRF_On_OFF 역할 커버하는지 분석 | qcc, nrf, on-off, coverage, architecture-shift | [20260629_qcc-nrf-onoff-coverage](20260629_qcc-nrf-onoff-coverage/) | **완료** (분석) | 2026-06-29 | - | 8노드 fan-out. 평결 `architecture_shift` - 결정 주체가 E8300 에서 QCC 펌웨어로 위임됨. 5조건 중 조건_4(Mapping_BLE_Off)만 실질 gap, 조건_5 는 의도적 정책 반전. 할루시네이션 0. 코드 변경 0 |
| 범용 디버깅 프로토콜 파일 분리 | general-debug, tdc, refactor, file-split, behavior-preserving | [20260701_general-debug-protocol-split](20260701_general-debug-protocol-split/) | **완료** (구현·검증) | 2026-07-01 | - | `remoteControl.c` 의 0x8F 처리 108줄을 `tdc_ble_general_debug.c/.h` 로 분리. **패킷 포인터 주입 + `tx_index` 반환** 순수함수형 패턴 확립 - 이후 `tdc_ble_gain_control.c` 가 재사용. 회귀·컴파일 정합성 검증 전수 PASS |
| BLE 패킷 버퍼 8비트 전환 및 구조 재설계 | spi, dma, uint8, word-size, unit-module, descriptor-table, hw-5gate | [20260727_ble-8bit-packet-redesign](20260727_ble-8bit-packet-redesign/) | **완료** (B1~B3c 전환·실기 확인) | 2026-07-27 ~ 07-28 | <- 20260727_ble-protocol-restructure (전제 재검토) | 은수님 제안 - SPI1 은 이미 8비트 워드인데 DMA 만 32비트라 패킷 버퍼가 `int[]`(84바이트)이 됐다는 지적. 5게이트 검증 후 `WORD_SIZE_8BITS_TO_8BITS` 전환. **`transferLength`(21)·주소 증가는 손댈 필요 없음**을 부트로더 `dma_len_mul` 로 실증. 최초 실기 게이트로 잡았던 니블 재배치는 원문 조건절(`If byte ordering is enabled`)이 현 설정에서 불성립이라 폐지. **어댑터 병행으로 파일 단위 이행** - B1(HAL) -> B2(RX) -> B3a(ble) -> B3b(isd) -> B3c(dfu·sys+어댑터제거). 전 단계 빌드 무결·실기 정상. **정적 메모리 186B·코드 1,448B 절감**, 잔여 `int` 조립 버퍼 26->0. 전환 중 기존 결함 2건 발견·수정(바이폴라 경계·EEPROM 슬롯0) |
| **BLE 응답 조립 공통화 (단계_2)** | reply-builder, response-assembly, stateless-api, refactor | [20260728_ble-reply-builder](20260728_ble-reply-builder/) | **완료** (구현·실기 확인) | 2026-07-28 | <- 20260728_ble-folder-restructure (적용 지점 명확화) | 에러 응답은 공통화돼 있었으나 정상 응답은 명령마다 손으로 조립하던 것을 빌더로 흡수. **핵심은 API 형태 결정** - `tx_index` 가 함수 진입 시 한 번만 0 이 되고 각 응답 후 리셋되지 않는 구조(switch case 가 배타적이라 성립)라, 빌더가 인덱스를 소유하면 리셋 시점을 새로 정해야 해 동작 변경 위험이 생긴다. **호출자가 인덱스를 계속 관리하는 stateless API** 로 원천 회피. 16비트 분해 표기가 4변형(마스크 생략·`(char)` 캐스팅·`0x00FF`·`0xff & (v>>8)`)이었으나 `uint8_t` 대입의 하위 8비트 절단으로 **전부 등가**(음수 포함)임을 확정해 하나로 통일. `ble/` **133곳 전환**(header 41 · u8 81 · u16 8 · u32 3). 검증은 **조건 분기 총수 `+0`** - 빌더 전환은 분기를 만들지도 없애지도 않으므로 필요조건이자 사실상 충분조건. 5개 전환 커밋 전부 `+0`, 설명 불가 차이 0건. **크기 -568B**(계획 상한 +300B의 반대 방향). 조사 누락 2건(32비트 분해 3곳 · `sound1/` 25곳)은 전환 중 발견해 처리. `isd/` 21곳은 역결합 우려로 의도적 제외 |
| **BLE `ble/` 폴더 개편 (단계_1a·1b)** | refactor, folder-split, function-extraction, elf-identical, file-prefix-map, disassembly | [20260728_ble-folder-restructure](20260728_ble-folder-restructure/) | **완료** (구현·실기 확인) | 2026-07-28 | <- 20260727_ble-protocol-restructure (설계안 단계_1a·1b) | 설계안의 전제 3건을 실측으로 뒤집고 진행. ① **"`.elf` 바이트 동일"이 그대로는 불성립** - `TDC_PRINTF_E` 가 `__FILE__` 을 써서 소스 경로가 `.rodata` 에 박힌다(실측 +8B). `-ffile-prefix-map` 으로 정규화해 **바이트 동일 확증**(md5 `BB7F5AF6…`). 1차 대조 FAIL 의 원인은 **링크 순서**였고 순서를 맞추자 일치. ② **설계안이 함수 지역 static 4개를 인지 못함** - `prev_subCommandData_Num_index` 가 잔류·stim·flash 3그룹에 23곳 공유. 접근자 2개로 노출하고 읽기/쓰기 분류표를 어셈블리 호출 수와 1:1 대조. ③ `mapping/` 3분할 분류가 명령 5개(0x6B·0x6E·0x6F·0x70·0x71) 누락 -> flash 로 재분류. `system/` 은 넣을 파일이 없어 미생성(0x30~0x36 은 `tdc_ble_communication.c` 내부). **`tdc_ble_mapping.c` 2,196 -> 664줄, `fetch_packet` 1,724줄 -> 디스패치만.** 5커밋(`e5cd6a4`·`3eabdb0`·`373bffc`·`9cbb6c8`·`684096d`). 검증은 **조건 분기 총수 보존**(265/254/143/112 전부 `+0`) + 호출 대상 집합 + 범위 검사 상수 + 브레이스, 설명 불가 차이 0건 |
| **BLE 라이브 상태 계약 명시 (단계_4)** | state-contract, hold-on, guard, live-stimulation, layered-state | [20260729_ble-live-state-contract](20260729_ble-live-state-contract/) | **완료** (구현·실기 확인) | 2026-07-29 ~ 07-30 | <- 20260729_ble-descriptor-table (단계_3 계층 네이밍 위에서 진행) | 설계안 단계_4. 라이브 상태 4층의 층간 규칙이 각 함수 안 조건문에만 존재하는 것을 코드 계약으로 승격. **핵심 판단 - 설계안이 제안한 "서브커맨드별 허용 선행 상태 테이블"을 채택하지 않는다**: 단계_3 에서 같은 성격의 테이블이 가독성 문제로 기각됐고 게이트가 4개뿐이라 규모 이점도 없다. 대신 **전제를 함수 이름으로 선언**(`tdc_ble_cmd_0x66_require_hold_on()`)하고 조기 반환으로 본문 들여쓰기를 걷는다. **실측** - 층_2(`subCommand`) 접근 43곳(`cmd_0x66_live.c` 22 · `mapping.c` 2 · `isd_map_live.c` 19), `en__HoldOn` 게이트 4곳(하위 3·4·5·6)이 **완벽 균일**(차이는 포맷상 줄바꿈뿐), 네 함수 모두 `else` 가 마지막 블록이라 조기 반환 안전. **`lineNumber` 는 어떤 설계든 값이 바뀐다** — 계약이 아님을 근거 3건으로 확정(`stub_ble.h` 명시 · 단계_1b 에서 이미 전량 변경 · 카탈로그도 라인 참조 폐기)하고, 보존할 성질("명령마다 다른 값")은 `__LINE__` 인자 전달로 유지. **현재 테스트가 게이트 4곳 중 2곳만 덮음**(하위 5·6 커버리지 0) → 가드 수렴 시 한 곳이 틀리면 4곳이 동시에 깨지므로 테스트 선행이 단계_3 보다 중요. 그룹_3(조건 없는 하위 2·7·8 이 어느 상태에서도 수락되는지)이 가드 오부착 검출용 핵심 안전장치. `isd/` 19곳과 재진입 가드 구조 변경은 범위 밖(단계_5·결함_4 정량화 선행). **구현 결과** — 커밋_1(테스트 26케이스, 기존 코드로 통과) → 커밋_2(가드 수렴). **게이트 수렴 검증**: `subCommand == en__HoldOn` 5->1 · `en__Command_Order` 5->1 · `else` 14->10, 반면 **본문 지표는 전부 불변**(`if (` 45 · `for (` 18 · `en__OutOfDataRange` 7) — 분기를 없앤 게 아니라 이름 있는 자리로 옮긴 것. 수락 조건을 9개 하위 명령 머리에 전부 명시(조건 없는 것도 "없음"이라 적어 가드 누락과 구분). **301 PASS · ARM 에러 0 경고 0**. **실기 통과**(2026-07-30) — 게이트 **통과**(라이브 중 4개 명령) · **거부**(라이브 아닐 때 거부 + 명령 순서 에러) · **무조건 수락**(시작 · 종료 · 상태 읽기) 세 경로 전부. 정적 검증은 게이트 **오부착**과 조건 **반전**을 잡지 못하므로, 항목 3·4·5 동시 통과가 세 오류 유형을 배제한 실질 근거다 |
| **BLE 0x66 라이브 자극 계층 네이밍 재구조화 (단계_3)** | command-scoped-file, layered-naming, live-stimulation, tests-first, short-enums, readability | [20260729_ble-descriptor-table](20260729_ble-descriptor-table/) | **완료** (구현·실기 확인) | 2026-07-29 ~ 07-30 | <- 20260728_ble-reply-builder (단계_2 후속) · [tests/20260728_tests-infra](../tests/20260728_tests-infra/) (안전망 전제) | 설계안 단계_3. 409줄짜리 `tdc_ble_map_stim_live_all_parameter()` 의 데이터 인덱스 1~15 를 분해. **두 설계를 시도해 1차가 기각됐다.** ① **디스크립터 테이블**(23엔트리 + 순회 루프) — 지표는 전부 우수(`text` **-1,276B**, 함수 409->101줄, 검증 6종 통과)했으나 **은수님 검토에서 기각**: 매크로 인자 `(f, 0, 17, 1, 1, 100)` 이 정의를 찾아가야 뜻을 알고, 필드 배열 + 슬롯 `first` 오프셋의 **이중 관리**라 인덱스 1 에 필드 하나만 추가해도 슬롯 14줄이 연쇄로 밀렸다. `05c3a81` 로 revert. ② **확정 — 프로토콜 계층을 함수 이름에**(`tdc_ble_cmd_0x66_subNN_idxKK_<이름>`) + **명령 단위 파일**(`tdc_ble_cmd_0x66_live.c`). 데이터 인덱스 15개가 각각 18~21줄 독립 함수가 되고 남은 `switch` 는 **15줄 목차**. 하위 명령 2~8 은 단계_1b 에서 이미 분리돼 있었고 **하위 1 만 비대칭**이었던 것을 해소. **크기는 +588B 로 늘었으나 의도된 교환**(`-O0` 호출 오버헤드, 판단 기준이 지표가 아니라 가독성). **발견_1: "페이로드 필드는 전부 `int`" 전제가 거짓** — `stimulationMode` 만 enum 이고 ARM EABI `-fshort-enums` 기본이라 **1바이트**, 호스트 컴파일러는 4바이트 → 폭 기반 접근 시 **호스트 테스트는 통과하고 실기에서만 깨진다**. **발견_2: `default:` 는 죽은 코드가 아님** — 데이터 인덱스 카운터를 `map_flash`(최대 34)와 공유해 16~35 가 실제 도달. **커밋_1 을 테스트 선행으로 배치**(인덱스 2~15 는 커버리지 0 이었다) 후 기존 코드로 통과시켜 안전망을 성립시키고 뮤테이션 2건으로 검출력 확인 — 이 89케이스가 두 설계를 모두 검증했다. 재검증은 **조건 분기 총수 6종 원본과 동일** + 범위 상수 다중집합 일치. **275 PASS / 0 FAIL · ARM 에러 0 경고 0**. 0x65 는 확대 단계로 이월. **후속 — `명령 카탈로그.md` 소스 참조 73건을 라인 번호에서 함수 이름 기준으로 전환**(은수님 확정). 라인 참조는 리팩토링마다 통째로 깨져(단계_1b 로 73건 일괄 무효, 단계_3 으로 또 변경) 기준 자체를 바꿨다. 라인 참조 **0건**, 함수 참조 114건. **발견_3** — 카탈로그를 함수 기준으로 재작성하니 미분해 그룹이 드러났다: 리모콘 0x40~0x59 는 전부 `tdc_ble_remote_fetch_packet()`(약 1,340줄) 안, Ezairo 0x33~0x36 은 전부 `setting_nrf_ble_adv_info()`(183줄, 이름과 달리 배터리·전원·LED·클래식 상태 응답을 모두 담음) 안 — **다음 분해 후보**. **실기 통과**(2026-07-30) — Eclipse F5 Refresh 후 Clean 재빌드로 신규 파일 인식(text **184,908**), 라이브 전체 파라미터 **15단계 완주** + 자극 반영 동일. 정적 검증이 못 덮던 **파싱 순서·`prev_subCommandData_Num_index` 공유 상태**가 이로써 확증됐다 |
| **바이폴라 기준전극 99 - 0x65 경로 방어 (이월_D)** | isd, bipolar, out-of-bounds, s-tdc-table, defect-fix, hw-5gate, register-mask | [20260730_bipolar-99-specific-stim](20260730_bipolar-99-specific-stim/) | **완료** (구현·실기 확인) | 2026-07-30 | <- 20260730_ble-cmd-0x65-rename (부수 발견) · 20260727 실기 이월 | **은수님 지적으로 착수** — *"`tdc_isd_map_specific_stim_step()` 에도 바이폴라 인덱스 99 이슈가 있을 텐데 확인해 달라"*. 사실로 확증됐다. 라이브(0x66) 경로는 2026-07-27 에 **2중 방어**(범위 검사 `tdc_isd_stim_para_setting.c:486~496` + `0x1F` 마스크 `:577`·`:605`)됐으나 **0x65 경로는 0중**이었다. **인과 사슬 완성** — 파싱(`tdc_ble_cmd_0x65_specific.c:86`)이 99 를 `stimulatonMode` 무관하게 통과(주석은 "for 모노폴라"인데 모노폴라인지 검사 안 함) → 바이폴라면 `:206` 건너뛰기가 안 걸려 `case 3` 실행 → `electrodeMap[98]` = `0x20008258`, 배열 끝 **+264B 초과** → **`s_tdc_table`**(`0x200081d0`, 384B = `ui/tdc_ui_command.c:120` 의 `command_entry_t[32]`, 포인터 3개x32) 의 포인터 값 → 마스크 없이 레지스터 OR → 비트[4:0] 초과 → **ISD FIFO 클리어 → 백텔 카운트 0**. **앞단은 실기 로그값으로 확정** — "REF ELEC NUM : 99 (PCB : 70378)" 의 70378 = **0x112EA** 가 `.text` 범위(0x10000~0x3d244) 안. **뒷단은 라이브 주석(`:576`)에 이미 있었다** — "비트 [4:0] 범위, 범위 초과 시 FIFO 클리어 → 백텔 카운트 0". `para_setting.c:471~473` TODO 가 미확정이라 한 인과가 이로써 양단 모두 확정됐다(재현만 남음, 이월_E). **조치** — `case 3` 대입 앞 범위 검사(실패 시 더미값 31 유지) + `case 5`·`case 7` 마스크. `df_MaxNumTransferableChannel`(24) 실측으로 `case 5`(0~23)+`case 7`(24~31)이 **32원소 빈틈·겹침 0** 임을 확인해 마스크 2곳 전량 커버 확정. 파싱은 미변경 — 라이브도 `isd/` 에서 막으므로 **방어 위치 대칭 유지**(파싱 차단은 양쪽 함께 판단, 이월_H). **HW 5게이트 통과** — 0~31 에서 `0x1F & v == v` 라 정상 경로 비트 등가. **검증 — "정상 경로 불변 + 예측된 분기만 증가"** 신설 불변량으로 `case` 17 불변 · `if` +1 · `else` +1 · `for` 불변 · `\|\|` +3 · `&&` 불변 **6개 전부 예측 일치**. text **184,908 -> 185,072 (+164B)**. 301 PASS · 에러 0 경고 0. **발견 — 검증 지표가 주석에 오염된다**: `grep -o "case "` 가 19->21 거짓 FAIL(기준선 19 = 실제 레이블 17 + 주석 언급 2, 신규 주석이 2건 추가). **구조 지표는 행 앵커(`^\s*`), 연산자는 주석 라인 제외**로 재야 한다 — 이 프로젝트는 방어 코드에 경위를 길게 남기는 관례(라이브 11줄, 이번 20줄)라 특히 취약하며, 기존 작업들의 "분기 총수 보존"도 주석 증감이 없어 상쇄됐을 뿐이다(이월_I) |
| **BLE 0x65 특정 자극 계층 네이밍 개명 (이월_1)** | rename, cmd-0x65, specific-stimulation, layered-naming, git-mv, text-invariant, bipolar-99 | [20260730_ble-cmd-0x65-rename](20260730_ble-cmd-0x65-rename/) | **완료** (구현·실기 확인) | 2026-07-30 | <- 20260729_ble-descriptor-table (단계_3 계층 네이밍 확대) | 단계_3 의 계층 네이밍을 0x65 로 확대. **착수 즉시 이월_1 의 전제가 반전됐다** — 0x65 는 `subCommand` 참조 **0건** · 데이터 인덱스 참조 **0건**으로 **분해할 계층이 없다**(0x66 은 `subCommand` 27건). 단일 패킷 8필드를 한 번에 받고 끝나므로 `subNN_idxKK` 세그먼트가 붙을 자리가 없다. 이월_1 이 그렇게 적힌 것은 단계_3 착수 당시 0x65·0x66 이 같은 `tdc_ble_map_stim.c`(845줄)에 있었기 때문이며, 0x66 이 떠난 뒤 남은 것은 92줄 단일 함수다. **은수님 판단으로 범위를 개명으로 한정** — 92줄 중 58줄인 범위 검사 8블록은 무변경(블록마다 필드 하나 + 한글 주석으로 이미 읽히고, 테이블은 기각된 방식이며 함수 추출은 3줄x8개 과분해). **불변량을 새로 정의했다 — `text` 완전 동일.** `objdump` 로 `.rodata` 가 별도 섹션이 아니고(`.text` 병합) 파일명은 `.debug_*`, 심볼명은 `.strtab` 에만 있어 **로드 섹션에 기여 경로가 없음**을 확정(대상 파일에 `TDC_PRINTF(__FILE__)` 0건). 실측 결과 **184,908 / 4,668 / 21,804 세 값 완전 동일** — 개명 유형의 불변량이 실증됐다(인수인계 §5 노하우_2 신규 유형). **3커밋 분해** — 커밋_1 `git mv` 만(md5 로 무변경 확증, `rename 100%` · 0 insertions 기록) -> 커밋_2 심볼·참조(호출 1+11 · include 2 · `run.sh`) -> 커밋_3 문서. `git mv` 와 내용 수정을 섞으면 유사도 임계로 삭제+추가가 되므로 분리. **`__LINE__` 완전 불변**(`:109`) — `.c` 주석 무변경. **발견_3: 개명 작업의 문서 참조는 갱신 대상이 아니다** — `docs/` 구 심볼 **57건 중 갱신은 카탈로그 1건**뿐이고 나머지 56건은 과거 기록이라 고치면 "단계_1b 가 `cmd_0x65_specific.c` 를 만들었다"는 거짓이 된다. 부수로 **자기 참조 집계 함정**도 확인(문서를 쓰는 동안 `grep` 총계가 77->91 로 움직임 -> 자기 폴더 제외로 기준 고정). **검증 도구 교정 2건** — `grep -rn` 이 `src/.metadata/`(Eclipse 캐시, gitignore)를 훑어 203건 오탐 -> **`git grep` 으로 0건**; `grep -i "Error"` 가 `tdc_sys_error.o` 를 잡는 오탐. **부수 발견 — 은수님 지적으로 바이폴라 99 결함의 0x65 경로 무방비를 확증**(이월 항목 참조). 301 PASS · ARM 에러 0 경고 0 |
| **0x63·0x64 eCAP 파싱 범위 검사 (위험_1·위험_2)** | ecap, out-of-bounds, range-check, cmd-0x63, cmd-0x64, electrode-map, register-bitwidth, host-test-only | [20260805_1740_ble-ecap-parsing-range-check](20260805_1740_ble-ecap-parsing-range-check/) | **완료** (구현·호스트 검증) | 2026-08-05 | <- 20260730_bipolar-99-specific-stim (본보기) · [상시 점검 대장 §2](../../상시%20점검%20대장.md) | 상시 점검 대장 위험_1(중)·위험_2(낮음) 조치. **조치 위치를 인수인계 원안(`isd/`)에서 파싱으로 뒤집었다** — `isd/` 는 호스트 러너가 컴파일하지 않고(`run.sh:119~121`) 앱이 0x63 을 안 보내(`tdc_ble_protocol.h:91`) 실기도 불가해 **두 게이트가 모두 닫힌다**. 측정 계열은 0x62 가 이미 파싱 차단(`map_measure.c:27~53`)이라 계열 내부 대칭도 맞는다. **불가침 «방어 위치 대칭» 은 자극 계열(0x65·0x66) 규칙이지 측정 계열이 아님을 확정.** **발견_1 - 위험군 2건이 오탐**: `bipolarReferenceElectrodeNum` 은 `isd:461`·`:494` 에서 루프 변수와 **비교만** 하고 불일치 시 `electrodeMap[31]` 폴백이라 OOB 아님(0x65 는 `:225` 에서 직접 인덱스였던 것과 구조가 다르다), `maskerProbeInterval_numFrame` 은 `isd:355`·`:366` 에 **전용 에러코드 2종까지 붙은 검사가 이미 존재**하고 상한이 런타임 값 의존이라 파싱 판정 불가. **실제 OOB 벡터는 `measurementElectrodeNum`·`stimulationElectrodeNum` 2개**(`:688`·`:749`·`:906`·`:1130`). **발견_2 - 근거를 1차 정의로 승격**: `pulseWidth` 하한 13 은 0x65 리터럴 차용이 아니라 `FPGA_pulsePhaseWidth_minimum`(`FPGA_ver2_7_0.h:133`) 이며 미만이면 `map_ecap.c:935`·`:1160` 뺄셈이 **언더플로**, `stimulatonMode` 1~6 은 `EN___STIMULATION_MODE`(`tdc_shm.h:67~76`)에서 `en__referenceNA`(0) 를 뺀 실모드 전체(`isd:873` default 주석이 5·6 을 명시 의도), 전극 1~32 는 `int electrodeMap[32]`(`electrodeMapping.c:3`). **원소값이 전부 0~31 이라 인덱스를 1~32 로 강제하면 «`0x1F` 마스크 0건» 지적도 함께 무해해진다.** **발견_3 - 기존 테스트 값이 이미 무효**: `adcSamplingFreq = 12` 가 레지스터 3비트(`:730`·`:764`)를 넘고 있었다 — 사람이 필드 구분용 순번(11·12·13·14)으로 고른 값이 하드웨어 폭을 넘은 실증. **미검사 5필드는 근거 부재로 의도적 제외** — 특히 `adcPreampGain` 은 코드가 `<< 8` 인데 주석은 `[3:0]` 이고 그 주석 문구가 다른 필드 라벨(*"측정 갯수"*)의 복사 흔적이라 확정 불가(추측 금지). **검증 - 실기 불가를 호스트 테스트로 대체**: 노하우_5 의 «정상 입력에서 방어가 걸리지 않을 것» 실기 항목을 쓸 수 없으므로 경계 4점 시험이 유일한 과잉 차단 검출 수단. `test_map_measure` **52 -> 125 PASS**(전체 374). 구조 지표 6종 **사전 예측 완전 일치**(`if` 5->24 · `\|\|` 1->16 · `else` 0->2 · `&&`·`return`·`fetched_command=` 불변 - 뒤 셋이 0x62 무변경의 증거). 행 앵커·주석행 제외로 측정(이월_I 대응). text 185,072 -> 185,436(+364B). 4커밋 |
| **BLE 프로토콜 구조 분석 및 재구조화 설계** | protocol, mapping, live-stimulation, complexity, coupling, analysis, restructure | [20260727_ble-protocol-restructure](20260727_ble-protocol-restructure/) | **완료** (분석·설계, 코드 무변경) | 2026-07-27 | <- cm3/20260723_cm3-full-refactor-2nd (프로토콜 존치 원칙) | 7노드 fan-out + 직접 재검증 8건. **은수님 가설(데이터 인덱스 분기가 주원인) 참으로 확정**(비-라이브 결정 포인트 약 59%)하되 20바이트 MTU 제약의 필연이라 **제거 아닌 흡수 대상**으로 규정. 근인 3건 규명(함수 분해 부재 `fetch_packet` 1,724줄 · 라이브 상태 **4층 분산** · 파서가 자극 FSM 구동). **`ble/`-`isd/` 가 하나의 상태머신을 두 폴더에 쪼개 보유**(역호출 56회)가 분할 최대 걸림돌. 결함 13건 기록(0x90 상시 에러 · 0x51/0x53/0x58 핸들러 부재 · 라이브 전이 구간 Stop 거부). 6단계 로드맵 설계. 참고 문서 [`docs/참고/ble/`](../../참고/ble/README.md) 정비 |

## 이월 항목

> [!IMPORTANT]
> **이 절이 BLE 이월 항목의 단일 출처다.** 아래 §재구조화 이월(알파벳 라벨)은 2026-08-05 에 통합됐다.
>
> 그전까지 `이월_A`~`이월_I` 라벨은 **각 작업 폴더의 `계획.md`·`구현-검증.md` 안에만** 있었다. 살아 있는 인덱스에 없으니 *"뭐가 남았지"* 를 물을 때마다 과거 폴더를 뒤져야 했다. 배터리 매직넘버와 같은 문제(흩어져서 관계가 안 보임)라 같은 방식으로 해결한다 - **한 곳에 모은다.**

### 재구조화 이월 (알파벳 라벨)

단계_3·이월_1·이월_D 진행 중 «이번 범위 밖» 으로 밀어낸 것들이다. 원 등록처를 «출처» 에 남겨 판단 근거를 찾아갈 수 있게 한다.

| # | 항목 | 규모 | 출처 | 상태 |
|---|---|---|---|---|
| **이월_2** | **리모콘 0x40~0x59 분해** - `tdc_ble_remote_fetch_packet()` 약 1,340줄 | **최대** | 20260729 단계_3 발견_3 | **미결.** 남은 것 중 부채 최대. 실사용 기능이라 **실기 검증이 가능**하다 |
| 이월_A | `stimulatonMode` 오타 26곳 정정 | 중 | 20260730 개명 제외_2 | 미결. `isd/` 2파일 포함, 필드명 변경이라 별도 검증 축이 필요 |
| 이월_B | `알려진 결함 목록.md` 결함_14 의 이중 stale 참조 갱신 | 소 | 20260730 개명 부수_1 | 미결. 카탈로그처럼 **함수 기준 전환**이 근본 대응 |
| 이월_C | `map_flash`(10명령) · `map_measure`(3명령) 명령별 분해 | 중 | 20260730 개명 부수_2 | 미결. 계층 네이밍을 붙이려면 파일 분할이 선행 |
| ~~이월_D~~ | ~~`specific_stim` 바이폴라 99 방어~~ | - | 20260730 개명 부수 발견 | **해소 (2026-07-30)** [20260730_bipolar-99-specific-stim](20260730_bipolar-99-specific-stim/) |
| 이월_E | `tdc_isd_stim_para_setting.c:471~473` TODO 문구 갱신 | 소 | 20260730 이월_D | 미결. 인과 앞단·뒷단 모두 확정됨. **재현 확인만 남음** |
| ~~이월_F~~ | ~~`case 7` 하드코딩 `24`·`32` 상수화~~ | - | 20260730 이월_D 분석-검증 결함_1 | **해소 (2026-08-05)** [20260805_1925_carry-f-channel-constant](20260805_1925_carry-f-channel-constant/). 라이브·0x65 동시 |
| ~~이월_G~~ | ~~`tdc_isd_map_ecap.c` 의 `electrodeMap` 접근 4곳 점검~~ | - | 20260730 이월_D 제외_4 | **해소 (2026-08-05)** [20260805_1740_ble-ecap-parsing-range-check](20260805_1740_ble-ecap-parsing-range-check/). 전수 점검 + 파싱 차단까지 완료 |
| 이월_H | 파싱 단계에서 특수값(99·255) 거부 여부 | 판단 | 20260730 이월_D 제외_1·2 | **부분 해소.** eCAP(0x63·0x64)은 2026-08-05 에 결론 남 - 255 파싱 거부, 99 는 예외 허용. **자극 계열(0x65·0x66)은 여전히 `isd/` 차단이라 미결** |
| 이월_I | 구조 지표 측정을 앵커 패턴으로 통일 | 소 | 20260730 이월_D 발견 | 미결. **기존 작업들의 "조건 분기 총수 보존" 검증도 같은 취약점**을 갖는다(주석 증감이 없어 상쇄됐을 뿐) |
| ~~이월_1~~ | ~~0x65 계층 네이밍 확대~~ | - | 20260729 단계_3 | **해소 (2026-07-30)** [20260730_ble-cmd-0x65-rename](20260730_ble-cmd-0x65-rename/). 착수 후 전제가 반전돼 개명으로 축소 |

### 주제별 이월

| 항목 | 출처 | 상태 |
|---|---|---|
| BLE 리모콘 패스키 인증 비활성 | cm3 2차 리팩토링 | **이월 유지** - `#if 0` 이 아니라 `remocon_passkey_Match = true;` 무조건 대입(`tdc_ble_remote.c:698`). 원 비교 루프의 입력이던 `tdc_shm_read_remocon_passkey_connected_isd()` 는 호출부 없는 고아 함수. 보안 결정 사안 |
| ~~`en__mapping_testStimulation`(0x90) 처리 방향~~ | 20260727 구조 분석 | **해소** - 미결이 아니었다. [`cm3-소스-트리-규약.md:41`](../../참고/cm3-소스-트리-규약.md) 에 "핸들러는 제거"로 확정 기록돼 있는 **의도적 미지원**. 조치 불필요 |
| 라이브 재진입 가드의 Stop 거부 위험 창 정량화 | 20260727 구조 분석 | **미결** - 정량화가 조치의 선행 조건. [`알려진 결함 목록.md`](../../참고/ble/재구조화/알려진%20결함%20목록.md) 결함_4 |
| **바이폴라 전극번호 경계 결함 정밀 재측정** | 20260727 실기 | **수정 적용·재측정 필요** - `electrodeMap[99-1]` 범위 밖 읽기를 범위 검사로 방어했고 1차 실기에서 증상 소멸 확인. 단 **쓰레기값이 백텔 COUNT 0 의 직접 원인인지 미확정.** 재측정 절차는 [`바이폴라 전극번호 경계 결함.md §5`](../../참고/ble/재구조화/바이폴라%20전극번호%20경계%20결함.md) |
| ~~**`specific_stim` 바이폴라+기준전극99 조합 미방어**~~ | 20260727 실기 | **해소 (2026-07-30, 이월_D)** - [20260730_bipolar-99-specific-stim](20260730_bipolar-99-specific-stim/) 에서 라이브 경로 2중 방어를 이식. 범위 검사 1곳 + `0x1F` 마스크 2곳. 실기 대기. 아래는 확증 기록. **2026-07-30 은수님 지적으로 전수 확증.** 라이브(0x66) 경로는 2026-07-27 에 **2중 방어**(범위 검사 `tdc_isd_stim_para_setting.c:486~496` + `0x1F` 마스크 `:577`·`:605`)됐으나 **0x65 경로는 0중**이다. 파싱(`tdc_ble_cmd_0x65_specific.c:86`)이 99 를 `stimulatonMode` **무관하게** 허용하고(주석은 "for 모노폴라"인데 모노폴라인지 검사 안 함), `stimulatonMode == en__bipolar` 이면 `tdc_isd_map_specific_stim.c:206` 의 건너뛰기가 안 걸려 `case 3` 이 실행되어 `:225` 의 `electrodeMap[98]` 을 읽는다. **침범 대상 실측 - `electrodeMap`(0x200080d0, 128B) 끝에서 +264B 초과한 0x20008258 은 `s_tdc_table`(0x200081d0, 384B) 내부**이며, 이는 `ui/tdc_ui_command.c:120` 의 `command_entry_t[32]`(포인터 3개 x 32 = 384B)다. **실기 로그 "REF ELEC NUM : 99 (PCB : 70378)" 의 70378 = 0x112EA 가 `.text` 범위(0x10000~0x3d244) 안**이므로 쓰레기값이 그 테이블의 포인터임이 수치로 확정됐다. 게다가 `:258`·`:284` 에 `0x1F` 마스크가 없어 오염이 전극번호 필드를 넘어 **레지스터 주소·명령 필드까지** 번진다. 안전 지점 확인 - `:224` 좌변·`:455`·`:534` 는 `stimulationElectrodeNum`(1~32 검사, 예외 없음)이라 무해. **위험 지점은 `:225` 단 한 곳.** 조치는 라이브 경로 방어를 그대로 이식(범위 검사 + 마스크 2건). 상세: [`20260730_ble-cmd-0x65-rename/구현-검증.md §3`](20260730_ble-cmd-0x65-rename/구현-검증.md) |
| ~~**eCAP·impedance 배열 인덱스 잠재 위험 3건**~~ | 20260730 은수님 지시 조사 | **위험_1·위험_2 해소 (2026-08-05, [20260805_1740_ble-ecap-parsing-range-check](20260805_1740_ble-ecap-parsing-range-check/))** — 파싱 범위 검사 9+6블록. **위험_3(impedance `0x1F` 마스크 비대칭)만 미결**이며 인덱스 자체는 안전하므로 방어 관례 통일 차원의 별건이다. 아래는 조사 원 기록. **미결 - 상시 관리 대상.** 전문은 [`상시 점검 대장.md §2`](../../상시%20점검%20대장.md) 가 단일 출처. **위험_1(중) - 0x63 eCAP masking**: 파싱(`tdc_ble_map_measure.c:59~91`)에 범위 검사 **0건**인데 `isd/tdc_isd_map_ecap.c:688`·`:749`·`:906`·`:1130` 이 `electrodeMap[... - 1]` 로 쓴다. `uint8_t` 라 **0~255 전 구간 무검증** — 0 이면 `electrodeMap[-1]`(`name.1`), 255 면 `electrodeMap[254]` = `0x200084c8` 로 **`s_manu_table`**(`pwr/tdc_pwr_clock.c:15`, 256B) 침범. **바이폴라 99 보다 넓다**(그쪽은 99 특정값). `0x1F` 마스크도 0건. 완화 - `tdc_ble_protocol.h:91` 에 "현재 버전 매핑 앱에서 사용 X" 표기라 미발현이나 **실행 경로는 살아 있다**(`mapping.c:389`). **위험_2(낮음) - 0x64**: `mapping.c:403` 실행부 주석이라 무해하나 **주석 풀면 즉시 위험_1 동급**(검사 0건 · 같은 구조체 공유) — 되살리려면 범위 검사 선행. **위험_3(낮음) - impedance 마스크 비대칭**: `map_impedance.c:363`·`:410` 마스크 없으나 인덱스는 안전(`channel` 1~32 또는 255 검증 + 255->0 분기 `:81~88` + 상한 검사 `:604`). **안전 확인** - `map_ecap.c:465`·`:497` 의 `bipolarFIFO_index` 는 루프 변수(0~31), `:469`·`:501` 의 `electrodeMap[31]` 은 하드코딩 상수. 조치는 이월_D 작업([20260730_bipolar-99-specific-stim](20260730_bipolar-99-specific-stim/))이 본보기 |
| 리모콘 0x51·0x53·0x58 핸들러 부재 | 20260727 구조 분석 | **미결** - 실사용 여부 확인 후 구현 또는 미지원 명문화 |
| 재구조화 로드맵 단계 선택 | 20260727 구조 분석 | **미결** - 6단계 중 어디까지 적용할지 은수님 판단 |

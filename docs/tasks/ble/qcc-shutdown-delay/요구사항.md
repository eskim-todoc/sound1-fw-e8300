---
name: QCC OTA 후 셧다운 진입 딜레이 요구사항
purpose: ci_ble_control_boot.c 의 OTA DFU 후 QCC 절전 모드 진입 딜레이 확보 변경 정리 — 사용자 설명 대기 (placeholder)
type: 요구사항
revision: Rev.0 (placeholder)
status: 대기
applies_to: [Sound1, ble, qcc, ota]
tags: [qcc, shutdown, delay, ota, placeholder]
---

# QCC OTA 후 셧다운 진입 딜레이 — 요구사항 (Rev.0 placeholder)

**TL;DR**: [`signalProcessing/nofm-merge`](../../signalProcessing/nofm-merge/) 분석 task ([`분석.md §6.1`](../../signalProcessing/nofm-merge/분석.md)) 부수 발견 — `ci_ble_control_boot.c` 의 OTA DFU 후 재부팅 시 QCC 절전 모드 진입 딜레이 확보 변경 (`snd_qcc_set_mode(SND_QCC_MODE_SHUTDOWN)` 신규 호출 + 딜레이 `100×10ms` → `2200×1ms` = 2.2 초). 본 task = 변경 의미 정리 + 영향 확인 + 코드 commit (placeholder 단계, 사용자 설명 대기).

> [!IMPORTANT]
> **현재 placeholder 상태**. 사용자(다) 설명 대기. 그 전까지 본 문서 내용을 추정하거나 임의로 진행하지 않는다.

## 1. 배경

- **트리거**: [`signalProcessing/nofm-merge`](../../signalProcessing/nofm-merge/) 분석 ([`분석.md §6.1`](../../signalProcessing/nofm-merge/분석.md)) 에서 다니엘 작성 NofM 코드와 함께 묶여 있던 CM3 측 변경이 NofM 무관임을 분별. 사용자(다) 명시 — "CM3 측 2 파일은 별도로 ... 진행하자".
- **사용자(다) 명시 설명** (signalProcessing/nofm-merge 진입 시): "ci_ble_control_boot.c는 OTA DFU 후 재부팅 시 QCC의 절전 모드 진입을 위한 딜레이 시간 확보가 목적".
- **변경 사이즈**: +15/-15. L10 신규 include `<snd_qcc.h>`, L117 신규 호출 `snd_qcc_set_mode(SND_QCC_MODE_SHUTDOWN)`, L119-123 딜레이 변경 (100×10 ms → 2200×1 ms).

## 2. 목적·범위 (잠정)

- **대상 파일**: `src/2__cm3/Cortex-M3-src/BleCommunication/ci_ble_control_boot.c`
- **변경 본질**: QCC 칩의 절전 모드 진입 시퀀스 — set_mode 호출 + 딜레이 ≥ 2 초 확보 (QCC 사양 추정).
- **모듈 분류**: `ble`
- **연계 작업**: [`bootloader/service-rtt-debug-console`](../../bootloader/service-rtt-debug-console/) (OTA DFU 흐름 인접) 및 [`LED/ota-dfu-fw-variant`](../../LED/ota-dfu-fw-variant/) (OTA fw variant 분기 도입) — 검토 시 참조.

## 3. 요구사항

(사용자 설명 대기)

## 4. 진행 단계

- [ ] 요구사항.md Rev.1 — 사용자 설명 반영 후 본 문서 확장
- [ ] 분석.md — 변경 본질 + QCC 칩 사양 정합성 (절전 진입 시 ≥ 2 초 유지) + OTA DFU 흐름 (`ci_ble_control_boot.c` 의 호출 경로)
- [ ] 구현계획.md (필요 시)
- [ ] ci_ble_control_boot.c staging·commit
- [ ] 이력.md

## 5. 갱신 이력

| 날짜 | 변경 |
|---|---|
| 2026-05-14 | Rev.0 placeholder 등록. [`signalProcessing/nofm-merge`](../../signalProcessing/nofm-merge/) 분석 task 부수 발견 → CM3 측 NofM 무관 변경 분별 → 사용자(다) 명시 별도 task 분리 권고로 본 task 신설. 사용자(다) 설명 대기. |

---
name: BLE 라이브 상태 계약 명시 이력 및 결과
purpose: 재구조화 단계_4(라이브 상태 계약 명시) 작업의 시계열 로그와 완료 요약
type: tasks/이력
applies_to: [Sound1]
tags: [ble, restructure, stage4, live-stimulation, state-contract, hold-on]
---

# 이력 및 결과 - BLE 라이브 상태 계약 명시 (단계_4)

**상태**: **완료** - 구현 · 정적 검증 · 실기 확인 전부 통과 (실기 2026-07-30, 단계_3 과 함께).

**TL;DR**: [`재구조화 설계안.md`](../../../참고/ble/재구조화/재구조화%20설계안.md) 단계_4 실행. 라이브 상태 4층의 층간 규칙이 각 `case` 안 조건문에만 암묵적으로 존재하는 것을 **코드에 계약으로 승격**한다. **설계안이 제안한 "허용 선행 상태 테이블"은 채택하지 않는다** - 2026-07-29 단계_3 에서 테이블 주도 방식이 가독성 문제로 기각됐기 때문이다. 대신 **계층 네이밍 철학을 그대로 이어간다.**

> [!NOTE]
> **실기 게이트를 단계_3 과 함께 통과했다** (2026-07-30 은수님 확인). 분석·계획은 실기 대기 중 코드를 건드리지 않는 범위로 선행했고(2026-07-29), 구현은 은수님 지시로 2026-07-30 에 진행했다. 계획 §8 이 걸었던 "단계_3 실기 통과 선행" 조건은 지시로 완화됐고, **두 단계가 함께 실기를 받아 함께 통과했다.**
>
> 실기 전 안전망은 호스트 테스트 **301 PASS** 였다. 게이트 4곳이 가드 하나로 수렴했으므로 그 가드가 틀리면 4개 명령이 동시에 깨지는데, 커밋_1 의 26 케이스가 그 위험을 덮었다.

## 시계열 로그

### 2026-07-29

| 시각 | 단계 | 내용 |
|---|---|---|
| 착수 | 배경 | 단계_3(0x66 계층 네이밍) 구현·정적 검증 완료, 실기 게이트 대기. 카탈로그 함수 기준 전환 완료 |
| 착수 | 선택 | 실기 확인 전이라 **코드 무변경 작업**을 골랐다. 단계_4 는 0x66 라이브 상태를 다루므로 방금 재구조화한 것 위에서 이어진다 |
| 착수 | 자료 | [`상태머신 사양.md`](../../../참고/ble/라이브모드/상태머신%20사양.md) §1(4층 규명) · §2(서브커맨드 전수표) · §4(재진입 가드) 정독 |
| 착수 | 실측 | 층_2(`subCommand`) 접근 **43곳** - `tdc_ble_cmd_0x66_live.c` 22 · `tdc_ble_mapping.c` 2 · `tdc_isd_map_live.c` 19 |
| ① | 요구사항 | [`요구사항.md`](요구사항.md) |
| ② | 요구사항 검증 | [`요구사항-검증.md`](요구사항-검증.md) |
| ③ | 분석 | [`분석.md`](분석.md) |
| ④ | 분석 검증 | [`분석-검증.md`](분석-검증.md) |
| ⑤ | 계획 | [`계획.md`](계획.md) |
| ⑥ | 계획 검증 | [`계획-검증.md`](계획-검증.md) |
| ⑥ 후 | 착수 | 은수님 지시로 진행 (실기 게이트는 단계_3 과 함께 대기) |
| ⑦ | 커밋_1 | `5a7b32b` - 선행 상태 계약 테스트 26 케이스. **기존 코드로 301 PASS** |
| ⑦ | 커밋_2 | `4cd72d1` - 가드 수렴 + 조기 반환 + 수락 조건 주석 9개 |
| ⑧ | 구현 검증 | [`구현-검증.md`](구현-검증.md) - 검증_1~5 **전부 PASS**. 게이트 5->1 수렴, 본문 지표 전부 불변 |

### 2026-07-30 - 실기 게이트 통과

| 시각 | 단계 | 내용 |
|---|---|---|
| 착수 | 상태 재확인 | 세션 자체 검증 - 호스트 **301 PASS / 0 FAIL**, ARM 강제 재컴파일 후 에러 0 · 경고 0 · text **184,908** (인수인계 기재치 일치) |
| ⑧ | 실기 요청 | [`20260730 세션 인수인계 §3`](../../../세션_인수인계/20260730_ble-단계3-4-실기대기.md) 의 체크리스트를 은수님께 제시. 단계_4 몫은 **게이트 통과 경로 · 거부 경로 · 무조건 수락 경로** 세 항목 |
| ⑧ | **실기 통과** | 은수님 확인 - **"잘 동작해"**. 실기 게이트 **PASS**. 단계_4 대상 확인 내용 - ① 라이브 중 **볼륨 · 마이크 감도 · 알림 자극 · 이퀄라이저 읽기** 정상 ② **라이브가 아닐 때** 같은 4개 명령이 **거부 + 명령 순서 에러** ③ **시작 · 종료 · 상태 읽기**는 조건 없이 수락 |

## 완료 요약

**상태**: **완료** - 구현 · 정적 검증 · 실기 확인 전부 통과.

| 항목 | 결과 |
|---|---|
| 브랜치 | `claude_feature_ble-descriptor-table` (단계_4 분 2커밋 - 테스트 선행 · 가드 수렴) |
| 커밋 | `5a7b32b`(테스트 26 케이스) · `4cd72d1`(가드 수렴 + 조기 반환 + 수락 조건 주석 9개) |
| 호스트 테스트 | **301 PASS / 0 FAIL** (착수 시 275 -> +26 신규) |
| ARM 빌드 | 에러 0 · 경고 0 |
| **실기** | **통과** (2026-07-30) - 게이트 통과 경로 · 거부 경로 · 무조건 수락 경로 |
| 게이트 수렴 | `subCommand == en__HoldOn` **5 -> 1** · `en__Command_Order` **5 -> 1** · `else` **14 -> 10** |
| 본문 불변 | `if (` **45 불변** · `for (` **18 불변** · `en__OutOfDataRange` **7 불변** |

### 이 작업에서 배운 것

> [!IMPORTANT]
> **리팩토링 유형마다 불변량이 다르다.** 가드 수렴에서는 "조건 분기 총수 보존"을 쓸 수 없다 - 게이트를 함수 하나로 합치는 것이 목적이므로 게이트 관련 지표는 **줄어야 맞다**. 대신 **게이트 관련은 수렴 · 본문은 불변**이 옳은 불변량이다.
>
> `if (` 가 **45 로 불변**인 것이 "분기를 없앤 게 아니라 이름 있는 자리로 옮겼다"를 증명한다. 만약 본문 지표까지 줄었다면 그것은 수렴이 아니라 **동작 변경**이다.

**설계안 미채택의 일관성** - 단계_3 에서 테이블이 기각된 기준(*사람이 눈으로 보고 관리하기 쉬운가*)을 단계_4 에도 그대로 적용해 "허용 선행 상태 테이블"을 쓰지 않았다. 게이트가 4개뿐이라 규모 이점도 없었다. 대신 **9개 하위 명령 머리에 수락 조건을 한 줄씩** 적어, 조건 없는 명령도 "없음"이라 명시함으로써 **가드 누락과 의도적 무조건 수락을 구분**할 수 있게 했다.

## 핵심 판단

### 설계안의 제안을 그대로 따르지 않는다

[`재구조화 설계안.md §3 단계_4`](../../../참고/ble/재구조화/재구조화%20설계안.md) 는 다음을 제안한다.

| 설계안 제안 | 이 작업의 판단 |
|---|---|
| "볼륨은 HoldOn 일 때만" 을 **서브커맨드별 허용 선행 상태 테이블**로 선언 | **미채택.** 단계_3 에서 같은 성격의 테이블이 기각됐다 |
| 층별 상태 조회·전이 인터페이스 명시 | **채택** - 접근자 방식 |
| 재진입 가드를 명령별 수락 조건의 일부로 편입 | **조건부** - 동작 변경 위험이 커서 분리 |

**기각 사유의 재확인** (2026-07-29 단계_3): 테이블은 지표가 좋아도 *"배열에 숫자를 집어넣어 한참 분석해야 하는"* 코드가 된다. 은수님 판단 기준은 **사람이 눈으로 보고 관리하기 쉬운가**이며, 그 기준을 단계_4 에도 그대로 적용한다.

### 대안 - 전제를 함수 이름으로 선언한다

현재 4개 하위 명령이 똑같은 게이트를 각자 품고 있다.

```c
if (p_mappingPacket->tdc_isd_map_live_step.subCommand == en__HoldOn)
{
    ... 본문 전체가 여기 들여쓰기 안에 ...
}
else
{
    tdc_sys_error_send_to_app(..., en__Command_Order, __LINE__);
    p_mappingPacket->tdc_isd_map_live_step.subCommand = en__Standby;
}
```

이를 이름 있는 가드로 바꾼다.

```c
if (!tdc_ble_cmd_0x66_require_hold_on(p_mappingPacket, __LINE__))
{
    return;
}
```

**테이블을 만들지 않고도 "이 명령은 라이브 자극 중에만 받는다"가 코드에 선언된다.** 들여쓰기도 한 단계 걷힌다. 상세는 [`분석.md`](분석.md).

---
name: BLE 라이브 상태 계약 명시 구현 검증
purpose: 단계 ⑦ 구현(커밋_1~2)의 게이트 수렴·동작 보존·빌드 결과 검증
type: tasks/검증
applies_to: [Sound1]
tags: [ble, stage4, verification, implementation, guard, hold-on]
---

# 구현 검증 - BLE 라이브 상태 계약 명시 (단계_4)

**TL;DR**: 계획의 검증_1~5 **전부 통과**. 게이트 4곳이 가드 1개로 수렴하면서 `en__HoldOn` 비교 · `en__Command_Order` 전송이 각각 **5 -> 1**, `else` **14 -> 10** 이 됐고, **본문 지표(`if (` 45 · `for (` 18 · `en__OutOfDataRange` 7)는 전부 불변**이다. **실기 게이트도 2026-07-30 통과**(§5) - 게이트 통과 · 거부 · 무조건 수락 세 경로 전부.

## 1. 커밋

| 커밋 | 해시 | 내용 |
|---|---|---|
| 커밋_1 | `5a7b32b` | 선행 상태 계약 테스트 26 케이스 추가 (기존 코드로 통과) |
| 커밋_2 | `4cd72d1` | 가드 수렴 + 조기 반환 전환 + 수락 조건 주석 9개 |

## 2. 검증 결과

| # | 검증 | 결과 |
|---|---|---|
| 검증_1 | 커밋_1 - 신규 테스트가 **기존 코드로** 통과 | **PASS** - 301 PASS / 0 FAIL (map_stim 148 -> 174) |
| 검증_2 | 커밋_2 - 테스트 + ARM 빌드 | **PASS** - 301 PASS, 에러 0 경고 0 |
| 검증_3 | **게이트 수렴 대조** | **PASS** - §3 |
| 검증_4 | 에러 인자 보존 | **PASS** - 명령 · major · minor 동일, `lineNumber` 는 호출 지점 값 |
| 검증_5 | 수락 조건 주석 9개 | **PASS** - 하위 1~8 각 함수 머리 8개 + 진입점 `default`(하위 9) 1개 |
| 검증_6 | 실기 게이트 | **PASS** - 2026-07-30 은수님 확인 (§5) |

## 3. 게이트 수렴 대조 (검증_3)

계획-검증 보완 지시_1 이 정한 기준 - **"게이트 관련은 수렴, 본문은 불변"** 으로 대조했다.

| 지표 | 전환 전 | 전환 후 | 기대 | 판정 |
|---|---|---|---|---|
| `subCommand == en__HoldOn` | 5 | **1** | 수렴 | PASS |
| `en__Command_Order` | 5 | **1** | 수렴 | PASS |
| `subCommand = en__Standby` | 14 | **10** | -4 (게이트 else) | PASS |
| `else` | 14 | **10** | -4 | PASS |
| **`if (`** | 45 | **45** | **불변** | PASS |
| **`en__OutOfDataRange`** | 7 | **7** | **불변** | PASS |
| **`for (`** | 18 | **18** | **불변** | PASS |

> [!NOTE]
> **`if (` 가 45 로 불변인 것이 이 전환의 성격을 보여준다.** 게이트 4개와 가드 1개(총 5개)가 사라지고, 가드 내부 `if` 1개와 호출부 `if (!...)` 4개(총 5개)가 생겨 정확히 상쇄됐다. 분기를 **없앤 것이 아니라 이름 있는 자리로 옮긴 것**이다.
>
> 전환 전 카운트가 4가 아니라 5인 이유는, 대조 기준 파일이 가드 함수를 이미 추가한 시점이기 때문이다(게이트 4 + 가드 1).

## 4. 무엇이 달라졌나

### 4.1 전제가 함수 이름으로 올라왔다

```c
// 수락 조건 : 라이브 자극 유지 중(en__HoldOn)에만.
static void tdc_ble_cmd_0x66_sub03_volume_adjust(const uint8_t *Rx_dataPacket, int index)
{
    ...
    if (!tdc_ble_cmd_0x66_require_hold_on(p_mappingPacket, __LINE__))
    {
        return;
    }

    value = Rx_dataPacket[index++];   /* 본문이 한 단계 왼쪽으로 */
```

### 4.2 9종의 수락 조건이 각 함수 자리에 전부 적혔다

테이블을 만들지 않고도 조건이 한눈에 보인다. 조건 없는 명령도 "없음"이라고 적어, **조건이 없는 것과 가드를 빠뜨린 것이 구분**된다.

| 하위 | 주석 |
|---|---|
| 1 | 없음. 다만 데이터 인덱스는 1씩 증가해야 한다 |
| 2 · 7 | 없음. 어느 상태에서도 수락한다 |
| **3 · 4 · 5 · 6** | **라이브 자극 유지 중(en__HoldOn)에만** |
| 8 | 없음. 다만 `fetch_packet` 의 재진입 가드에서 먼저 걸릴 수 있다 |
| 9 | 앱이 보낼 수 없는 내부 상태값. `default` 로 낙하 (진입점에 명시) |

### 4.3 테스트가 4곳을 한 번에 보증한다

게이트가 한 함수로 수렴했으므로 그 함수가 틀리면 4개 명령이 동시에 깨진다. 커밋_1 의 26 케이스가 그 위험을 덮는다.

| 그룹 | 케이스 | 역할 |
|---|---|---|
| 거부 | 12 | 하위 3·4·5·6 이 Standby 에서 거부 · minor · Standby 유지 |
| 통과 | 4 | HoldOn 이면 게이트 통과 (하위 5 는 스텁 맵데이터 0 때문에 minor 로 판정) |
| **무조건** | **6** | **하위 2·7·8 이 Standby 에서도 수락** - 가드 오부착 검출 |
| 값 보존 | 4 | 거부된 명령이 기존 값을 덮지 않음 |

## 5. 실기 게이트 - 통과 (2026-07-30)

> [!NOTE]
> **은수님 실기 확인 완료 - "잘 동작해".** 검증_6(실기 게이트) **PASS**. 단계_3 과 함께 실기를 받아 함께 통과했다.
>
> PMIC 테스트 블록은 `67c91c1` 에서 `source/tests/tdc_test_tx_pmic.c` 로 분리되고 `main.c:474` 에서 **주석 처리**되어, 우려했던 `func_normal()` 초반 무한 루프는 해소된 상태로 실기에 들어갔다(크기 **184,908** 로 확인 - 187,260 이면 주석이 풀린 상태).
>
> | # | 확인 항목 | 결과 |
> |---|---|---|
> | 1 | Eclipse F5 Refresh + Clean 재빌드 (text **184,908** · 에러 0 · 경고 0) | **통과** |
> | 2 | 라이브 전체 파라미터 15단계 전송 -> 자극 시작 (단계_3) | **통과** |
> | 3 | **자극 볼륨 · 마이크 감도 · 알림 자극 · 이퀄라이저 읽기** - 라이브 중 정상 동작 (게이트 **통과** 경로) | **통과** |
> | 4 | **라이브가 아닐 때** 위 4개 명령이 거부되는지 (게이트 **거부** 경로) | **통과** |
> | 5 | 라이브 시작 · 종료 · 장치 상태 읽기 - 조건 없는 명령이 그대로 수락되는지 | **통과** |

### 실기가 정적 검증에 더해 확증한 것

정적 검증(검증_1~5)이 보인 것은 **게이트 지표는 5->1 로 수렴하고 본문 지표는 불변**이라는 구조적 사실까지였다. 실기가 추가로 확증한 것은 **가드가 옳은 함수에 붙었는가**다.

| 오류 유형 | 정적 검증이 잡는가 | 실기 항목 |
|---|---|---|
| 가드 **누락** (필요한 곳에 안 붙음) | 부분적 - `en__HoldOn` 잔존 수로 추정 | 4 (거부돼야 하는데 수락되면 누락) |
| 가드 **오부착** (불필요한 곳에 붙음) | **못 잡는다** | 5 (조건 없는 명령이 거부되면 오부착) |
| 가드 조건 **반전** | **못 잡는다** | 3 과 4 가 짝 - 둘 중 하나만 통과하면 반전 |

항목 3·4·5 가 셋 다 통과한 것이 세 오류 유형을 동시에 배제한다. 특히 **항목 5(조건 없는 하위 2·7·8)는 호스트 테스트 그룹_3 과 같은 역할** - 가드 오부착 검출용 안전장치이며, 실기에서도 같은 축을 덮었다.

## 6. 범위 밖으로 남긴 것

| 항목 | 사유 |
|---|---|
| `isd/tdc_isd_map_live.c` 의 19곳 | `ble/`-`isd/` 역결합 해소는 단계_5 |
| 재진입 가드 구조 변경 | 결함_4(Stop 거부 위험 창) 정량화 선행 필요 |
| 층_2 접근자 도입 | 읽기만 정리했고 쓰기는 `isd/` 와 짝이라 한쪽만 감싸면 비대칭 (분석 미결_2) |

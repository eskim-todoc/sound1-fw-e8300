---
name: ble 폴더 개편 추출 함수 입출력 계약표
purpose: 단계_1b 에서 추출한 각 함수의 입력·출력·부수효과·공유 상태 접근을 명시해 누락을 방지
type: tasks/산출물
applies_to: [Sound1]
tags: [ble, folder-restructure, contract, function-extraction]
---

# 입출력 계약표 - 추출 함수

**TL;DR**: `fetch_packet` 에서 추출한 각 함수가 무엇을 읽고 무엇을 바꾸는지 명시한다. 전역·static 접근이 많아 시그니처만으로는 계약이 드러나지 않으므로, **읽기/쓰기 대상을 전수 기재**한다. 수용_1b_3 의 산출물이다.

## 0. 공통 계약

| 항목 | 내용 |
|---|---|
| 입력 | `const uint8_t *Rx_dataPacket` - 수신 패킷 21바이트. **인덱스 0 은 명령 헤더로 호출자가 이미 읽었다.** 각 함수는 인덱스 1(`df_payloadStartIndex`)부터 파싱한다 |
| 반환 | `void` - 응답 송신까지 함수 안에서 완결 |
| 공유 상태 | `mappingPacket` 은 **`tdc_ble_mapping_get_packet()`** 으로 접근 (포인터 반환, 쓰기 가능) |
| 에러 응답 | `tdc_sys_error_send_to_app(command, majorError, minorError, __LINE__)` |

> [!IMPORTANT]
> **`__LINE__` 은 에러 응답 패킷의 5·6번째 바이트로 앱에 전송된다**(`tdc_sys_error.c:168-169`). 함수를 추출하면 라인 번호가 바뀌므로 **에러 응답의 이 2바이트는 값이 달라진다.** 에러 분류는 앞선 `majorError`/`minorError` 가 담당하고 라인 번호는 발생 위치 표시용이므로 동작에는 영향이 없다고 판단했다. 검증 시 이 2바이트는 대조에서 제외한다.

## 1. measure 그룹 (`tdc_ble_map_measure.c`) - 1b-1

원본: `tdc_ble_mapping.c` (추출 전) `:129`~`:219`

### 1.1 `tdc_ble_map_measure_impedance_check` (0x62)

| 구분 | 대상 |
|---|---|
| 패킷 읽기 | 인덱스 1~6 (6바이트) |
| **쓰기** | `mappingPacket.impedanceCheck.{iterationNum, channel, pulseWidth_start_usec, pulseWidth_end_usec, stimulationLevel_uA}` |
| **쓰기** | `mappingPacket.fetched_command` = `en__mapping_impedanceChekck` (전 범위 검사 통과 시에만) |
| 범위 검사 | `iterationNum` 1~255 · `channel` 1~32 또는 255 · `pulseWidth_start_usec` 13~255 · `pulseWidth_end_usec` 13~255 · `stimulationLevel_uA` 1~1024 |
| 실패 시 | `tdc_sys_error_send_to_app(0x62, en__EN__BLE_PROTOCOL_ERROR, en__OutOfDataRange, __LINE__)` |
| 공유 static | **없음** |
| 원본 대비 | 중첩 if 통과 시 `break` -> `return` 으로 전환 (조기 탈출 의미 동일) |

### 1.2 `tdc_ble_map_measure_ecap_masking` (0x63)

| 구분 | 대상 |
|---|---|
| 패킷 읽기 | 인덱스 1~16 (16바이트) |
| **쓰기** | `mappingPacket.eCapMeasurement.{iterationNum, pulseWidth, firstPulsePhase, stimulatonMode, stimulationElectrodeNum, bipolarReferenceElectrodeNum, measurementElectrodeNum, stimulationLevel_uA_masker, stimulationLevel_uA_probe, maskerProbeInterval_numFrame, adcPreampGain, adcSamplingFreq, adcMeasurementDelay, measurementSampleNum}` |
| **쓰기** | `mappingPacket.fetched_command` = `en__mapping_eCAP_Measurement_masking` (무조건) |
| 범위 검사 | **없음** (원본에도 없다) |
| 공유 static | **없음** |

### 1.3 `tdc_ble_map_measure_ecap_alternative` (0x64)

| 구분 | 대상 |
|---|---|
| 패킷 읽기 | 인덱스 1~8 (8바이트) |
| **쓰기** | `mappingPacket.eCapMeasurement.{iterationNum, pulseWidth, stimulatonMode, stimulationElectrodeNum, bipolarReferenceElectrodeNum, measurementElectrodeNum, stimulationLevel_uA_masker}` |
| **쓰기** | `mappingPacket.fetched_command` = `en__mapping_eCAP_Measurement_alternative` (무조건) |
| 범위 검사 | **없음** |
| 공유 static | **없음** |

> [!NOTE]
> 0x63 과 0x64 는 **같은 `eCapMeasurement` 구조체를 쓰되 채우는 필드가 다르다.** 0x64 는 `firstPulsePhase`·`stimulationLevel_uA_probe`·`maskerProbeInterval_numFrame`·`adc*` 4종을 건드리지 않으므로 **직전 0x63 의 값이 남는다.** 원본과 동일한 동작이며 이 작업에서 바꾸지 않았다.

### 1.4 호출자 측 변경

`tdc_ble_mapping.c` 의 case 3개가 함수 호출로 대체됐다. `index`·`value` 지역 변수는 다른 case 가 계속 쓰므로 선언을 유지했다.

```c
case en__mapping_impedanceChekck:  // 0x62
{
    tdc_ble_map_measure_impedance_check(Rx_dataPacket);
}
break;
```

## 2. flash 그룹 (`tdc_ble_map_flash.c`) - 1b-2

원본: `tdc_ble_mapping.c` (1b-1 시점) `:897`~`:1692`

### 2.1 이관한 static

| 심볼 | 원래 위치 | 이관처 | 근거 |
|---|---|---|---|
| `stimulPara_index` | `fetch_packet` 함수 지역 static | `map_flash.c` 파일 static | flash 전용 (0x68·0x6C 는 리셋만, 0x6D 가 누적 사용) |
| `writingStartSlot_index` | `mapping.c` 파일 static | `map_flash.c` 파일 static | 0x70·0x71 에서만 대입 |

> [!NOTE]
> `writingStartSlot_index` 는 **이 파일에서 대입만 되고 읽는 곳이 없다**(0x70 에서 2, 0x71 에서 1). `tdc_ble_remote.c:48` 의 동명 변수는 별개의 파일 지역 static 이다. 사실상 죽은 변수이나 **결함 수정과 구조 리팩토링을 섞지 않는다**(원칙_4)에 따라 그대로 이관했다.

### 2.2 공유 상태 - `prev_subCommandData_Num_index` 읽기/쓰기 분류

접근자 경유로 전환한 15곳을 전수 분류했다. 어셈블리 호출 수(`get` 3 / `set` 12)와 일치함을 확인했다.

| 함수 | 읽기 (`get_seq_index`) | 쓰기 (`set_seq_index`) |
|---|---|---|
| `write_original_isd_user` (0x68) | 1 (`!= get() + 1`) | 4 (인덱스 1 리셋 · 순서오류 리셋 · 정상 대입 · 완료 리셋) |
| `write_slot_data` (0x6C) | 1 | 4 (동) |
| `write_map_data` (0x6D) | 1 | 4 (동) |
| **합계** | **3** | **12** |

### 2.3 함수별 계약

| 함수 | 명령 | 패킷 읽기 | 주요 쓰기 | 범위 검사 | 공유 상태 |
|---|---|---|---|---|---|
| `read_original_isd_user` | 0x69 | 없음 (헤더 only) | `fetched_command` | - | - |
| `write_original_isd_user` | 0x68 | 인덱스 1~ (분할 2패킷) | `p_RepositoryFor_ISD_info[0..29]` · `rx_orignal_ISD_info.{ISD_id, location, userName, id_check_is_completed, id_match}` · `command` | 수술 위치 1~2 | seq · `stimulPara_index` |
| `read_slot_data` | 0x6A | 인덱스 1 | `ReadWriteMapData_Flash.{slot_index, map_index}` · `fetched_command` | 슬롯 1~`MaxNumUser` | - |
| `write_slot_data` | 0x6C | 인덱스 1~ (분할 3패킷) | `p_RepositoryFor_ISD_info[0..45]` · `ReadWriteMapData_Flash.slot_index` · `command` | 슬롯 1~4 · 수술위치 1~2 · 패스키 영숫자 · 맵번호 1~4 · 볼륨/감도/LED/알림/텔레코일 | seq · `stimulPara_index` |
| `read_map_data` | 0x6B | 인덱스 1~2 | `ReadWriteMapData_Flash.{slot_index, map_index}` · `fetched_command` | 슬롯 1~`MaxNumUser` · 맵 1~`MaxNumMap` | - |
| `write_map_data` | 0x6D | 인덱스 1~ (분할 15패킷) | `p_RepositoryFor_stimulPara[..]` · `ReadWriteMapData_Flash.{slot_index, map_index}` · `fetched_command` | 자극기법 1~3 · 선행위상 0~1 · 자극모드 1~6 · 펄스폭 13~255 · 밴드수 1~32 · 알람채널 1~32 · 전극 1~100 · 자극크기 0~1800 | seq · `stimulPara_index` |
| `erase_slot` | 0x6E | 인덱스 1 | `fetched_command` · `slot_index` | 슬롯 1~4 | - |
| `erase_map` | 0x6F | 인덱스 1~2 | `fetched_command` · `slot_index` · `map_index` | 슬롯 1~4 · 맵 1~4 | - |
| `recover_except_slot1` | 0x70 | 인덱스 1~2 | `writingStartSlot_index`=2 · `fetched_command` · `slot_index` · `map_index` | - | `writingStartSlot_index` |
| `recover_all` | 0x71 | 없음 | `writingStartSlot_index`=1 · `fetched_command` | - | `writingStartSlot_index` |

### 2.4 원본 대비 의도적 차이

| # | 차이 | 사유 |
|---|---|---|
| 차이_1 | `tempValue` 를 `= 0` 으로 초기화 | 원본은 미초기화 선언(`int tempValue;`). 사용 전 항상 대입되므로 동작은 같고, 함수 분리 후 미초기화 경고를 피한다 |
| 차이_2 | 0x6E·0x6F·0x70·0x71 이 `command` 를 **인자로** 받음 | 원본은 `tempCommand` 지역 변수를 참조했다. 값이 case 라벨과 동일하나 상수로 치환하지 않고 인자로 넘겨 원본 대입 형태를 유지 |
| 차이_3 | `numPacket_writeMapData_*` 를 위해 `tdc_isd_map_data.h` include 추가 | 원본은 `tdc_ble_mapping.c` 가 이미 include 하고 있었다 |

## 3. stim 그룹 - 1b-3

*(작성 예정)*

---
name: QCC 1.8V 테스트 제한 사항 - 이력 및 결과
purpose: 전류 측정용 코드 제한 정리·커밋·원격 반영 작업의 시계열 로그와 완료 요약
type: tasks/이력
applies_to: [Sound1]
tags: [hw-test, qcc, power, history]
---

# 이력 및 결과 — QCC 1.8V 테스트 제한 사항

**TL;DR**: 은수님이 QCC 1.8V 전류 측정을 위해 직접 적용한 코드 제한 10건을 전수 조사해 [`QCC 1.8V 테스트 제한 사항.md`](QCC%201.8V%20테스트%20제한%20사항.md) 로 정리하고, 코드·문서를 커밋한 뒤 `claude_hw_test_qcc_1.8v_current` 브랜치를 원격에 반영했다.

## 작업 성격

**8단계 프로세스 면제.** 코드 변경은 은수님이 이미 실기 측정을 위해 직접 적용한 상태였고, 본 작업의 범위는 그 변경의 **사후 기록·커밋·원격 반영**이다. 요구사항→분석→계획 산출은 대상이 없으므로 생략하고, 사실 근거(`파일:라인`) 확인은 정리 문서 §5 에 남겼다.

## 시계열 로그

| 시각 | 단계 | 내용 |
|---|---|---|
| 2026-07-23 | 착수 | 은수님 지시: "FPGA·터치센서·ISD 연결과정 등 제한·제거 내용 정리 → 문서화 → 커밋 → 동일 브랜치명으로 원격 반영" |
| 2026-07-23 | 조사 | `git status` 상 미커밋 변경 5파일 확인 (`tdc_hal_dio.c` · `tdc_isd.c` · `tdc_isd_stim_standalone.c` · `main.c` · `tdc_sys_init.c`). 브랜치 선행 커밋 `8a353a0`(CFX ADC FBDAC DEM 비활성화)도 같은 목적임을 확인해 정리 범위에 포함 |
| 2026-07-23 | 조사 | 변경 후 파일 기준 grep 으로 전 항목 라인 번호 확정. 신규 사용 심볼 3종 정의 위치 확인 (`ManufacturingDefault_ISD_No`=1 · `BackelCircuitDisabled_readPcmFired_duringLiveStimulation`=2 · `done_setting_StimulPara_variable()`) |
| 2026-07-23 | 조사 | 잔존 의존 2건 발견 — ① 절전 진입 루프(`main.c:1088~1156`)가 초기화되지 않은 IQS323 을 직접 호출 ② `power_button` 상시 `false` 로 전원 버튼 트리거 소실. 정리 문서에 경고로 명시 |
| 2026-07-23 | 문서 | [`QCC 1.8V 테스트 제한 사항.md`](QCC%201.8V%20테스트%20제한%20사항.md) 작성 — 제한 11건 전수 표 + 잔존 의존 경고 + 항목별 복원 절차 + 비기능(clang-format) 변경 분리 |
| 2026-07-23 | 커밋 | 코드 커밋(`Test(cm3)`) · 문서 커밋(`Docs(power)`) 분리. 원격 `origin` 에 동일 브랜치명으로 push |

## 결정 사항

| 결정 | 내용 | 사유 |
|---|---|---|
| 문서 위치 | `docs/tasks/power/20260723_qcc-1p8v-test-limits/` | 변경 파일은 cm3·cfx 횡단이나 작업 목적이 QCC 1.8V 레일 전류 측정이라 `power` 모듈로 귀속 |
| 전역 인덱스 갱신 생략 | `docs/tasks/작업 목록.md` · `docs/tasks/모듈 현황.md` 미갱신 | 본 브랜치는 `claude_develop` 에 병합하지 않는 실험 전용. 전역 인덱스를 건드리면 develop 계열과 불필요한 충돌만 남는다 |
| 커밋 분리 | 코드 / 문서 2커밋 | 저장소 컨벤션(`Test(...)` · `Docs(...)`) 유지 및 필요 시 코드 커밋만 선택적 revert 가능 |
| clang-format 변경 동반 | 코드 커밋에 함께 포함 | 기능 무관 재정렬이며, 분리 커밋을 위해 작업 트리를 재편집하면 은수님 실기 환경과 불일치 위험 |

## 완료 요약

- **변경**: 정리 문서 1건 + 이력 1건 신규. 코드는 은수님 적용분(5파일) 그대로 커밋
- **검증**: 제한 항목 11건 전부 `파일:라인` 근거 확인. 빌드·실기 검증은 미수행(측정 세션 소관)
- **다음**: 실기 전류 측정 진행. 측정 종료 후 본 브랜치는 폐기하거나 참고용으로 보존하고, 정상 작업은 `claude_develop` 에서 이어간다

> [!CAUTION]
> 본 브랜치 펌웨어는 정상 동작하지 않는다(ISD FSM 무조건 성공 전이 · 자극 10V 미인가 · 전원 버튼 무효). `claude_develop` 병합 금지.

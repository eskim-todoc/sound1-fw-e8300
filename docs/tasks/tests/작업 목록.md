---
name: tests 모듈 작업 목록
purpose: 테스트 인프라·검증 자동화 관련 작업 인덱스
type: tasks/인덱스
applies_to: [Sound1]
tags: [tests, index, host-build, offline-test]
---

# 작업 목록 - tests

**TL;DR**: 호스트(PC)에서 펌웨어 로직을 실행해 검증하는 테스트 인프라 계통. 2026-07-28 에 **오프라인 파싱 테스트 하네스**가 구축되어 `bash tests/unit/run.sh` 한 줄로 186 케이스가 돈다. 설계안 단계_3 의 회귀 안전망 역할을 한다.

| 작업명 | 태그 | 폴더 | 상태 | 시작·완료 | 연계 | 요약 |
|---|---|---|---|---|---|---|
| **오프라인 파싱 테스트 인프라 구축** | host-build, mingw, offline-test, stub, harness | [20260728_tests-infra](20260728_tests-infra/) | **완료** (구현·검증) | 2026-07-28 | <- ble/20260728_ble-folder-restructure (함수 분리로 테스트 진입점 확보), -> ble 단계_3 (회귀 안전망) | 설계안이 단계_3 의 전제로 요구한 인프라. 착수해 보니 **호스트 컴파일러가 하나도 없어**(gcc·clang·cl·MSYS2·VS·QEMU 전부) 오프라인 테스트가 원천 불가였다. 은수님 승인 후 winget 으로 MinGW-w64 gcc 16.1.0 설치. **SDK 헤더가 호스트에서 그대로 컴파일**되어 스텁 헤더가 불필요했고, 스텁 10개로 `mapping/` 3파일 전부 커버. 실제 펌웨어 소스를 사본 아닌 원본으로 컴파일하며 `tdc_ble_reply.c` 는 실물을 써서 **응답 바이트열까지 바이트 단위 단언**한다. **186 PASS / 0 FAIL** (measure 52 · flash 75 · stim 59). 0x66 하위 8종이 static 이라 진입점 간접 테스트로 전환. `src/` 변경 0 을 불가침으로 지켰다. **도입 즉시 프로덕션 경고 11건 발견**(ARM 빌드에 `-Wall` 이 없어 안 보이던 미사용 변수) -> 고치지 않고 [`알려진 결함 목록.md`](../../참고/ble/재구조화/알려진%20결함%20목록.md) 결함_14 로 기록 |

## 이월 항목

| 항목 | 출처 | 상태 |
|---|---|---|
| `remote/`·`sound1/` 파싱 테스트 | 20260728 인프라 구축 | **미착수** - 인프라는 확장 가능하게 만들었다. 단계_3 대상이 `mapping/` 이라 우선순위 밖이었을 뿐 |
| `isd/` 자극 FSM 테스트 | 동 | **보류** - 하드웨어 타이밍 의존이라 호스트 실행이 부적합 |
| CI 연동 | 동 | **미착수** - 로컬 실행을 우선했다 |
| 커버리지 측정 | 동 | **미착수** |
| **결함_14 정리** (미사용 변수 11건) | 동 | **미결** - `tdc_ble_map_stim.c:759-772` 선언 삭제. 별도 커밋 사안 |

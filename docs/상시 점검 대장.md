---
name: 상시 점검 대장
purpose: 세션마다 확인하고 은수님께 상기해야 하는 항목 (임시 변경 원복·잠재 위험·주기 점검)
type: 운영
applies_to: [Sound1]
tags: [checklist, 상시, 원복, 잠재위험, reminder, out-of-bounds]
---

# 상시 점검 대장

**TL;DR**: **세션 시작 시 이 문서를 읽고, 완료 보고마다 §1 의 «매번 알림» 항목을 은수님께 상기한다.** 2026-08-06 현재 «매번 알림» 은 **알림_2 실기 미검증 병합분 누적** 1건이고(은수님 원격 근무로 실기를 나중에 몰아서 한다), **«잠재 위험» 은 다시 0건**이다 (2026-08-06 발견한 위험_4 리모콘 0x4D 무검증도 같은 날 조치 완료).

> [!IMPORTANT]
> **이 문서는 Claude 의 의무 체크리스트다.**
>
> | 시점 | 할 일 |
> |---|---|
> | **세션 시작** | 이 문서 Read → §1 «매번 알림» 항목의 **현재 상태를 코드로 확인** → 첫 응답에 보고 |
> | **완료 보고마다** | §1 «매번 알림» 항목을 **한 줄이라도 반드시 언급** |
> | **작업 착수 전** | §2 «잠재 위험» 이 이번 작업 범위와 겹치는지 확인 |
> | **항목 해소 시** | 이 문서에서 «해소» 로 옮기고 근거 기록 |

## 1. 매번 알림 (완료 보고마다 상기)

> [!NOTE]
> ~~알림_1 `tdc_sys_control.c` 임시 변경 원복~~ → **2026-08-05 원복 완료** (§4 참조).

### 알림_2 - 실기 미검증 병합분 누적

| 항목 | 내용 |
|---|---|
| 사유 | **은수님이 원격 근무 중이라 실기가 불가하다**(2026-08-05 지시 *"원격이라 실기가 불가능 해. 한번에 실기한다치고"*). 실기를 나중에 몰아서 하기로 하고 병합을 진행한다 |
| 상태 | **누적 중** |

> [!CAUTION]
> **실기 없이 `claude_develop` 에 병합된 변경이 쌓이고 있다.** 하나씩 확인할 때와 달리 **문제가 생기면 어느 변경 탓인지 좁히기 어렵다.** 아래 목록을 실기 시점에 순서대로 확인해야 한다.

#### 실기 대기 목록

| 병합 | 작업 | 확인 항목 | 우선 |
|---|---|---|---|
| `dc0442c` | 이월_2 리모콘 분해 | ① 연결·패스키 통과 ② 자극 볼륨 증감(상하한) ③ 마이크 볼륨 ④ 텔레코일·인디케이터·LED On/Off ⑤ 맵 번호 변경 ⑥ **맵 데이터 읽기·쓰기(0x4B·0x4D)** ⑦ **슬롯 데이터 읽기·쓰기(0x4A·0x4C)** ⑧ 정상 동작에서 에러 로그 없을 것 | **⑥⑦ 최우선** |
| `d715f2b` | 이월_F 채널 경계 상수화 | 바이폴라 자극(라이브·특정자극) 정상 동작 | 낮음 — 로드 섹션 차이가 `__LINE__` 즉치 1개뿐 |
| `e1788ca` | 배터리·시간 임계 상수화 | 불필요 — `.elf` 바이트 동일 | - |
| (커밋 `25a4906`) | 위험_3 impedance 마스크 | 임피던스 측정 1회. 정상 경로 비트 등가라 위험 낮음 | 낮음 |
| `77ef2ce` | 0x63·0x64 eCAP 범위 검사 | 임피던스 측정 1회(회귀 확인용). 0x63·0x64 자체는 앱이 안 보내 트리거 불가 | 낮음 |
| (미병합) | **위험_4 맵 쓰기 범위 검사** | ① **리모콘으로 맵 데이터 쓰기(0x4D) 정상 완료** ② **매핑 앱으로 맵 쓰기 정상 완료** ③ 라이브 자극 ④ 바이폴라 자극 | **중** - 호스트 52케이스가 경계값을 덮지만 «정상값을 잘못 막지 않는가» 는 실기로만 확인된다 |
| (미병합) | **자극 모드 인코딩 통합** | ① **특정자극(0x65)** 모노폴라·바이폴라 정상 출력 ② **라이브 자극** 정상 (**바이폴라 맵으로도**) | **중** - 호스트 31케이스가 모드->비트 값을 덮지만, **그 비트가 레지스터 맞는 자리에 들어갔는지는 실기로만** 확인된다. eCAP 경로는 앱 미사용이라 확인 불가 |
| (미병합) | **specific_stim 분해** | ① **0x65 특정자극 모노폴라** ② **0x65 특정자극 바이폴라** ③ 자극 유지시간만큼 유지 후 정상 종료 ④ 정상 동작에서 에러 로그 없을 것 | **중** - flow 1 이 모드로 갈라져 **바이폴라만 3·5·7(기준전극 전송)을 거친다.** 한쪽만 보면 나머지 경로가 미검증이다. 이 파일은 호스트가 컴파일하지 않아 자동 검증이 «컴파일·구조지표·크기» 만 덮는다 |
| (미병합) | **stim_para_setting 분해** | ① **라이브 자극 모노폴라** ② **바이폴라** ③ **공통접지** ④ **모드가 다른 맵 사이 전환** ⑤ 정상 동작에서 에러 로그 없을 것 | **높음** - 일상 청취의 자극 본체다. 디스패처가 모드로 갈라 모노폴라·공통접지는 `_monopolar`, 바이폴라만 `_bipolar` 를 탄다. **두 경로가 이제 별개의 static 집합을 갖는다** - 한쪽만 보면 나머지가 미검증이고, 맵 전환은 두 카운터의 독립성을 드러낸다 |
| (미병합) | **impedance 분해** | ① **전 채널 임피던스 측정**(0x62 channel=255) ② **개별 채널 측정** ③ 측정값이 앱에 정상 표시 | **중** - 664줄 1함수를 11함수로 나눴다. 피팅 진단이라 일상 청취를 막지는 않는다 |
| (미병합) | **live 분해** | ① 라이브 시작·정지 ② **자극 볼륨 조정** ③ **마이크 감도** ④ **자극 알림음** ⑤ 이퀄라이저 읽기 ⑥ 기기 상태 읽기 ⑦ **연결 끊김 후 재연결**(HoldOn 재설정 경로) | **높음** - 하위명령 10갈래가 각각 별개 함수가 됐다. **분해 전에는 어느 갈래를 눌러도 같은 함수를 지나갔지만 이제는 아니다** - 한둘만 보면 나머지가 미검증이다. ⑦은 평소 안 지나가는 경로다 |
| (미병합) | **clang-format 일괄 적용** | **전반 동작에서 에러 로그 없을 것** (다른 항목 확인 중에 함께 본다) | **낮음** - 순수 포맷이다. **심볼 1,423개 중 크기가 바뀐 것이 1개**뿐이고 그 −4바이트도 `__LINE__` 즉치가 `movw`→`movs` 로 바뀐 것으로 명령어까지 설명됐다. `data`·`bss` 불변. 특정 경로를 겨냥할 근거가 없어 확인 항목을 얕게 잡는다 |
| (미병합) | **PMIC 모드 콘솔 명령** | ① 진입 시 `mode = m0 NORMAL` ② `c` 가 `0x81` · `EN_AND 1 DISCH 0 DVSRATE 0 FMODE 0 TYPE1 1` ③ `m2` 후 `c` 가 **`0x89`**(다른 필드 불변) ④ `m3` 후 **`0x8D`** + 경고 2줄 ⑤ **`m3` 에서 TX 전압이 배터리 전압까지 떨어지는가**(승압 정지 확인) ⑥ `m0`·`r` 로 복귀 ⑦ `m1` 거부 ⑧ 기존 명령(`+N`·`-N`·`=N`·`?`·`r`·방향키) 무회귀 | **중** - **테스트 콘솔 전용이라 정상 동작 경로에는 없다.** 다만 ③④ 가 «다른 비트 보존» 예측을 실증하는 유일한 수단이다. **⑤ 는 자극을 걸지 말고 무부하로 볼 것** - Forced Bypass 에는 과전류 보호가 없다(데이터시트 §5.10) |

**⑥⑦ 이 최우선인 이유**: 이월_2 에서 `fetch_packet` 의 지역 `static` 2개(`prev_subCommandData_Num_index` · `stimulPara_index`)를 파일 수준으로 승격했다. **단일 인스턴스로 동작하는지는 «여러 패킷에 걸친 순차 전송» 으로만 드러난다.** 한 패킷짜리 명령(볼륨·On/Off)으로는 확인되지 않는다.

**해소 조건**: 위 목록을 실기로 확인하고 결과를 §4 에 기록한다. 문제가 나오면 해당 병합을 특정해 되돌린다.

### 참고 - 미커밋 유지 중인 은수님 파일

| 파일 | 내용 | 성격 |
|---|---|---|
| `src/4__eeprom/nvmupdate.bat` | J-Link 시리얼 `603006635` -> `603006416` | 은수님 장비 설정 |
| `1__cfx.elf` | 빌드 부산물 | untracked |
| `tests/diff/` | 은수님 비교본 | untracked |
| `docs/참고/전극 번호/*.png` | 채널 매핑 이미지 2건 | untracked |
| `docs/참고/clang-format/example.c` | 포맷 본보기 (= `main.c` 바이트 동일) | **2026-08-06 커밋됨** |

**병합·커밋 시 절대 포함하지 말 것** (위 표의 untracked 항목).

> [!NOTE]
> **2026-08-06 변경**: `docs/참고/전극 번호/` 의 **`.md` 문서 3건**(이식본 2 + README)과 `docs/참고/clang-format/` 전체는 **커밋했다.** 은수님 지시로 이식·포맷 규칙을 sound1 에 적용한 산출물이라 다음 세션이 읽어야 한다. **`.png` 2건은 untracked 로 남겼다** — 커밋 여부는 은수님 판단.

## 2. 잠재 위험 (주기 점검 — 해소되지 않은 채 남아 있음)

> [!IMPORTANT]
> **은수님 지시(2026-07-30)**: *"ecap 쪽과 impedance 쪽에서도 배열 인덱스 이슈가 있다면 그것도 해야할 잠재 위험으로 정리해서 관리좀 해줘. 나한테 주기적으로 해야한다고 알려주면 좋겠어."*
>
> 아래는 2026-07-30 전수 조사 결과다. **바이폴라 99 결함**(`specific_stim`·`para_setting` 은 해소)과 **같은 구조**의 위험이 `ecap` 에 남아 있다.

### 2.1 조사 결과 요약 — `electrodeMap[32]` 접근 전수

| 명령 | 파싱 범위 검사 | `isd/` 실행 | 인덱스 안전성 | 앱 사용 | **위험 등급** |
|---|---|---|---|---|---|
| 0x62 impedance | **있음** — 1~32 또는 255 (`map_measure.c:31~33`) | 실행 | **안전** — 255 를 `electrodeNum = 0` 으로 분기(`map_impedance.c:81~88`), 증가 시 상한 검사(`:604`) | 사용 | **낮음** (마스크 비대칭만) |
| ~~0x63 eCAP masking~~ | **있음** (2026-08-05 신설, 9블록) | 실행됨 (`mapping.c:389`) | **안전** — 전극번호 2필드 1~32 강제 | 사용 X | ~~중~~ → **해소** |
| ~~0x64 eCAP alternative~~ | **있음** (2026-08-05 신설, 6블록) | 주석 처리 (`mapping.c:403`) | 안전 | 사용 X | ~~낮음~~ → **해소** |

### 2.2 위험_1 (해소, 2026-08-05) — 0x63 eCAP masking

> [!NOTE]
> **해소됐다.** [`tasks/ble/20260805_1740_ble-ecap-parsing-range-check`](tasks/ble/20260805_1740_ble-ecap-parsing-range-check/) 에서 **파싱에 범위 검사 9블록**을 넣었다. 아래 원 기록은 보존한다.

**조치 위치를 `isd/` 가 아니라 파싱으로 잡았다.** `isd/` 는 호스트 러너가 컴파일하지 않고(`tests/unit/run.sh:119~121`) 앱이 0x63 을 보내지 않아 실기도 불가하므로, **파싱이 유일하게 검증 가능한 위치**였다. 측정 계열은 0x62 가 이미 파싱에서 막고 있어 계열 내부 대칭도 맞는다.

**부수 해소 — `0x1F` 마스크 부재**: `electrodeMap[]` 원소값이 전부 0~31 이므로(`board/electrodeMapping.c:3`), 인덱스를 1~32 로 강제하면 마스크가 없어도 5비트를 넘지 않는다.

**정정**: 아래 원 기록의 «15개 필드» 는 실제 **14개**다(`ST__MAPPINGPAYLOAD_eCAP`, `tdc_ble_mapping.h:20~36`).

<details><summary>원 기록 (2026-07-30 조사)</summary>

**파싱에 범위 검사가 단 한 건도 없다.** `tdc_ble_map_measure_ecap_masking()`(`map_measure.c:59~91`)은 필드를 읽고 곧바로 `fetched_command` 를 설정한다.

| 위치 | 표현 |
|---|---|
| `isd/tdc_isd_map_ecap.c:688` | `if (electrodeMap[(mappingPacket->eCapMeasurement.measurementElectrodeNum - 1)] < 16)` |
| `:749` | `... \| (electrodeMap[(... measurementElectrodeNum - 1)])` |
| `:906` · `:1130` | `... \| ((electrodeMap[(... stimulationElectrodeNum - 1)]) ...)` |

| 입력 | 인덱스 | 주소 | 침범 대상 |
|---|---|---|---|
| `electrodeNum = 0` | `electrodeMap[-1]` | `0x200080cc` | `name.1` (`0x200080c4`, 11바이트) |
| `electrodeNum = 255` | `electrodeMap[254]` | `0x200084c8` | **`s_manu_table`** (`0x20008474`, 256바이트) = `pwr/tdc_pwr_clock.c:15` |

**바이폴라 99 결함보다 범위가 넓다** — 그쪽은 99 라는 특정 값이 통로였지만 여기는 0~255 전 구간이 무검증이었다.

</details>

### 2.3 위험_2 (해소, 2026-08-05) — 0x64 eCAP alternative

> [!NOTE]
> **해소됐다.** 파싱 검사 6블록을 넣어 *"주석을 푸는 순간 위험_1 과 동급"* 이라는 조건을 선제 제거했다. **실행부(`mapping.c:403`)는 주석 상태 그대로 둔다** — 되살리는 것은 기능 판단이라 별건이다.

### 2.4 위험_3 (해소, 2026-08-06) — impedance 의 `0x1F` 마스크 비대칭

> [!NOTE]
> **해소됐다.** `tdc_isd_map_impedance.c:363`·`:410` 에 `0x1F` 마스크를 넣어 라이브·0x65 와 대칭을 회복했다. 마스크 분포가 impedance 2 · specific_stim 2 · para_setting 7 로 맞았다.
>
> **동작은 바뀌지 않는다** — 0~31 에서 `0x1F & v == v` 라 정상 경로 비트 등가다(HW 5게이트 통과). 목적은 *"왜 여기만 마스크가 없지"* 를 다음 사람이 다시 묻지 않게 하는 것, 그리고 인덱스 안전성이 의존하는 파싱 검사가 언젠가 느슨해질 때 **마지막 방어**를 남기는 것이다.
>
> 아래는 원 기록이다.

`isd/tdc_isd_map_impedance.c:363` · `:410` 이 마스크 없이 OR 한다.

```c
w_isd_registerValue = w_isd_registerValue | electrodeMap[electrodeNum];                                    // :363
w_isd_registerValue = w_isd_registerValue | (electrodeMap[electrodeNum] << electrodIndexPositionAtPCM_Mold);  // :410
```

**인덱스는 안전하다** — `channel` 이 1~32 또는 255 로 검증되고, 255 는 `electrodeNum = 0` 으로 분기되며, 증가 시 `df_MaxNumOfElectrode` 상한 검사가 있다. 따라서 `electrodeMap[]` 값이 0~31 이라 마스크가 없어도 무해하다.

**다만 라이브·0x65 경로가 마스크를 갖는 것과 비대칭**이다. 방어 관례를 통일하려면 함께 넣는 것이 맞다.

### 2.5 안전 확인된 지점 (오탐 배제 기록)

| 위치 | 인덱스 | 판정 근거 |
|---|---|---|
| `map_ecap.c:465` · `:497` | `bipolarFIFO_index` | **안전** — 루프 변수. `:453~454` 에서 0 부터 `df_MaxNumTransferableChannel`(24)까지, `:487` 에서 8회 더 = 0~31 |
| `map_ecap.c:469` · `:501` | `electrodeMap[31]` | **안전** — 하드코딩 상수, 유효 범위 끝 |
| `map_impedance.c:363` · `:410` | `electrodeNum` | **안전** — §2.4 |

### 2.6 위험_4 (해소, 2026-08-06) - 리모콘 `0x4D` 맵 쓰기에 범위 검사 부재

> [!CAUTION]
> **같은 12필드를 매핑 앱 경로는 전부 검증하고 리모콘 경로는 하나도 검증하지 않는다.** 두 경로가 **같은 저장소**에 쓴다.

발견 경위: 2026-08-06 은수님 지시로 원천 프로젝트(`sullivan-1-5-fw-hw-test-link-pwr-bt-period`)의 전극번호 이식 문서를 sound1 에 대조하던 중, **체크리스트 7번 «루프 상한 이중가드»** 에서 걸렸다. 상세는 [`참고/전극 번호/README.md`](참고/전극%20번호/README.md) §2.4.

#### 비대칭 전수

| 하위명령 | 필드 | 매핑 앱 `ble/mapping/tdc_ble_map_flash.c` | 리모콘 `ble/remote/tdc_ble_remote.c` |
|---|---|---|---|
| 1 | 12필드 (자극기법·선행펄스·자극모드·펄스폭·**주파수밴드수**·알람채널) | **각 필드 범위검사** `:580~619` | **없음** `:421~424` |
| 2·3 | 사용가능 전극번호 32개 | **1~100** `:650~676` | **없음** `:447~460` |
| 4·5 | 바이폴라 기준전극 32개 | **1~100** | **없음** |
| 6·7 | 밴드 출력순서 32개 | 검사 있음 | **없음** |
| 8~15 | T/C 레벨 | 검사 있음 | **없음** |

리모콘 `tdc_ble_cmd_0x4D_parse_write_mapdata()`(`:371~580`)가 `dataRangeError` 를 세우는 것은 **`slot_index`·`map_index` 둘뿐**이다.

#### 왜 `numFrequencyBand` 가 가장 위험한가

다른 필드는 하류에 2차 방어가 있다(전극번호는 `stim_para_setting.c:492~495` 범위검사 + `continue`). **`numFrequencyBand` 만 2차 방어가 없고, 그것이 루프 상한으로 쓰인다.**

도달 사슬 (전 구간 코드로 확인):

| # | 지점 | 근거 |
|---|---|---|
| 1 | 리모콘 0x4D 가 `repository[10]` 에 무검증 바이트(0~255) 기록 | `tdc_ble_remote.c:421~424` |
| 2 | 저장소는 맵 구조체를 **평평한 int 배열로 쓴 것** | `tdc_shm.c:542` — `(int*) &...readWritemapData` |
| 3 | `ST__MAPPING_DATE` 가 정확히 **6필드**라 `[6]`=자극기법 … **`[10]`=`numFrequencyBand`** | `tdc_shm.h:57~65`. `map_flash` 의 `case 6`~`case 11` 라벨과 정확히 일치 |
| 4 | 저장소 → 플래시 | `tdc_cfx_eeprom_write.c:96` |
| 5 | 플래시 → `currentMapData` | `tdc_cfx_eeprom_read.c:89` |
| 6 | `currentMapData` → `p_mapdata` | `tdc_isd_stim_para_setting.c:454` |
| 7 | **`for (i = 0; i < p_mapdata->numFrequencyBand; i++)`** 가 `usableStimulationElectrodIndex[32]` 를 인덱싱 | `tdc_isd_stim_para_setting.c:465` |

#### 피해 한도 (과장하지 않기)

| 항목 | 판정 |
|---|---|
| 전극 인덱싱 | **안전** — `:492~495` 가 1~32 밖을 `continue` 로 거른다 |
| `bipolarReferenceElectrodeNum[]` 쓰기 | **안전** — 인덱스가 `electrodeMap[]` 값(0~31) |
| 구조체 범위 초과 읽기 | **발생** — 구조체는 문제 배열 뒤에 32칸 배열 6개(192 int)를 더 갖는다. `i` 가 32~223 이면 **구조체 안의 엉뚱한 배열**을 읽고, **224 이상이면 구조체 밖**으로 나간다 |
| 실제 증상 | **바이폴라 기준전극이 잘못 설정된다.** 메모리 손상보다 «조용한 오설정» 이 문제다 |

#### 같은 패턴 (함께 볼 것)

`tdc_ble_cmd_0x66_live.c:698` 도 `for (int li = 0; li < numFrequencyBand; li++)` 로 `T_level_uA[32]` 를 읽는다. 라이브 경로는 `:110~111` 파싱 검증이 있어 지금은 안전하지만, **상한 가드가 없는 것은 같다.**

#### 조치 결과 (2026-08-06 은수님 *"조치해줘"*)

작업: [`tasks/ble/20260806_0942_map-write-range-guard`](tasks/ble/20260806_0942_map-write-range-guard/)

**«나 + 다» 를 함께 적용했다.** 이식 문서가 입력 검증과 루프 가드를 별개 항목으로 요구한다.

| 층 | 조치 | 막는 것 |
|---|---|---|
| 입력 검증 | 검사를 `ble/tdc_ble_map_field_range.c` 로 빼고 **매핑 앱·리모콘이 함께 호출** | 앞으로 들어올 이상값 |
| 루프 가드 | `stim_para_setting.c:465` · `0x66_live.c:698` 에 `&& (i < df_MaxNumOfElectrode)` | **이미 플래시에 있는** 이상값 · 미래의 새 쓰기 경로 |

**복제(안 가)를 고르지 않은 이유**: 이 결함의 구조적 원인이 «같은 로직이 두 경로에 복사돼 한쪽만 갱신됐다» 이다. 복제를 늘리면 같은 결함을 다시 만든다.

**검증**: 리모콘 `dataRangeError` 지점 2 → **12**. 호스트 **431 → 483 PASS**(신규 52케이스가 경계값을 고정). `map_flash` 75 PASS 유지 = 매핑 앱 경로 무회귀. ARM 에러 0 · 경고 0 · text +428B · data·bss 불변.

> [!NOTE]
> **부수 성과 - 호스트 게이트가 열렸다.** 검사 함수가 `int` 를 받아 `bool` 을 내는 순수 함수라 `isd/` 의 의존 사슬에 닿지 않는다. [isd 분해 분석](tasks/isd/20260806_0834_isd-대형함수-분해-분석/분석.md) §6.2 가 예측한 방법의 첫 실증이다.

## 3. 이월 항목 (작업 대기 — `tasks/ble/작업 목록.md` 과 중복 관리 안 함)

전체 이월 목록은 [`tasks/ble/작업 목록.md`](tasks/ble/작업%20목록.md) §이월 항목이 단일 출처다. 여기에는 **상시 점검과 연결된 것만** 적는다.

| # | 항목 | 이 대장과의 연결 |
|---|---|---|
| 이월_E | `tdc_isd_stim_para_setting.c:471~473` TODO 문구 갱신 | 인과 양단 확정됨. 재현 확인만 남음 |
| ~~이월_F~~ | ~~`case 7` 하드코딩 `32` 상수화~~ | **해소 (2026-08-05)** — 라이브·0x65 양쪽 동시. `24 + 8 = 32` 관계가 `df_MaxNumTransferableChannel`·`df_MaxNumOfElectrode` 로 코드에 드러났다 |
| ~~이월_H~~ | ~~파싱 단계에서 특수값(99·255) 거부 여부~~ | **종결 (2026-08-06)** — 갈림의 기준은 계열이 아니라 **그 값이 정상 입력인가**다. 99 는 앱의 관례값(미사용 밴드 표시)이라 파싱 거부 시 정상 명령이 막힌다. 255 는 앱이 보내지 않는 명령의 무의미값이라 거부해도 무해하다. 현행 유지 |
| ~~이월_I~~ | ~~구조 지표 측정을 앵커 패턴으로 통일~~ | **해소 (2026-08-06)** — `tests/metrics.sh` 로 고정 |

## 4. 해소된 항목 (기록 보존)

| 항목 | 해소 시점 | 근거 |
|---|---|---|
| 바이폴라 99 - 라이브(0x66) 경로 | 2026-07-27 | `tdc_isd_stim_para_setting.c:486~496` 범위 검사 + `:577`·`:605` 마스크 |
| 바이폴라 99 - 특정자극(0x65) 경로 | **2026-07-30** | [`tasks/ble/20260730_bipolar-99-specific-stim`](tasks/ble/20260730_bipolar-99-specific-stim/). 실기 확인 완료 |
| `main.c` TX PMIC 콘솔 주석 확인 | 2026-07-30 | `67c91c1` 로 `source/tests/` 분리 + `main.c:474` 주석. 크기 지표로 교차 확인 가능 |
| **위험_1 - 0x63 eCAP 무검증 전극번호** | **2026-08-05** | [`tasks/ble/20260805_1740_ble-ecap-parsing-range-check`](tasks/ble/20260805_1740_ble-ecap-parsing-range-check/). 파싱 9블록. 실기 불가라 **호스트 테스트로 검증**(52 -> 125 PASS) |
| **위험_2 - 0x64 주석 해제 시 동급화** | **2026-08-05** | 동. 파싱 6블록. 실행부 주석은 유지 |
| **위험_3 - impedance `0x1F` 마스크 비대칭** | **2026-08-06** | `tdc_isd_map_impedance.c:363`·`:410` 에 마스크 추가. 정상 경로 비트 등가(0~31 에서 `0x1F & v == v`). text +8B. **이로써 §2 잠재 위험이 전부 해소됐다** |
| **알림_1 - `tdc_sys_control.c` 임시 변경 원복** | **2026-08-05** | 은수님 *"전력소모 측정이 끝났으니 원래대로 돌려줘"*. `git checkout HEAD --` 로 커밋 상태 복원 → `bool very_low_battery = (battery_percent < 40);` 및 임시 주석 제거. **저전력 자동 종료가 다시 동작한다.** 빌드 확인 text 185,436 -> **185,444**(+8B, `false` 상수가 실제 비교식으로 돌아온 몫). 2026-07-29 도입 이후 7일간 «매번 알림» 으로 운영됐다 |

## 5. 이 대장의 운영 규칙

| 규칙 | 내용 |
|---|---|
| 추가 기준 | ① 은수님이 "매번/주기적으로 알려달라"고 지시한 것 ② 코드에 남은 임시 변경 ③ 조사됐으나 미해소인 잠재 위험 |
| 제외 기준 | 일반 작업 이월(→ `작업 목록.md`), 완료된 작업 기록(→ `tasks/`) |
| 갱신 시점 | 항목 추가·해소·상태 변화가 있을 때 즉시 |
| 중복 방지 | 이월 목록의 단일 출처는 `작업 목록.md`. 여기는 **상시 점검과 연결된 것만** 참조 |

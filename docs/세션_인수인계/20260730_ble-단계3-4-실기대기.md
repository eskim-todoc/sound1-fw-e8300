---
name: BLE 단계_3·4 실기 통과 세션 인수인계
purpose: 2026-07-29~30 세션(0x66 계층 네이밍·카탈로그 전환·라이브 상태 계약·PMIC 콘솔)의 상태와 실기 결과를 새 세션에 인계 - 실기 통과 완료, 병합 승인이 다음 지점
type: 세션인수인계
applies_to: [Sound1]
tags: [ble, restructure, stage3, stage4, layered-naming, hold-on, pmic, 인수인계, 실기통과]
---

# 인수인계 - BLE 단계_3·4 실기 대기

**TL;DR**: 설계안 6단계 중 **단계_3(0x66 계층 네이밍)과 단계_4(라이브 상태 계약)를 구현 완료**하고 **2026-07-30 실기까지 통과했다.** 브랜치 `claude_feature_ble-descriptor-table` 에 17커밋. 호스트 테스트 **301 PASS**, ARM 빌드 **text 184,908 · 에러 0 · 경고 0**. **남은 일은 `claude_develop` 병합 승인**이며, 그 다음은 §6 이월_1(0x65 확대) 또는 이월_2(리모콘 분해)다.

> [!NOTE]
> **실기 완료 (2026-07-30)** — 은수님 확인 *"잘 동작해"*. 두 단계 분량이 함께 밀려 있었으나 함께 실기를 받아 함께 통과했다. 단계_3 을 실기로 확인한 뒤 단계_4 를 올리는 것이 원래 계획이었고, 은수님 지시로 단계_4 를 먼저 구현해 실기 전 안전망은 호스트 테스트 301 PASS 뿐이었다.
>
> 실기 전 세션 자체 검증: 호스트 **301 PASS**, `mapping/` 5파일 + `tdc_test_tx_pmic.c` **강제 재컴파일 + 재링크** 후 에러 0 · 경고 0 · text **184,908**(인수인계 기재치 일치).

## 0. 할일을 어디서 따라가는가

| 계층 | 위치 | 역할 |
|---|---|---|
| **할일의 원천** | [`재구조화 설계안.md`](../참고/ble/재구조화/재구조화%20설계안.md) **§3 로드맵** | 6단계 중 무엇을 할지 |
| | [`알려진 결함 목록.md`](../참고/ble/재구조화/알려진%20결함%20목록.md) | 결함 14건 |
| **기준표** | [`명령 카탈로그.md`](../참고/ble/프로토콜/명령%20카탈로그.md) | 명령 63개 ↔ 처리 함수. **2026-07-29 함수 이름 기준으로 전환** |
| | [`상태머신 사양.md`](../참고/ble/라이브모드/상태머신%20사양.md) | 라이브 4층 상태. 라이브 건드리면 필독 |
| **한 일의 기록** | `docs/tasks/ble/작업 목록.md` | 모듈 인덱스 |

### 로드맵 진척

| 단계 | 작업 폴더 | 상태 |
|---|---|---|
| 단계_0 결함 트랙 | [`20260727_ble-8bit-packet-redesign`](../tasks/ble/20260727_ble-8bit-packet-redesign/) | 완료·실기 확인 |
| 단계_1a·1b | [`20260728_ble-folder-restructure`](../tasks/ble/20260728_ble-folder-restructure/) | 완료·실기 확인 |
| 단계_2 | [`20260728_ble-reply-builder`](../tasks/ble/20260728_ble-reply-builder/) | 완료·실기 확인 |
| **단계_3 계층 네이밍** | [`20260729_ble-descriptor-table`](../tasks/ble/20260729_ble-descriptor-table/) | **완료 · 실기 확인** (2026-07-30) |
| **단계_4 상태 계약** | [`20260729_ble-live-state-contract`](../tasks/ble/20260729_ble-live-state-contract/) | **완료 · 실기 확인** (2026-07-30) |
| 단계_5 파서-FSM 분리 | - | 미착수 (고위험, 별도 판단) |

> [!IMPORTANT]
> **설계안 §3 본문의 단계_3·4 설계 제안은 채택되지 않았다** — 둘 다 테이블 주도 방식이라 기각됐다. 설계안에 §3 진척 현황과 CAUTION 을 달아 놨으니 그쪽을 먼저 볼 것. 본문을 구현 기준으로 삼으면 이미 기각된 설계를 다시 만든다.

## 1. 지금 상태

| 항목 | 값 |
|---|---|
| 브랜치 | `claude_feature_ble-descriptor-table` (`claude_develop` 기준 **17커밋**) |
| 호스트 테스트 | **301 PASS / 0 FAIL** (`bash tests/unit/run.sh` - measure 52 · flash 75 · stim 174) |
| ARM 빌드 | 에러 0 · 경고 0 |
| 크기 | text **184,908** / data 4,668 / bss 21,804 |
| 실기 | **통과** (2026-07-30, 단계_3·4 동시) |
| 다음 | **`claude_develop` 병합 승인 대기** |

### 미커밋 파일 (건드리지 말 것)

| 파일 | 내용 |
|---|---|
| `src/2__cm3/source/sys/tdc_sys_control.c` | **은수님 임시 변경** - `very_low_battery = false` (전력소모 측정용, 주석에 "260729 by 김은수"). **측정 후 원복 필요** |
| `src/4__eeprom/nvmupdate.bat` | 은수님 변경 - J-Link 시리얼 `603006635` -> `603006416` |
| `tests/diff/`, `1__cfx.elf` | 은수님 비교본 · 빌드 부산물 |

## 2. 무엇을 했나

### 2.1 단계_3 - 0x66 계층 네이밍 (`fb76532`)

**두 설계를 시도해 1차가 기각됐다.** 이 경위가 이 세션의 핵심 자산이다.

| 시도 | 결과 |
|---|---|
| ① 디스크립터 테이블 (23엔트리 + 순회 루프) | 지표는 전부 우수(`text` **-1,276B**, 함수 409->101줄, 검증 6종 통과)했으나 **은수님 검토에서 기각** -> `05c3a81` revert |
| ② **확정 - 프로토콜 계층을 함수 이름에** | `tdc_ble_cmd_0x66_subNN_idxKK_<이름>` + 명령 단위 파일 |

기각 사유는 [`구현-검증.md`](../tasks/ble/20260729_ble-descriptor-table/구현-검증.md) 에 남겼다. 요지는 *"배열에 숫자를 집어넣어 한참 분석해야 한다"* 였다. 매크로 인자 `(f, 0, 17, 1, 1, 100)` 이 정의를 찾아가야 뜻을 알고, 필드 배열 + 슬롯 `first` 오프셋의 **이중 관리**라 인덱스 1 에 필드 하나만 추가해도 슬롯 14줄이 연쇄로 밀렸다.

```
mapping/
├── tdc_ble_mapping.c        디스패치
├── tdc_ble_cmd_0x66_live.c  0x66 라이브 자극  <- 신규 894줄
├── tdc_ble_map_stim.c       0x65 만 잔류 (845 -> 116줄)
├── tdc_ble_map_flash.c
└── tdc_ble_map_measure.c
```

409줄 한 덩어리였던 `all_parameter` 가 **15줄 목차 + 18~21줄 함수 15개**가 됐다. 크기는 **+588B 늘었으나 의도된 교환**이다.

### 2.2 명령 카탈로그 함수 기준 전환 (`cbda6d3`)

소스 참조 **73건이 라인 번호였고 전부 stale** 이었다(최대 참조가 662줄 파일의 `:1885`). 리팩토링마다 통째로 깨지므로 **기준 자체를 함수 이름으로 바꿨다.** 라인 참조 **0건**, 함수 참조 114건.

### 2.3 단계_4 - 라이브 상태 계약 (`5a7b32b` · `4cd72d1`)

`en__HoldOn` 게이트 4곳(하위 3·4·5·6)을 이름 있는 가드 하나로 수렴했다.

```c
// 수락 조건 : 라이브 자극 유지 중(en__HoldOn)에만.
static void tdc_ble_cmd_0x66_sub03_volume_adjust(...)
{
    if (!tdc_ble_cmd_0x66_require_hold_on(p_mappingPacket, __LINE__))
    {
        return;
    }
```

설계안이 제안한 "허용 선행 상태 테이블"은 **채택하지 않았다** - 단계_3 에서 기각된 방식이고 게이트가 4개뿐이라 규모 이점도 없다. 대신 **9개 하위 명령 머리에 수락 조건을 한 줄씩** 적었다.

### 2.4 TX PMIC 측정 콘솔 (`67c91c1`) - BLE 와 무관

은수님 요청으로 만든 계측 도구. `source/tests/tdc_test_tx_pmic.c/.h` 로 분리했고 **main.c 에는 호출 한 줄이 주석 상태**로 있다.

## 3. 실기 체크리스트 - 통과 (2026-07-30)

> [!NOTE]
> **은수님 확인 완료 - "잘 동작해".** 사전 확인 사항이던 `main.c:474` 의 `tdc_test_tx_pmic_console();` 은 **주석 상태**였고, 크기 **184,908**(187,260 아님)로 교차 확인했다.

| # | 항목 | 기대 | 대상 단계 | 결과 |
|---|---|---|---|---|
| 1 | **Eclipse F5 Refresh 후 Clean 재빌드** | 신규 파일 2개(`tdc_ble_cmd_0x66_live.c` · `tdc_test_tx_pmic.c`)가 빌드에 잡힌다. 에러 0 · 경고 0, text **184,908** | 공통 | **통과** |
| 2 | 매핑 프로그램으로 **라이브 전체 파라미터 15단계 전송** | 15단계 전부 정상 응답, 중간 에러 없음 | 단계_3 | **통과** |
| 3 | 15단계 완료 후 **라이브 자극 시작** | 전극 · T/C 레벨 · 밴드 순서가 이전과 동일하게 반영 | 단계_3 | **통과** |
| 4 | 라이브 중 **자극 볼륨 · 마이크 감도 · 알림 자극 · 이퀄라이저 읽기** | 정상 동작 (게이트 통과 경로) | 단계_4 | **통과** |
| 5 | **라이브가 아닐 때** 위 4개 명령 | 거부 + 명령 순서 에러 (게이트 거부 경로) | 단계_4 | **통과** |
| 6 | 라이브 **시작 · 종료 · 장치 상태 읽기** | 조건 없이 수락 | 단계_4 | **통과** |

**1번의 Refresh 가 관문이었다.** `Debug/` 는 gitignore 라 신규 파일을 Eclipse 가 스스로 인식해야 한다. CLI 빌드용 `subdir.mk` 는 세션에서 수동으로 맞춰 뒀으나 git 에 올라가지 않는다 — **다음에 신규 파일을 추가할 때도 같은 Refresh 안내가 필요하다.**

### 실기가 정적 검증에 더해 확증한 것

정적 검증으로 덮이지 않는 축이 단계별로 다르다.

| 단계 | 정적 검증이 보인 것 | 실기만 확증하는 것 |
|---|---|---|
| 단계_3 | 조건 분기 총수 6종 원본 동일 · 경계 상수 다중집합 일치 | **파싱 순서**와 `prev_subCommandData_Num_index` **공유 상태** — 15개 독립 함수로 쪼개며 순서가 어긋났다면 15단계 전송 중간에 끊긴다 |
| 단계_4 | 게이트 지표 5->1 수렴 · 본문 지표 불변 | **가드가 옳은 함수에 붙었는가** — 오부착(항목 6)과 조건 반전(항목 4·5 가 짝)은 정적으로 **못 잡는다** |

**남은 것은 병합 승인**이다 — `claude_develop` 으로 `--no-ff` 병합.

## 4. 불가침 (이번 세션 신규 + 기존)

| 항목 | 이유 |
|---|---|
| **`tdc_ble_cmd_0xNN_subMM_idxKK_<이름>` 계층 네이밍** (신규) | 프로토콜 계층을 이름으로 드러내는 체계. `grep "0x66_"` 로 명령 전체를 훑을 수 있고 카탈로그와 1:1 대조된다 |
| **테이블 주도 재도입 금지** (신규) | 지표가 좋아도 기각된다. 판단 기준은 **사람이 눈으로 보고 관리하기 쉬운가** |
| **카탈로그의 함수 이름 참조** (신규) | 라인 번호로 되돌리면 다음 리팩토링에서 또 전량 깨진다 |
| `tdc_ble_reply_*` stateless 계약 | 2026-07-28 |
| `tests/unit/` 의 `src/` 무변경 원칙 | 동 |
| `prev_subCommandData_Num_index` 접근자 | 동 |
| BLE 프로토콜 정의 · 공유 ABI · `sections.ld` | 기존 |

## 5. 검증 노하우 (이 세션의 자산)

### 노하우_1 - 지표가 좋은 설계가 기각될 수 있다

단계_3 의 1차 구현은 `text` **-1,276B**, 함수 **409->101줄**로 모든 정량 지표가 우수했고 검증 6종을 통과했다. 그럼에도 기각됐다. 확정 구현은 정반대로 크기가 **+588B 늘었다.**

**설계 판단의 실질 기준**은 "프로토콜이 바뀌면 몇 곳을 고치는가"였다.

| 설계 | 인덱스 1 에 필드 하나 추가 시 |
|---|---|
| 디스크립터 테이블 | 필드 배열 1줄 + **슬롯 14줄 연쇄** + 단언 1줄 = **3곳** |
| 계층 네이밍 | 함수 하나 = **1곳** |

### 노하우_2 - 리팩토링 유형마다 불변량이 다르다

| 유형 | 지표 |
|---|---|
| 파일 이동 | `.elf` 바이트 동일 (`-ffile-prefix-map` 필요) |
| 함수 추출 | **조건 분기 총수 보존** |
| 테이블 주도 전환 | 분기 총수를 쓸 수 없다 -> **파싱 필드·경계 상수 집합** |
| **가드 수렴** (신규) | **게이트 관련은 수렴, 본문은 불변** |

단계_4 실측: `en__HoldOn` 비교 5->1 · `en__Command_Order` 5->1 · `else` 14->10, 반면 `if (` 45 **불변** · `for (` 18 **불변** · `en__OutOfDataRange` 7 **불변**. `if (` 가 불변인 것이 "분기를 없앤 게 아니라 이름 있는 자리로 옮겼다"를 증명한다.

### 노하우_3 - 테스트 선행 + 뮤테이션

단계_3·4 모두 **커밋_1 을 테스트로 두고 기존 코드에서 통과시킨 뒤** 전환했다. 단계_3 에서는 뮤테이션 2건(청크 첨자 밀기 · 범위 상한 완화)으로 **테스트가 실제로 잡는지**까지 확인했다.

테스트가 없던 영역을 먼저 덮는 것이 핵심이다. 단계_3 착수 시 데이터 인덱스 2~15 는 커버리지 0, 단계_4 착수 시 게이트 4곳 중 2곳이 0 이었다.

### 노하우_4 - 플랫폼별 `sizeof` 차이는 호스트 테스트로 못 잡는다

`stimulationMode` 만 `EN___STIMULATION_MODE` 이고 **ARM EABI 는 `-fshort-enums` 가 기본이라 1바이트**, 호스트 테스트 컴파일러(MinGW x86)는 **4바이트**다. 폭을 상수로 적으면 **호스트 테스트가 전부 통과하면서 실기에서만 인접 바이트를 덮는다.**

이 구조체를 폭 기반으로 다루는 코드(직렬화 · `memcpy` · 오프셋 접근)를 쓸 때 반드시 고려할 것.

### 노하우_5 - 공유 상태는 파싱 유효 범위까지 바꾼다

데이터 인덱스 카운터를 `map_flash`(최대 **34**)와 공유하므로 0x66 하위 1 에 **16~35 가 실제 도달**한다. `default:` 는 죽은 코드가 아니라 살아 있는 방어 코드다.

### 보조 - 도구 함정 (누적)

| 함정 | 대응 |
|---|---|
| `make` 가 PATH 에 없음 | `export PATH="/c/Program Files (x86)/ON Semiconductor/Ezairo 8300 SDK/arm_tools/bin:$PATH"` |
| `make` 가 sh 에서 실패 | `make SHELL=cmd all` |
| **bash 파라미터 확장 `${v//p/r}` 에서 백슬래시 경로** (신규) | 패턴의 `\2` 등이 이스케이프로 먹힌다. 백슬래시 없는 앵커를 쓸 것 |
| **sed 치환문의 `&` 와 백슬래시 경로** (신규) | `\2` 가 back reference 로 해석돼 `Invalid back reference`. awk 나 bash 치환으로 우회 |
| `PowerShell Copy-Item` 타임스탬프 보존 | `.o` 삭제 + `touch` 필수 |
| `Select-String "error"` 오탐 | `": error:"` 로 좁힐 것 |

## 6. 이월 항목

| # | 항목 | 상태 |
|---|---|---|
| 이월_1 | **0x65 를 계층 네이밍으로 확대** | 단계_3 시범 결과를 실기로 본 뒤 판단. 92줄로 작다 |
| 이월_2 | **리모콘 0x40~0x59 분해** | `tdc_ble_remote_fetch_packet()` 하나가 **약 1,340줄**. 카탈로그를 함수 기준으로 바꾸니 드러났다 |
| 이월_3 | **Ezairo 0x33~0x36 분해** | `setting_nrf_ble_adv_info()` **183줄** - 이름은 nrf 광고 정보 설정인데 배터리·전원·LED·클래식 상태 응답을 모두 담고 있다 |
| 이월_4 | `isd/tdc_isd_map_live.c` 의 `subCommand` 19곳 | 단계_5 역결합 해소 영역 |
| 이월_5 | 재진입 가드 구조 변경 | 결함_4(Stop 거부 위험 창) 정량화가 선행 조건 |
| 이월_6 | 리모콘 0x51·0x53·0x58 핸들러 부재 | 명령이 무응답으로 멈춘다 |
| 이월_7 | `isd/` 응답 조립 21곳 | 단계_2 부터 이월. 단계_5 이후 재검토 |
| 이월_8 | DFU/OTA 실기 검증 · 바이폴라 정밀 재측정 | 2026-07-27 부터 이월 |
| 이월_9 | CI 연동 · 커버리지 | 테스트 인프라 확장 |

## 7. 새 세션 시작 시 권장 순서

> [!NOTE]
> **1~5 는 2026-07-30 세션에서 완료됐다.** 실기 통과까지 끝났으므로 새 세션은 **6번(병합 승인)부터** 시작한다.

1. ~~루트 헌법 `E:\workspace\rules\CLAUDE.md` 명시 Read~~ (세션마다 반복)
2. ~~이 문서 (특히 §3 실기 체크리스트)~~ **완료**
3. ~~`bash tests/unit/run.sh` 로 현재 상태 확인 (301 PASS 여야 정상)~~ **완료** - 301 PASS 확인
4. ~~`main.c` 의 `tdc_test_tx_pmic_console();` 이 **주석 상태**인지 확인~~ **완료** - `main.c:474` 주석 상태
5. ~~**은수님께 §3 실기 항목을 요청**~~ **완료** - 2026-07-30 전 항목 통과
6. **병합 승인 요청 -> `claude_develop` 에 `--no-ff`** <- **여기가 현재 지점**
7. 그 다음 이월_1(0x65 확대) 또는 이월_2(리모콘 분해) 중 선택

### 병합 시 주의

| 항목 | 내용 |
|---|---|
| 미커밋 파일 | §1 의 "미커밋 파일" 4건(`tdc_sys_control.c` · `nvmupdate.bat` · `tests/diff/` · `1__cfx.elf`)은 **은수님 것이므로 병합 대상에서 제외** |
| `tdc_sys_control.c` | `very_low_battery = false` 임시 변경이 살아 있다. 전력소모 측정이 끝났으면 **원복 시점을 은수님께 확인** |
| 브랜치 복귀 | 병합 후 `claude_develop` 복귀 (`claude_main` 체류 금지) |

> [!IMPORTANT]
> 은수님은 **빌드·실기를 직접 수행하는 게이트 담당**이다. "빌드해 보세요"가 아니라 **무엇을 어떤 순서로 확인해야 하는지**(Refresh 필요 여부, 확인할 동작, 기대 로그·크기)를 명시해 드려야 한다.
>
> **CLI 빌드와 호스트 테스트는 세션에서 직접 돌릴 수 있다.** 타입 오류·경고·파싱 회귀는 실기 요청 전에 스스로 잡을 것.
